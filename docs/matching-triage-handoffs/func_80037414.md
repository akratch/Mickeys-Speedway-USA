<!-- plateau-handoff:func_80037414:start -->
### `func_80037414` plateau handoff

- source: `src/main/frontend_37D50.c`
- score: 59 differing words
- frame: 0x40
- relocations: 42
- first mismatch: +0x10
- summary: VI transition semantics compile, but argument homes and carrier allocation remain structurally different.
#### 2026-09-09: the frame is three decompiler spill names

Three of the seven declared locals (`sp2C`, `sp20`, `sp1C`) are written and
never read: they are the decompiler's names for the compiler's own spill slots
around the `func_800371BC` call, not source variables. Deleting them and their
two assignment groups takes the frame from `0x40` to `0x38` with no change to
the instruction count, and the target's is `0x30`. `sp28` is different -- it is
read again at `sp28 * D_8007BEAC` -- so it stays.

Read the target's homes as a declaration census before anything else: with
frame `0x30`, `var_a1` is homed at `0x2C`, `sp28` at `0x28`, `var_t0` at
`0x20` and `var_a2` at `0x1C`, so the declared block is at least five long and
its order is fixed by those four positions. `arg0` and `arg3` and `arg6` are
not locals at all -- they live at `0x30`, `0x3C` and `0x48`, their own incoming
homes.

The remaining `+4` is the same spill-versus-colour question as
`func_8000A39C`: the candidate colours one truncated value into `s0` and pays
a save and a restore where the target spills to a local home. No JFG donor
covers this function; PR #37's `src/menu.c` is the menu renderer, and neither
it nor any other reference file carries this transition state machine.

<!-- plateau-handoff:func_80037414:end -->
