<!-- plateau-handoff:func_80020E4C:start -->
### `func_80020E4C` plateau handoff

- source: `src/main/models.c`
- score: 25/113 words
- frame: 0x40
- relocations: 13
- first mismatch: +0xC
- summary: Guard and compound-exit shapes normalize; pointer strength reduction remains blocked.

#### 2026-09-11, lane p7-res: the exception loop's cursor form is refuted as a class

Re-measured at 25 masked words, size delta 0, frame 0x40 on both sides with all
ten stack slots agreeing. The aligner splits it 89 byte-exact, 15 register
naming, 1 immediate, 9 really different, with one surplus word at +0xC0 against
one missing word at +0xAC -- the whole structural residual is the inner
exception loop, where the target runs a strength-reduced pointer cursor with its
base computed in the preheader and this candidate recomputes the scaled index
each iteration.

The obvious inference -- write the cursor explicitly -- is wrong, and it is
wrong for every spelling. Seven forms were measured: plain array indexing, a
pointer initialised from the array, a pointer initialised from the indexed
element, a pointer initialised from the byte-offset expression, `* 2` instead of
`<< 1`, `exception[0]` with `+= 1`, and a byte offset advanced by two. **All
seven compile to the same object**, four bytes shorter than the target with the
first mismatch at +0x64 and 77 masked words. So the retained byte-offset
spelling is not one of a family; it is the only member of the family that keeps
the word the target keeps, and the cursor is not reachable by respelling this
loop.

`tools/register_census.py` reads the naming half as 85% coherent over two
windows with two closed cycles in the dominant mapping, s6 against s7 and a1
against s5. That is an L127 shape and it is the part of this residual most
likely to move at zero byte cost -- but it sits behind the structural word, so
the order of work is the loop first.

Decision variable: what stops uopt from strength-reducing this loop when the
address is written as a byte offset. Both reads use the same induction variable
with the second at a constant displacement, which is the case strength reduction
normally takes; the guard `if (*exceptions != -1)` ahead of the loop and the
compound exit condition are the two candidate causes, and neither has been
varied.

#### 2026-09-12, lane p15-res: guard and compound-exit source forms normalize

The fresh baseline remains 25 masked words at equal 113-word geometry: 15
naming, 1 immediate and 9 structural rows, including the same one-word surplus
and one-word deficit. Reversing the compound exit operands regresses to 26,
with 10 structural rows. The initial guard as `exceptions[0] != -1` is
byte-identical. Moving the compound exit into a post-increment sentinel-or-
excluded break is also byte-identical. Thus neither guard spelling nor compound
exit control shape reaches the missing strength-reduced cursor; retain the
byte-offset form and seek a non-loop source fact that changes uopt's induction
classification.
<!-- plateau-handoff:func_80020E4C:end -->
