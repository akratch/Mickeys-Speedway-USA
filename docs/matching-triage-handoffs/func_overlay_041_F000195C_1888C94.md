<!-- plateau-handoff:func_overlay_041_F000195C_1888C94:start -->
### `func_overlay_041_F000195C_1888C94` plateau handoff

- source: `src/overlays/o041/overlay41EnqueueTransition.c`
- score: 29/105 words
- frame: frameless
- relocations: 22
- first mismatch: +0x8
- summary: Leaf cursor-definition placement regressed to 30 words; exact relocation identity leaves only an authentic cursor-lifetime decision.

- layered evidence: canonical tool merge `e9890238417f69494b40918602eaaef8ea8e1b6e`; authorization `f959b4d67f696ea49a825cdaa720f7eb58cdf2fe`; pinned source `26d47ab7147b62fa1e528eac0ff6f2ae5ae15921`; pinned prior plateau `9559c13091d074eb97a7a17079f5fcdb99e70324`.
- boundary and geometry: overlay 41 text `+0x195C..+0x1B00`, exactly 420 bytes / 105 words. Configured C reproduces the exact instruction count and both target and candidate are frameless; the next owner starts at `+0x1B00`.
- relocation proof: preflight is complete with 22 candidate and 22 target records. Offset/type, stable identity, and effective identity align 22/22 under overlay 41 section-and-offset ownership; no shared-VMA inference is used.
- fresh residual: 76/105 positional words are exact, 29 raw and relocation-masked words differ, and first mismatch is `+0x8`. Workbench remains `structure-mismatch`, with 13 structural, 12 register, and three constant aligned sites plus five insertions and five deletions.
- actionable diagnosis: the first code-generation divergence is the cursor pool web (`v0` in the target and `v1` in the candidate); all 37 temporary-lane assignments agree. Complete identity proof removes the old relocation blocker but does not distinguish one natural cursor lifetime or CFG spelling from the already-exhausted family.
- authorization result: changing the redundant `else if` after the first returning block into an independent `if` was byte-flat at 76/105 words. Old cursor, pointer, and offset forms, flags, permutation, fake carriers, and this natural block-order form remain closed.
- next action: reopen only with Mickey-authenticated original cursor lifetime evidence that uniquely changes the pool web while retaining exact geometry, framelessness, and all 22 identities.

#### 2026-09-12, lane p9-mid: the residual is one instruction's placement, and the decision variable is named

Baseline reproduces: 420 bytes, 105 of 105 words, delta 0, masked 29, raw 29,
frameless. Aligner buckets, unchanged because nothing was adopted: 81
byte-exact, 12 register naming, 2 immediate-only, 11 really different, with one
surplus candidate word at +0x78 against one missing at +0xBC and a displacement
tax of 4. Register census: one source register, v1, mapping to v0 at 13 sites,
100 percent coherent, one window, no cycle.

**The whole residual is one instruction moved by one word, and the rest is the
shift it causes.** Read positionally rather than through the shape aligner, the
two streams agree exactly from +0xC4 onward and again from +0x100 onward, and
everything between +0x74 and +0xC0 is the same instruction sequence offset by
one. The candidate emits the second cursor advance at +0x78, before the entry
block that follows it; the shipped code emits it at +0xC0, after that block's
return delay slot. Because the candidate's advance is hoisted above the block,
it cannot be an in-place update: it writes a second register, so the candidate
carries a base cursor in v1 and an advanced cursor in v0 where the shipped code
carries one cursor in v0 and updates it in place. Every one of the 13 v1-to-v0
substitutions is that one fact.

**The addressing tell.** In the two delay slots at +0x7C and +0x88 the shipped
code loads the next entry's active byte as displacement 23 off the un-advanced
base; the candidate loads it as displacement 11 off the advanced pointer. The
shipped form is uopt folding the +12 into the displacement, which is what leaves
the advance free to sink below the block. So the decision variable is: **what
makes uopt reassociate a pointer advance into the following access's
displacement instead of materialising the advanced pointer** -- an L107-family
question about the region boundary around the advance, not a lifetime or pool
question.

**The allocator is exonerated, and the call test was checked first.** This
function issues no call and its records are p2 only, 40 of them, consistent with
the standing classification. The two cursor webs are p2 web 0 (save 9.000,
nocs 4, totalsave 36, colour v0) and p2 web 2 (save 4.333, nocs 3, totalsave 13,
colour v1). Forcing the swap, with acceptance verified from the record's forced
field, scores **56** against the baseline 29, and forcing either web alone is
either declined or the same 56. Whatever the shipped code is doing, it is not
this colouring with the roles exchanged -- consistent with it having one cursor
web where this candidate has two.

**Axes covered and flat.** All eight advance-or-displacement schedules across
the three cursor advances, and then the full enumeration of advance amounts in
multiples of 12 against reachable displacements (twelve distinct schedules): the
incumbent is the only one at size delta 0, and every schedule that drops an
advance loses 4, 8 or 12 bytes, so the shipped code does perform all three
advances. Dropping volatile from the entry macro (32), a volatile pointer
cursor and a plain pointer cursor (both +36 bytes), s32 for the cursor, an
explicit two-variable advance, `cursor = cursor + 12` for `cursor += 12`, a
redundant `+ 0` in the macro, initialising the cursor after the globals block
(30), making the second block a plain `if`, five redundant-mask ring draws on
the queue reads, three line joins around the advance and the store group, and
swapping the last two stores in every block: all 29 or worse. The
redundant-mask ring draw that was worth 16 words on this overlay's sibling
`func_overlay_041_F0001298_18885D0` today does **not** transfer here -- every
placement is flat or much worse -- which is the sibling signal tested and
answered.
<!-- plateau-handoff:func_overlay_041_F000195C_1888C94:end -->
