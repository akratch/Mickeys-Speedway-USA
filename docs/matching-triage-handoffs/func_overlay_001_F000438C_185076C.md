<!-- plateau-handoff:func_overlay_001_F000438C_185076C:start -->
### `func_overlay_001_F000438C_185076C` plateau handoff

- source: `src/overlays/o001/overlay_001_tail.c`
- score: 1508 differing words
- frame: 0x230
- relocations: 65
- first mismatch: +0x0
- summary: Target 0x138 frame versus m2c 0x230; recover typed ABI/stack layout and canonical symbol ownership before further CFG work.

Fresh evidence on base `67991097249736eab861570908d30f2116290aa9`:

- authenticated owner: overlay 1 `.text` offset `0x438C..0x5BA4`
  (`0x1818` / 6,168 bytes), exactly 1,542 executable words ending at the
  target epilogue; the next fallback owner begins at `+0x5BF4`;
- ABI evidence: the current Mickey-local draft is
  `void (f32 *object, s32 step)`, keeps the object carrier in `s1`, obtains
  its state pointer into `s0`, and saves `ra`, `s0`, `s1`, and `f20`, matching
  the target's opening call and callee-save set. No independently named caller
  or export provides stronger type evidence;
- configured full-TU V0: 1,600 candidate words / 6,400 bytes (`+58` words /
  `+232` bytes), 1,510 raw and 1,508 relocation-masked positional
  differences from `+0x0`. This exactly reproduces the retained scheduling
  geometry rather than trusting its stale ledger;
- frame evidence: the target uses `0x138` bytes and 292 non-save stack bytes;
  the draft uses `0x230` bytes and 540 non-save stack bytes while retaining
  the same 20-byte save area. The overallocated m2c scalar/array lifetime web
  is therefore the first structural blocker, not a local expression-order
  residual;
- workbench verdict: `structure-mismatch`, with 186 alignment gaps, 1,407
  opcode mismatches, 252 aligned structural residuals, and a net 58-word
  geometry surplus. Its constant-audit footer is downstream of the
  unauthenticated frame/layout and cannot make this candidate permutation
  ready;
- relocation evidence: the assembled target fallback exposes 65 symbolic
  records while the provisional full-TU candidate emits 452 records in its
  owned range. Workbench reports 272 metadata and 276 symbol-identity
  mismatches; many candidate `LOCAL_*`/`ext_o*` bindings are not authenticated
  retail owners, so linked relocation identity is far from promotable;
- donor evidence: `skeleton_scan.py similar --target '1:+0x438C' --top 15`
  found no credible sibling (best permitted published-decomp similarity was
  `0.054`). The same-offset JFG kiosk symbol is unrelated scheduling evidence,
  not a donor;
- tooling limitation: `wb_compare.sh` currently fails preflight because the
  same TU's unrelated `overlay1UpdateObjectPhysics` friendly alias does not
  select a unique C definition. V0 was therefore compiled as the configured
  `NON_MATCHING=1` full TU and compared against a local untracked target
  wrapper; no target-derived rows or workbench artifacts are tracked.

Next lever: replace the m2c local soup with source-authentic typed object and
stack aggregates that naturally recover the `0x138` frame, while resolving
the provisional `LOCAL_*` and `ext_o*` owners. Re-run preflight after the
friendly-alias issue is fixed; do not repeat flags, constant tweaks, or bounded
permutation until geometry and relocation identities improve naturally.
<!-- plateau-handoff:func_overlay_001_F000438C_185076C:end -->
