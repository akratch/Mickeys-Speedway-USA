<!-- plateau-handoff:func_8000D1B8:start -->
### `func_8000D1B8` plateau handoff

- source: `src/main/track.c`
- score: 114 differing words
- frame: 0x28
- relocations: 8
- first mismatch: +0x38
- summary: Retain unsigned command decoding at 114 differences; structural forms plateau. Next: packed-delta and relative-UV lifetime evidence.

#### Mickey m2c reconstruction audit, TU batch 2026-09-08

- Gate: `base-only` at batch base `d0bdcaf16a9b0a7fb7371eb435c0940cb33f5c19`. Fresh configured baseline and retained result: 130 candidate versus 128 target words, 114 raw/masked differences from `+0x38`, matching `0x28` frames, eight candidate/target relocations and six exact offset/type/identity tuples. No matching credit is added.
- Workbench verdict `structure-mismatch`, playbook `constant-audit`, lever 1. The dimension threshold and both mask scales match Mickey's target. The first structural discrepancy is branch/schedule geometry rather than a wrong constant. The context-aware m2c draft confirms the packed command array's existing unsigned word type and the signed extraction of its two 12-bit deltas.
- Attempt 8 removes the candidate's signed pointer cast, retains the packed word as `u32`, and makes the signed high-byte extraction explicit. This avoids signed left shifts of the packed input while preserving the target's bit arithmetic and the complete measured code. This type correction is retained at the original score. Attempts 9 and 10 move delta extraction to its m2c consumers and recover typed batch/triangle fields; both remain at 130 words and 114 differences.
- Attempt 11 reconstructs the mask assignment inside the short-circuit condition and reaches 129 words, but 115 differences. Attempt 12 restores explicit early delta assignments; IDO still sinks extraction into the batch path and produces the same 129/115 result. Carrier-preserving m2c then identifies four relative UV values before the base-coordinate updates. Attempt 13 expresses those values and produces 129 words, 128 differences and frame `0x30`; attempt 14 reuses the base-U carrier and remains identical. Attempt 15 reconstructs the counted-loop CFG with one word counter per loop, producing 132 words/132 differences/frame `0x30`; attempt 16 widens the updated UV carriers and remains identical.
- Stopping evidence: the mask compaction at attempt 11 is the last smaller structural geometry. The following five attempts do not improve the best residual or identify a new call/global/field identity; the reconstructed UV/counter forms either collapse to identical output or regress frame and geometry. No additional untested m2c identity or CFG remains in this packet. Baseline control flow and source layout are restored with only the unsigned packed-command correction retained and recompiled.
- Next concrete lever: authenticated source lifetimes explaining why the target retains both packed-scroll deltas outside the batch loop while using four independent relative UV values without the extra stack word. The inferred carrier forms measured here are closed; allocator manipulation and random permutation were not substituted for structural reconstruction.
- All nine source attempts (batch counts 8 through 16), hypotheses, full-TU sources/objects, summaries and both m2c drafts remain under ignored `build/wb/tu-track/func_8000D1B8/` and `build/wb/`. Commands include configured `wb_compare.sh`, workbench `diagnose`/`guide constant-audit`, context-aware m2c with a carrier-preserving diagnostic view, checkpoints, restoration/reproof and `finalize_plateau.py`. The original guard and assembly fallback remain active.

#### Pinned donor audit, 2026-09-08

- Assignment gate: `base-only` at `32a75d648e8954f7455897fb8f16a8ef5f05df11` for this exact symbol and source path.
- Jet Force Gemini `efd5abb1c79636e297b831f7c2d5bf47eac39c0c` has 12 C implementations and 53 assembly placeholders in `src/track.c`. Its implemented routines do not supply this target's body. The closest masked track-object hit, `trackUpdateTextureScroll` (Jaccard 0.4400), corresponds by verified TU order to the still-assembly `trackUpdateTextureScroll` in that pinned source. This is diagnostic structural context, not an adopted name, ABI identity, or exact donor match.
- Fresh configured full-TU measurement: 128 target words and 130 candidate words; 114 raw and 114 relocation-masked differences; first +0x38; target frame 0x28, candidate frame 0x28. There are 8 candidate versus 8 target relocation records, with 6 stable identities at matching offsets/types.
- Workbench verdict `structure-mismatch`, playbook `constant-audit`. The existing diagnosis still requires source-shape/lifetime evidence; the donor-only reopen does not authorize substituting a flag, allocator, or permuter mechanism.
- ADR 0018 stop: zero new source attempts for this target. The pinned source disproves the new matched-donor-C hypothesis. No unchanged flag lattice or previously exhausted source family was repeated. The compiled candidate is unchanged and remains behind `NON_MATCHING` and its original `GLOBAL_ASM` fallback; zero new matching bytes are credited.
- Next concrete lever: a published matched counterpart with an authenticated ABI and useful source lifetimes, followed by Mickey-specific field, branch and call proof. The remaining general levers below require separately authorized changed evidence.
- Commands: `tools/wb_compare.sh --summary-json func_8000D1B8`, workbench `diagnose` on the configured full-TU object, the track-object masked skeleton audit, `tools/finalize_plateau.py`, `gmake cleanroom`, and `gmake check-docs`. Source, object, scores, first mismatch and previous handoff are retained under ignored `build/wb/`; no instruction rows are included here.

#### Prior committed plateau evidence (historical)


- source: `src/main/track.c`
- score: 114 differing words
- frame: -0x28
- relocations: 8
- first mismatch: +0x4
- summary: Candidate is 130 versus 128 instructions with four shifted relocation sites. Next lever is packed-scroll delta lifetime and six halfword-load scheduling.
<!-- plateau-handoff:func_8000D1B8:end -->
