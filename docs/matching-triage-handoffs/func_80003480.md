<!-- plateau-handoff:func_80003480:start -->
### `func_80003480` plateau handoff

- source: `src/main/audio_manager_36D0.c`
- score: 41/94 words
- frame: 0x48
- relocations: 5
- first mismatch: +0x0
- summary: Authorized V0 reproduced 41/94 words, frames 0x48/0x30, and 4/5 aligned relocations; donor search lacks a size owner. Body forms stay closed.

Authorized maintenance evidence on layered base
`eb8f485d2c44178a2e7105fef0d1003b854b2aa3`:

- the unique fallback boundary is resident VMA `0x80003480..0x800035F8`,
  ROM `0x4080..0x41F8`: exactly `0x178` / 376 executable bytes with no
  padding before `func_800035F8`;
- the configured `NON_MATCHING` V0 is also 94 words, matches 41 words, and
  differs in 53 raw and relocation-masked positions from entry; its `0x48`
  frame is 24 bytes larger than the target's `0x30` frame;
- workbench classifies the residual as structure mismatch with four aligned
  structural, 31 register, and 15 constant differences. The target and
  candidate `amSndStop` call are displaced by one word; this is a real CFG and
  allocation divergence rather than a relocation-only residual;
- target and candidate each have five static relocations. All five candidate
  identities resolve; four sites align by offset, type, and stable identity,
  while the displaced `R_MIPS_26` call prevents exact relocation proof;
- the sole source caller passes the computed point, volume, pitch, pan, and
  effects values when the point's update group is nonzero. Adjacent update and
  removal routines corroborate the 16-byte `AudioUpdateEntry` layout;
- `skeleton_scan.py similar` fails closed because this resident symbol has no
  `size:0x..` ownership row. The permitted JFG peer remains assembly-only, so
  no new source donor was authenticated;
- the authorization allowed fresh V0 and structured finalization only. Old
  source forms, the flag lattice, permutation, fake carriers, and C-body
  mutation remained closed and were not repeated.

Next lever: add independently authenticated resident size ownership to unlock
the donor oracle, or obtain procedure-scoped compiler evidence that names the
frame/web divergence. Do not reopen source forms without one of those new
witnesses.
<!-- plateau-handoff:func_80003480:end -->
