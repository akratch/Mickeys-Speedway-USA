<!-- plateau-handoff:func_80003480:start -->
### `func_80003480` plateau handoff

- source: `src/main/audio_manager_36D0.c`
- score: 53/94 words
- frame: 0x48
- relocations: 5
- first mismatch: +0x0
- summary: Final-cursor controls leave the 0x48 frame unchanged; named proc-15 draw evidence still does not reach the target 0x30 frame.

Authorized maintenance evidence on layered base
`eb8f485d2c44178a2e7105fef0d1003b854b2aa3`:

- the unique fallback boundary is resident VMA `0x80003480..0x800035F8`,
  ROM `0x4080..0x41F8`: exactly `0x178` / 376 executable bytes with no
  padding before `func_800035F8`;
- the configured `NON_MATCHING` V0 is also 94 words, matches 41 words, and
  differs in 53 raw and relocation-masked positions from entry; its `0x48`
  frame is 24 bytes larger than the target's `0x30` frame;
- workbench classifies the residual as structure mismatch with four aligned
  structural, 31 register, and 15 constant differences. The target and
  candidate `amSndStop` call are displaced by one word; this is a real CFG and
  allocation divergence rather than a relocation-only residual;
- target and candidate each have five static relocations. All five candidate
  identities resolve; four sites align by offset, type, and stable identity,
  while the displaced `R_MIPS_26` call prevents exact relocation proof;
- the sole source caller passes the computed point, volume, pitch, pan, and
  effects values when the point's update group is nonzero. Adjacent update and
  removal routines corroborate the 16-byte `AudioUpdateEntry` layout;
- `skeleton_scan.py similar` fails closed because this resident symbol has no
  `size:0x..` ownership row. The permitted JFG peer remains assembly-only, so
  no new source donor was authenticated;
- the authorization allowed fresh V0 and structured finalization only. Old
  source forms, the flag lattice, permutation, fake carriers, and C-body
  mutation remained closed and were not repeated.

Next lever: add independently authenticated resident size ownership to unlock
the donor oracle, or obtain procedure-scoped compiler evidence that names the
frame/web divergence. Do not reopen source forms without one of those new
witnesses.

#### 2026-09-13, lane l1: measured schedule controls

Fresh procedure-15 baseline is 94 words at delta zero, with 53 masked
differences from entry. Alignment is 46 exact, thirty naming, eleven immediate
and six paired structural rows, plus one candidate-only and one target-only
word. The candidate frame remains 0x48 against target 0x30; both objects have
five static relocations. Sixteen draws and 156 emissions are measured.

The new named Ucode receipt supplies the procedure-scoped witness requested
by the preceding maintenance note. Two final-cursor controls test whether
removing the selected-pointer lifetime releases its frame or temporary. Folding
the final subtract-three into the selected-entry address leaves stock text
byte-identical: sixteen draws in the same order, with one extra location
emission overall. Reusing the completed entry cursor instead of selected also
leaves sixteen draws and their sequence unchanged, but worsens alignment to
44 exact words. Neither changes the frame or the 53 masked floor. Both are
rejected; the original guarded body remains. These controls do not unlock the
frame axis. Stop early with the preceding body/flag evidence still closed;
the next action is a named spill-home/allocation witness or independently
proved ownership enabling the donor oracle, not another cursor spelling.

Named Ucode mapping and full stock/capture fidelity pass for the baseline and
each retained experiment. Sources, stock objects, scores, frame/relocation
censuses and aligned deltas remain under ignored build/l1/func_80003480. Commands:
configured compilation, allocator_trace_receipt mapping, draw_census profile
and comparison, residual_map object comparison, finalize_plateau, and
tools/gates.sh verify cleanroom check-docs. No matching credit is claimed.

<!-- plateau-handoff:func_80003480:end -->
