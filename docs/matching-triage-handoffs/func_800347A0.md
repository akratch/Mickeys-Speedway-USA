<!-- plateau-handoff:func_800347A0:start -->
### `func_800347A0` plateau handoff

- source: `src/main/textures_35024.c`
- score: 2 differing words
- frame: 0x30 (target 0x28)
- relocations: 7
- first mismatch: +0x0
- summary: Fresh remeasurement confirms exact 43-word geometry and seven aligned relocation identities; only symmetric 0x30-versus-0x28 frame immediates differ.

Fresh configured remeasurement on base
`b37238606283cd5f6e6638b9e971a453a974d68f` proves the resident owner at
`0x800347A0..0x8003484C` / ROM `0x353A0..0x3544C`, with no padding. Fourteen
runtime overlay calls authenticate its resident identity. The target and best
candidate are both 43 instructions and save `ra` at `sp+0x14`; all seven static
relocation offsets, types, and identities are exact.

The original direct-struct loop was 16/43 words, first `+0x30`, despite an
exact `0x28` frame. The JFG-shaped two-word cache index, expressed against
Mickey's raw `s32`/pointer table rather than JFG's non-equivalent typed draft,
reproduces every target opcode and register and improves to 41/43. The sole
differences are the symmetric stack adjustments: target `0x28`, candidate
`0x30`. The candidate's SHA-1 is `a988205d2a34`.

Ten bounded forms isolated the frame mechanism. Removing the shared sentinel
adds an instruction and breaks the downstream registers. Reusing the loop
index or eliminating the named table index shrinks or reshapes the function
and loses exact allocation. `register`, block-scope, and narrow sentinel forms
either compile identically at frame `0x30` or regress. A repeated-expression
CSE form preserves all 43 instructions and registers but moves the one call
spill from `sp+0x18` to `sp+0x1C`, leaving four operand differences.

The retained source is the 41/43 allocation-exact form. Reopen only with
producer/whole-TU evidence identifying the unused automatic home that
rounds the non-save frame from 36 to 44 bytes; do not repeat local ablation,
scope, `register`, narrow-width, index-reuse, or repeated-expression forms.
<!-- plateau-handoff:func_800347A0:end -->
