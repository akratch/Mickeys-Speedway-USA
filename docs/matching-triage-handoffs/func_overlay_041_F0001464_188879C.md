<!-- plateau-handoff:func_overlay_041_F0001464_188879C:start -->
### `func_overlay_041_F0001464_188879C` plateau handoff

- source: `src/overlays/o041/overlay41ProcessEntry.c`
- score: 3/123 words
- frame: 0x48
- relocations: 5
- first mismatch: +0x10
- summary: Fresh V0 remains five words oversized with 120 differences; displaced call proxies lack an exact witness, so no new mechanism is authenticated.

- geometry: The owned Overlay 41 target remains `+0x1464..+0x1650`, ROM `0x188879C..0x1888988`, `0x1EC`/492 bytes/123 words. The configured candidate is `0x200`/512 bytes/128 words and overruns the owner by `0x14`/five words; `overlay41AddSlot` begins immediately afterward.
- ABI/flags: `void func_overlay_041_F0001464_188879C(Overlay41Input *, Overlay41Context *, s32)` uses Overlay 41 game-code `-O2 -mips2 -32`. Target and candidate frames are both `0x48`.
- relocation proof: Target and candidate each have five `R_MIPS_26` records. Target sites are `+0xC8`, `+0x14C`, `+0x16C`, `+0x1A8`, and `+0x1CC`; candidate sites are `+0xD8`, `+0x160`, `+0x180`, `+0x1BC`, and `+0x1E0`. No site aligns.
- diagnosis: Preflight and summarized WB proof fail closed because the candidate escapes authenticated ownership. The bounded raw report confirms 120 target-word differences from `+0x10`, 114 opcode mismatches, five excess instructions, and an exact frame.
- caller/donor evidence: Two `overlay41UpdateCurveObject` calls authenticate the three-argument ABI. Fresh donors remain weak: Perfect Dark `menuitem_tick` leads at 0.046 Jaccard; no result supplies source or relocation evidence.
- proxy recheck: The four proxy names occur only in this unmatched function. The dormant rebind specification is keyed to target offsets and is not used by this object's configured postprocess, which only trims metadata to `0x1EC`; it cannot authenticate displaced candidate sites.
- authorization: Integrated authorization `d76d840dfd984fdcb066a0db85d5ab5b28d8ab5a` permits only fresh V0 and structured finalization. The guarded C body, prior source forms, flags, and permutation remain closed and unchanged.
- next action: Reopen only if independent exact ownership evidence authenticates the call proxies or a genuinely new source mechanism removes the five-word excess while naturally aligning the call sites.
<!-- plateau-handoff:func_overlay_041_F0001464_188879C:end -->
