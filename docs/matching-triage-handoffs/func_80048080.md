<!-- plateau-handoff:func_80048080:start -->
### `func_80048080` plateau handoff

- source: `src/main/fx.c`
- score: 43 differing words
- frame: 0x48
- relocations: 4
- first mismatch: 0x74
- summary: JFG efd5abb remains assembly-only; zero source attempts. Need new transform-loop load/spill order evidence.


Reopening audit (2026-09-08), evidence D: PROVENANCE inspection of Jet Force
Gemini public decomp `src/fx.c` and `src/fx.h` at
`efd5abb1c79636e297b831f7c2d5bf47eac39c0c` found no new target C body.
No donor source, names or values were adopted. Mickey remains authoritative.

Configured full-TU measurement: candidate 352 bytes / 88 words,
target 356 bytes / 89 words; size delta -4 bytes.
Raw and relocation-masked differences are 43 and 43, respectively,
first mismatch +0x74; candidate/target frames are
0x48/0x48. Candidate/target static relocation counts are
4/4; 4 tuples agree in function-relative offset, type and symbol.
These are fallback-object comparisons, not linked-C promotion evidence.

Workbench comparison: `structure-mismatch`. Diagnosis: `mixed(structural:9, register:6)`;
playbook `structure-buckets`, lever `none-known`. The named guides were read.
This heuristic routing supplies no new donor evidence to reopen exhausted forms.
Next concrete lever requires new transform-loop donor C explaining the load/spill order after the already-tested R4300 hazard mode,
then a fresh gate and configured full-TU comparison.

Stopping evidence: zero source attempts; the authorized donor mechanism supplies
no new implementation. This is the assignment's early-exhaustion stop, not a
five-attempt stall. The prior TU lattice is not repeated: since its recorded
`func_8004ACC4` audit at `4be95a3d`, this TU differs only in EOF handoff comments.
See [the anchor donor audit](func_80049E4C.md) for the full delta and batch disposition.

Ignored evidence is retained in `build/tu-fx-audit/` and `build/wb/`: source and
full-TU object, gate receipts, summaries, diagnoses and relocation tuples.
Commands: `lane_status.py --symbol`, `wb_compare.sh --summary-json`, workbench
`diagnose` and `guide`, and `finalize_plateau.py`. The unchanged guarded C stays
NON_MATCHING and receives zero new exact bytes.

Prior plateau evidence retained (historical measurements and exhausted forms):

- configured result: 46/89 exact words (43 differing), 352/356 bytes
- configured proof: frame 0x48 exact; 4/4 relocation offsets, types, and identities exact
- baseline: 4/89 exact words (85 differing), 336/356 bytes, first
  mismatch +0x0, frame 0x78, and 2/4 exact relocation tuples
- retained improvement: direct narrow angle parameters, the post-decrement
  old-count carrier, the third trigonometric result's stack lifetime, and
  volatile pointer writeback close four instructions and move the first
  mismatch by 0x74 bytes
- exhausted diagnostic: the 119-combination flag lattice has no exact result;
  `-O2 -mips2 -32 -Wab,-r4300_mul` is exact-sized at 89 words with 77/89
  exact. `tools/tu_flag_impact.py` subsequently proved all 22 guarded `fx.c`
  consumers under that delta: four improve, one regresses, 17 are unchanged,
  and no masked-exact function is gained or lost. The mixed TU-wide result
  does not justify a Make policy change; retain the flag only as a diagnostic
  scheduling clue for this source shape.
- attempts: direct parameter and cursor models, countdown spellings, result
  spill lifetimes, pointer stack homes, and input-load ordering. No generic
  permuter was run.
- remaining blocker: the configured mode omits one target scheduling word;
  after the R4300 hazard mode supplies it, twelve load/spill/register-order
  words remain.
- JFG usefulness: JFG's assembly-only `func_8006A224` confirms the same
  cone-point transform role, 0x48 frame class, four trigonometric calls, and
  packed output-record loop. Its 444-byte guarded conversion path and fixed
  alpha differ, so no JFG source was adopted. The retained Mickey candidate
  is a useful C scaffold for JFG maintainers specifically when reconstructing
  that transform loop, cursor countdown, and packed color/alpha writes; it is
  not an exact result for either project.

<!-- plateau-handoff:func_80048080:end -->
