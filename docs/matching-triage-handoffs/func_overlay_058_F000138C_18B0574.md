<!-- plateau-handoff:func_overlay_058_F000138C_18B0574:start -->
### `func_overlay_058_F000138C_18B0574` plateau handoff

- source: `src/overlays/o058/func_overlay_058_F000138C_18B0574.c`
- score: 3693 differing words
- frame: 0x158
- relocations: 1285
- first mismatch: +0x0
- summary: 244 calls recovered; configured mips1 excludes 38 target branch-likely opcodes. Next: authorized ISA-context review on reconstructed C.

## Structural reconstruction, 2026-09-08

The prior configured candidate was 118 instructions short, with 613 text
relocations and a `0x2F0` frame. The guarded source now contains a Mickey-only
structural reconstruction; it remains `NON_MATCHING`. No matching credit is
claimed. The best admissible structural candidate is the retained source,
not an earlier lower scalar with missing identities or arguments.

Configured attempts 18 and 19: 3747 candidate words against 3614 target
words, 3693 relocation-masked and 3696 raw differing words, first mismatch
`+0x0`. Candidate frame is `0x158`; target frame is
`0x138`. The earlier `0x2F0` frame described the original candidate,
not the target. The candidate has 1285 text relocations against the
target's 1253; only 10 have exact offset/type/identity agreement.

### Evidence and reconstruction

- Decoded the owned runtime relocation surface using Mickey overlay-table
  exports and the reserved resident section selectors. The identity-resolved
  diagnostic assembly reference reproduces all 14,456 owned ROM bytes. This
  authenticates the reference, not the compiled C.
- Recovered 25 distinct callee identities and all 244 ordered calls. Static
  call-event analysis agrees on all 245 successor sets and all 13 switch-entry
  mappings. It deliberately abstracts branch predicates and memory effects;
  it is not a semantic-equivalence or relocation-exact proof.
- Recovered race entries, save records, language text, texture descriptors,
  animation-path state and colour-cycle accesses from Mickey source, headers
  and instructions. Race entries use a 40-byte stride, a halfword rank, and
  byte counter/flag fields. Coordinate tables are signed halfwords while
  arithmetic coordinate locals are full words. Callee contracts also prove
  the four-byte controller-map array and sentinel-terminated texture-node
  array; declaring their addresses as scalar objects was a draft limitation.
- Recovered backing-array identities for portraits, colour channels, text
  positions and coordinate tables. The latter use a player-count-minus-one
  index; a relocation addend pointing before an array is not its declaration.
- Restored the signed level argument at all three `levelGetBlurEffect` calls.
  Corrected draft snapshots that lost post-call or post-loop global reloads
  in transitions, result tables, the selected-name cursor and position clamps.
  Earlier smaller drafts with these errors are diagnostic artifacts, not
  admissible alternatives to restore for their scalar scores.
- Reconstructed nested loops, short-circuit selection, and common index and
  coordinate lifetimes as ordinary C. No flags, permuter, instruction patches,
  dummy padding, or invented observable operations were used.

### Remaining deficit and early stopping evidence

The workbench still reports `structure-mismatch`. The current net excess is
133 words; the instruction census contains 142 additional no-ops and no
branch-likely instructions, versus 38 branch-likely instructions in the target.
The candidate is 14,988 bytes against 14,456 target bytes: a remaining
532-byte excess. This is not exact geometry or a closed size residual.

The committed Makefile explains both relevant ISA mechanisms: its string-TU
rule records that IDO emits branch-likely only with `-mips2`; its main-game
rule records load-delay no-ops under `-mips1` and deliberately scopes the
`-mips2` override to `src/main/`. This overlay's configured compile still uses
`-O2 -mips1 -32`. The 38 target branch-likely instructions therefore rule out
exact output from ordinary C under the authorized configuration. No flag was
changed or swept in this packet.

Early stop after attempt 19 uses that committed evidence and the assignment's
mechanism boundary. It is not a claimed five-attempt stall: the structural
series continued to improve, and attempt 19 proved additional array contracts
without changing code bytes. Further source-only score tuning cannot remove
this necessary ISA mismatch. Source reconstruction and the measured blocker
are banked rather than substituting the expressly forbidden flag mechanism.

Narrow memory-operation counts now agree: byte loads, signed and unsigned
halfword loads, and byte/halfword stores. The three missing word stores are
first writes immediately overwritten at the case 3, 12 and 13 transition
boundaries. Both C assignments remain present; the configured optimizer drops
the first. Aggregate operation counts support the type audit but do not prove
semantic equivalence. Register allocation, ordinary word-load/store choices,
frame size and exact relocation placement remain unproved.

The next concrete lever is a separately authorized ISA/context parity review
on this reconstructed full TU, followed by workbench classification in the
proved configuration. This is structure-buckets lever 4, using the newly
recovered ABI and source context; it is not permission to repeat the old
collapsed-input sweep. Review the flag group's consumers before any canonical
flag change. New local data aliases and compiler-owned switch-table placement
also need canonical binding proofs before candidate linking or promotion.

The standard summary wrapper rejects this oversized candidate because it
escapes TU ownership. Measurements above use the configured full-TU object
with direct workbench comparison and decoded relocation records. No promotion
guard is waived. Ignored lane artifacts retain every measured source/object,
comparison, first mismatch, runtime identity record and call-event analysis.

The banked candidate passed `gmake verify`, `gmake check-nonmatching-builds` (243 TUs),
`gmake check-docs`, and `gmake cleanroom`. The ROM verification proves the
retained assembly fallback, not equivalence of the guarded C.
The stronger donor scan is currently blocked by its pinned JFG checkout:
observed `efd5abb1c79636e297b831f7c2d5bf47eac39c0c`, expected
`c82affffe8f11cb5b440cfa918f4582ad8573279`. No external checkout was changed.

### Reproduction and handoff

Files changed are the owned C file and this symbol-owned handoff shard.
Structural commit: `600ae225a8c1215824deaa7f0d279aca59780d52`. The final
metadata commit appends the tool-generated guarded plateau record and updates
this evidence; no source body is changed by that step.

The baseline used `tools/wb_compare.sh` and workbench `diagnose`, followed by
`guide structure-buckets`. `./generate_ctx.sh` succeeded; the standard m2c
wrapper failed on the unrelated unsized `Overlay8ScaleOutput.outputs` field,
so the draft used a focused context made from Mickey declarations. The
identity-resolved reference, generated drafts and analysis scripts remain
ignored under `build/whale-o58/` and `m2cfiles/`. They contain ROM-derived
evidence and must not be committed.

Each material attempt used the configured `gmake NON_MATCHING=1` object
rule, direct workbench comparison, decoded relocation-surface measurement
and a shared checkpoint with monotonic attempt count. Attempt artifacts 0
through 19 retain source, object and measurements; the final-validation
artifact preserves the same body with its EOF handoff metadata.
`tools/finalize_plateau.py` recorded 3,693 differing words, frame `0x158`,
1,285 candidate relocations and first mismatch `+0x0`, and passed its
source-only gates. No candidate has been linked as an exact result.
<!-- plateau-handoff:func_overlay_058_F000138C_18B0574:end -->
