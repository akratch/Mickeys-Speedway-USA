<!-- plateau-handoff:func_80008B94:start -->
### `func_80008B94` plateau handoff

- source: `src/main/objects.c`
- score: 126 differing words
- frame: 0xB8
- relocations: 45
- first mismatch: +0xA8
- summary: Workbench structure-mismatch: structure-buckets. Next: authenticate the optional multiplier home transfer and remaining floating-point allocation.
The configured full-TU baseline had 420 words against 419 owned words,
397 raw/masked differences, first +0xC, the exact 0xB8 frame, and 45
relocations per side with five authenticated identities. Its frame included
an unsupported unused volatile reserve. The retained candidate removes that
reserve and reaches the genuine 0xB8 frame, exact 1676-byte size, 293 exact
words, 126 raw/masked differences, first +0xA8, and all 45 relocation
offset/type positions. Authenticated identities are 43/45. It remains
guarded NON_MATCHING C and earns no new matching credit.

The live assignment gate initially failed on an unrelated integration-ledger
entry. No source was edited while it refused. A fresh zero-exit base-only
verdict opened the target after that ledger was repaired. The oracle supplied
only weak neighbors; no external C body was adopted. All 53 usable cached
TU flag objects remained nonexact; configured flags stayed unchanged. The
assembly-owned range was separately proved against linked ROM bytes.

Mickey's target restores both default colour commands, uses signed resource
indexing and a floating-point coordinate, rechecks the palette after its
getter, retains the current halved alpha, and captures player data before
camGetNo. The target alpha clamp was restored. A single mutually exclusive
colour ladder removed redundant control flow. Correct channel computation
order and normal stack-argument conversions recovered the colour-product
sequence. Scoped command pointers and the previously proved direct global
display-list increment recovered the relocation count. Scalar and pointer
homes were reconstructed without padding. A defined optional multiplier
carrier and an explicit palette else arm recovered the exact size; binding
both palette observations fixed the initial type-register mismatch. A common
white-channel value allowed the multiplier scale to return to a direct
expression while preserving the frame.

Attempt numbers are global to the lane. Baseline 172 precedes corrections
173–179, frame/carrier work 180–188, and the improvement at 191. The initial
typed-definition preparation failed against the existing void-pointer
prototype before comparison; it was corrected without counting a source
attempt. The final five probes 192–196 tested early null initialization,
its combination with the direct scale expression, resource/multiplier
declaration order, const qualification and the byte white-channel type.
The first two regressed; the others were flat. None resolved the remaining
mechanism. These five probes are the stall evidence. Cleanup 197 retained
attempt 191. Sources, objects, comparisons and rationale remain in ignored
build artifacts.

The workbench verdict is structure-mismatch, routed to structure-buckets.
Its instruction-shape lever now points to the optional multiplier transfer:
the target stores the nonnull lookup into its home and reloads the retained
pointer, while the candidate uses a defined null initialization and register
copy. This also reverses the resource and multiplier register roles. The
remaining floating-point allocation and temporary sequence follow that
transfer. Do not introduce an uninitialized C read to imitate the target's
unconditional machine load; authenticate the source lifetime and null-path
invariant first. Two relocation identities also need resolution before
promotion. Native command-word typing and address-word spelling were flat.

Validation: configured full-TU comparison, diagnosis and guides, cached flag
rescoring, linked fallback ownership proof, finalize_plateau.py, full gmake
verify, check-docs, cleanroom and check-nonmatching-builds. The ROM verification
uses the assembly fallback for this function; the C residual remains.

<!-- plateau-handoff:func_80008B94:end -->
