<!-- plateau-handoff:func_80018F08:start -->
### `func_80018F08` plateau handoff

- source: `src/main/lights.c`
- score: 74 differing words
- frame: 0x58
- relocations: 7
- first mismatch: +0x0
- summary: Proc 12 has 73 draws. Compact frame/slots are exact but alignment regresses; alias and scope controls leave the draw sequence unchanged.


### g1 draw-difference and frame receipt (2026-09-13)

The configured baseline is retained: 820 bytes, zero size delta, 74 masked
positional differences; alignment gives 138 exact, 52 naming, seven immediate,
and three structural paired rows, with five candidate-only and five target-only
words. The frame remains 0x58 versus 0x48. The seven relocation identities were
proved by the inherited candidate; this packet does not claim a new linked match.
No colour sweep was repeated.

The inherited zero-draw diagnosis was for the wrong procedure. The Ucode receipt
maps this function to procedure 12 of 28. Its stock and traced complete-TU text
are identical, and its baseline has 73 draws and 292 emission events. The residual
occupies seven 0x80-byte windows, with a closed ring in the second window; it is
not confined to two windows. The near-match query could not resolve the target's
missing symbol-size field, so no new donor claim is made.

Attempts 36–43 removed redundant typed-input, direction-address and coordinate-
offset carriers, then reused disjoint early index/late colour and early segment/
late direction lifetimes. Single removals left the frame at 0x58; removing the
input and offset aliases reached 0x50. The combined reuse reached 0x48, and placing
the rotation array first made all nine used stack offsets agree with the target.
This diagnostic still has 78 masked differences and only 134 aligned exact rows:
57 naming, two immediate and seven structural rows remain. Its 73 draws and draw
order did not change. Matching stack geometry therefore does not establish the
required temporary schedule. Owner-pointer snapshots across calls were preserved.
These compact candidates are ignored diagnostics, not the retained source.

Attempt 44 was a generator-boundary compile failure, repaired before attempt 45;
it supplied no matching evidence. Attempt 45 instead generated direction addresses
and reused the original index for the colour byte, keeping the segment pointer
separate. It reached the target frame and removed one insertion/deletion pair but
regressed to 79 masked differences and 131 aligned exact rows. Attempts 46–48 moved
the early locals, colour scalar, and final rotation/direction into their owning
scopes. They preserved all 73 draws and their order; the frame stayed 0x58, while
later controls merely shifted the rotation-array homes. Attempt 49 reused the
colour scalar for the early coordinate offset: unchanged draws, 76 differences.
Attempt 50 spelled the bounded nonnegative stride as shifts and subtraction:
the intended line gained two draws, giving 75 draws and 98 masked differences.
It moved the schedule but worsened alignment by 26 paired rows.

Attempts 51–53 promoted the signed index carrier, removed a redundant signed
segment-index cast, and reversed the stride multiplication operands separately.
All three produced the baseline text and unchanged draw sequence. These normalized
controls add no new mechanism or improvement; stop on this measured stall.
The best baseline, compact-frame alternatives, failed-control logs, source
snapshots, objects, census JSON and aligned maps remain under
`build/g1/func_80018F08/`. The declaration/frame axis can move; the tested alias,
scope and normalization edits cannot move the draw sequence. A next packet needs
a source form that changes the early ring schedule without sacrificing the
existing alignment, then must re-prove frame, relocations and linked owned bytes.

Commands: `build/g1/map_proc.py`, `allocator_trace_receipt.py --map-only`,
`build/g1/measure.py` (configured compile, census and aligned residual),
`draw_census.py --compare`, `residual_map.py --object --against`,
`tools/gates.sh verify`, and `tools/finalize_plateau.py --commit`.

<!-- plateau-handoff:func_80018F08:end -->
