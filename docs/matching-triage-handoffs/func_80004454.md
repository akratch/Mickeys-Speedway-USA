<!-- plateau-handoff:func_80004454:start -->
### `func_80004454` plateau handoff

- source: `src/main/objects.c`
- score: 3 differing words
- frame: 0x88
- relocations: 6
- first mismatch: +0x50
- summary: Reopened index/cursor probe regressed to 55 words; permuter 15-word scratch result did not transfer. Baseline remains 3 register words.

The retained configured candidate owns 316 bytes / 79 words at resident
`0x80004454..0x80004590`, ROM `0x5054..0x5190`. Stock IDO produces the target
`0x88` frame, with 3 raw and relocation-masked differing words from `+0x50`.
All six relocation offsets, types, and identities now align. This corrects
the previous handoff: its six-word candidate had only five aligned relocation
sites because the global-address low relocation moved with the schedule.
The assembly fallback remains canonical; this is not matching credit.

The bounded source-equivalent search improved the strict permuter score from
85 to 15. Grouping the initial byte-offset/pointer assignments with the loop
header fixes the two scheduling words; reversing the pure equality operands
fixes the branch-operand word. Both changes reproduce in the configured TU.
The previous unused volatile frame reserve is replaced by two used coordinate
carriers, preserving the target frame and floating-point register shape.

Workbench routing is now `register-permutation`; forced-color-oracle lever
19 applies to the initial list-index web. Named Ucode maps the isolated
procedure to ordinal zero. The trace-off instrumented object passes section,
relocation, and symbol fidelity against stock IDO. All 13 recorded coloring
decisions are in p1; there are no p2 decisions. Forcing the measured initial
index web to its target color yields zero differing words, but that object is
diagnostic only and is never promoted.

The source follow-ups retain the same residual: a properly typed object-list
return, a local pointer to the start output, a named initial induction carrier,
and a two-element bounds array. The address-pointer form also enlarges the
frame to `0x90`; the other forms retain `0x88`. These probes establish no source
lever for the forced coloring. Do not repeat those forms or the declaration,
commutative-operand, condition, struct-reference, compound-assignment, and line
grouping search without changed evidence. The TU flag lattice was run once;
53 usable cached objects were rescored, none improving the original six words.
The remaining 66 configurations were preparation failures, not scored results.

The last residual improvement was the permuter line/equality candidate.
The subsequent return-pointer, address-pointer, induction-carrier, and bounds
array probes neither improve that residual nor identify a new causal source
handle for the allocation. The three-word initial-index residual persists
through all four, meeting the stall rule. The forced oracle proves the ceiling
and owning coloring decision; it does not prove natural source reachability.

Next lever: require new source/def-use evidence that changes initial-index
color preference or interference before restarting forced-color-oracle lever
19. Preserve call ABI, the two output reads, byte-offset arithmetic, and the
six relocation identities. No dummy padding or forced output is admissible.

Local evidence is retained under the ignored `build/m6-objects/func_80004454/`
folder: calibrated compiler input and context guard, all emitted permuter
improvements, stock/trace/forced objects, source probes with hypotheses, and
configured summaries. `tools/finalize_plateau.py` maintains the source guard
and this symbol-owned handoff. No donor source was adopted: the bounded nearest
JFG result is assembly-backed, including upstream revision `efd5abb`.
The search finished at its declared 20-minute cap (1,202 seconds including
shutdown), with its final improvement at 22 seconds and no later improvement.
Validation passed: `tools/wb_compare.sh --summary-json func_80004454`,
`gmake -j14 verify`, `gmake check-nonmatching-builds` (246 TUs),
`gmake check-docs`, and `gmake cleanroom`. The canonical ROM hash remains exact
through the assembly fallback. No source-only partial score is called a match.

#### The a0-versus-a1 residual, and what does not move it

The whole remaining deficit is one caller-saved colour on one web: the target
takes `a1` at every site, the candidate `a0`, with identical opcodes,
schedule, frame, constants and relocation identities. `func_80009AA8` in the
same translation unit shows the same thing on an unrelated web, so this is a
property of the compilation rather than of any one source shape. Measured
against the real full-TU object in this lane and eliminated:

- Comparison spelling at the use, including L87's `field == (group ^ 0)` on
  both operands, the reversed pure comparison, `!(a >= b)`, `!= 0` versus a
  bare truth test, splitting the `&&` into nested `if`s, and swapping the two
  `&&` terms. Best result unchanged; the reversed pure comparison and the
  swapped `&&` both regress, consistent with L67.
- Dead-store colour reservation (L87's `value = 0` clause), assigned to each
  existing local before the call and to a fresh local. The existing locals
  merge into their own later webs and change nothing; a fresh local moves the
  frame instead.
- Naming the loaded bound, the loaded start, or both as extra locals; each
  costs the exact frame.
- Dropping the m2c `temp_v0` return carrier, retyping it as `void **`,
  parenthesising the pointer sum differently, hoisting the initial index
  multiply out of the guard, and writing the shift instead of `* 4`.
- The floating-point argument copies: dropping `queryX`, `queryY` or both in
  favour of the parameters, swapping their assignment order, swapping their
  declaration order, moving them after the call, and adding a third for
  `arg2`. Only the base and "assignments after the call" hold at three words.
- Splitting the preheader's two assignments off the loop-header line, which
  costs two words and confirms the recorded line-grouping result is at its
  optimum rather than merely at a local one.

Next lever is unchanged in kind but sharper in scope: find what reserves `$a0`
in the retail build. It is not a source-level web this lane can create.
#### Confirmed exhausted on 2026-09-09; lever 50 does not apply

Remeasured unchanged: 79 words, frame `0x88`, 6/6 relocation identities,
three register words from `+0x50`. The single web is the initial list index
read back from `124(sp)` after `func_8000572C`, taking `a0` here and `a1` in
the target at the load, the `slt` against the bound, and the `sll` that forms
the byte offset.

Lever 50 (an unrelocated literal address pair scoring as a false residual) was
checked first and does not apply: the workbench reports `aligned_constant = 0`,
`register_mismatches = 3`, and all six relocation offsets, types and identities
resolved, which is the opposite of that signature. Lever 51 does not apply
either -- the hand-written `u8 *` cursor beside the byte index is exactly the
pair of induction variables the target emits, and both registers already match.

The sibling `func_80009AA8`'s refreshed shard carries the dead-local carrier
family that was newly eliminated today; the mechanism is shared, so treat that
result as covering all three of this TU's register-only rows.
<!-- plateau-handoff:func_80004454:end -->
