<!-- plateau-handoff:func_80004454:start -->
### `func_80004454` plateau handoff

- source: `src/main/objects.c`
- score: 6 differing words
- frame: 0x88
- relocations: 6
- first mismatch: +0x50
- summary: Fresh remeasurement confirms exact 79-word geometry, 0x88 frame, and six aligned relocation identities; six schedule/register words await the bounded permuter.

Configured remeasurement on base `c09ddb83d2b3233904155b37e21a35a231bda135`
confirms the historical scalar evidence against the current source:

- the resident owner is `0x80004454..0x80004590` / ROM
  `0x5054..0x5190`, exactly `0x13C` bytes with no boundary ambiguity;
- configured IDO emits exactly 79 instructions and the target's `0x88` frame.
  Six words differ from `+0x50`: the workbench classifies two as scheduling
  and four as register allocation, with no floating-point residual;
- target and candidate each carry six relocations. All records align by
  function-relative offset and type, and all six symbol identities resolve;
- the guarded body and its eight-byte compiler frame reserve remain unchanged.
  The assembly fallback stays canonical during this maintenance pass.

The next bounded mechanism is the previously reserved permuter sweep over
source-equivalent declaration and expression order. Preserve the fallback
unless untouched configured IDO output becomes byte-identical and the linked
range plus full ROM verify.
<!-- plateau-handoff:func_80004454:end -->
