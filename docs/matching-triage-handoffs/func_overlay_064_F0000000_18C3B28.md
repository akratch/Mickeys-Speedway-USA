<!-- plateau-handoff:func_overlay_064_F0000000_18C3B28:start -->
### `func_overlay_064_F0000000_18C3B28` plateau handoff

- source: `src/overlays/o064/overlay64GenerateTexture.c`
- score: 401/420 words
- frame: 0x78
- relocations: 18
- first mismatch: +0x0
- summary: Home ladder now matches the target's; the residual 8 bytes is one extra compiler temporary, not a declaration.

## 2026-09-11 the 8 bytes are a compiler temporary, not a declaration (lane `lane/o11-frames`)

Adopted: a declaration reorder, byte-inert.  401 masked, 94 aligned byte-exact,
190 naming, 23 immediate-only, 132 really-different, size delta 0, before and
after.  What it buys is the home ladder.

**The frame identity.**  Censusing both objects' stack slots, the argument build
(0x00..0x0F), the single `ra` save at 0x14 and the 64-byte declaration block are
the same on the two sides.  The whole 8-byte difference is the region between
the save and the block, which holds the compiler temporaries: this candidate
materialises five, the target four.  The two sides' temporary traffic is
otherwise in lockstep -- the first four spill and reload at the same points in
the same order -- and the fifth is a `width * 2` spill late in the function.
The target puts that spill in the slot its first temporary vacated; this
candidate opens a new one.

**The home ladder now matches.**  With `newSelect`, a `u8`, declared second, the
four-byte local after it aligns to -12 and the six homed locals land on the
target's own offsets from its frame top:

- -4 width, -12 height, -16 selected, -40 write, -56 x, -64 distance

which fixes the declaration list at sixteen entries in the order recorded in the
source.  The previous order had them at -4, -8, -12, -24, -40, -44.

**A warning for the next lane.**  Three different single-declaration removals
were measured, and every one of them closes the frame to 0x70:

- merging `pixels` into `dest`: 398 masked, 100 exact, 189 naming, 21 immediate,
  129 different, delta 0
- merging `selected` into `source`: 393 masked, 82 exact, 202 naming, 7
  immediate, 151 different, delta 0
- merging `below` into `selected`: 393 masked, 86 exact, 202 naming, 10
  immediate, 144 different, delta +4

All three close the frame by making the block *smaller than the target's*, which
moves every home off the ladder above.  A closed frame is not the objective here;
the block is already the right size.  The lever for the remaining 8 bytes is one
fewer spilled value, which is downstream of the 190 naming rows, not a source
spelling.
<!-- plateau-handoff:func_overlay_064_F0000000_18C3B28:end -->
