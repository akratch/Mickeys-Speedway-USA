<!-- plateau-handoff:func_80055104:start -->
### `func_80055104` plateau handoff

- source: `src/main/anim.c`
- score: 425 differing words
- frame: 0xD0
- relocations: 23
- first mismatch: +0x0
- summary: Target 445 words and frame 0xB8; relocation identities agree, but stack lifetimes drift. Add resident size metadata to unlock donor and flag tools.

Assignment evidence:

- The initial `lane_status` result was `stale-ledger/source-identity` because
  this symbol was a direct bare `GLOBAL_ASM`, not because it had an exhausted
  C candidate. The Session A coordinator explicitly approved that override.
- Canonical inventory authenticated the unique mapping
  `src/main/anim.c:func_80055104` to
  `asm/nonmatchings/main/anim/func_80055104.s`, 445 words, with no active or
  committed exact-symbol lane.
- The scheduled scratch draft was absent in this isolated worktree, so the
  candidate was regenerated with the repository m2c tool and then typed from
  Mickey-local ABI, call-site, field-offset, and relocation evidence.
- Candidate and target each have 23 relocations with identical type and symbol
  multisets. Their offsets differ because the candidate is 429 words with a
  `0xD0` frame while the target is 445 words with a `0xB8` frame.
- Both the donor oracle and full flag sweep currently fail closed because the
  bare resident symbol lacks a `size:` ownership entry in
  `symbol_addrs.us.txt`. Adding authenticated resident size metadata is the
  concrete unlock before another source-shape campaign.
<!-- plateau-handoff:func_80055104:end -->
