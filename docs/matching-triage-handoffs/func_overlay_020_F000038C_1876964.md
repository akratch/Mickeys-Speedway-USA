<!-- plateau-handoff:func_overlay_020_F000038C_1876964:start -->
### `func_overlay_020_F000038C_1876964` plateau handoff

- source: `src/overlays/o020/func_overlay_020_F000038C_1876964.c`
- score: 256 differing words
- frame: -0x38
- relocations: 3
- first mismatch: +0x0
- summary: Target is 270 words/frame 0x40 versus 264/frame 0x38; all three call relocation sites remain shifted.

Fresh evidence on base `2fa4cf591ba9944b10bc01e430830d36d46585c7`:

- authenticated owner: overlay 20 `.text` offset `0x38C..0x7C4`
  (`0x438` / 1,080 bytes), exactly 270 executable words with no target
  padding before the next owner at `0x7C4`;
- ABI: `Overlay20InitGrid *(Overlay20InitGrid *)`; the runtime export is ORT
  1691 and one `R_MIPS_26` inbound reference was found in overlay 20 at
  `+0x1E4`;
- configured V0: IDO `-O2 -g3 -mips2 -32`, 264 candidate words against 270
  target words (`-24` bytes), 256 raw and masked differing words from entry,
  and a `0x38`-byte frame against the target's `0x40`. This freshly
  reproduces the retained ranking rather than relying on it;
- workbench classified 91 aligned structural, 76 register, and two constant
  residuals, with 78 alignment gaps. The first allocator divergence is the
  second saved-register pool slot, consistent with the missing `s7`/frame
  lifetime recorded by the earlier cap;
- relocation surface: target and candidate each have three resolved call
  records, but none align by offset/type. Candidate sites at
  `+0x6C/+0x1B0/+0x3E8` remain shifted from target sites at
  `+0x78/+0x1C0/+0x3F8`, and exact effective identities are not established;
- donor scan: the best available DKR/BK masked 4-gram similarity was `0.050`
  (`obj_loop_fogchanger`), with no credible donor or sibling mechanism;
- the historical ten-form cap, full flag lattice, constant audit,
  texture-lifetime, cursor, command/store-order, and saved-register probes are
  already exhausted. No route was repeated without new natural evidence.

Next lever: recover a new source-authentic texture/index lifetime that keeps
the target's extra saved-register web alive, producing the `0x40` frame and
six missing instructions before revisiting call-placement identities or a
bounded batch.
<!-- plateau-handoff:func_overlay_020_F000038C_1876964:end -->
