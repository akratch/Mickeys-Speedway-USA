<!-- plateau-handoff:func_80041CE4:start -->
### `func_80041CE4` plateau handoff

- source: `src/main/particles.c`
- score: 27 differing words
- frame: 0x80
- relocations: 9
- first mismatch: +0x48
- summary: six integer webs, no consistent rotation. An inert index aid reaches 10/153 with the frame exact; permuter base 150 transfers but stalls at 60.

- Residual identity (2026-09-09): configured full-TU C is 153/153 instructions,
  frame `0x80`, all nine relocation tuples exact, zero opcode and zero
  floating-point differences. Every differing word is an integer register
  substitution. The workbench resolves them into six consistent webs of 6, 8,
  4, 3, 3 and 4 sites; class `uopt-globalcolor`, pool-rotation family.
- Reachability proved: an **inert allocation aid** at the inner loop's index
  initialiser -- the index assigned a shift-and-add expression multiplied by
  zero, so the stored value and every observable effect are unchanged --
  closes four of the six webs and reaches **10/153 words with the frame,
  instruction count and every relocation still exact**. The aid is not
  committed (project policy keeps unmatched candidates policy-clean; see
  docs/cleanup-queue.md), but it proves those four webs are pool-position and
  source-reachable, not an allocator wall.
- The aid's strength is graded by how many expression temporaries it creates,
  not by its value: one temporary gives 25 words, two or three give 10, five
  or more go back to 16. Four positions for the same aid (loop index, outer
  index, between the two display-list commands, before the texture call) all
  give the identical 10, so the last two webs cannot be reached by dead-web
  placement at all -- consistent with the workbench's rule that a dead web
  takes the next free slot and cannot reach past a live one.
- The two surviving webs are the outer-count carrier (target one colour
  higher than the candidate's) and the second display-list command's carrier
  (target two colours higher). Both are caller-saved, so they are coloured by
  the ascending-web-number sweep; reaching them needs a *live* web numbered
  before them, not a dead one.
- Flat and eliminated this pass, each measured: naming the second command's
  carrier separately, and writing both commands in the gbi `_g` block shape
  (both drop the register residual to 23 but grow the frame `0x80` -> `0x88`,
  so both are illegal -- one extra pointer local costs eight bytes of frame);
  calling the real `gDma1p` macro instead of writing the two command words by
  hand (identical to the `_g` blocks, same frame growth); naturalising the
  point walk to the tracked `points[]` array base, to a dereference, or to a
  post-increment (30 words, i.e. rotation in the wrong direction); indexing
  the array by the loop counter (+1 instruction); fusing or reordering the
  count-doubling and the vertex-address computation (flat); giving the texture
  helper a non-void return type, the workbench's named coalescing lever
  (flat, the result is unused so no web is created); splitting either
  `carrier = list++` into two statements (flat); the empty-condition inert
  form the workbench names, at four positions (24 to 40 words, all worse than
  the arithmetic aid); and the loop-index initialiser moved to five different
  positions.
- Permuter: base score 150 against a 27-word residual, and scores transfer at
  about 5.5 points per word -- its 60-point candidate is exactly the 10-word
  aid above. Restarted from that improved base it ran 40 minutes at `-j 3`
  and never beat 60. Note the scratch is only faithful once its
  `_permuter latedefine` pragmas are materialised into real `#define`s;
  compiled without that step it turns `gDPPipeSync` into a call and is one
  instruction short, which is a false residual, not a real one.
- ADR 0018 stall: the last five attempts (return-type coalescing, split
  increments, address-of-element respelling, truthy active test, aid
  placement sweep) each eliminated a hypothesis and none improved on 10.
- Next lever: a *live* carrier numbered before the outer-count web -- a
  natural value that stays live across the outer loop -- not another dead
  web, not more flags, and not another generic permuter batch from this base.
<!-- plateau-handoff:func_80041CE4:end -->
