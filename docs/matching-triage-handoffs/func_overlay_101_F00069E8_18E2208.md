<!-- plateau-handoff:func_overlay_101_F00069E8_18E2208:start -->
### `func_overlay_101_F00069E8_18E2208` plateau handoff

- source: `src/overlays/o101/func_overlay_101_F00069E8_18E2208.c`
- score: 641/963 words
- frame: 0x50
- relocations: 110
- first mismatch: +0x2C
- summary: 963 words, 644 raw/641 masked; five stalled attempts. Next: authorized causal trace of setup-count/coordinate register lifetimes.

## Structural reconstruction handoff

The configured full-TU baseline was rederived at assignment base
`0d21245897f0469ec630a49a07024b5e51ad04b4`: 967 candidate words versus
963 target words, 841 raw and 840 relocation-masked differing words, first
mismatch +0x2C, and matching 0x50 frames. The owner is overlay 101 text
+0x69E8..+0x78F4 (3852 executable bytes).

The retained candidate has 963 words, 644 raw and 641 relocation-masked
word differences, first mismatch +0x2C, and a 0x50 frame. This is compiled
NON_MATCHING C, with zero newly matched bytes. The configured comparison
reports 110 candidate and 110 runtime relocations, 97 matching offset/type
sites, 58 resolved candidate identities and 50 exact identities. The extracted
fallback object exposes 78 text relocations plus one procedure-metadata
relocation; its placeholder symbols are not
complete runtime identity evidence.

### Recovered evidence

- Tier B: this function's runtime table resolves ten calls to resident
  `func_800355A0`, four to `func_80036DD0`, seven local calls to
  `overlay101ByteLength`, and the final call to `overlay101Reset`. The four
  screen loads take one argument. Removing the four invented second arguments
  closes the entire 16-byte size overrun. The byte-length return is `s32`,
  corroborated by its matched local implementation.
- Tier B: ROM-exact `overlay101Cleanup` and `overlay101DrawSlots` provide
  independent relocation witnesses for the order-record base, order list,
  order count, two resource arrays and their counts. The root is a contiguous
  array of 0x1C records, including record zero; this function fills records
  one through six. Owner addresses retain their independently reconstructed
  aliases. Text inputs use byte pointers; opaque address-only data objects
  use byte-array declarations.
- The global-header m2c invocation failed on an unrelated unsized array in
  context. A Mickey-only draft without that context, followed by a typed
  draft using the recovered call/global identities and this TU's context,
  was retained solely in ignored `m2cfiles/`. No generated draft was adopted.
  A diagnostic target with the missing relocations restored reproduces all
  3852 owned ROM bytes under the original link placeholders. That receipt
  establishes target-input fidelity, not a candidate promotion.
- The actual IDO-preprocessed baseline passes context self-comparison. All
  candidates use the configured full TU and unmodified stock compiler.
  No flag lattice, permuter, instruction editing or allocator instrumentation
  was used. The donor scan-check failed on the JFG reference checkout's pin;
  no donor material was consulted or adopted. The local skeleton oracle was
  dominated by this function's own objects and supplied no donor evidence.

### Attempts and stopping evidence

- Attempt 1: Resolve calls, prototypes and true argument counts; 963 words, 693 raw / 690 masked differences.
- Attempt 2: Direct pre-call node32 count; 966 words, 920 raw / 919 masked differences.
- Attempt 3: Isolate first node from recurrent setup; 963 words, 699 raw / 697 masked differences.
- Attempt 4: Initial group height/width/mode stores; 963 words, 694 raw / 691 masked differences.
- Attempt 5: Corroborated global names and pointer types; 963 words, 693 raw / 690 masked differences.
- Attempt 6: Node32 initializer in record field order; 963 words, 644 raw / 641 masked differences.
- Attempt 7: Shared coordinate narrowed to destination width; 963 words, 644 raw / 641 masked differences.
- Attempt 8: Complete root-record array; 963 words, 644 raw / 641 masked differences.
- Attempt 9: Distinct setup-count local; 968 words, 928 raw / 928 masked differences.
- Attempt 10: Node32 old-head snapshots and publication; 969 words, 909 raw / 909 masked differences.
- Attempt 11: Earlier shared-coordinate initialization; 963 words, 644 raw / 641 masked differences.
- Attempt 12: Text old-head snapshots after conversion join; 987 words, 938 raw / 938 masked differences.
- Attempt 13: Owner aliases expressed as array elements; 963 words, 645 raw / 641 masked differences.

The last residual improvement was attempt 6; the last independently proved
structural fact was attempt 8. Attempts 9–13 did not improve the retained
residual, prove another identity, or establish a causal compiler mechanism.
They are the requested five-attempt stall, not an attempt-count cap. The
retained source is attempt 8 with formatting and evidence comments refreshed;
its generated instructions equal the best attempt 6. Regressions, objects,
raw reports and rationale remain in ignored `build/reconstruction69e8/`.

The workbench verdict remains `structure-mismatch`. The required
`structure-buckets` playbook was read before editing. Its constant-audit
lever was checked against the target; the apparent early constant discrepancy
is the scheduling of the first node's x/y stores, with both values present.
The frame is already exact, so the spill-slot census supplies no frame deficit.
The remaining deficit includes the initial count temporary, the coordinate
and insertion-index saved-register split, and store scheduling across builders.
The broad structure and register alignment is insufficient for promotion.

The next concrete lever is a separately authorized causal register-web/lifetime
trace of the first node's setup count, insertion index and shared coordinate,
using this corrected-call, size-exact full-TU baseline. Do not reopen the flat
index/publication spellings or rerun flags/permutation without changed evidence.
That different mechanism was not attempted under this reconstruction assignment.

### Preservation and validation

- `841b091a`: recovered call identities and arities.
- `e10798e5`: recovered global identities and pointer types.
- The closing plateau commit carries the improved initializer, complete root
  array, and this evidence shard.
- Measurements: `tools/wb_compare.sh --summary-json`, workbench `diagnose` and
  `guide structure-buckets`, IDO context self-comparison, runtime relocation
  decoding, and independently proved matched-sibling relocation witnesses.
- Closing validation: configured candidate recompilation, canonical
  `gmake verify`, `gmake check-nonmatching-builds`, `gmake cleanroom`, and
  `gmake check-docs`. ROM verification exercises the retained assembly guard;
  it does not turn the candidate into matched C.
- Integration action: review and integrate the guarded reconstruction commits
  as a plateau; bank no matching credit and retain the current symbol/atlas
  matching status.

<!-- plateau-handoff:func_overlay_101_F00069E8_18E2208:end -->
