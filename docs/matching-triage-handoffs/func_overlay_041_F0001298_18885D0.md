<!-- plateau-handoff:func_overlay_041_F0001298_18885D0:start -->
### `func_overlay_041_F0001298_18885D0` plateau handoff

- source: `src/overlays/o041/overlay41UpdateProgress.c`
- score: 14/115 words
- frame: 0x20
- relocations: 4
- first mismatch: +0x108
- summary: Redundant s16 mask fixes the ring draw; the remaining divisor-copy coalescing decision is unresolved.

#### 2026-09-12, lane p9-mid: 34 -> 14, and the closure named the wrong pass

The closure says the residual is p1 pool routing plus "the tail temporary
phase". The second half is right and the first is not: this was a ugen ring
phase throughout, and globalcolor never had a say in it.

Aligner buckets before: 89 byte-exact, 25 register naming, 0 immediate-only, 2
really different, at 34 masked and size delta 0. After: 104 byte-exact, 8
register naming, 0 immediate-only, 4 really different, at 14 masked and delta 0.

**The census said ring, and it was right.** Before anything was adopted the
register census read a closed ten-cycle over the whole integer temp bank,
t9 to t0 to t1 and so on back to t9, at 100 percent coherence in a single
window. That is not ten colour questions, it is one fact: every temp from
+0x150 on sat exactly one ring position early, because the shipped code draws
one more ring temp than this candidate before it reaches that region.

**A 35-cell force sweep exonerates globalcolor.** Seven p1 webs crossed with
the five lowest caller-saved colours, each acceptance verified by reading the
record's forced field rather than by whether the object changed. Not one cell
scores better than the unforced baseline; the best is one word worse. Ring
temps are not p1 colours, so this is what the census predicted.

**Two edits, priced.**

- Hoisting the start read to the top of the block: 34 to 30. All 32 orders of
  the divisor copy and the start read against the three core statements were
  measured. Start at the top is the unique optimum; the divisor copy's position
  is completely inert, which is itself worth knowing.
- A redundant 16-bit mask on the flags06 read-modify-write: 30 to 14. flags06
  is s16, so masking to 0xFFFF before the OR cannot change the stored value and
  the object is the same size; but it draws one ugen ring temp and the ten-cycle
  disappears. Found by a greedy subset search over thirteen redundant-mask
  sites; four other sites are individually worth 3 to 11 words and none composes
  with this one.

**What is left, and the decision variable.** All 14 remaining words are one
region, +0xE8 to +0x160. The shipped code emits a real move for the
divisor-equals-limit copy before the clamp block, and then reuses limit's
register for the start read after it. This candidate coalesces divisor into
limit, so start needs a second register and gets a copy. The decision variable
is therefore: **what makes IDO refuse to coalesce a copy whose source is not
redefined between the copy and the use.** The same variable is open on
overlay34CreateRecord, where the answer there was a device that costs two words
elsewhere.

Axes covered and flat at 14 or size-changing: a second read of input->limit26,
a cast, a redundant mask on the divisor, a self round-trip through limit, a
redefinition of divisor inside the clamp block, a definition in both arms of it,
an explicit delta local, a two-statement split of the value expression,
dividing by limit directly, both operand orders of the multiply, moving the
addend, three declaration orders for divisor, u32 and s32 for start, and a
16-toggle greedy subset search over redundant masks everywhere else in the
function.
<!-- plateau-handoff:func_overlay_041_F0001298_18885D0:end -->
