<!-- plateau-handoff:func_80007118:start -->
### `func_80007118` plateau handoff

- source: `src/main/objects.c`
- score: 181 differing words
- frame: 0x38
- relocations: 71
- first mismatch: +0x2C
- summary: Workbench structure-mismatch: register-role. Next: authenticate model-pointer coloring and callback lifetimes; resolve owned local-branch relocations.

The configured baseline had 362 words against the complete 459-word,
1836-byte owned range, 445 raw/443 masked differences, first +0, a
104-byte frame against 56, and 66/76 relocations with one authenticated
identity. The interior function alias does not end this ownership range.
The retained C has exactly 459 words and the 56-byte frame, with 186 raw
and 181 masked differences, first +0x2C, and 71 candidate versus 76 target
relocations; 53 identities are authenticated at matching positions. It
remains guarded NON_MATCHING C and adds no matching credit.

The gate freshly returned base-only. Weak oracle neighbors supplied no
counterpart body; no external C was adopted. All 53 usable original TU
flag objects were rescored and remained nonexact. Configured flags stayed
unchanged. Linked fallback ownership was proved separately.

The original switch used consecutive placeholder cases. Mickey's jump table
proves a sparse mapping with a 92-case span; every implemented case and
shared case group was reconstructed from that table. Target null guards,
nine optional model releases, two callback walks, attachment detachment and
payload arguments were restored. Owner resources are direct pointers, not
an extra pointer indirection. The first child cleanup preserves the original
payload rather than treating its return value as a new payload. Trigger
cleanup belongs between the sprite and light loops. Lists, counts, data and
resources are reloaded after calls where the target observes them again.

Using the incoming object directly removed redundant home reloads. Scalar
and pointer declaration order recovered the target frame and two retained
pointer homes. Separating linked-resource and owner lifetimes from the
model payload removed four extra spill instructions and reached exact size
at attempt 214. These are useful TU findings: unrelated resource kinds
should not share a single long-lived generic pointer merely because all
are temporarily represented as byte pointers.

The final five valid probes 215, 217, 218, 219 and 220 tested broader carrier
merging, a separate child pointer, reuse of the byte-offset counter, an
explicit advancing callback cursor and declaration order. They regressed or
tied the best primary residual and established no exact mechanism. This is
the stopping evidence. Preparation 216 used an overbroad replacement that
changed release operands; it was rejected as inadmissible, archived with an
INVALID note, and corrected before further work. It is not a meaningful
source attempt. Cleanup 221 restored 214.

The workbench reports structure-mismatch and routes through structure-buckets,
with the first divergence identified as a register-class decision. The
remaining model-pointer web uses an argument register where the target uses
a return register; callback index/cursor roles and three auxiliary pointer
transfers differ. Authenticate those source lifetimes before another search.
The target object also carries five PC16 relocations to owned global case
labels that the compiler resolves locally. Those must be resolved by the
resident relocation proof; equal call counts alone do not establish exact
relocations or a match. The compiler-generated jump-table mapping also needs
its normal promotion proof.

All attempts retain full source, object, score and rationale in ignored
build artifacts. Full-TU collateral inspection found only relocation-field
addend changes in seven other functions, caused by the larger generated
switch table; their instruction bits, relocation shape and names stayed
unchanged. Validation includes configured comparison and diagnosis, guides,
flag rescoring, finalize_plateau.py, full gmake verify,
check-nonmatching-builds, check-docs and cleanroom. ROM verification uses
the assembly fallback for this function.

<!-- plateau-handoff:func_80007118:end -->
