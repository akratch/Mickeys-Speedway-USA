<!-- plateau-handoff:func_80007118:start -->
### `func_80007118` plateau handoff

- source: `src/main/objects.c`
- score: 186 differing words
- frame: 0x38
- relocations: 71
- first mismatch: +0x2C
- summary: The L101 reading above is refuted: web 110 holds v0 by ordinary interference, forcing the split regresses to 451, and freeing v0 for web 5 measures 123.

The configured baseline had 362 words against the complete 459-word,
1836-byte owned range, 445 raw/443 masked differences, first +0, a
104-byte frame against 56, and 66/76 relocations with one authenticated
identity. The interior function alias does not end this ownership range.
The retained C has exactly 459 words and the 56-byte frame, with 186 raw
and 181 masked differences, first +0x2C, and 71 candidate versus 76 target
relocations; 53 identities are authenticated at matching positions. It
remains guarded NON_MATCHING C and adds no matching credit.

The gate freshly returned base-only. Weak oracle neighbors supplied no
counterpart body; no external C was adopted. All 53 usable original TU
flag objects were rescored and remained nonexact. Configured flags stayed
unchanged. Linked fallback ownership was proved separately.

The original switch used consecutive placeholder cases. Mickey's jump table
proves a sparse mapping with a 92-case span; every implemented case and
shared case group was reconstructed from that table. Target null guards,
nine optional model releases, two callback walks, attachment detachment and
payload arguments were restored. Owner resources are direct pointers, not
an extra pointer indirection. The first child cleanup preserves the original
payload rather than treating its return value as a new payload. Trigger
cleanup belongs between the sprite and light loops. Lists, counts, data and
resources are reloaded after calls where the target observes them again.

Using the incoming object directly removed redundant home reloads. Scalar
and pointer declaration order recovered the target frame and two retained
pointer homes. Separating linked-resource and owner lifetimes from the
model payload removed four extra spill instructions and reached exact size
at attempt 214. These are useful TU findings: unrelated resource kinds
should not share a single long-lived generic pointer merely because all
are temporarily represented as byte pointers.

The final five valid probes 215, 217, 218, 219 and 220 tested broader carrier
merging, a separate child pointer, reuse of the byte-offset counter, an
explicit advancing callback cursor and declaration order. They regressed or
tied the best primary residual and established no exact mechanism. This is
the stopping evidence. Preparation 216 used an overbroad replacement that
changed release operands; it was rejected as inadmissible, archived with an
INVALID note, and corrected before further work. It is not a meaningful
source attempt. Cleanup 221 restored 214.

The workbench reports structure-mismatch and routes through structure-buckets,
with the first divergence identified as a register-class decision. The
remaining model-pointer web uses an argument register where the target uses
a return register; callback index/cursor roles and three auxiliary pointer
transfers differ. Authenticate those source lifetimes before another search.
The target object also carries five PC16 relocations to owned global case
labels that the compiler resolves locally. Those must be resolved by the
resident relocation proof; equal call counts alone do not establish exact
relocations or a match. The compiler-generated jump-table mapping also needs
its normal promotion proof.

All attempts retain full source, object, score and rationale in ignored
build artifacts. Full-TU collateral inspection found only relocation-field
addend changes in seven other functions, caused by the larger generated
switch table; their instruction bits, relocation shape and names stayed
unchanged. Validation includes configured comparison and diagnosis, guides,
flag rescoring, finalize_plateau.py, full gmake verify,
check-nonmatching-builds, check-docs and cleanroom. ROM verification uses
the assembly fallback for this function.

#### Region partition, and the one variable that carries it

Re-measured against the configured TU: 459 words on each side, size delta 0,
186 relocation-masked differing words in 8 regions. The recorded 181 was
stale; nothing in the candidate changed to produce the difference.

- 105 words at +0x474..+0x694
- 29 at +0x2B8..+0x38C
- 20 at +0x3B0..+0x450
- 14 at +0x220..+0x294
- 8 at +0x6B0..+0x6E0
- 7 at +0x2C..+0x54
- 2 at +0x70C..+0x720, and one single word at +0x1F8

The partition is real but it is not the useful decomposition, because a
register histogram collapses all eight regions into one decision. Over the
whole function the target names v0 143 times and a1 exactly once; the
candidate names a1 117 times and v0 16. The single a1 in the target is the
prologue store of the second parameter to its frame slot at 60(sp), and that
slot is never read again on either side, so the parameter is genuinely unused
in both. Every other a1 in the candidate is the general carrier the target
puts in v0.

That is L101's shape: the carrier's web reaches call results, so v0 is not
offered to it and it takes a1 instead. Both sides split the carrier the same
way -- each of the roughly forty rungs spills to 40(sp) before its call and
reloads immediately after, so the segments between calls are identical in
extent. Only the colour differs. The lever is therefore not the spelling of
any one rung.

Measured flat (all 186, and the schedule-tolerant multiset distance also
unchanged at 163):

- 28 declaration-order forms: each of `payload`, `value`, `entry` and `owner`
  moved through all seven positions of the local list.

Measured and refuted:

- The parameter-count hypothesis. Because one call site passes four arguments
  through an unprototyped declaration, the target's parameter list might have
  been longer, which would have kept a1..a3 out of the pool. Three-, four- and
  five-parameter forms each add prologue stores the target does not have (+4
  and +8 bytes) and do not move a single register: the multiset distance stays
  at 163 and 164. The pool composition is not set by the parameter list.

The next attempt should establish, on this function specifically, what
excludes v0 from the candidate's pool. No call in the body consumes a result,
so no call-result value is ever live; if L101's exclusion is triggered by a
call merely *defining* v0 rather than by a consumed result, that is worth
recording as a refinement of the law, and it would predict the same failure
in every long call ladder.

## 2026-09-11 closure re-test (lane `lane/f10-fell`): the v0 exclusion fires without a consumed result

Nothing in the candidate changed; this answers the open question above from
the decision records and names the variable the 28 declaration-order forms
could not move.

The a1 carrier is web 5, symbol `payload`, home 40(sp), one web for the whole
function because uopt forms a live range per symbol (L115): every case's
`payload = object->unk64` and every rung's use is the same web, and it
spans ten or more calls. Its `p1cost` list, read from the instrumented uopt
with `.text` checked byte-identical to the tree's object, is v1 at an
infinite cost, a1 a2 a3 at 20.200001, t0..t5 at infinity and s2..s8 at
50.950001. v0 is not in the list at all. No call in this body consumes a
result, so the exclusion fires on a span that merely reaches a call defining
v0. That is the weaker condition, and it predicts the same for every long
call ladder whose carrier is one symbol.

The decision itself: save 43/16 = 2.6875, decision=color at a1 because net 43
exceeds the a1 cost of 20.2. The target puts v0 on the same spill-and-reload
pattern, which one coloured web spanning calls can never be offered, so the
target's carrier reaches p1 as pieces that do not span a call: either a split
decision (net at or below the crossing cost) or more than one symbol. The
frame has one home for it, which argues for the split. The lever is therefore
L100's symbol-boundary family on `payload`, bringing the single symbol's net
under the cost of colouring it across its calls, and not any ordering of the
locals. Not probed from source in this lane; the force oracle cannot request a
split, so the next measurement is a source form and the `decision=` field of
web 5's record.

## 2026-09-11 (lane `lane/w3-mid`): the closure re-test above is itself wrong

Unchanged at 186 words. Aligned: 291 byte-exact, 141 register naming, 5
immediate only, 23 really different; size delta 0. `tools/frame_census.py`
reports seven stack slots on each side at identical offsets and frame 0x38 on
both, so no part of this is a home-set problem.

Both claims in the section above are refuted by the allocator records for
globalcolor invocation 33 of `src/main/objects.c`, read from the instrumented
uopt with its `.text` checked byte-identical to the tree's object.

First, v0 is not withheld from web 5 by L101. Web 5's decision record carries
`forbidden0=0x50030000`, which decodes to exactly colours 1, 3, 14 and 15, and
its interference list names webs 110, 9, 196 and 34 as already assigned to those
same four colours. v0 is absent from the cost list because interfering web 110
holds it. That is ordinary interference with a web decided one scan position
earlier on save 3.0 against web 5's 2.6875, not a call-span exclusion, and the
open question the earlier section believed it had answered is therefore not
answered by this function at all.

Second, the split is not the lever. `CDX_FORCE=p1:w5=s` forces the split path,
is never declined for any mask, and regresses this function to 451 words with a
four-byte size gain.

What the lever is, measured: move web 110 off v0 and colour web 5 with it, and
the residual falls from 186 to 123, byte-exact rising from 291 to 372. All 91
sites where the candidate names a1 and the target names v0 resolve. Which colour
web 110 is moved to is immaterial; c2, c5 and c6 all give 123. The decision
variable is save(web 5) against save(web 110), 43 over 16 against 9 over 3.
Raising web 5's totalsave from 43 to 49 or more, cutting web 110's from 9 to 8
or less, or separating their live ranges all satisfy it.

Source forms measured against that and refuted, each because it lengthens
`payload`'s range and buys spills: folding `linkedPayload` into `payload`
entirely scores 320 with a sixteen-byte size gain, into `owner` scores 193, and
per switch case scores 277, 295 and 320.

The 123 that would remain is a second allocator layer: a0 becoming v0 on 16
sites, s1 becoming s0 on 11, s0 becoming s1 on 8, a0 becoming v1 on 7.
<!-- plateau-handoff:func_80007118:end -->
