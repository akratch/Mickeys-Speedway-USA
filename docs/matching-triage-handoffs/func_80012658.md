<!-- plateau-handoff:func_80012658:start -->
### `func_80012658` plateau handoff

- source: `src/main/track.c`
- score: 154 differing words
- frame: 0x38
- relocations: 18
- first mismatch: +0x0
- summary: Mickey m2c face and endpoint reconstruction stays above 154 differences. All 18 relocation sites align. Next: persistent node home evidence.

#### Mickey m2c structural audit, 2026-09-08

- Fresh assignment gate returned `base-only`. Configured baseline: 175 candidate / 177 target words, 154 raw and masked differences, first +0x0, frame 0x38 / 0x40, and all 18 relocation records exact in count, offset, type and identity. Workbench verdict `structure-mismatch`, playbook `constant-audit`; the first differing immediate is frame extent. Flag bit, output stride, vertex stride, corner bound and limit field agree with Mickey.
- The fresh Mickey m2c draft confirms the record stream, signed face/batch indices, byte corner indices, endpoint conversion and capacity exit. Its concrete differences are a repeated face-table root load and output-address/store lifetimes. The historical outer-for result was reopened only after changing those inputs, not as an unchanged loop retry.
- Attempts 35–39 recover the face root per corner, endpoint-delta/node store order, draft outer do-while, unsigned-byte corner index, and the second vertex address after the first x store. Scores are 159, 155, 155, 155, and 160 differences; frames remain 0x38, and only 8/18 relocation sites align. No attempt improves the original 154-word residual or adds an unknown identity. Five consecutive attempts therefore meet the stopping rule.
- Retained best: unchanged baseline, guarded by `NON_MATCHING` over the original `GLOBAL_ASM`; zero new matching bytes. All variants, full-TU objects, summaries and diagnoses remain ignored under `build/wb/tu-track/func_80012658/`.
- Next concrete lever: original declaration/home evidence for the persistent node pointer. Target has a pointer home load/store surrounding the stream; simply reproducing m2c's uninitialized stack temporary as source would be an unsupported source-shaping device. The eight-byte home gap requires separately authorized compiler-lifetime evidence.
- Validation: configured full-TU workbench comparisons, finalizer, `gmake verify`, `gmake cleanroom`, and `gmake check-docs` before commit.

#### Pinned donor audit, 2026-09-08

- Assignment gate: `base-only` at `32a75d648e8954f7455897fb8f16a8ef5f05df11` for this exact symbol and source path.
- Jet Force Gemini `efd5abb1c79636e297b831f7c2d5bf47eac39c0c` has 12 C implementations and 53 assembly placeholders in `src/track.c`. Its implemented routines do not supply this target's body. The closest masked track-object hit, `trackGetLedgeCrossed` (Jaccard 0.0635), corresponds by verified TU order to the still-assembly `trackGetLedgeCrossed` in that pinned source. This is diagnostic structural context, not an adopted name, ABI identity, or exact donor match.
- Fresh configured full-TU measurement: 177 target words and 175 candidate words; 154 raw and 154 relocation-masked differences; first +0x0; target frame 0x40, candidate frame 0x38. There are 18 candidate versus 18 target relocation records, with 18 stable identities at matching offsets/types.
- Workbench verdict `structure-mismatch`, playbook `constant-audit`. The existing diagnosis still requires source-shape/lifetime evidence; the donor-only reopen does not authorize substituting a flag, allocator, or permuter mechanism.
- ADR 0018 stop: zero new source attempts for this target. The pinned source disproves the new matched-donor-C hypothesis. No unchanged flag lattice or previously exhausted source family was repeated. The compiled candidate is unchanged and remains behind `NON_MATCHING` and its original `GLOBAL_ASM` fallback; zero new matching bytes are credited.
- Next concrete lever: a published matched counterpart with an authenticated ABI and useful source lifetimes, followed by Mickey-specific field, branch and call proof. The remaining general levers below require separately authorized changed evidence.
- Commands: `tools/wb_compare.sh --summary-json func_80012658`, workbench `diagnose` on the configured full-TU object, the track-object masked skeleton audit, `tools/finalize_plateau.py`, `gmake cleanroom`, and `gmake check-docs`. Source, object, scores, first mismatch and previous handoff are retained under ignored `build/wb/`; no instruction rows are included here.

#### Prior committed plateau evidence (historical)


- source: `src/main/track.c`
- score: 154 differing words
- frame: 0x38
- relocations: 18
- first mismatch: +0x0
- summary: Output-address ordering and outer for-loop preserve all relocation identities; a two-word and eight-byte frame-allocation gap remains.
<!-- plateau-handoff:func_80012658:end -->
