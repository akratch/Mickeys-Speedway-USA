<!-- plateau-handoff:func_overlay_050_F0000334_1896CA4:start -->
### `func_overlay_050_F0000334_1896CA4` plateau handoff

- source: `src/overlays/o050/func_overlay_050_F0000334_1896CA4.c`
- score: 1422 differing words
- frame: 0x110
- relocations: 315
- first mismatch: +0x0
- summary: Size-exact guarded C; 8-byte frame deficit and structure residual. Next: recover stack-home ownership and reconcile reserved-selector identity proof.

#### Reconstruction packet, 2026-09-08

The retained candidate is **NON_MATCHING**, with no new matching credit. It
compiles in the configured full TU. The canonical build still uses the assembly
fallback. Size equality and relocation totals are diagnostic evidence only;
this C has not passed linked owned-range equality or relocation-identity proof.

- Executable bytes: baseline 6288; retained 6300; target 6300.
- Instruction words: baseline 1572; retained 1575; target 1575.
- Raw differing words: baseline 1544; retained 1422; target 0 required.
- Relocation-masked differing words: baseline 1544; retained 1422; target 0 required.
- Frame bytes: baseline 416; retained 272; target 280.
- Relocation records: baseline 205; retained 315; target 315 runtime records.

The retained object has 59 R_MIPS_26, 128 HI16, and 128 LO16 records. The
independently decoded target runtime surface has 59 R_MIPS_26, 127 HI16, and
129 LO16 records. Thus even the relocation type totals differ. The extracted
fallback assembly object has a smaller static relocation surface and must not
be substituted for the shipped runtime table when checking these totals.
First raw and masked mismatch remains `+0x0`.

Tier B call/global evidence comes from Mickey's own runtime exports and local
section identities. The 59 call sites now name 31 distinct callees, including
the existing local time-glyph submission function. Reconstruction separated
initialized-data and BSS identities that had shared numerical addends, notably
the glyph field and descriptor pointer formerly conflated at the same addend.
It also recovered the resource table, menu object array, item lookup, and
read-only speed-label/scale identities. The template and lap-list subrange
anchors remain explicit relocation names, with their common storage identity
recorded in source comments.

Tier D layout evidence is the caller's access widths, offsets, and strides,
checked with IDO layout assertions. The source now uses typed glyph, racer,
object, level, and menu records; real stack glyph arrays; promoted item/lap
counts; proper floating-point call arguments; and independent lap-offset,
lap-time, and vertical-position strides. The two integer position loops and
final lap-offset loop preserve intermediate stores lost by the old temporary
chains. A direct four-step FP loop lets IDO form the pipeline. The item-state
flow and the late gauge/lap counter lifetime were reconstructed from the
control-flow and call-boundary evidence. These are source reconstructions;
the generated m2c draft was not copied into tracked source or comments.

#### Alternatives and stopping evidence

Attempt 40 is retained because exact executable geometry ranks ahead of the
lower raw residual on an oversized function. The lower raw-score alternative
is attempt 37: 1578 words, 1186 raw differences, 1173 masked differences,
272-byte frame, and 315 candidate relocations. It exceeds ownership by 12
bytes. Attempt 31 fits within ownership at 1570 words and has 1256 raw / 1243
masked differences, but its runtime-identity summary is rejected. All sources,
objects, reports, and scripts remain in the ignored `build/wb/attempts/` tree;
the generated draft and annotated target remain in ignored `m2cfiles/`.

The final five material attempts did not improve the retained geometry/residual
or the best raw residual, prove another identity, or establish another useful
mechanism:

- Attempt 41, common switch exit: 1577 words, 1454 raw differences, 280-byte frame.
- Attempt 42, adjacent time-output record: 1577 words, 1454 raw differences, 280-byte frame.
- Attempt 43, direct banner quad-phase entries: 1582 words, 1455 raw differences, 280-byte frame.
- Attempt 44, early null-object exit: 1577 words, 1454 raw differences, 280-byte frame.
- Attempt 45, counted outer lap traversal: 1576 words, 1455 raw differences, 272-byte frame.

The output-record and outer-exit variants emitted unchanged executable code
relative to their inputs. The other variants changed already-understood CFG
spellings without reducing the remaining deficit. This is the assignment's
five-attempt stall, not a fixed total-attempt cap. Restoring attempt 40 and
renaming residual draft-style locals for readability did not change its
executable bytes or relocation records.

#### Proof-tool limitation and next lever

The workbench verdict is `structure-mismatch`; the routed `structure-buckets`
lever is the **spill-slot census**. Save slots agree at 12 bytes, while the
candidate's non-save frame is 260 bytes versus 268. Recover the actual homes
and lifetimes of the carried counter, selected item, lap count, and stack
message records before further allocation work. Also recover the target's
HI16/LO16 address-carrier reuse; equal relocation totals do not close that
surface. Whole-function counter merging regressed, and moving counter
declarations alone was flat.

The final raw comparison succeeds, but `wb_compare.sh --summary-json` rejects
its relocation summary with:

> candidate relocation symbol D_8007C1B0 has conflicting runtime identity

Closer earlier candidates similarly report `D_8007C180`. Inspection identifies
a namespace conflict between resident auto-name identities and the runtime
correlator's reserved selector identities. Those selectors intentionally remain
separate in the current proof tool. The conflict is unresolved; no authenticated
exact-identity count is claimed, and the guard was not weakened. A proof-tool
owner should reconcile those identities using the loader/export evidence before
this candidate can receive a complete relocation proof. Oversized alternatives
also correctly fail the ownership gate.

#### Files, commits, and validation

Changed files are this handoff and
`src/overlays/o050/func_overlay_050_F0000334_1896CA4.c`.
The coherent source commits are:

- `7998a8ec`: recover runtime call identities.
- `bc368b62`: reconstruct types and distinct storage identities.
- `70c2a843`: reconstruct loops and item-state flow.
- `052774c2`: retain the size-exact candidate and finalize the plateau.

The plateau commit `052774c2` retains attempt 40 with descriptive local names and
the synchronized marker generated by `tools/finalize_plateau.py`.

Commands/evidence used include `./generate_ctx.sh`, `./mips_to_c.sh`, the
near-match oracle, `tools/wb_compare.sh`, full-TU `decomp-workbench diagnose`,
`decomp-workbench guide structure-buckets`, Mickey runtime relocation decoding,
IDO layout assertions, and function-sized ELF/relocation inspection. The final
validation runs `gmake verify`, `gmake cleanroom`, and `gmake check-docs`, followed
by `tools/finalize_plateau.py --commit`. Passing ROM verification proves the
canonical fallback, not the guarded candidate.

<!-- plateau-handoff:func_overlay_050_F0000334_1896CA4:end -->
