<!-- plateau-handoff:func_overlay_050_F0000334_1896CA4:start -->
### `func_overlay_050_F0000334_1896CA4` plateau handoff

- source: `src/overlays/o050/func_overlay_050_F0000334_1896CA4.c`
- score: 1193 differing words
- frame: 0x110
- relocations: 315
- first mismatch: +0x0
- summary: Frame gap is 2 extra ugen spill temps, not a declaration defect; declared-local geometry is identical. Next: pool-web divergence at slot 18.

#### Reconstruction packet, 2026-09-08

The retained candidate is **NON_MATCHING**, with no new matching credit. It
compiles in the configured full TU. The canonical build still uses the assembly
fallback. Size equality and relocation totals are diagnostic evidence only;
this C has not passed linked owned-range equality or relocation-identity proof.

- Executable bytes: baseline 6288; retained 6300; target 6300.
- Instruction words: baseline 1572; retained 1575; target 1575.
- Raw differing words: baseline 1544; retained 1422; target 0 required.
- Relocation-masked differing words: baseline 1544; retained 1422; target 0 required.
- Frame bytes: baseline 416; retained 272; target 280.
- Relocation records: baseline 205; retained 315; target 315 runtime records.

The retained object has 59 R_MIPS_26, 128 HI16, and 128 LO16 records. The
independently decoded target runtime surface has 59 R_MIPS_26, 127 HI16, and
129 LO16 records. Thus even the relocation type totals differ. The extracted
fallback assembly object has a smaller static relocation surface and must not
be substituted for the shipped runtime table when checking these totals.
First raw and masked mismatch remains `+0x0`.

Tier B call/global evidence comes from Mickey's own runtime exports and local
section identities. The 59 call sites now name 31 distinct callees, including
the existing local time-glyph submission function. Reconstruction separated
initialized-data and BSS identities that had shared numerical addends, notably
the glyph field and descriptor pointer formerly conflated at the same addend.
It also recovered the resource table, menu object array, item lookup, and
read-only speed-label/scale identities. The template and lap-list subrange
anchors remain explicit relocation names, with their common storage identity
recorded in source comments.

Tier D layout evidence is the caller's access widths, offsets, and strides,
checked with IDO layout assertions. The source now uses typed glyph, racer,
object, level, and menu records; real stack glyph arrays; promoted item/lap
counts; proper floating-point call arguments; and independent lap-offset,
lap-time, and vertical-position strides. The two integer position loops and
final lap-offset loop preserve intermediate stores lost by the old temporary
chains. A direct four-step FP loop lets IDO form the pipeline. The item-state
flow and the late gauge/lap counter lifetime were reconstructed from the
control-flow and call-boundary evidence. These are source reconstructions;
the generated m2c draft was not copied into tracked source or comments.

#### Alternatives and stopping evidence

Attempt 40 is retained because exact executable geometry ranks ahead of the
lower raw residual on an oversized function. The lower raw-score alternative
is attempt 37: 1578 words, 1186 raw differences, 1173 masked differences,
272-byte frame, and 315 candidate relocations. It exceeds ownership by 12
bytes. Attempt 31 fits within ownership at 1570 words and has 1256 raw / 1243
masked differences, but its runtime-identity summary is rejected. All sources,
objects, reports, and scripts remain in the ignored `build/wb/attempts/` tree;
the generated draft and annotated target remain in ignored `m2cfiles/`.

The final five material attempts did not improve the retained geometry/residual
or the best raw residual, prove another identity, or establish another useful
mechanism:

- Attempt 41, common switch exit: 1577 words, 1454 raw differences, 280-byte frame.
- Attempt 42, adjacent time-output record: 1577 words, 1454 raw differences, 280-byte frame.
- Attempt 43, direct banner quad-phase entries: 1582 words, 1455 raw differences, 280-byte frame.
- Attempt 44, early null-object exit: 1577 words, 1454 raw differences, 280-byte frame.
- Attempt 45, counted outer lap traversal: 1576 words, 1455 raw differences, 272-byte frame.

The output-record and outer-exit variants emitted unchanged executable code
relative to their inputs. The other variants changed already-understood CFG
spellings without reducing the remaining deficit. This is the assignment's
five-attempt stall, not a fixed total-attempt cap. Restoring attempt 40 and
renaming residual draft-style locals for readability did not change its
executable bytes or relocation records.

#### Proof-tool limitation and next lever

The workbench verdict is `structure-mismatch`; the routed `structure-buckets`
lever is the **spill-slot census**. Save slots agree at 12 bytes, while the
candidate's non-save frame is 260 bytes versus 268. Recover the actual homes
and lifetimes of the carried counter, selected item, lap count, and stack
message records before further allocation work. Also recover the target's
HI16/LO16 address-carrier reuse; equal relocation totals do not close that
surface. Whole-function counter merging regressed, and moving counter
declarations alone was flat.

The final raw comparison succeeds, but `wb_compare.sh --summary-json` rejects
its relocation summary with:

> candidate relocation symbol D_8007C1B0 has conflicting runtime identity

Closer earlier candidates similarly report `D_8007C180`. Inspection identifies
a namespace conflict between resident auto-name identities and the runtime
correlator's reserved selector identities. Those selectors intentionally remain
separate in the current proof tool. The conflict is unresolved; no authenticated
exact-identity count is claimed, and the guard was not weakened. A proof-tool
owner should reconcile those identities using the loader/export evidence before
this candidate can receive a complete relocation proof. Oversized alternatives
also correctly fail the ownership gate.

#### Files, commits, and validation

Changed files are this handoff and
`src/overlays/o050/func_overlay_050_F0000334_1896CA4.c`.
The coherent source commits are:

- `7998a8ec`: recover runtime call identities.
- `bc368b62`: reconstruct types and distinct storage identities.
- `70c2a843`: reconstruct loops and item-state flow.
- `052774c2`: retain the size-exact candidate and finalize the plateau.

The plateau commit `052774c2` retains attempt 40 with descriptive local names and
the synchronized marker generated by `tools/finalize_plateau.py`.

Commands/evidence used include `./generate_ctx.sh`, `./mips_to_c.sh`, the
near-match oracle, `tools/wb_compare.sh`, full-TU `decomp-workbench diagnose`,
`decomp-workbench guide structure-buckets`, Mickey runtime relocation decoding,
IDO layout assertions, and function-sized ELF/relocation inspection. The final
validation runs `gmake verify`, `gmake cleanroom`, and `gmake check-docs`, followed
by `tools/finalize_plateau.py --commit`. Passing ROM verification proves the
canonical fallback, not the guarded candidate.


#### Measured session, 2026-09-09

The retained candidate moves from 1422 to **1193** relocation-masked differing
words. Alongside it, `geometry_edit_distance` falls 1050 -> 784 and the
shift-tolerant count of exactly identical instruction rows rises 783/1575 ->
920/1575 (49.7% -> 58.4%). Executable words go 1575 -> 1572, three under the
6300-byte owned extent, which the configured `trim_elf_section` extent pads;
this is inside ownership, unlike the oversized attempt 37. Frame stays 0x110,
relocations stay 315 (59 R_MIPS_26, 128 HI16, 128 LO16), first mismatch stays
+0x0. Still NON_MATCHING: the canonical build keeps the assembly fallback and
`gmake verify` still proves that fallback, not this candidate.

Four source edits, each with its own measurement:

1. **A peel bound computed above a call keeps two values live across it.**
   At the `-0x140`/`0x1900` interpolation site the peel bound was assigned
   outside the positive-rate guard, so it and the loop counter were both
   carried across the sound call and homed there. Moving the assignment inside
   the guard, where the other four sites already have it, removed two homing
   stores and two reloads and put the bound where the target computes it.
   **-173 words**, the largest single move.
2. **Both 4x interpolation bodies had their last two updates transposed.**
   The retained spelling was `B C B C B C C B`; the target's register carry
   wants strict alternation. **-5 words**, and the transposition was a
   hand-fit, not evidence.
3. **The first interpolation site's peel bound spelled as the expression**
   rather than the cached local. **-3 words.**
4. **The centisecond fudge was one expression with an embedded
   post-increment.** Splitting `c = c - c % 10 + counter++` into a subtract, an
   add and an increment closed **48 words** and took `geometry_edit_distance`
   down by 241 on its own. This is the campaign's embedded-assignment lever
   reproducing here.

#### Frame identity: proved, and the earlier reading corrected

Under this unit's `-O2 -g3`, **every declared local takes a 4-byte home**, in
declaration order, counted down from the top of the local block, whether or not
it is ever reached through the stack. Proved twice: adding one unused `s32`
before the first declaration grew the frame by 8 and moved every home by 4; and
a whole-list permutation that hoisted six trailing declarations to the head put
every home at the offset the law predicts, including the two array bases.

The frame decomposes as 40 bytes of outgoing arguments, 8 bytes of saved
registers (only two registers are saved on either side), the declared local
block, and the ugen spill temps, rounded to 8:

- target: 40 + 8 + 216 + 16 = 280
- candidate: 40 + 8 + 216 + 8 = 272

**Both sides declare exactly 54 local words.** The 8-byte gap is two extra ugen
spill temp words in the target, allocated below the declared block; the target
uses four temp words and the candidate two. This is visible in the reuse
pattern -- a temp slot homes several unrelated registers inside one block,
which no declared local does -- and in the two sides using the same temp index
counted down from the top of the temp region for the same code.

That corrects the previous packet's routing. The 8 bytes are **not** recoverable
by editing the declaration list, and two ways of trying were measured and
rejected:

- Adding a 55th declared local reaches 0x118 only through the 8-byte rounding.
  It displaces every home by 4 and buys 2 words (1422 -> 1420). Not the fix.
- The whole-list permutation reproduced the predicted homes exactly and left
  the score at 1422, flat. Declaration order is measurable here and, on its
  own, inert -- matching the caution measured on another overlay unit the same
  day.

The declaration-order fact that *is* proved, from relative geometry alone: the
target declares **six more local words before `hudX`** than this candidate --
two before `messageX`, three between `messageY` and the message glyph array,
and one between `timeMagnitude` and `minutes` -- so its trailing group is nine
words, not fifteen. All nine of the target's trailing declarations stay in
registers: between its temp region and `hudX` there is no stack traffic at all.
This candidate spills eight of its fifteen. That register-pressure difference is
the same one that produces the two extra spill temps, so the frame and the
trailing declarations are one question, not two.

#### Flags: settled by measurement, not signature

`tools/flag_sweep.py` over the 119-combination lattice ranks the configured
`-O2 -g3 -mips2 -32 -Wo,-loopunroll,0` first at 1422 with size delta 0. Next is
the same combination without `-g3`: +88 words and 4 bytes short. Every `-mips1`
and `-mips3` row, every `-O1`/`-O0`/`-g` row, and every unroll factor other than
0 is far worse.

- `-Wab,-r4300_mul` combined with the unroll suppression is not in the swept
  lattice; measured directly here it adds two words and loses size-exactness.
  `mul_scheduler_scan.py` names this unit on a single multiply pair, the
  weakest possible evidence, and the flag is eliminated.
- `-Wo,-loopunroll,1` is byte-identical to `,0`; `,3` and `,8` explode.
- **The 4x loop bodies are not the compiler's unroller output.** Rewriting all
  seven loop sites as simple counted loops and enabling the unroller gives 1656
  words (+81) and a 312-byte frame; with the unroller still suppressed it gives
  1260 (-315). The retail source hand-unrolled these loops, and the retained
  hand-unrolled spelling at exactly 1575 words is the right shape.

#### Where the residual is, and whether the sides re-converge

They re-converge, repeatedly. On a shift-tolerant alignment 920 of 1575 rows
are now exactly identical, in runs of 40, 40, 38, 32, 30 and 23 rows spread from
the prologue to the epilogue, and the running shift stays inside +/-6 across the
whole body. **This is a dense local defect, not a structural mismatch**, and the
`structure-mismatch` verdict is an artefact of comparing positionally across
early insertions.

Per 50-word window, agreement is best (37-47 of 50) through the opening
timer/glyph region and the tail, and worst in four places:

- the two 4x interpolation bodies (9/50 and 11/50),
- the speedometer block (12/50),
- the lap-offset loop (14/50).

The register signature places the first divergence at colored-web slot 18,
ugen temp-ring slot 36, and FP-pool slot 7. In the interpolation bodies this
candidate colors two argument registers where the target uses result and
argument registers -- two more simultaneous colors, which is the same pressure
that buys the target's two extra spill temps.

#### Alternatives measured and rejected this session

Against the 1193 candidate unless noted:

- inverting the item-fade accumulate arms: +56
- inverting the item-fade gate to an early arm: +157
- re-caching the peel bound to buy a second carrier at either interpolation
  site: +4 and +4 (0 at the second site alone)
- inlining the peel bound at the two banner sites or the lap-offset site: +3
  each, so the cached local is right at those three
- `B B B B C C C C` grouping of the 4x bodies: +60 and 12 words short
- three further spellings of the centisecond split are byte-identical to the
  adopted one; a fourth that folds the modulo into the increment is +78, and a
  fifth that reaches exactly 1575 words scores 1421
- the six-hoisted-declaration permutation with three trailing locals merged
  away: local block exactly right (non-save 268 = 268) but two extra saved
  registers and 10 fewer words

#### Next lever

The routed lever is no longer the spill-slot census; that is closed. It is the
**colored-web divergence at slot 18 inside the two 4x interpolation bodies**.
The target loads both accumulators once per trip, carries all four steps in
registers with explicit register-to-register copies, and stores once; this
candidate stores an accumulator mid-body. Closing that removes the extra live
range, which is the same pressure that produces the two-temp frame gap. The
four windows named above hold most of the remaining 1193.

The proof-tool limitation from the previous packet is unchanged: relocation
identity cannot be authenticated here, because the extracted target object
attributes every `R_MIPS_26` to the overlay's first function symbol and carries
no relocation at all for overlay-internal data, so 356 of the reported
relocation-target differences are artefacts of that extraction, not evidence.
`wb_compare.sh --summary-json` still rejects the summary on the
`D_8007C1B0` reserved-selector conflict.

<!-- plateau-handoff:func_overlay_050_F0000334_1896CA4:end -->
