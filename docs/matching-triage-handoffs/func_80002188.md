<!-- plateau-handoff:func_80002188:start -->
### `func_80002188` plateau handoff

- source: `src/main/audiomgr.c`
- score: 7 differing words
- frame: 0x50
- relocations: 22
- first mismatch: +0x28
- summary: Fresh remeasurement confirms exact 115-word geometry and 22 aligned relocation identities; seven stack-home operands await a bounded permuter sweep.

Configured remeasurement on base `a98cf6be213295cc9d342a1a2b6fdc7d5409c593`
confirms the historical scalar evidence against the current source:

- the resident owner is `0x80002188..0x80002354` / ROM
  `0x2D88..0x2F54`, exactly `0x1CC` bytes with no size ambiguity;
- configured IDO emits exactly 115 instructions and the same `0x50` frame,
  opcode schedule, and register allocation as the target. Seven literal
  operands differ, beginning at function offset `+0x28`; all are localized
  stack-home offsets rather than control-flow or semantic differences;
- target and candidate each carry 22 relocations. Every record is aligned by
  offset and type, and all 22 symbol identities resolve without ambiguity;
- the guarded body remains the Diddy Kong Racing SGI `__amDMA`-shaped
  reconstruction already disclosed in source. The assembly fallback remains
  canonical, and this maintenance pass changes no function body.

The next bounded mechanism is a permuter sweep over source-equivalent local
declaration and expression order, with the seven operand sites as the only
acceptance signal. Preserve the fallback unless untouched configured IDO
output becomes byte-identical and the linked range plus full ROM verify.
<!-- plateau-handoff:func_80002188:end -->
