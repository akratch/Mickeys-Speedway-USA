<!-- plateau-handoff:func_overlay_026_F0001158_187B550:start -->
### `func_overlay_026_F0001158_187B550` plateau handoff

- source: `src/overlays/o026/overlay26DrawGroups.c`
- score: 88 differing words
- frame: 0x58
- relocations: 4
- first mismatch: +0x4C
- summary: Clean V0 is one word short. Prior exact-size form used a prohibited inert read; flags, constant, pointer, sibling CFG and permutation routes are exhausted.
#### 2026-09-09: the missing word is the negation, and it is already in the source

The target computes the node cursor as `negu t3, fp` then `addu t4, t2, t3`;
the candidate folds the same source (`negativeNodeOffset = -nodeOffset;
nodeEntry = context->nodeTable + negativeNodeOffset;`) into a single `subu`.
So the negation has to survive as a value, which means `negativeNodeOffset` is
read somewhere else in the target's source. That is what the withdrawn inert
read was standing in for, and it is why the inert form reached the exact 134
words: it is the right *mechanism* with the wrong *spelling*. Find the second,
real use before spending anything on the 65-word remainder -- with the size
wrong every positional row after `+0x4C` is renumbered and unreadable.

Also measured: the constant hoists differ before that point. The target
materialises `0xFB000000` and `-0x100` first and the loop constants `3` and
`0xC` later; the candidate hoists `3` and `0xC` to the top. That is a
statement-order question in the block above the loop, independent of the
negation.

<!-- plateau-handoff:func_overlay_026_F0001158_187B550:end -->
