<!-- plateau-handoff:func_8006E7E0:start -->
### `func_8006E7E0` plateau handoff

- source: `src/libultra/block_6F3E0.c`
- score: 2 differing words
- frame: frameless
- relocations: 0
- first mismatch: +0x0
- summary: Empty C emits two instructions while the target is a one-word hole with padding; this remains unrepresentable as generated C.

#### Settled 2026-09-09: this is not a function

The whole 0x40-byte region `0x8006E7E0..0x8006E820` (ROM `0x6F3E0..0x6F420`) is
zero, and `func_8006E7E0` is the four-byte splat artefact at its head: one zero
word, with the remaining fifteen unclaimed. It is not code, and no C body can
emit a single zero word with no return sequence -- an empty function emits two
instructions, which is the entire recorded `+4`. `mickey.us.yaml` already
records the region as unidentified padding matching nothing in any of the five
reference builds, and the boundary argument above it (`corrupted`, the last
routine of libultra's `io/pfschecker.c`) is settled separately.

The four bytes are therefore permanently unmatchable as C and should be taken
out of the function census rather than iterated on: the clean fix is a segment
reclassification in `mickey.us.yaml`, which is shared configuration and belongs
to an integration pass, not a lane. Do not spend attempts here.
<!-- plateau-handoff:func_8006E7E0:end -->
