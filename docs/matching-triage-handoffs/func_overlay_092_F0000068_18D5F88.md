<!-- plateau-handoff:func_overlay_092_F0000068_18D5F88:start -->
### `func_overlay_092_F0000068_18D5F88` plateau handoff

- source: `src/overlays/o092/overlay92FindNearestCourse.c`
- score: 142/168 words
- frame: 0x90
- relocations: 7
- first mismatch: +0x4
- summary: Fidelity-clean allocator/UGEN trace found no source-semantic, home, or target-chronology selector; resumption bar tested and not met.

#### Prior authenticated evidence retained

- original remeasurement base: `ebf4d71e`; bounded plateau with zero exact
  bytes gained over overlay 92 text `+0x68..+0x308`, with no target padding.
- ABI: `s32 (O92Racer *, f32 *, f32 *, f32 *, s32 *)`; two overlay-local
  callers at `+0x76C` and `+0x914` remain consistent.
- configured recipe: `-O2 -mips2 -32 -Wab,-r4300_mul`, with `NON_MATCHING`
  selecting the guarded C body. The complete 119-row lattice was nonexact;
  66 rows were unsupported and the configured recipe was uniquely best.
- pinned donor scans were negative. The best size-compatible similarity was
  0.067 and did not qualify as a body donor.
- prior byte-flat or regressing forms covered the FP carrier copy,
  comma-grouped dependent index/cursor initialization, course-position cache,
  racer alias, index order, valid-branch initialization, declaration order,
  typed locals, and debug/source-line variants. No natural gain authorized a
  permutation batch.

#### Explicit-dereference mechanism reproof (2026-08-31)

- assignment base: `33379b39`; the current `81d26e0e` plateau predates the
  exact adjacent Overlay 22 explicit-`(*pointer).field` mechanism at
  `73f59461`. That mechanism directly qualified a bounded reopen because this
  target's documented blocker includes first-distance FP coloring over object
  and racer pointer-field expressions.
- configured V0 freshly reproduced the exact 672-byte / 168-word extent and
  `0x90` frame, with 142 positional words exact and 26 residual. The aligned
  diagnosis remains 24 register, four schedule, and two structural sites;
  first mismatch is `+0x4`.
- the target runtime surface contains seven relocations and all seven candidate
  records align by offset and type. Static candidate inference resolves two
  identities and remains fail-closed for five; this is not promotion evidence.
- three isolated, source-equivalent probes applied explicit dereference to the
  first-distance object and racer fields together, object fields only, and
  racer fields only. Every probe retained 168 words and the `0x90` frame but
  was byte-score flat at 142/168 with the same residual classes. V0 is restored.
- the already exhausted 119 flags, comma-grouped cursor form, FP carrier copy,
  older term-order family, and generic permutation were not repeated. Resume
  only with genuinely new fidelity-clean saved-register or FP-pool chronology
  evidence selecting a source-authentic carrier or lifetime form.

#### Saved-register / FP-pool evidence gate (2026-09-08)

- assignment base: `de7ee5a8`. Configured V0 reproduced 168 words, a `0x90`
  frame, 142 positional matches and 26 raw/relocation-masked residual words;
  first mismatch remains `+0x4`. The aligned diagnosis remains 24 register,
  four schedule, and two structural sites. No source hypothesis was attempted.
- preflight confirms seven target and seven candidate relocations, with all
  offsets and types aligned. Static candidate inference now resolves six
  identities and remains fail-closed for one; this is not promotion evidence.
- a fresh pinned-IDO allocator capture mapped the symbol unambiguously to the
  only Ucode procedure. The traced object passed `.text`, `.rodata`, `.data`,
  relocation, and symbol fidelity against the stock configured object.
- the receipt observed 25 integer global-colour decisions, six FP-pool
  decisions, and procedure-scoped UGEN temporary events. It could not emit a
  source semantic, virtual or final stack home, or authenticated target-side
  temporary chronology. Its first-mechanism comparison is therefore
  `unavailable`; candidate line/register coincidence cannot select an original
  carrier or lifetime.
- the aligned comparator likewise reports no evidence-supported lever: the
  integer temporary, shared, and FP-temporary lanes agree, while the first
  integer-pool and FP-pool divergences have no authenticated home, ring, or
  source-line-order selector.
- no previously exhausted mechanism, flag row, or permutation was retried, and
  no structural reshape was authorized. **No new evidence; plateau stands.**
  The resumption bar was tested and not met. Reopen only with independently
  authenticated target chronology plus a source/home identity that selects one
  natural carrier or lifetime form.

#### Re-open under laws L90 / L92 (2026-09-10, lane/c3-reopen2)

- lever set the closure actually searched: the 119-row flag lattice, the
  comma-grouped index/cursor initialization, the FP carrier copy, the
  course-position cache, the racer alias, index order, valid-branch
  initialization, declaration order, typed locals, source-line variants,
  three explicit-dereference probes, and two allocator/UGEN receipts. Every
  one of those is a spelling of the same loop shape; none of them changed
  which exit test uopt builds.
- **L90 measured, and it confirms the retained shape.** Four natural exit-test
  forms were compiled against the configured recipe: the plain counted loop
  over the array is -16 bytes, the guarded do-while on the same bound is
  -16 bytes, the cursor-versus-computed-limit form is -12 bytes, and the
  index-plus-cursor form without the `limit` local is -8 bytes. The -16 is
  uopt hoisting the memory-resident bound exactly as L90 predicts. The target
  re-reads that bound on every iteration, so the retained `limit = end * 4` in
  both arms of the type test is load-bearing, not incidental. L90 closes this
  axis rather than opening it.
- **L92 measured inert.** Lever 54's `(s32)` cast on the array base and the
  written commutative swap of the same add both leave the object at 26 masked
  words and delta 0. Reading the residual expression by expression, every
  commutative operand order in the function -- the three float products, the
  two float sums, and the base-plus-scaled-index add -- already agrees with
  the target. The residual carries no misordered commutative operand for L92
  to reach.
- the deciding variable, named: two allocator decisions, neither of them a
  spelling. First, uopt's web number for the scaled-index web relative to the
  racer parameter's copy web -- the target colours scaled-index into the lower
  saved register and racer into the higher, the candidate the reverse, which
  alone accounts for 16 of the 24 register sites. Second, the FP pool's
  first-free colour for the loop's delta web: the target opens that web at the
  lowest FP colour and the candidate one colour higher, rotating three FP webs
  across six sites. Both loop-block colourings are otherwise identical, and the
  tail block's FP colours already agree exactly.
- what was tried against those variables and failed: an exhaustive
  single-declaration sweep, moving each of the 17 locals to the front and to
  the back of the block (32 compiles), never beat the base -- the best row was
  27 against 26, and the median was 33. Splitting the loop's deltas from the
  tail's deltas into separate locals holds the exact 0x90 frame but regresses
  to 44 in either declaration order, which measures the split as real and
  wrong. Reading the vehicle pointer after the range call regresses to 52.
- **verdict: the closure was correct and remains correct under L90 and L92.**
  Resume only with evidence that selects a web *number*, not a spelling: a
  calibrated uopt colouring receipt that attributes the scaled-index and racer
  webs to source, or a donor whose loop body fixes their relative order.
#### Carrier identity tested and flat (2026-09-10, lane/c6-band-a)

Base `bfa8365f`; configured V0 reproduces 26 masked / 26 raw words, 168
instructions, `0x90` frame, delta 0. One axis was added to the closure list --
**which existing local carries which value**, the axis that closed
`func_overlay_002_F0001A94_185888C` and cut `func_overlay_009_F00010B4_186772C`
by five words the same day. It is not the same axis as the declaration-order
sweep already recorded above, and it is flat here:

- the scaled index in `limit` with the bound in `index` is byte-identical (a
  pure rename, as expected);
- the scaled index in `start`, which is dead after `index = start * 4`, grows
  the function by 12 bytes;
- the tail's `gOverlay92DistanceParameters[1]` carried in `distance` after its
  last use is byte-identical; carried in `scale`, or with `scale` carried in
  `nearestDistance`, it costs 12 words.

So the two deciding variables named in the re-open section stand unchanged, and
they are now known not to be carrier-identity facts either. Resume only with a
calibrated uopt colouring receipt that attributes the scaled-index and racer
webs to source, or a donor whose loop body fixes their relative order.
<!-- plateau-handoff:func_overlay_092_F0000068_18D5F88:end -->
