<!-- plateau-handoff:func_overlay_008_F0001000_185ED58:start -->
### `func_overlay_008_F0001000_185ED58` plateau handoff

- source: `src/overlays/o008/overlay_008.c`
- score: 159 differing words
- frame: -0x18
- relocations: 18
- first mismatch: +0x8
- summary: Fresh V0 is one instruction short; authentic three-argument ABI retains the unused-a0 home, a3 state carrier, and rollover-join mismatch.

Fresh maintenance evidence on base `3c1be7d853a319f363ae12dd44ad90806b879ba4`:

- The owned overlay-8 text range is `+0x1000..+0x1294` (660 bytes, 165 words), exported as overlay ROM-table record 1518. The configured `NON_MATCHING` C emits 656 bytes/164 words with the same `0x18` frame, first mismatch `+0x8`, and 159 relocation-masked/raw positional differences.
- The authenticated ABI is `f32 (void *, O8PhaseState *, f32)`. The overlay-1 caller at `+0x4DE0` passes owner in `a0`, state in `a1`, and the float bits in `a2`; overlay 8 has another inbound `R_MIPS_26` call at `+0x14C4`. The shared TU uses its configured `-Wab,-r4300_mul` flag.
- Workbench classifies V0 as `structure-mismatch`: the candidate homes unused `a0`, assigns the long-lived state carrier differently, and diverges through the rollover join. The target and candidate each expose 18 relocations, but none have matching baseline offsets/types because of the structural drift; candidate identities are also unresolved.
- A four-argument probe produced exact geometry and 101 positional differences by moving state to incoming `a3`, but both real callers and the target's initial `a1` state load disprove that ABI. It was rejected and is not retained as a score-only change. IDO rejects an unnamed parameter in a prototype-style definition.
- The best external skeleton similarity was only 0.032 (`bk:func_803890DC`); no credible donor exists. Prior context, flag-lattice, parameter/register, local-address, control-flow, alias, volatility, result-local, store-order, and bounded permutation probes are already exhausted.
- Resume only with a new source-authentic mechanism that suppresses the unused-`a0` home while preserving the proven `a1` state input and naturally recreates the target `a3` lifetime/rollover join. Do not repeat the disproven ABI, flags, or prior form lattice.
<!-- plateau-handoff:func_overlay_008_F0001000_185ED58:end -->
