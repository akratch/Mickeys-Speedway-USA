<!-- plateau-handoff:func_80041530:start -->
### `func_80041530` plateau handoff

- source: `src/main/particles.c`
- score: 36 differing words
- frame: 0x168
- relocations: 4
- first mismatch: +0x4C
- summary: 36 words, and the head twelve are now explained. The candidate lets uopt CSE vertexCount * 8 from the triangle-list bases into a callee-saved colour across the func_800349A4 call; the target recomputes it into a ring temp that dies at once, and that single colour offsets the ring for every word from +0x4C to +0x94. Breaking the CSE -- shift spelling on the triangle-list bases together with vertexCommandLength written as vertexCommandCount plus vertexCount times two plus eight -- makes every word through +0xE4 exact and moves the first mismatch to +0xE8 at delta 0. That is the shape the withdrawn claim described, reached by a different spelling and landing at a different offset, and it is not adopted because it costs 56 words in the vertex loop and moves the volatile length home from 96 to 80 where the target has 72; the base spelling already places vertexCommandCount at the target's 76. Flat and eliminated at delta 0: four operand orders of that sum compile byte-identically, which is the L92 signature; six declaration positions and volatile placements for the length local cost one to two words; five triangle-list index spellings either restore the CSE or add a word.

- 2026-09-09 pass, the head is now causally explained and half of it is
  reachable. The target spells the volatile command length as
  `(vertexCount << 3) + (vertexCount << 1) + 8`, not `vertexCount * 10 + 8`:
  writing it that way reproduces the target's own three-instruction strength
  reduction, drops the opcode residual from 4 to 2, and makes aligned rows
  19-59 exact -- the first mismatch moves from `+0x4C` to `+0xF0`. It is not
  retained, because the same edit costs 55 words in the tail (a consistent
  t6/t8 two-cycle across the four-times-unrolled vertex store loop) for a net
  91. Under L88 this is the "each half alone is a regression" shape: the head
  spelling is almost certainly the target's, and the tail needs its own edit.
- The remaining structural fact: the candidate common-subexpression-eliminates
  `vertexCount * 8` across the `func_800349A4` call into a *saved* register
  (`s0`), and stores that to the count's home; the target computes it twice,
  once into a temp before the call for the triangle-list lookup and once fresh
  after it. Every attempt to break that CSE was measured and is worse:
  five spellings of the two triangle-list lookups (indexed `void **` base 112
  to 125, `entry->vertexCount` in the byte-offset form far worse, the
  `- 4` folded into the base byte-identical at 36), four spellings of the
  count/length pair, and `volatile` on the count as well as the length (flat).
- Flat and eliminated this pass, each measured: the `particlePtr` cursor
  replaced by `entry->particles[particleIndex]` (37, +3 opcode words); the
  `outputPtr` cursor replaced by `output[i][*]` (442, +12 instructions); the
  `addressBase` local inlined (458 instructions -- unlike func_80041CE4, this
  one really is a local, because the constant is materialised twice without
  it); a second `Gfx *` command carrier (frame `0x168` -> `0x170`, the
  declaration count is saturated); all four loop forms of both inner loops
  (`for`, `while`, guard-plus-`do` -- uopt normalises them, byte-identical);
  two `alpha` spellings; and a 200-cell sweep of the two command scalars'
  declaration position across all three scopes crossed with four length
  spellings, which moves the achieved home offsets (92 and 96 and 100 and 104
  are all reachable) without moving the score off 36 at all. Declaration order
  here buys stack homes and nothing else, exactly as the field guide says.
- Next lever: the tail edit that pairs with the shift spelling -- the ring
  phase entering the unrolled vertex store loop -- not another declaration
  census and not another loop form.

#### 2026-09-10, lane c2-resident: the head claim does not reproduce, and the tail is inert

Still 36, and this pass is a falsification rather than a search.

**The shift spelling's benefit is not reproducible against the current tree.**
The pass above records that `(vertexCount << 3) + (vertexCount << 1) + 8`
"makes aligned rows 19-59 exact" and moves the first mismatch from `+0x4C` to
`+0xF0`. Measured now with `tools/score_symbol.py`, that spelling scores **107
masked with the first mismatch still at `+0x4C`**, size delta 0 -- the same
first mismatch as the retained `vertexCount * 10 + 8` form, which scores 36 at
`+0x4C`. Three further shift groupings (`((a) + (b)) + 8`, `8 + (a) + (b)`, and
the two shifts written in the other order) land at 107, 107 and 108. So the
head is *not* causally explained, and the "next lever" the pass above names --
find the tail edit that pairs with the shift spelling -- rests on a measurement
that does not stand. Anyone resuming should re-derive the head from the objects
before spending a pass on the pairing.

The likely cause of the discrepancy is a measurement trap worth naming: a
stale candidate object left by an earlier sweep reads as a large, plausible,
completely wrong diff (here 430 differing words at a size delta of +48 against
the real 36 at delta 0), and it is invisible unless the size delta is checked.
Score with `tools/score_symbol.py`, or check `size_delta` before believing a
hand-rolled comparison.

**The tail is inert to its own text.** A 65-cell cross product of five length
spellings against thirteen tail shapes -- the guarded `do`, a top-tested
`while` and a `for` over the vertex store loop, each crossed with the written
operand order of the three component adds and with a `(f32)` weight cast on
either operand, plus an indexed `output[i][*]` cursor -- is byte-identical
within each length spelling. uopt normalises all three loop forms, and, unlike
`func_8003F154`'s final adds where the same cast moves five words, the cast is
**completely inert** here: on this site the two float adds' operand order is
already whatever weight chooses and the cast does not change it. So the tail
residual is not reachable from the loop body's own spelling in any of these
axes, which is a stronger negative than the previous pass's "all four loop
forms are byte-identical".

#### 2026-09-10, lane w8-bigclose: the head is two problems, and only one of them is the spelling

The previous pass asked whoever resumed to re-derive the head from the objects
before spending another pass on the pairing. Done, and it separates two facts
that three passes have run together.

**The score falsification stands, reproduced independently.** The two-shift
spelling of the command length measures **107 masked at delta 0, first differing
word still at `+0x4C`**; the parenthesisation variants are 107, 107 and 108;
carrying the count into the sum is 109; and computing the count first so the sum
can read it back is 411 at delta -1. No spelling of the length's arithmetic
improves on 36.

**But the target's head arithmetic really is the two-shift form, and it shares
one value with the count.** Read off the objects: the target computes the
eight-times value once into a caller-saved temp that dies immediately, doubles
the vertex count separately, sums the two, adds eight, stores that sum as the
command length, and stores the eight-times value itself as the command count.
The candidate builds the length as a four-times / add / double chain and stores
the **CSE'd** eight-times value out of a callee-saved register. So the 2026-09-09
pass was right about the shape and wrong about the score, and the 2026-09-10
pass was right about the score and concluded the shape was unexplained. Both
halves are now settled.

The head is therefore two independent problems:

- the arithmetic, where the target shares the count's value with the sum and the
  candidate does not -- the CSE this ledger already describes; and
- the command length's **stack home**, which is frame offset **72** in the target
  and 96 here. The recorded 200-cell declaration sweep reached 92, 96, 100 and
  104 and never 72, so the home is not a position in the existing declaration
  list at all: reaching it needs the list's *shape* to change, which declaration
  order cannot do ([L99] orders the list, it does not resize it). A second home
  differs the same way -- one loop carrier spills to 108 here and to 92 in the
  target.

**The home is bounded, not merely unreached.** Reading the whole frame back from
the candidate object -- every stack displacement the function touches -- the
candidate occupies 76, 96, 108, 120, 124, 136, 140, 144, 148 and 152 and the
target occupies 72, 76, 92, 120, 124, 136, 140, 144, 148 and 152: ten
memory-resident items each, agreeing everywhere except that one pair. Sweeping
the ladder against that readout rather than against the score -- 110 single
moves of the eleven inner declarations, 72 of the nine outer, all six orders of
the three innermost, the `volatile` in all four placements over the two command
scalars, and the pair relocated to every position of both other scopes, some 190
cells -- reaches 80, 92, 96, 100 and 104 for the length and **never 72**. Nor can
the list grow into it: a twelfth declaration moves the frame to 0x170 in every
form measured, **including the one that ought to be free** -- naming the shared
`(s32)vertexStart + addressBase` temporary, which already owns a spill slot. The
ladder's floor at eleven declarations is 76; the target has an item below it at
the same total frame size; and neither declaration order nor declaration count
reaches that.

**Next lever: the frame's shape, and it needs the `.mdebug` census rather than
another order sweep** -- which item owns each slot, and what makes the candidate
leave 80..92 empty where the target leaves 96..116 empty. Price the length's
spelling only after that; pricing the spelling first is what produced two
contradictory head claims.

<!-- plateau-handoff:func_80041530:end -->
