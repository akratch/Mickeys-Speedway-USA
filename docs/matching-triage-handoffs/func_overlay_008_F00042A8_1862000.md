<!-- plateau-handoff:func_overlay_008_F00042A8_1862000:start -->
### `func_overlay_008_F00042A8_1862000` plateau handoff

- source: `src/overlays/o008/overlay_008.c`
- score: 384 differing words
- frame: 0xA0
- relocations: 40
- first mismatch: +0x44
- summary: Configured flags remain best; reconstruct FP lifetimes and named data identities to supply 17 words and 22 relocation records without changing the exact frame.

Fresh maintenance evidence (base `f6d1bdbe8338886e3cce45818144bf68cbec5720`):

- Ownership is overlay 8 `.text` offset `0x42A8..0x49A4`, exactly 1,788 executable bytes with no target padding. The following function begins at `0x49A4`; the TU-level padding at `0x5128..0x5130` is separately owned.
- The guarded candidate uses IDO 5.3 with configured `-O2 -mips2 -32 -Wab,-r4300_mul` and ABI `void (O8P42A8Actor *, O8P42A8Owner *, f32)`. It is exported at overlay offset `0x42A8` and has resident and overlay-92 `R_MIPS_26` callers.
- Fresh V0 is 430 words (1,720 bytes), 17 words short of the 447-word target. Candidate and target frames are both `0xA0`. Workbench reports `structure-mismatch`, 384 positional/raw differences, 75 alignment gaps, and the first relocation-identity difference at `+0x38`; the relocation-masked first word difference is `+0x44`.
- The target has 40 runtime relocation records while the candidate has 18 static records. Only seven offset/type sites align, no stable/effective identities align, and all 18 candidate identities remain unresolved.
- The complete 119-form flag lattice confirms the configured flags are best at 384 differences and `-68` bytes. The next rows are at least 390 differences; the only exact-size row, `-O2 -mips3 -32 -Wab,-r4300_mul`, regresses to 421 differences.
- The nearest permitted donor scan found no credible sibling; the best result was Dinosaur Planet `camnormal_func_12E8` at Jaccard `0.0442`.
- Prior rounds already tested MIPS-I geometry, target-motion home pressure, saved-angle lifetime, mode placement, acceleration and steering homes, and target-motion volatility. No bounded permutation is justified without a new natural gain.
- Next lever: replace the remaining offset-only data proxies with authenticated overlay-local identities while reconstructing the FP lifetimes that keep the exact frame but add 17 instructions. This must improve both code geometry and the 22-record relocation deficit before allocator tuning.
<!-- plateau-handoff:func_overlay_008_F00042A8_1862000:end -->
