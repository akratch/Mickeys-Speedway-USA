<!-- plateau-handoff:func_800084C4:start -->
### `func_800084C4` plateau handoff

- source: `src/main/objects.c`
- score: 168/343 words
- frame: 0xB0
- relocations: 8
- first mismatch: +0x2C8
- summary: One candidate-only word at +0x2D4 drives a coherent seven-register ring rotation; explicit cast and signed parameter forms are flat.
The configured full-TU baseline had 348 candidate words against 343 owned
words, 314 raw/masked differences, first +0x0, a 0x98 frame against 0xB0,
and eight relocations on each side with three authenticated identities.
The retained candidate has 344 words (one excess instruction), 168 raw
and masked differences, first +0x2C8, and the exact 0xB0 frame. All named
stack homes and the ordered floating-point instruction sequences agree.
The eight relocations have seven exact offset/type pairs and authenticated
identities. This remains guarded NON_MATCHING C; it earns no matching credit.

The assignment gate returned base-only. The oracle's closest result was
JFG objDrawTracer, whose published body remains assembly; no external C
was adopted. Rescoring the 53 usable cached full-TU flag objects favored a
mode without the multiply workaround by a small positional margin, but it
remained structurally nonexact. Configured flags stayed unchanged. The
1372-byte ownership range was independently proved from linked fallback
bytes against the baserom before the source experiments.

Moving clipped-coordinate copies after the early cull corrected their
store order. Nine scoped command pointers, one retained vertex cursor,
and reuse of the consumed squared-length scalar recovered the target
frame without padding. Declaring squared length before the deltas recovered
the remaining scalar homes. Reversing equality operands accounted for
IDO's load order. Updating the copied clipping coordinates in place then
recovered every floating-point operation and register sequence. Complete
SDK-style unsigned-int byte-field packing preserved the vertex command's
explicit mask. Defined unsigned subtraction for physical addresses retained
a shared constant and moved the first mismatch far into the function.

Attempt numbers are global to the lane. Attempts 119–126 recovered frame
and scalar homes; 127–132 tested native word types, flag carriers and command
packing. Attempt 133 recovered the clipping sequence; 135 reached the best
residual. The final five probes 136–140 tested native unsigned flag type,
signed address carriers with addition, native signed flag type, commuted
flag OR, and signed-minimum constant spelling. None improved the residual
or resolved the missing carrier relationship. These five probes are the
stall evidence. Cleanup 141 retained attempt 135's measurement. Sources,
objects, first mismatches and rationales remain in ignored build artifacts.

The workbench verdict is structure-mismatch and its structure-buckets
lever requires resolving instruction shape before allocator experiments.
The next concrete lever is the texture-call argument expression: the
candidate retains an extra move for the OR-ed flags. After that, establish
why the target shares a colored physical-address constant while generating
addition; the retained subtraction shares the constant but changes two
operation forms. Addition spellings tested so far rematerialize it and lose
the earlier register sequence. Authenticate the final relocation identity
before any promotion. Frame padding and new volatile qualifiers were not
used.

Validation: configured full-TU compilation/comparison, diagnosis and guides,
cached flag rescoring, linked fallback ownership proof, finalize_plateau.py,
full gmake verify, check-docs, cleanroom and check-nonmatching-builds. The
full ROM proof uses assembly for this function; the C deficit remains.

<!-- plateau-handoff:func_800084C4:end -->
