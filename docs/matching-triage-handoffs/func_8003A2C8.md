<!-- plateau-handoff:func_8003A2C8:start -->
### `func_8003A2C8` plateau handoff

- source: `src/main/menu.c`
- score: 1 differing words
- frame: frameless
- relocations: 6
- first mismatch: +0x14
- summary: Wide carrier remains coupled to global-address color; precomputed form reaches 5 words. Five-minute real-TU sweep found no improvement.

- 2026-09-09 pass, the residual identified and the wall measured. The target
  keeps two values alive across the guard: the masked mode as an int, and its
  byte-wide copy. The equality test reads the int; both bit tests read the
  byte copy; the store writes the int. The candidate's test reads the byte
  copy, which is the single differing word, and every other word including the
  global's address register is exact.
- Making the test read the int is easy and always costs the same word back.
  Every spelling that does it -- a comma expression, a separate statement for
  the byte copy, an int carrier assigned in the condition, an assignment
  through the byte pointer, a comma inside the mask operand -- lands the
  global's materialised address one colour later than the target, and the
  candidate then skips that colour entirely. Four words instead of one, with
  the branch right.
- Eliminated this pass, each measured: about fifty forms across six sweeps.
  Both operand orders of the test; the guard local's address alias present,
  absent, assigned before or after the state alias, assigned inside the body,
  and read at one, both or neither bit test; direct global against a cached
  pointer for the read and for the store; an int carrier declared before or
  after the byte carrier and assigned before, inside or across the condition;
  reading the global back after the store to get the byte copy by store
  forwarding, at both widths; an explicit byte mask on an int carrier; a
  second unused byte local; the xor-zero and cast forms of the use-side lock
  break, which are byte-identical to the baseline; and five physical line
  groupings, which are flat here.
- The address alias for the global turns out not to matter at all: dropping it
  keeps the one-word residual. What matters is the byte carrier's address
  alias, which is what stops the byte copy being coalesced into the argument
  register; without it the whole colouring shifts and the residual is 19.
- Next lever: the coupling is between the test's operand and the address
  colour, not between the test and the schedule, so look for a form that adds
  the int web without adding a colour consumer ahead of the address -- or for
  evidence that the byte copy is not a local at all but a second read of a
  byte-wide global that the store forwards.
<!-- plateau-handoff:func_8003A2C8:end -->
