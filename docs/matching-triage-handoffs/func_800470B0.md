<!-- plateau-handoff:func_800470B0:start -->
### `func_800470B0` plateau handoff

- source: `src/main/fx.c`
- score: 90 differing words
- frame: 0x168
- relocations: 3
- first mismatch: 0x44
- summary: JFG efd5abb remains assembly-only; zero source attempts. Need new fixed-bound loop carrier topology evidence.


Reopening audit (2026-09-08), evidence D: PROVENANCE inspection of Jet Force
Gemini public decomp `src/fx.c` and `src/fx.h` at
`efd5abb1c79636e297b831f7c2d5bf47eac39c0c` found no new target C body.
No donor source, names or values were adopted. Mickey remains authoritative.

Configured full-TU measurement: candidate 596 bytes / 149 words,
target 596 bytes / 149 words; size delta +0 bytes.
Raw and relocation-masked differences are 90 and 90, respectively,
first mismatch +0x44; candidate/target frames are
0x168/0x168. Candidate/target static relocation counts are
3/3; 0 tuples agree in function-relative offset, type and symbol.
These are fallback-object comparisons, not linked-C promotion evidence.

Workbench comparison: `structure-mismatch`. Diagnosis: `mixed(constant:3, structural:19, register:55)`;
playbook `constant-audit`, lever `stack-home`. The named guides were read.
This heuristic routing supplies no new donor evidence to reopen exhausted forms.
Next concrete lever requires new cone-point donor C explaining the fixed-bound loop carrier and its stack home,
then a fresh gate and configured full-TU comparison.

Stopping evidence: zero source attempts; the authorized donor mechanism supplies
no new implementation. This is the assignment's early-exhaustion stop, not a
five-attempt stall. The prior TU lattice is not repeated: since its recorded
`func_8004ACC4` audit at `4be95a3d`, this TU differs only in EOF handoff comments.
See [the anchor donor audit](func_80049E4C.md) for the full delta and batch disposition.

Ignored evidence is retained in `build/tu-fx-audit/` and `build/wb/`: source and
full-TU object, gate receipts, summaries, diagnoses and relocation tuples.
Commands: `lane_status.py --symbol`, `wb_compare.sh --summary-json`, workbench
`diagnose` and `guide`, and `finalize_plateau.py`. The unchanged guarded C stays
NON_MATCHING and receives zero new exact bytes.

Prior plateau evidence retained (historical measurements and exhausted forms):

- measured shape: target and candidate are both 149 words with frame 0x168; the retained C improves the refreshed baseline from 130 to 90 positional differences.
- relocation evidence: both objects carry the same three call identities, but none shares the target offset; the two trigonometric calls move together and the vertex helper remains one word late.
- bounded attempts: ten source hypotheses covered point-array extent, countdown CFG, integer index widths, call-loop spelling, declaration order, pointer typing, and web splitting; the 119-row flag lattice had no exact result and tied the canonical O2/MIPS-II row at 90 differences.
- donor evidence: the five-reference skeleton scan ranks JFG `func_80069320` first at 0.238 masked four-gram Jaccard, with the next JFG candidate at 0.082; coddog found no exact 80-byte window.
- JFG utility: the precise public-tree insertion point would be JFG `src/fx.c` at the `func_80069320` pragma. Mickey's retained C is only a scaffold for cone-point generation and vertex topology: JFG's function is 496 bytes versus Mickey's 596, JFG remains assembly-only, and this Mickey candidate is itself non-matching.
- next lever: explain or remove the fixed-bound register web that occupies `s6`; it shifts the three signed arguments, cone pointer, angle step, local array home, and every call offset as one allocator cascade.

<!-- plateau-handoff:func_800470B0:end -->
