<!-- plateau-handoff:func_80006534:start -->
### `func_80006534` plateau handoff

- source: `src/main/objects.c`
- score: 17 differing words
- frame: 0x38
- relocations: 7
- first mismatch: +0x11C
- summary: Region plus independent field-capture order: 188 exact, 15 naming, two schedule rows. Saved-register closure falls; phantom web and local ring remain.

The configured full-TU baseline was 207 words against 205 owned words,
198 raw and relocation-masked differences, first mismatch +0x0, and a
0x48-byte frame against the target's 0x38. The retained candidate is 205
words (820 bytes), with 142 exact words and 63 raw/masked differences,
first mismatch +0x38. All seven relocation count/type/offset/identity
comparisons are exact. Instruction opcodes, scheduling and frame size are
exact; this is compiled NON_MATCHING C, with no new matching credit.

The assignment gate returned base-only. The near-match oracle's closest
result was JFG `func_8000710C` (904 bytes, Jaccard 0.145); its available
source remains assembly-only, so no donor body was adopted. Rescoring the
53 usable objects from the TU's retained flag lattice gave no improvement
over configured O2/mips2. No shared compiler flags changed.

The source now reloads the actual loop bounds, retains the sprite frame
count across the random call, treats the stored object count as unsigned,
and models the resource list as its count, entries and trailer. Decoding
both packed descriptor indices before storing the sprite flags removed
duplicated extraction and reached the exact instruction layout. Reading
those indices after the store would add an alias-sensitive reload; that
experimental spelling was rejected and is not the retained candidate.
The cleanup sprite loop deliberately preserves the target's unchanged
sprite pointer. No extra null guard or reordered call was invented.

The campaign attempt count is global across this lane. Attempts 9–14
reduced the residual through loop-bound, flag-test and count-lifetime
corrections; attempts 15–20 tested signed flags, narrow indices, aliasing,
register qualifiers and split floating-point updates. Attempt 21 reached
63 differences and exact layout. Attempts 22, 23, 25, 26 and 27 then stayed
at exactly 63 differences with the same frame, layout and relocation
identities: raw-flag capture order, count declaration order, a typed list,
consistent unsigned flag fields, and byte-width frame count produced no
further change. Attempt 24 regressed to 68 differences. These provide the
five unchanged residual probes for this packet's stopping rule. Attempt
25 is retained because it gives the list a direct, evidenced type without
additional experimental flag or count types.

A three-minute report-only permuter batch failed before searching: its
isolated function recipe copied an unrelated whole-TU `.rodata` symbol
addition into a scratch object without that section. It produced no search
result and does not establish permutation exhaustion. The failure, all
material source/object attempts and scalar measurements remain in ignored
build artifacts. This technical failure was not counted as an attempt.

The workbench verdict is `allocation-mismatch`; its next playbook is
`register-role-audit`. The next concrete lever is to establish the competing
saved-register lifetimes of the sprite cursor and frame-count/resource-list
cursor, then use a bounded allocation search with a seed proved identical
to the configured full TU. Do not infer an allocator pool from the visible
register rotation alone. The current temporary flag/extracted-index webs
also differ, so fixing only one saved-register pair may not close it.

Validation: configured `wb_compare.sh --summary-json`, workbench diagnosis,
retained flag-object rescoring, `gmake verify` with the expected US ROM hash,
`gmake check-docs`, `gmake cleanroom`, and `tools/finalize_plateau.py`.
The canonical ROM proof covers the assembly fallback, not this nonexact C.


## 2026-09-11 (lane `lane/w3-mid`): all three residuals named and priced, none adopted

Unchanged at 63 words. Aligned: 142 byte-exact, 63 register naming, 0 immediate
only, 0 really different; size delta 0. A pure allocation residual over an
instruction stream that is already exact, which is why `frame_census` was not
needed and why every structural axis is retired by construction.

The allocator records below come from the instrumented uopt with its `.text`
checked byte-identical to the tree's object. This procedure is globalcolor
invocation 23 of `src/main/objects.c`, identified by emission order, and it
emits p1 records only, so the call test in the lane brief holds again and no
p2 or L106 axis exists here.

The register-substitution census partitions the 63 words into exactly three
independent regions, and forcing them in turn prices each one.

Region 1, worth 27 words (63 to 36): s2 becoming s1 on 22 sites and s1 becoming
s2 on 12. p1 web 3 is `sprite`, totalsave 164 over nocs 14, save 11.714286; p1
web 41 is the strength-reduced `list->entries` induction pointer, totalsave 62
over nocs 5, save 12.4. The decision variable is which save is larger, and the
target wants `sprite` decided first. This was also reproduced from source:
adding one loop-depth reference to `sprite` raises it to 184 over 14 and the
exchange flips with no force at all. The probe used for that -- advancing the
cursor in the cleanup sprite loop -- is refuted as the real source form,
because the target's loop tail is a branch-likely with a reload in its delay
slot rather than a cursor increment, so the probe costs two structural words and
lands at 38. The exact requirement is one more loop-depth reference to `sprite`
at zero instruction cost, or the induction web's totalsave cut from 62 to 58 or
below.

Region 2, worth 8 words (36 to 28): a0 becoming v1 on 7 sites, a1 becoming a0 on
5, a2 becoming a1 on 5. p1 web 77 is a phantom. It is decided first on save 50
over nocs 1, takes v1, and emits no register anywhere -- forcing it to a2 and to
a3 produces byte-identical objects, which is the proof. It is the common
subexpression of the repeated `record->unk8` read, and it sits ahead of `flags`
(web 74, totalsave 40 over nocs 2), `reference` (web 79) and `frameOffset`
(web 85), pushing all three one lane down the caller-saved pool. Displacing it
puts those three exactly where the target has them. Every source spelling that
lowers its weight also moves the instruction stream: naming the shared temp
removes web 77 and hands v1 to `flags` but costs 26 structural words and four
bytes; reading `reference` from the `flags` local drops web 77 from 50 to 40 and
costs 11 structural words; reading `frameOffset` from `flags` changes nothing at
all, which shows that read is already folded into the same web.

Region 3, the last 28 words: with regions 1 and 2 forced, the entire remainder
is a permutation of the integer temp ring, and no globalcolor force anywhere in
a c1 to c22 sweep of web 77 touches any of it. The stream is byte-exact through
+0x120 and the two ring states diverge from +0x124 onward, so the cause is a
single ring pop before +0x124 -- the L127 family, reachable with the ugen
free-list hooks and their source-line stamps, not with uopt's colour records.

Retired by this measurement: the whole structural family, the frame, and any
reading in which these 63 words are one problem rather than three.

## 2026-09-12, lane p19-reopen: one region closes 44 words

Before: 142 exact, 63 naming, zero immediate, zero structural; after: 186
exact, 19 naming, zero immediate, zero structural. Both are 205 words with
frame 0x38, zero displacement tax and seven exact relocation identities.
The first mismatch moves from +0x38 to +0x11C. An empty region immediately
after both packed-field captures and before the sprite flag store is the
only adopted edit. It changes no value, access order, call or executable size.

The configured stock compiler re-proves the result. The baseline instrument
was gated against the full configured TU, retaining all per-file flags.
Ordinal 23 still emits p1 only. On the adopted shape, sprite web 3 has
totalsave 174 over nocs 14, save 12.428572, against the unchanged list cursor's
62 over 5, save 12.4. It therefore takes s1 before the cursor takes s2.
The original sprite total was 164: this region supplies the missing weighted
reference at zero emitted width. The inherited claim that further structural
source changes were retired does not survive this experiment. L142 is inert
for this saved-register pair; L139's total-versus-cost rule remains satisfied.

The region also changes the unused packed-read web 77 from total 50 to 40
and the flags web from 40 to 50 over two components. Web 77 still wins v1,
leaving flags/reference/frameOffset one caller-saved colour too high. An
accepted force of web 77 to c5 gives eight naming differences at delta zero;
the decision and colour rows record forced=5. A c13 force is also accepted
but costs a word and is rejected. This force is diagnostic, never matching C.

The eight forced residual rows occupy +0x124 through +0x164 and are four
producer/consumer pairs: the signed scale-field load, the two packed-index
masks, and the sprite-byte copy. The instrumented ugen trace names the exact
source lines and draw sequence. The effect is local; the later ring agrees.
The next lever must remove or displace the unused packed-read web while
preserving this instruction stream, then change those mask/load draws.

Measured against the correct definition: unsigned typed field reads either
leave the object unchanged or remove a word; signed shifts change four
structural rows. Mask-before-shift adds a word. Separate loop index identities
regress or enlarge the frame. Moving sprite initialization later regresses.
Regions before either packed-field capture add a word; the boundary after
both is the 44-word gain. On that new shape, reading the fields from flags,
adding earlier boundaries, or moving the store before the captured-field
extractions is nonexact. Splitting each mask into a later assignment is
text-inert in four positions. Three final identity-operation variants on the
index locals are also text-inert, with no new decision information: ADR 0018
stall. Preserve the 19-word guarded candidate.

A harness initially selected an earlier prototype and ran ten baseline
repeats. Those are not negative evidence. The boundary selection was fixed,
every mutation was checked against the actual body, and all probes above
were rerun. The shared activity count was kept monotonic. External private
scratch retains sources, objects, scores, configured commands, decisions and
the ugen trace. Commands include residual_map, align_symbol, register_census,
frame_census, direct configured comparisons and the final plateau tool.


### New-shape order check: 19 to 17, final retained source

The region invalidates the old order closure (L146). A guarded move-one climb
ran 186 evaluations to a fixed point in two passes and swapped only the two
independent packed-field captures. Both still precede the sprite flag store;
no alias-sensitive read or call crossed that store. Final buckets are 188
exact, 15 naming, zero immediate and two schedule rows, with 17 masked and
raw differences, 205 words and first +0x11C. This is the retained source.

On this shape, a signed flags carrier gives 27; unsigned descriptor storage
and consistent signed reads are text-inert. Narrow or unsigned index locals
add 8 to 40 bytes. Forwarding frameOffset from flags adds one word; forwarding
reference from flags is text-inert. These controls do not improve the new
optimum or introduce a further route through the phantom web. The earlier
19-word object, its accepted force receipt and every new control are retained
separately. No unforced object is exact.

The four default gates passed on the region-only source; verify, cleanroom
and check-docs were rerun for this final order and evidence update. The ROM
proof covers the guarded assembly fallback and gives no matching credit.

<!-- plateau-handoff:func_80006534:end -->
