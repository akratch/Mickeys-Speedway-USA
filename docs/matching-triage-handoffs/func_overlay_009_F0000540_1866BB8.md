<!-- plateau-handoff:func_overlay_009_F0000540_1866BB8:start -->
### `func_overlay_009_F0000540_1866BB8` plateau handoff

- source: `src/overlays/o009/overlay_009.c`
- score: 8/129 words
- frame: 0x58
- relocations: 10
- first mismatch: +0x4C
- summary: 8 words, one saved-FPR colour ordering. 2026-09-12 lane p8-close force-verified the target assignment instead of inferring it: colouring the first hoisted load to c35, the second to c34, the third to c33 and the two sixteens to c31 and c32 returns an object at ZERO masked words and delta 0, so the residual is exactly that colour order and nothing else. The ladder it demands is strict -- zero, then the negative sixteen, the sixteen, the third load, the second, the first -- and the two halves now have different status. Half one, the constants above the loads, is REACHED for the first time: a zero-instruction basic block at the end of the else arm raises only the three loads' divisor from 4 to 5, taking their save from 2.75 to 2.2 while the sixteens hold 2.5, because a block after the constants' last use still sits inside the loads' range. It costs 4 bytes in the one spelling measured and it un-splits the twenty, so the candidate scores 101, but the mechanism is real and is not in any earlier record. Half two is the blocker: the three loads tie, a tie breaks on ascending web number, and their numbers track the emission order the object pins. 120 hoist orders and 120 declaration orders, each crossed with three region placements, are all 8 or worse, and the class-2 ladder is bit-identical across all 32 hoist-membership subsets.

#### Evidence (2026-09-09)

Workbench: allocation-mismatch, insns 129 of 129, frame -88 of -88, opcodes 0,
gaps 0, aligned_total 11, words 8, raw 9, regs 8, fp 8. Every one of the eight
differing words is a saved-FPR register number. Nothing else in the function
differs.

The residual is a single six-way colour assignment over the loop-invariant
block that is hoisted ahead of the do/while. The target puts the three
threshold loads in the top three callee-saved float registers, descending in
emission order, and the three materialized literals in the bottom three, also
descending. The candidate does the opposite: the loads land in the bottom
three ascending, the two non-zero literals in the top two descending, and the
zero in the lowest.

The allocation law behind that was measured on this translation unit with two
controlled experiments. With three hoisted loads and two hoisted literals the
loads take the three lowest callee-saved float registers ascending in emission
order and the literals the two highest descending. Dropping one load to an
in-loop read leaves two loads in the lowest two ascending and moves the
literals to the three highest descending. Making every value an in-loop read
gives the literals the highest four descending and spills the three global
addresses into saved integer registers instead, which costs four words. So the
group a value belongs to is fixed by whether it is a hoisted memory load or a
materialized literal, and each group's direction is fixed with it. The target's
assignment inverts both directions at once, which no partition of these five
values into the two groups can produce.

Exhausted without reaching below eight:

- A 15,360-point source lattice: all 120 orderings of the five hoist
  statements, times four placements of the declarations (block scope, function
  scope, literals at function scope with loads in the block, and the reverse),
  times initializer-in-declaration versus separate assignment, times all
  sixteen operand orders of the four float comparisons. The whole lattice
  scores only eight or nine, never less, and every eight-scoring point has the
  identical eight differing offsets.
- The `register` storage class on the literals, on the loads, and on all five.
- Assigning the literals inside the loop body rather than before it, naming the
  zero as its own local, deriving one literal from the other by negation, and
  joining the hoist statements onto one physical line.
- A fourteen-point compiler flag lattice on the whole translation unit,
  including -O1, -O3, -g3, -mips1, -mips3, -KPIC, -float, -Olimit 0, dropping
  -Wab,-r4300_mul, and four -Wo options. The canonical
  `-O2 -mips2 -32 -Wab,-r4300_mul` is the unique optimum; every other setting
  either scores worse or changes the executable size, so the translation unit's
  flags are confirmed by measurement and are not the explanation.

Reopen only with a new mechanism that changes which allocation group a hoisted
value joins, or with evidence that this function was compiled in a different
translation unit from the rest of overlay 9. Re-running the order, scope,
initializer, operand-order, storage-class or flag axes will re-derive eight.

#### 2026-09-09, lane win-b: independent reproduction, two more axes eliminated

Re-measured on an independent harness (direct `tools/ido/cc` full-TU compile at
this TU's real flags, which do include `-Wab,-r4300_mul`): 8 words, 129 of 129
instructions, same eight rows. The recorded law reproduces exactly.

**Sharper statement of the residual.** Written as a colour list over the six
callee-saved float webs, both sides agree on two entries and reverse the other
four. Taking the colours in ascending register order f20..f30, the value each
holds is:

- target, f20 through f30: zero, -16.0, 16.0, D_14, D_10, D_C
- ours, f20 through f30: zero, D_C, D_10, D_14, -16.0, 16.0

The zero and the third load are fixed points; the remaining five values are
assigned in the **exact reverse** of our web order. The target's whole
assignment is one rule -- take the highest free callee-saved FPR in emission
order, f30 down to f20 -- while ours applies two rules, loads ascending from
the bottom and literals descending from the top. Since a load's defining
instruction *is* its emission, reversing the web order and reversing the
emission order are the same edit, and the emission order is pinned by the three
`lui`/`lwc1` pairs at +0x48..+0x5C. That is why the 120 hoist orders floor at 8:
every one that fixes the colours breaks the load rows.

**Two axes not in the record above, both flat at 8:**

- **Declaration order** (distinct from the four declaration *placements*
  already recorded): all 120 orders of the five `f32` locals, statement order
  held fixed, produce a byte-identical object. Lever 46 does not reach the FPR
  pool here.
- **Group cardinality.** Hoisting a sixth value to move the split -- `20.0f`
  into a named `scale`, the `0.0f` into a named `zero` (first or last), and
  both together -- leaves 129 instructions and the same eight rows: uopt sinks
  the extra literal straight back into the loop, so the group sizes cannot be
  changed from source. `const` on the loads and initialiser-in-declaration are
  likewise flat.

The reopen condition is unchanged and now better specified: what is needed is a
mechanism that makes a hoisted *memory load* join the literal allocation group
(or the reverse), because nothing that only reorders, rescopes, retypes or adds
values reaches the target list.

#### 2026-09-10 lane `c2-reopen`: reopened against four new laws; the closure stands

Baseline reproduces: 516 bytes, raw 9, relocation-masked 8, zero size delta.

- **Operand weight (heavier commutative operand left) explains an axis the
  closure only measured.** That law is commutative arithmetic only and does not
  reach comparison operand order, so the sixteen float-comparison operand
  orders inside the 15,360-point lattice were inert by construction rather than
  by coincidence. The lattice's real width is a quarter of its nominal size.
- **Loop shape is the one axis the closure never swept, and it is now
  available.** The guarded `do`/`while` was rewritten three ways with the
  iteration count preserved -- top-tested `while` on the pre-decrement, an
  equivalent `for`, and a pre-decrement bottom test off a positive guard. All
  three change the executable size (+12, +12 and -8 bytes) against a target
  pinned at 129 of 129 instructions, so the axis cannot be spent here whatever
  it does to the web numbering.
- The induction-exit-test and co-region-read laws do not reach saved-FPR group
  membership, which is what the reopen condition asks for.

Verdict: **this closure was correct and remains correct under the new laws.**
The reopen condition is unchanged -- a mechanism that moves a hoisted memory
load into the materialized-literal allocation group, or the reverse.

#### 2026-09-10, lane `o7-mid`: bounded reopen — the closure holds, and the residual is restated as a tie-break

Base reproduces: 516 bytes, raw 9, relocation-masked **8**, size delta 0. No
edit was made.

**Read from the object rather than the record**, the six callee-saved FP
assignments in emission order are:

- emission order is the same on both sides: `D_C`, `D_10`, `D_14`, `16.0`,
  `-16.0`, `0.0`.
- target colours, in that order: f30, f28, f26, f24, f22, f20.
- our colours, in that order: f22, f24, f26, f30, f28, f20.

So `D_14` (f26) and the zero (f20) are **fixed points**, and the whole residual
is that the pair `{D_C, D_10}` and the pair `{16.0, -16.0}` exchange their
colour blocks. The target's assignment is one descending run f30..f20 in
emission order; ours is not.

**That is better described as a colouring ORDER than as group membership.** All
five hoisted values carry exactly one loop-level reference each, so their
`save` terms tie ([L7], [L100]), and a p1 tie is broken by web number and
nothing else ([L31], [L84]). Under that reading the reopen condition should be
read as *"find a web-number lever for `f32` locals"* rather than *"move a
hoisted load into the literal group"*:

- [L85], the one measured renumbering spelling, is a narrow-type truncation
  written at the store and does not apply to `f32` at all;
- [L86] already records declaration order, relational operand order, an added
  local and a hoist as moving no web number.

That is consistent with the recorded 15,360-point lattice flooring at 8, and it
explains *why* it does, which the group-membership framing did not.

**Tier.** This is inference from the two colour sequences (structural, tier D),
not a trace. A CDX capture settles it directly: if the five webs tie on save the
record says so, and if they do not, the save arithmetic names a cost lever the
group framing hides. Until then the existing closure stands and its 15,360-point
lattice should not be re-run.
#### 2026-09-11, lane w3-low: the CDX capture the closure asked for, and the five webs do NOT tie

The o7-mid entry closed with a named instrument and a falsifiable prediction:
"if the five webs tie on save the record says so, and if they do not, the save
arithmetic names a cost lever the group framing hides." The capture was taken.
**They do not tie, and the tier-D inference that they did is wrong.**

Baseline reproduces: 516 bytes, 129 of 129 instructions, delta 0, frame 0x58,
8 relocation-masked words, raw 9, first mismatch +0x4C. Aligner: 121 byte-exact,
8 register naming, 0 immediate-only, 0 really-different. No edit adopted.

Read from this procedure's own decision records, the six callee-saved FP webs in
decision order carry these figures. The zero constant is net 20.000000 over a
divisor of 4 for a save of 5.000000. Each of the three hoisted global loads is
net 11.000000 over 4 for a save of 2.750000. Each of the two materialized
sixteens is net 10.000000 over 4 for a save of 2.500000. A seventh web, the
twenty in the else arm, has the same net 10.000000 but a cost of 30.000000 and
splits.

**So the two "allocation groups" the original closure described are one arithmetic
fact: a hoisted load's web carries its definition occurrence and a materialized
constant's does not, and that single unit of net is the whole ordering.** The
records also give the groups a name the inference could not: the three loads are
type-3 webs numbered from the local symbol table with stack-slot metadata, and
all four constants are type-2 webs numbered from the constant table, which is why
the two families can never interleave by web number. A p1 tie between a load and
a constant would go to the load, because every load's number is far below every
constant's.

**Declaration order and statement order leave that arithmetic bit-for-bit
unchanged, measured on the records rather than on the object.** Reversing all
five declarations and, separately, reversing all five hoist statements were each
captured: the loads still take net 11.000000 and save 2.750000, the constants
still net 10.000000 and save 2.500000, the loads still precede the constants, and
the colours come out in the same order. Statement reversal renumbers the load
webs (they move up by six) but does not reorder them relative to each other or to
the constants. That is why the recorded 15,360-point lattice and the 120
declaration orders floor at eight, and it is a stronger statement than the
lattice alone: those axes are not merely unproductive here, they are provably
inert on the deciding quantity.

**Restated reopen condition, now as arithmetic.** The target's decision order is
the zero, then the negative sixteen, then the sixteen, then the three loads in
the reverse of their hoist order. Reaching it needs either a constant's net to
exceed 11.000000 while a load's stays at 11.000000, or a load's net to fall to
9.000000 or below, and it additionally needs the three loads separated from each
other by net rather than by web number, since their tie-break runs the wrong way
and no axis tested moves their numbers relative to one another.

**L109, the one documented zero-instruction way to move net, does not survive to
be counted on this body.** 120 probe cells: five locals crossed with four
value-guarded and truth-test forms crossed with six placements. All 80 in-loop
placements are byte-identical to the base, so the probe is eliminated before the
web builder and creates no occurrence; all 20 pre-loop placements cost 4 bytes
against a target pinned at 129 of 129 instructions; the 20 post-loop placements
do not compile, because the five locals are scoped to the guarded block.

That is the same negative levelFreeAll records for L109, now with a second
witness and with the records showing why: an occurrence has to reach uopt's web
builder, and a discarded read of an f32 local does not.

#### 2026-09-11, lane `p6-small`: re-measured after the float-bank census fix, still eight

Unchanged at eight relocation-masked words (nine raw), size delta 0, frame
0x58. `register_census.py`, rerun after the 2026-09-11 fix that stopped the
tool reporting float traffic under integer register names, now reports this
residual correctly and agrees with the hand reading in the record above: eight
sites, ZERO integer substitutions, one coherent float mapping over one window
at 100%, forming a single closed four-cycle over the callee-saved float bank.
That is one fp-ring-phase fact, which is what the earlier notes concluded from
the two colour sequences alone.

New negatives, all measured, all size delta 0 unless stated:

- **The hoist-versus-inline distinction is inert.** Writing the two sixteens as
  literals at their use inside the loop, rather than as hoisted locals, is
  BYTE-IDENTICAL to the hoisted form -- same six values, same emission order,
  same registers. So the "materialized literal" family membership the record
  describes is not a consequence of the source form, and the reopen condition
  cannot be reached by moving a value between hoist and use.
- **Region boundaries do not reach it.** An L97 region around the hoist block,
  in both spellings, costs 4 bytes and 101 words. The block-splitting lever
  that matched `func_8004BA8C` and moved `debug_text_width` on the same day is
  about symbol-level integer interference and has no purchase on the float
  bank here.
- **Reading the thresholds through const pointers is inert** (8 words, same
  offsets), as are all 120 declaration orders and six further hoist orders, in
  both that form and the base -- consistent with the recorded 15,360-point
  lattice.
- **A single-base struct or array view of the three thresholds is refuted.**
  The target reads them at three offsets from ONE relocated base where the
  retained C reads three separate symbols, which is the single raw-only
  difference. Expressing that as a pad-and-field struct view over the pool
  base, or as an array subscript of it, changes the whole function (122 words,
  first mismatch at +0x04), so the addressing that produces the target's
  addends is not simply a cast of the one symbol this file already exports.

The reopen condition in the record above is unchanged and remains the right
one: a constant at net 12 or above, or a load at net 9 or below.


#### 2026-09-12, lane `p8-close`: the target assignment is forced to zero, and the reopen condition splits

Baseline reproduces: 516 bytes, 129 of 129 instructions, delta 0, frame 0x58,
8 relocation-masked words, raw 9, first mismatch +0x4C.

**The residual is now a forced result rather than an inference.** Forcing this
procedure's five callee-saved float webs -- the three hoisted loads in hoist
order to colours 35, 34 and 33, and the two sixteens to 31 and 32 -- returns an
object at ZERO relocation-masked words and size delta 0, with no declined
force. So the whole residual is that one colour order; nothing else in the
function is in question, and a future pass can check a candidate against the
ladder directly instead of against the score.

The colour table here runs from colour 30 at the lowest callee-saved float
register up to colour 35 at the highest, and phase one colours in decreasing
save, lowest free colour first, ties on ascending web number. Our ladder is the
zero at 20 over 4, the three loads at 11 over 4 and the two sixteens at 10 over
4. The target's requires the sixteens strictly ABOVE the loads, and the loads
strictly DESCENDING in web number.

**Half one -- the sixteens above the loads -- is REACHED, and the mechanism is
new to this shard.** The divisor is not a property of the procedure; it is per
web, and a basic block placed after the sixteens' last use but still inside the
loads' live range raises only the loads' divisor. An `if (1) { }` at the end of
the else arm takes the loads to 11 over 5, which is 2.2, while the sixteens
hold 10 over 4, which is 2.5, and the records then show the sixteens coloured
first -- the first time any pass on this function has produced that order. The
one spelling measured costs four bytes and also stops the twenty splitting, so
the candidate scores 101; the value is the mechanism, not the number.

**Half two is the blocker and it is now arithmetic.** With the loads at divisor
5 and the sixteens at 4, separating the three loads needs three integer
numerators a greater than b greater than c with a over 5 below 2.5, so a is at
most 12, while c cannot fall below 11 -- ten for the single in-loop reference
plus one for the definition occurrence -- and no such triple exists. At divisor
6 the triple 13, 12, 11 does exist. So the reopen condition is: two such
zero-cost blocks after the sixteens' last use, PLUS one extra out-of-loop
occurrence of the second load and two of the third, all at zero instruction
cost against a target pinned at 129 instructions. L109 supplies none of them
for an `f32`, which the previous pass already measured from the other side.

**Negatives added this pass.**

- 120 hoist orders crossed with three region placements, and separately 120
  declaration orders crossed with the same three: all 720 candidates score 8 or
  worse at delta 0, and no ordering changes which load holds which web number.
- All 32 subsets of the five values written as a hoisted local against written
  at their use: the class-2 save ladder is bit-identical in every one, so
  hoist membership is provably inert on the deciding quantity rather than
  merely unproductive.
- Occurrence weight is NOT discounted by conditional nesting. Moving the
  damping multiply behind an `if` leaves its numerator at 11 and raises every
  web's divisor uniformly, so per-arm placement cannot separate the three
  loads. That closes the one remaining cheap route to half two.
- Eighteen zero-cost and near-zero-cost block insertions at six positions in
  three spellings: only the two at the end of the else arm move the ladder at
  all, and a bare brace never does, which is L97 reproduced on a float bank.

**Force hygiene for the zero result above.** `CDX_FORCE` is silently ignored
unless `CDX_PROC` also names the procedure ordinal, and an ignored force returns
a byte-identical object that reads exactly like L101's already-forbidden
decline. The five-web force quoted here was run with `CDX_PROC=1`; all five
records come back `decision=color` with `forced` equal to the requested colour
and no `force_declined` line, and the same force without `CDX_PROC` returns the
unforced 8-word object with every record at `forced=-2`.
<!-- plateau-handoff:func_overlay_009_F0000540_1866BB8:end -->
