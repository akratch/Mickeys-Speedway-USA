<!-- plateau-handoff:func_overlay_036_F0000818_1883CD0:start -->
### `func_overlay_036_F0000818_1883CD0` plateau handoff

- source: `src/overlays/o036/overlay36CheckNearbyHeight.c`
- score: 60/63 words
- frame: 0x80
- relocations: 3
- first mismatch: +0x0
- summary: Declaration order fixes every in-frame displacement; the residual is the frame SIZE alone, and block arithmetic proves a six-scalar source can never reach 0x70.

#### 2026-09-10 lane `c2-reopen`: the eight-byte floor is one budget, not one mechanism

Baseline reproduces: 252 bytes, seven relocation-masked words, zero size delta,
frame 0x80 against the target's 0x70; the residual is still only the two `addiu
sp` words and five sp-relative displacements.

The induction-exit-test law adds a second, independent route to eight bytes
that the closure never had: writing the scan loop's exit test as an explicit
`!=` against the counter gets test replacement, drops the counter, and takes
the frame to 0x78 without dropping any declared name. Three spellings of it
were measured and all three reach 0x78.

That route does not compose with the closure's own. Combining the `!=` exit
test with a direct `results[i]->y` read -- two mechanisms that each free eight
bytes alone -- still lands at 0x78, which promotes the closure's observation
that "dropping more frees nothing" from a declaration-surgery result to a
general one: **the eight bytes are a single budget, and no combination of
counter-drop and name-drop reaches 0x70.**

The route is also not usable on its own terms: every `!=` spelling unrolls the
scan loop, adding 224-236 bytes against a zero-delta target.

The operand-weight law reaches nothing here: the only commutative arithmetic is
a float add against a literal, which that law makes byte-identical either way.

Verdict: **the closure stands.** Its blocker is sharpened rather than moved --
the second aligned eight-byte quantum is not reachable by any counter or name
mechanism now measured, and a producer that changes the temp population is
still what is needed.

#### 2026-09-10 lane `o7-ovl`: seven words to three, and the frame closed arithmetically

Declaration ORDER is an axis no earlier pass swept, and it is worth four of the
seven words. cfe lays declared locals from the frame top in declaration order,
so a local's home is `frameTop - (bytes declared at or before it)` and the
array's base is `frame - 52 - (bytes declared before it)`. **All 5,040 orders
of the seven declarations were compiled**: 120 score 3, 1,080 score 5, 3,840
score 7. The 3-word set is exactly the orders with four scalars ahead of the
array and `state` sixth, which lands the array at sp+60 and the `state` spill
at sp+56 -- the target's slots -- while the frame is still 128. The adopted
order is `center, low, high, i, results, state, nearby`.

The residual is now the frame **size** and nothing else: the two `addiu sp`
words and the incoming-argument home at frame+4, which follows from them.

**The frame is pure bookkeeping, and that closes the ordering axis.** Only six
sp displacements are ever touched in the whole function -- 16/20 outgoing
arguments, 32/36 saves, 56 `state`, frame+4 the argument home -- so every other
home is reserved and never read. The frame is `round8(40 + block + temps)` with
40 the argument area plus saves, `block = 52 + 4 * (scalars declared after the
array)`, and `temps` the compiler's spill area. The target's 112 requires
`block + temps = 72`, and a seven-name block is 76 on its own. **A source
declaring six scalars beside the array cannot reach 0x70 in any order.** The
target declares at most five, and a five-scalar source needs `temps` to be
exactly zero.

That is the named decision variable the next lane inherits: **the compiler
spill-temp budget**, which is 12 bytes at seven names and 8 at six. No source
form measured here sets it to zero. Re-measured negatives, each also moving
registers so none is a candidate on its own terms: dropping `nearby` 20 words
(frame 0x78), `center` 11 (frame 0x80), `low` 23 (0x78), `nearby`+`center` 22
(0x78), and a three-name source (`results`, `state`, `i`, bounds inlined) 47
words at size delta +4 with the frame still 0x78 -- the sunk `sub.s`/`add.s`
confirmed. The prior passes' closures all stand.

<!-- plateau-handoff:func_overlay_036_F0000818_1883CD0:end -->
