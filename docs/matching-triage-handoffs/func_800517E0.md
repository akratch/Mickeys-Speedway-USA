<!-- plateau-handoff:func_800517E0:start -->
### `func_800517E0` plateau handoff

- source: `src/main/anim.c`
- score: 1782 differing words
- frame: 0x160
- relocations: 245
- first mismatch: +0x0
- summary: 288 aligned exact words; 83 words short. Ordinary halfword carrier retained; addressed-home and divisor-force experiments stay diagnostic.

### Final structural reconstruction plateau

The retained guarded C compiles to 6888 executable bytes (1722 words), against
7232 bytes (1808 words) at resident text 0x800517E0 through 0x80053420 and ROM
0x523E0 through 0x54020. There is no target padding to claim. The size deficit
is 344 bytes; this packet did not close it and claims zero matched bytes.
The raw and identity-preserving masked residuals are both 1786 differing
words, first mismatch +0x0. The candidate frame is 0x160 versus target 0x1B0.
Ten external relocation records agree at exact offset, type and symbol name.

The rederived configured baseline was 1806 versus 1808 words, eight bytes
short, with 1794 raw and identity-preserving masked differences from +0x4.
Its apparent exact 0x1B0 frame included an invented 64-byte volatile pad and
volatile spill substitutes. Those are removed. The inherited 1789-word score
masked mismatched relocation identities; its reported 395 relocations was a
mismatch count, not the target relocation count.

Mickey's main relocation table resolves all thirteen trampoline calls to ten
distinct destinations. This is tier-B identity evidence, independent of a
positional comparison or a shared synthetic overlay address:

- ORT 1449: overlay 41 `+0x854`, curve update; call offsets `+0x12C`, `+0x1C4`.
- ORT 1455: overlay 40 `+0x448`, set values; call offsets `+0x810`.
- ORT 1456: overlay 100 `+0x0`, motion creation; call offsets `+0xB48`.
- ORT 1457: overlay 40 `+0x0`, add entry; call offsets `+0xE18`.
- ORT 1458: overlay 40 `+0x84`, remove entry; call offsets `+0xE28`.
- ORT 1459: overlay 41 `+0x1740`, spawn items; call offsets `+0xF1C`.
- ORT 1460: overlay 41 `+0x195C`, enqueue transition; call offsets `+0xF88`.
- ORT 1461: overlay 41 `+0x1650`, add slot; call offsets `+0x1424`, `+0x14E8`.
- ORT 1462: overlay 41 `+0x172C`, ignore callback; call offsets `+0x174C`.
- ORT 1463: overlay 56 `+0x10C`, set mode; call offsets `+0x190C`, `+0x192C`.


The target has 307 static relocations: 57 calls, 188 address-half records and
62 local assembly branches. The candidate has 245 records. Excluding the local
branches and separating compiler literal pools and jump tables, the game
call/global inventory agrees by count, type and declared alias-normalized
name. This is inventory evidence, not an exact relocation surface. Motion and
slot aliases need the normal symbol-only trampoline rebinding before any
promotion; no instruction editing is permitted.

A final read-only traversal of the two actual jump tables checks all 62 handled
opcodes and 75 possible conditional direct-call sequences, including the nine
nested subtype branches. The sequence sets agree after alias normalization;
there are no unresolved indirect call or dispatch sites. This audit compares
possible call sequences, not the equivalence of every branch predicate or the
whole function's semantics. Runtime trampoline destinations above are proved
separately from Mickey's relocation tables, not guessed from a shared VMA.

Recovered source facts include the two-argument audio fade ABI, four no-argument
resident APIs, f32 motion/slot arguments, the absence of a leading motion-angle
argument, a path reload after reset/start, eight-byte texture entries,
signed-halfword reference flags, integer object masks, full-width trig
arguments, current-command duration in timeline commands, the null-path scope
of the global default, and dependent sub-object pointer reloads. Local field
views preserve the actual widths, offsets and strides without changing shared
headers. Transition duration is decoded through f32 before its integer API.

The stream cursor uses the project's AnimStreamEntry header and explicit
variable byte advances. Five apparent globals are compiler-pool float literals;
the scroll scale preserves the observed f32 rounding of 60.0f times 0.01f.
The motion parser decodes successive packed fields before the trig calls and
uses a full-word carrier for the final channel spill. Shake durations convert
directly from unsigned halfwords. Scroll fields now have their complete typed
view. Final retention also keeps unsigned shifts before signed fixed-point
interpretation and wide float-to-integer conversions before short/byte stores.

### Stopping evidence and next lever

The workbench verdict remains structure-mismatch. Its structure-buckets and
constant-audit playbooks were read before reconstruction. The retained scalar
best is attempt 33, recompiled at finalization with the measured conversion
repairs. The last new structural observation was attempt 43: ordered command
test blocks recovered both missing register-divisor check sequences, but lost
the repeated playback-state address materialization. Its source, object and
measurements remain archived; its 1607-word body is not retained as a winner.

The five consecutive follow-ups 44 through 48 did not improve the admissible
best residual, authenticate another identity or resolve the remaining causal
hypothesis. The sentinel-only loop lost both contexts. The guarded do-loop
returned to the earlier residual. A signed transition-timer carrier was flat.
Early add/remove field decoding regressed the handler shape. A byte-typed
scroll-slot carrier added a move without recovering the target mask or shared
divisor. These spellings do not eliminate the broader shared-carrier allocation
explanation, and no exhausted spelling is a ready next attempt.

The concrete next lever is a separately assigned source/def-use investigation
of the two shared values: prove why the target keeps 6000 as a loop-carried
register divisor while spilling the path table, then identify the original
source dataflow that makes both choices coexist. The current candidate keeps
the path table in a saved register and spills two extra decoded motion channels;
the target uses the ninth saved integer register. Do not substitute a flag
lattice, permuter, dummy padding or new volatile accesses for that proof.

Other exhausted inputs include the generic narrow argument carriers,
chained/dependent timer stores, motion declaration scope, independent versus
shared packed/channel carriers, and named constant denominators. Narrowing
whole frame/index carriers moved size toward the target by adding non-target
conversions and spills and was rejected. Actual outgoing argument stores end
at stack offset 0x28; no hidden additional call or stack argument was found.

An ordinary cc -v -K capture retained CFE/UOPT/UGEN intermediates without
changing optimization flags. The captured attempt-42 function bytes and
relocation rows equal its configured object. The listing shows that some
apparent missing operations, such as the pitch-byte mask, exist before assembly
and disappear later. Workbench statement attribution covered only two recognized
schedule sites and is not proof that source lines explain the whole residual.
No compiler-output patch, phase override, flag lattice or permuter was used.

### Reproduction, preservation and validation

The ignored m2c draft uses Mickey's own extracted assembly and both adjacent
switch tables. Full header context hit an unsupported flexible array, and full
source context hit an unsupported K&R definition; a narrow project-header
context succeeded. An analysis-only assembly copy gives the thirteen trampoline
sites their independently recovered identities to avoid conflating prototypes.
No generated draft or extracted instruction listing is tracked or adopted
verbatim. Pointer-cast calls were rejected because IDO emitted jalr, which is
incompatible with the call-site trampoline protocol.

Configured measurements used tools/wb_compare.sh --summary-json func_800517E0
and build_non_matching/src/main/anim.c.o, not the canonical fallback object.
Workbench diagnose used build/wb/func_800517E0.target.o and the configured
full-TU candidate. The finalizer command supplied score 1786 differing words,
frame 0x160, 245 relocations and first mismatch +0x0. Rich evidence is retained
inside this shard after finalizer generation. The source keeps NON_MATCHING
with its GLOBAL_ASM fallback.

Every meaningful attempt has its own source, object, metrics, first mismatch
and rationale in ignored build/whale3/. The final source/object/metrics,
call-path audit, game-global inventory and phase capture are retained there
separately; no best attempt artifact was overwritten. Baseline measurements,
tool failures and unchanged reruns are excluded from the 48 source attempts.

Source checkpoints are 8a67e56b (call contracts and field layouts), 77c654bd
(command loop/literals), 493288dd (retained independent motion operands), and
d34cfd87 (decoded motion and scroll dataflow). This shard and its guarded source
form the final handoff commit. Only src/main/anim.c and this symbol-owned shard
change; symbol/atlas match status and byte credit do not change.

The configured candidate compiles. The all-TU NON_MATCHING build gate passed
243 translation units during the packet, and later source edits were compiled
in the configured full TU. Canonical anim.c text, rodata, data and relocation
rows remain identical to the baseline fallback object. Clean-room and document
gates pass, with hooks enabled on every commit.

Full gmake verify fails at the same unchanged overlay94UpdateController link
step seen before source reconstruction: unresolved func_800254FC,
func_8002565C, func_8002A878 and func_8002A910, plus truncated R_MIPS_26 calls
to func_8005ABA8, func_8005AF14, func_80019AB8 and func_8002B040. The bounded
checkpoint-33 rerun reproduced it. This blocks linked-range/full-ROM proof in
this checkout; the candidate is not object-exact or ROM-exact. The next owner
must preserve this distinction while investigating the remaining structure.

#### 2026-09-11, lane `a9-resident`: missing-run map, no source attempt

Offsets are function-relative target offsets with exclusive upper bounds. Missing and surplus counts are alignment operations, not a proof that that many semantic operations are absent; surplus operations are assigned to the target coordinate at their alignment position rather than comparing unrebased offsets. All interpretation is Tier D, Mickey-only structural inference. No external body or name was adopted, and no candidate was attempted -- the lane's source work was blocked by a stale reopen pin and it then ran out of Codex credits.

168 missing words in 99 runs; 87 surplus words in 54 runs; net 81 words short.
Run lengths: 57 singles, 28 doubles, seven triples, two quadruples, four
five-word runs, one six-word run. There is no single missing large loop.

  - Target region; Missing; Surplus; Interpretation
  - +0x0000–+0x0764: 35 missing, 12 surplus -- Entry, path/audio handlers, repeated dispatch-tail state
  - +0x0764–+0x0890: 11 missing, 11 surplus -- Screen-effect decoding and conversion; net zero
  - +0x0890–+0x0C90: 15 missing, 11 surplus -- Weather, fog, motion, shake and track handlers
  - +0x0C90–+0x0DA0: 15 missing, 0 surplus -- Scroll fixed-point division, including register-divisor exception checks
  - +0x0DA0–+0x1504: 43 missing, 18 surplus -- Later effect/object handlers, decoded-field placement and dispatch tails
  - +0x1504–+0x16C4: 15 missing, 15 surplus -- Object-byte store and sub-object mode switch; substantial reordering
  - +0x16C4–+0x1C40: 34 missing, 20 surplus -- Final object/timeline handlers and loop/return state

The two adjacent longest unpaired runs are +0xCF4 (five words) and +0xD14
(six words), inside command 0x49. They belong to the signed division guard
sequences for the two fixed-point scroll coordinates. Target uses a shared
register divisor; candidate uses constant division there. The target has eight
exception-trap sites against four in the candidate. Do not insert synthetic
checks: recover the source dataflow that keeps this divisor live.

The target materializes the value 6000 at 62 static sites; the candidate at
three. Most target sites are handler exits that restore the shared divisor,
not new divisions. Target-only command reloads, state-address setup and
branches are distributed throughout the handlers. This supplies a concrete
extent for the prior shared-divisor/dispatch-tail observation; raw insertion
count alone overstates absent game behavior.

The +0x1504–+0x16C4 cluster contains conspicuous runs at +0x1524 (five),
+0x153C (four) and +0x1570 (three), but also 15 nearby surplus words under
alignment. The candidate already contains the guarded object-byte write and
sub-object mode switch. These gaps do not prove an absent handler.

**New concrete layout defect:** the local `AnimCommandObject` declaration has
no four-byte member between `animations` and `table70`. `animations` is at
0x68 and ends at 0x6C, making every later field four bytes early. Independently
assembled target accesses and the freshly compiled candidate confirm:

  - Field; Candidate offset; Target offset
  - table70; 0x6C; 0x70
  - table74; 0x70; 0x74
  - flags80; 0x7C; 0x80
  - soundHandle; 0x80; 0x84
  - unk88; 0x84; 0x88
  - unk90; 0x8C; 0x90

For example, command 0x29's sound-handle loads, clear and outgoing handle
address all use 0x80 in the candidate and 0x84 in the target. The command 0x62
flag access uses 0x7C versus 0x80. The 0x6A table lookup uses 0x6C versus
0x70. Correct this local layout first once assignment is reopened; this is a
semantic memory-access defect, not a color preference. No source correction
was attempted while the gate was closed.

Local declarations in this function are scalars and pointers, with four more
scalar/pointer declarations in the scroll block. There is no local array or
aggregate whose unobservable tail can explain the 80-byte frame deficit.
The padded typedefs describe pointed-to memory, not stack allocation. L112
therefore does not directly supply a local array-length variable here.

#### 2026-09-12, lane `p17-anim`: regional measurements and allocator diagnostics

This section supersedes the earlier source-state claims. The missing `pad6C`
member is already present at this assignment's base; the earlier request to
add it is stale. No layout/header change was made. The assignment gate gave
`base-only` for both owned symbols before work. The near-match scan identified
the permitted JFG command-list counterpart, but it is an assembly fallback
without a C body; no donor code or new external name was imported.

The owned executable range is 7232 bytes, 1808 words. The dispatch's -324 size
delta is bytes, meaning 81 missing words, not 324 words. Configured baseline
geometry is 1727 words, frame 0x160 against target 0x1B0. Its aligned buckets
are 285 exact, 586 naming, 67 immediate and 958 structural; masked positional
differences are 1789, raw 1792. The best addressed-home diagnostic is attempt 33: 1753 words,
55 short, frame still 0x160. Its buckets are 312 exact, 577 naming, 88 immediate
and 923 structural; masked differences 1785, raw 1786, first mismatch +0x0.
The alignment displacement tax moves from 178 to 197. This is a guarded
source improvement and a sharper plateau, not new matched-byte credit.

For diagnostic 33, relocations are 243 candidate versus 307 target, with only nine exact
position/type/identity tuples. The target has 62 local branch relocations that
are resolved internally in the candidate; the remaining count difference is
one state-address pair. Both have 57 direct calls. Five existing typed trap
aliases name the same intended trap family differently in the candidate;
no call identity proof or relocation-exact claim follows from the equal count.

The following Tier D partition uses target-relative, upper-exclusive bounds.
Each row lists diagnostic-33 naming/immediate/paired-structural counts, missing and
surplus rows, and total residual before to after. Surplus rows are rebased to
their alignment target coordinate. Counts do not prove absent game effects.
The GPR and FPR census uses the bank-aware `register_census.py` decoder, because
`residual_map.py` currently skips COP1 arithmetic and mislabels float memory
operands in its register-only report. Its address/bucket output remains useful.

- +0x0000–+0x0764: 142/36/145, missing 22, surplus 12; residual 368 to 357.
  Entry and path/audio handlers: table-home loads, state-address colouring,
  and shared dispatch-tail restoration. GPR coherence 69 percent/15 windows;
  FPR 57 percent/5. Local cycles mix fixed registers or scattered float webs,
  so a whole-function ring rotation is not supported.
- +0x0764–+0x0890: 24/0/26, missing 12, surplus 12; residual 76 to 74.
  Screen-effect field decoding and float conversion order, with net-zero
  width. Census 77 percent/4 and 92 percent/2; neither bank has a closed cycle.
- +0x0890–+0x0C90: 79/8/134, missing 11, surplus 9; residual 237 to 241.
  Weather/fog/motion handlers: decoded-width conversions and per-handler
  scheduling, not one shared colour error. Census 75 percent/7 and 46
  percent/8. A four-register GPR cycle and three-register FP cycle are local
  and incoherent; forcing one global permutation is unjustified.
- +0x0C90–+0x0DA0: 17/2/22, missing 16, surplus 0; residual 56 to 57.
  Scroll fixed-point divisions: target register-divisor guards remain absent
  from constant-divisor lowering. Census 94 percent/2 and 100 percent/1;
  a local float four-cycle does not explain the 16 missing rows.
- +0x0DA0–+0x1000: 61/4/48, missing 18, surplus 5; residual 137 to 136.
  Later effects and animation decode: conversion timing, packed-field
  lifetimes and shared tails. Census 73 percent/9 and 79 percent/3; small
  mixed saved-register and float cycles do not form one phase.
- +0x1000–+0x1200: 39/8/58, missing 11, surplus 10; residual 131 to 126.
  This was the densest baseline 0x200-byte window: 115 paired differences,
  including 74 structural, with two exact rows. It now has 105 paired
  differences and 12 exact rows. Command 0x60 conversion/guard dataflow and
  the 0x61–0x65 object guards dominate. Census 68 percent/6 and 100 percent/1;
  the integer two-cycle is scattered, and there is no float cycle.
- +0x1200–+0x1504: 44/5/107, missing 19, surplus 13; residual 181 to 188.
  Object/timeline field access and sub-object dispatch order, with repeated
  tail restoration. Census 56 percent/8 and one isolated float substitution;
  the integer two-cycle does not describe a coherent allocation phase.
- +0x1504–+0x16C4: 52/5/34, missing 6, surplus 10; residual 110 to 107.
  Existing object-byte write and mode switch: reordering and per-web lifetimes,
  not an absent handler. Census 61 percent/4 and 70 percent/3; two local GPR
  cycles, no float cycle.
- +0x16C4–+0x1C40: 119/20/110, missing 32, surplus 21; residual 315 to 302.
  Final object/timeline handlers and return: frame homes, conversions and
  shared-tail splitting. Census 65 percent/9 and 64 percent/4; an integer
  two-cycle and float four-cycle each sit within an incoherent mapping.

The largest fixed window was priced before spelling experiments. Making the
existing object assignment part of each nonnull guard only in cases 0x60–0x65
reduces that window from 134 residual rows on the addressed-path shape to 121.
Applying the same edit across all fourteen guards regresses the whole function
and grows the frame. This is evidence for region-specific carrier lifetimes,
not a general guard rewrite. The diagnostic six guards evaluate the same path
and object fields with the same short-circuit protection and no intervening
call or store; assigning null to the phase-local object has no later use.

Diagnostic path accesses take the existing pointer's address without volatile.
The same-typed expression adds no game-memory read: it forces the local table
pointer into its home. This changes baseline to attempt 22 from 81 to 59 words
short, and the table home from no repeated loads to 26 loads and one store.
Target has 25 loads and one store. The candidate home is +0x15C, target +0x7C.
An earlier accepted phase-one split of the pointer web adds only two words;
source address-taking and an isolated allocator force are different levers.

For command 0x60, using the first decoded halfword in the existing u16
`duration` before reusing it restores the unsigned conversion path before the
object guard. The old s32 carrier allowed a shorter signed conversion to sink
into the guarded branches. Both source values are zero-extended halfwords;
the change preserves their numeric range and division association. Taking the
second float's address at its final use restores its conversion before the
guard as well, adding 12 words relative to attempt 27. Its spill is still too
early and at +0x134 instead of target +0xB0. Since `value2` is shared among
handlers, this edit also changes traffic outside command 0x60; the complete
partition above records those effects instead of claiming a local-only gain.

On diagnostic 33, the frame census finds 30 used candidate slots against 29 target slots, even
though the frame is 80 bytes too small. Candidate-only high homes include
+0xF0, +0x134, +0x13C and +0x15C, while the target has seven distinct missing
homes including +0xB0, +0xC8, +0xD0, +0xD4, +0xDC and +0x160. This is not
an array-tail adjustment: the source has only scalar and pointer locals.
No dummy padding or volatile storage was added to manufacture a larger frame.

The full-TU configured/instrumented text and relocation identity gate passes
both at baseline and on diagnostic 33. Procedure ordinal 30 uses phase one.
On baseline, the 6000 constant root web 857 has total saving 40 over 69 blocks,
against minimum saved-register cost 60. The target's desired argument register
is absent from its offered cost list. An accepted caller-register force gives
45 constant materializations versus target 62, but state-address relocations
collapse from 124 to six and the candidate becomes 114 words short. A saved
register force leaves one materialization and a 152-word deficit. Neither is
an adoptable constant-spelling recipe. The accepted split of pointer web 4
and declined state-address colour force were separately recorded; acceptance
was read from `forced`, never inferred from a byte-identical output.

Attempt 39 re-tests that mechanism on diagnostic 33, with root web 863.
Its saving and 69-block count remain unchanged. Caller costs are 570, saved
costs 60, and the desired argument colour remains forbidden. Forcing offered
colour 9 records `forced=9`; the object is scored directly with no unforced
rebuild. It has 1722 words, 86 short, aligned 349/571/73/924 and 1796 masked
differences. Constant materializations rise from three to 45, while state
address relocations fall from 122 to six. The scroll window moves from 57
residual rows to 54 and the dense object window from 126 to 118, but net
geometry loses 31 words. The shared divisor and state-address rematerialization
are coupled decisions. The smaller function's successful constant-force price
therefore does not transfer as an independent 48-word source opportunity.

The inherited missing-run interpretation survives: register division versus
constant division and dispatch tails explain distributed gaps. The reported
layout defect does not survive inspection of the current base because it is
already fixed. The no-local-array argument survives. Prior volatile or generic
spelling plateaus do not retire address-taking: attempt 22 changes the output
and recovers the table reload surface. Existing callee and game-field evidence
is retained; no argument order, game store, or call destination was changed.

Attempts 34 and 35 tested reuse of the halfword carrier. An unused reload
changes allocation but is diagnostic only; using that carrier for every packed
field causes a 170-word deficit and is rejected. Attempts 36, 37 and 38 then
addressed the packed high/low homes, inverted the animation-kind branch, and
reversed the independent post-call stores. They respectively regress, lose one
aligned exact row, and produce an identical object. They add no identity or
new mechanism and fail to improve the best, meeting ADR 0018's stall rule.
Attempt 39 is the final-shape price check, not an adopted source attempt.

Next action: solve the coupled divisor/state-address live-range decision before
trying another divisor spelling. For object handlers, the measured remaining
variables are packed-field lifetime, early value2 spill versus call-only spill,
and the corresponding declared-home set. Recover a supported source idiom for the measured path reloads;
do not add exception checks, padding, or duplicate updates to fill aligner gaps.

The candidate compiles under the configured full-TU command. Only the two owned
functions change among 55 emitted functions; the other 53 retain bytes and
function-relative relocations. `tools/finalize_plateau.py` refreshed the guarded
footer and this metric header. `tools/gates.sh` and
`gmake check-nonmatching-builds` are the closing checks. The normal ROM SHA1
proof validates the unchanged assembly fallbacks, not either C candidate.
All diagnostic sources, objects, metrics, decision records, first mismatches and
region maps remain in external lane scratch; no ROM-derived artifact is tracked.

#### Admissible source at handoff

ADR 0017 keeps nonexact deliberately inert source diagnostics in ignored
artifacts. The explicit same-type address/dereference forms used for `paths`
and `value2` therefore remain diagnostic evidence only. They are absent from
the committed C. The preceding attempt-33 numbers price mechanisms; they are
not the score of the committed candidate and do not authorize adoption.

Attempt 40 removes both address forms but keeps the six guards and the
halfword carrier: 294/601/67/935 aligned, 1787 masked differences, 89 words
short. Attempt 41 retains only the ordinary halfword carrier: 288/611/72/942,
1782 masked, 83 words short. Attempt 42 keeps only the guards: 292/583/71/946,
1789 masked, 87 words short. These separate measurements preserve admissible
alternatives instead of treating the diagnostic minimum as canonical source.
Attempt 41 is retained because it has the best admissible masked score and
recovers the target's unsigned first-fraction conversion without adding an
inert expression. It changes just the loaded local and its immediate float
conversion in command 0x60. The six guard rewrites are not retained.

Final baseline to committed buckets are 285 to 288 exact, 586 to 611 naming,
67 to 72 immediate, and 958 to 942 structural. Raw differences improve from
1792 to 1785, masked from 1789 to 1782. Geometry is 1725 of 1808 words,
332 bytes short, versus 324 bytes short initially. The frame remains 0x160
against 0x1B0. Relocations remain 245 versus 307, with five exact positional
identities; first mismatch remains +0x0. No size-zero or exact candidate was
found, and no matched executable credit is claimed.

For the committed source, the address partition is below. Each row gives
naming/immediate/paired-structural, missing/surplus, residual before to after,
then GPR and FPR coherence/window count. Interpretation follows the named
handler regions above; diagnosis is Tier D. Cycles are reported only within
each bank and region, never promoted to a whole-function ring claim.

- +0x0000–+0x0764: 166/24/131, 35/12, 368 to 368; GPR 59 percent/23,
  FPR 59 percent/5. Mixed fixed-register GPR cycle and five-register FP cycle.
- +0x0764–+0x0890: 27/1/26, 11/11, 76 to 76; GPR 84 percent/3,
  FPR 88 percent/2. No closed cycle in either bank.
- +0x0890–+0x0C90: 76/7/128, 15/11, 237 to 237; GPR 76 percent/7,
  FPR 43 percent/11. Four-register GPR cycle and two local FP cycles.
- +0x0C90–+0x0DA0: 16/2/23, 15/0, 56 to 56; GPR 87 percent/2,
  FPR 89 percent/2. No closed cycle in either bank.
- +0x0DA0–+0x1000: 56/5/53, 15/5, 137 to 134; GPR 72 percent/6,
  FPR 81 percent/4. Only the float bank has a three-register cycle.
- +0x1000–+0x1200: 36/2/74, 12/6, 131 to 130; GPR 66 percent/4,
  no differing FP operand slots among naming-only rows. Neither bank has
  a closed cycle. The 74 paired structural rows are still the main problem;
  absent naming-only FP rows do not prove the float schedule agrees.
- +0x1200–+0x1504: 43/3/98, 27/16, 181 to 187; GPR 62 percent/7,
  FPR 100 percent/1. No closed cycle in either bank.
- +0x1504–+0x16C4: 48/7/22, 15/17, 110 to 109; GPR 55 percent/6,
  FPR 100 percent/1. Local GPR three- and two-cycles, no FP cycle.
- +0x16C4–+0x1C40: 143/21/94, 43/27, 315 to 328; GPR 61 percent/12,
  FPR 52 percent/9. Local GPR three-cycle and FP two-cycle.

Attempts 40–42 isolate the admissible changes. Attempt 43 then gives the second
halfword a separate local: 293/588/72/948 aligned, 1783 masked, 73 words short.
It is preserved as an admissible geometry alternative, while attempt 41 keeps
the better masked score. On the admissible shape, attempts 44 and 45 (a local
second-float carrier and separate conversion/scaling statements) are each
byte-identical to 41. Attempt 46 inverts the animation-kind branch and loses
one aligned exact row without improving any other metric. Those three attempts
add no identity, eliminate no new mechanism, and do not improve the best.
This is the closing ADR 0018 stall evidence on the admissible shape, separately
from the earlier diagnostic-shape stall. The next packet should begin with the
recorded path-home and constant-split prices while reconstructing a supported
source idiom for the reloads. It must not copy the explicit inert address-taking
diagnostic into canonical source.

Final validation re-measures the in-tree configured candidate, runs
`tools/gates.sh` and `gmake check-nonmatching-builds`, and refreshes the plateau
footer/header with `tools/finalize_plateau.py`. ROM verification is exclusively
for the guarded fallback build. The best diagnostic, best admissible source,
and all inferior variants are preserved separately outside the tree.

<!-- plateau-handoff:func_800517E0:end -->
