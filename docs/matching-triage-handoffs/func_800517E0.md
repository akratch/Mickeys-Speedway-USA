<!-- plateau-handoff:func_800517E0:start -->
### `func_800517E0` plateau handoff

- source: `src/main/anim.c`
- score: 1797 differing words
- frame: 0x170
- relocations: 127
- first mismatch: +0x0
- summary: Intermediate ABI/type reconstruction: 1668/1808 words; all 57 calls recovered; shared loop carriers remain. Investigation continues.


### Structural reconstruction checkpoint (whale3)

This is an intermediate guarded source checkpoint; the assigned structural
reconstruction continues. No matched bytes are claimed.

The configured baseline was 1,806 versus 1,808 words (eight bytes short), with
1,794 raw and identity-preserving masked differences from `+0x4`, and a
`0x1B0` frame. The older 1,789-word report masked sites with mismatched
relocation identities and was a lower bound. The frame included an explicit
64-byte volatile pad and volatile spill substitutes; these are removed rather
than treated as evidence for the original source.

Mickey's main relocation table resolves all thirteen trampoline calls to ten
distinct destinations. This is tier-B identity evidence, independent of a
positional comparison or a shared synthetic overlay address:

- ORT 1449: overlay 41 `+0x854`, curve update; call offsets `+0x12C`, `+0x1C4`.
- ORT 1455: overlay 40 `+0x448`, set values; call offsets `+0x810`.
- ORT 1456: overlay 100 `+0x0`, motion creation; call offsets `+0xB48`.
- ORT 1457: overlay 40 `+0x0`, add entry; call offsets `+0xE18`.
- ORT 1458: overlay 40 `+0x84`, remove entry; call offsets `+0xE28`.
- ORT 1459: overlay 41 `+0x1740`, spawn items; call offsets `+0xF1C`.
- ORT 1460: overlay 41 `+0x195C`, enqueue transition; call offsets `+0xF88`.
- ORT 1461: overlay 41 `+0x1650`, add slot; call offsets `+0x1424`, `+0x14E8`.
- ORT 1462: overlay 41 `+0x172C`, ignore callback; call offsets `+0x174C`.
- ORT 1463: overlay 56 `+0x10C`, set mode; call offsets `+0x190C`, `+0x192C`.

The 57-call order is preserved after resolving the declared trampoline aliases.
Static target assembly has 307 relocations: 57 calls, 188 address-half records,
and 62 local branches. The candidate has 127 static records; the major deficit
is sixty repeated address pairs for playback state, not absent calls. Raw
candidate aliases are not an exact relocation surface. The new motion/slot
aliases require the normal symbol-only trampoline rebinding on promotion.

Recovered source facts include the two-argument audio fade ABI, four
zero-argument resident APIs, f32 motion/slot arguments, the absence of a leading
motion-angle argument, a path reload after reset/start, eight-byte texture
entries, signed-halfword reference flags, integer object masks, full-width trig
arguments, the current command's duration in timeline commands, the null-path
scope of the global default, and dependent sub-object pointer reloads. The
transition command also decodes its integer API duration through f32; the
entire unsigned-halfword domain is exactly representable.

The workbench verdict is `structure-mismatch`. Its `structure-buckets`
playbook and `constant-audit` footer were read. The next lever is the shared
loop-carrier reconstruction: most handlers lack the repeated playback-state
address/divisor materialization, and the scroll handler loses register-divisor
checks. No flag lattice or permuter was run.

Draft preparation required a narrow project-header context because the full
header context contains an unsupported flexible array; a full source context
also contains a K&R definition unsupported by m2c. Both extracted switch tables
were supplied from the adjacent rodata file. A second ignored draft assigns
distinct analysis names from the runtime table, avoiding m2c's conflation of
unrelated trampoline signatures. No generated draft is adopted verbatim.

Attempt sources, configured objects, summaries, first-mismatch reports, runtime
identity receipts, and rationale remain under ignored `build/whale3/`.
Pointer casts were rejected because IDO emits indirect calls, incompatible
with the call-site trampoline protocol. Conflicting scoped declarations were
rejected by the compiler. Typed weak aliases retain direct calls, following
the existing source idiom.

Canonical `anim.c` text, rodata, data, and relocation rows remain identical to
the baseline object. A full canonical build fails in the unchanged
`overlay94UpdateController` object: unresolved resident references and
truncated call relocations. That pre-existing integration failure blocks a
full-ROM proof in this checkout; it does not validate the candidate.
<!-- plateau-handoff:func_800517E0:end -->
