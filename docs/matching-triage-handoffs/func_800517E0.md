<!-- plateau-handoff:func_800517E0:start -->
### `func_800517E0` plateau handoff

- source: `src/main/anim.c`
- score: 1786 differing words
- frame: 0x160
- relocations: 245
- first mismatch: +0x0
- summary: Structural plateau: 1722/1808 words; call identities recovered; next prove the shared divisor and path-table allocation cause.

### Final structural reconstruction plateau

The retained guarded C compiles to 6888 executable bytes (1722 words), against
7232 bytes (1808 words) at resident text 0x800517E0 through 0x80053420 and ROM
0x523E0 through 0x54020. There is no target padding to claim. The size deficit
is 344 bytes; this packet did not close it and claims zero matched bytes.
The raw and identity-preserving masked residuals are both 1786 differing
words, first mismatch +0x0. The candidate frame is 0x160 versus target 0x1B0.
Ten external relocation records agree at exact offset, type and symbol name.

The rederived configured baseline was 1806 versus 1808 words, eight bytes
short, with 1794 raw and identity-preserving masked differences from +0x4.
Its apparent exact 0x1B0 frame included an invented 64-byte volatile pad and
volatile spill substitutes. Those are removed. The inherited 1789-word score
masked mismatched relocation identities; its reported 395 relocations was a
mismatch count, not the target relocation count.

Mickey's main relocation table resolves all thirteen trampoline calls to ten
distinct destinations. This is tier-B identity evidence, independent of a
positional comparison or a shared synthetic overlay address:

- ORT 1449: overlay 41 `+0x854`, curve update; call offsets `+0x12C`, `+0x1C4`.
- ORT 1455: overlay 40 `+0x448`, set values; call offsets `+0x810`.
- ORT 1456: overlay 100 `+0x0`, motion creation; call offsets `+0xB48`.
- ORT 1457: overlay 40 `+0x0`, add entry; call offsets `+0xE18`.
- ORT 1458: overlay 40 `+0x84`, remove entry; call offsets `+0xE28`.
- ORT 1459: overlay 41 `+0x1740`, spawn items; call offsets `+0xF1C`.
- ORT 1460: overlay 41 `+0x195C`, enqueue transition; call offsets `+0xF88`.
- ORT 1461: overlay 41 `+0x1650`, add slot; call offsets `+0x1424`, `+0x14E8`.
- ORT 1462: overlay 41 `+0x172C`, ignore callback; call offsets `+0x174C`.
- ORT 1463: overlay 56 `+0x10C`, set mode; call offsets `+0x190C`, `+0x192C`.


The target has 307 static relocations: 57 calls, 188 address-half records and
62 local assembly branches. The candidate has 245 records. Excluding the local
branches and separating compiler literal pools and jump tables, the game
call/global inventory agrees by count, type and declared alias-normalized
name. This is inventory evidence, not an exact relocation surface. Motion and
slot aliases need the normal symbol-only trampoline rebinding before any
promotion; no instruction editing is permitted.

A final read-only traversal of the two actual jump tables checks all 62 handled
opcodes and 75 possible conditional direct-call sequences, including the nine
nested subtype branches. The sequence sets agree after alias normalization;
there are no unresolved indirect call or dispatch sites. This audit compares
possible call sequences, not the equivalence of every branch predicate or the
whole function's semantics. Runtime trampoline destinations above are proved
separately from Mickey's relocation tables, not guessed from a shared VMA.

Recovered source facts include the two-argument audio fade ABI, four no-argument
resident APIs, f32 motion/slot arguments, the absence of a leading motion-angle
argument, a path reload after reset/start, eight-byte texture entries,
signed-halfword reference flags, integer object masks, full-width trig
arguments, current-command duration in timeline commands, the null-path scope
of the global default, and dependent sub-object pointer reloads. Local field
views preserve the actual widths, offsets and strides without changing shared
headers. Transition duration is decoded through f32 before its integer API.

The stream cursor uses the project's AnimStreamEntry header and explicit
variable byte advances. Five apparent globals are compiler-pool float literals;
the scroll scale preserves the observed f32 rounding of 60.0f times 0.01f.
The motion parser decodes successive packed fields before the trig calls and
uses a full-word carrier for the final channel spill. Shake durations convert
directly from unsigned halfwords. Scroll fields now have their complete typed
view. Final retention also keeps unsigned shifts before signed fixed-point
interpretation and wide float-to-integer conversions before short/byte stores.

### Stopping evidence and next lever

The workbench verdict remains structure-mismatch. Its structure-buckets and
constant-audit playbooks were read before reconstruction. The retained scalar
best is attempt 33, recompiled at finalization with the measured conversion
repairs. The last new structural observation was attempt 43: ordered command
test blocks recovered both missing register-divisor check sequences, but lost
the repeated playback-state address materialization. Its source, object and
measurements remain archived; its 1607-word body is not retained as a winner.

The five consecutive follow-ups 44 through 48 did not improve the admissible
best residual, authenticate another identity or resolve the remaining causal
hypothesis. The sentinel-only loop lost both contexts. The guarded do-loop
returned to the earlier residual. A signed transition-timer carrier was flat.
Early add/remove field decoding regressed the handler shape. A byte-typed
scroll-slot carrier added a move without recovering the target mask or shared
divisor. These spellings do not eliminate the broader shared-carrier allocation
explanation, and no exhausted spelling is a ready next attempt.

The concrete next lever is a separately assigned source/def-use investigation
of the two shared values: prove why the target keeps 6000 as a loop-carried
register divisor while spilling the path table, then identify the original
source dataflow that makes both choices coexist. The current candidate keeps
the path table in a saved register and spills two extra decoded motion channels;
the target uses the ninth saved integer register. Do not substitute a flag
lattice, permuter, dummy padding or new volatile accesses for that proof.

Other exhausted inputs include the generic narrow argument carriers,
chained/dependent timer stores, motion declaration scope, independent versus
shared packed/channel carriers, and named constant denominators. Narrowing
whole frame/index carriers moved size toward the target by adding non-target
conversions and spills and was rejected. Actual outgoing argument stores end
at stack offset 0x28; no hidden additional call or stack argument was found.

An ordinary cc -v -K capture retained CFE/UOPT/UGEN intermediates without
changing optimization flags. The captured attempt-42 function bytes and
relocation rows equal its configured object. The listing shows that some
apparent missing operations, such as the pitch-byte mask, exist before assembly
and disappear later. Workbench statement attribution covered only two recognized
schedule sites and is not proof that source lines explain the whole residual.
No compiler-output patch, phase override, flag lattice or permuter was used.

### Reproduction, preservation and validation

The ignored m2c draft uses Mickey's own extracted assembly and both adjacent
switch tables. Full header context hit an unsupported flexible array, and full
source context hit an unsupported K&R definition; a narrow project-header
context succeeded. An analysis-only assembly copy gives the thirteen trampoline
sites their independently recovered identities to avoid conflating prototypes.
No generated draft or extracted instruction listing is tracked or adopted
verbatim. Pointer-cast calls were rejected because IDO emitted jalr, which is
incompatible with the call-site trampoline protocol.

Configured measurements used tools/wb_compare.sh --summary-json func_800517E0
and build_non_matching/src/main/anim.c.o, not the canonical fallback object.
Workbench diagnose used build/wb/func_800517E0.target.o and the configured
full-TU candidate. The finalizer command supplied score 1786 differing words,
frame 0x160, 245 relocations and first mismatch +0x0. Rich evidence is retained
inside this shard after finalizer generation. The source keeps NON_MATCHING
with its GLOBAL_ASM fallback.

Every meaningful attempt has its own source, object, metrics, first mismatch
and rationale in ignored build/whale3/. The final source/object/metrics,
call-path audit, game-global inventory and phase capture are retained there
separately; no best attempt artifact was overwritten. Baseline measurements,
tool failures and unchanged reruns are excluded from the 48 source attempts.

Source checkpoints are 8a67e56b (call contracts and field layouts), 77c654bd
(command loop/literals), 493288dd (retained independent motion operands), and
d34cfd87 (decoded motion and scroll dataflow). This shard and its guarded source
form the final handoff commit. Only src/main/anim.c and this symbol-owned shard
change; symbol/atlas match status and byte credit do not change.

The configured candidate compiles. The all-TU NON_MATCHING build gate passed
243 translation units during the packet, and later source edits were compiled
in the configured full TU. Canonical anim.c text, rodata, data and relocation
rows remain identical to the baseline fallback object. Clean-room and document
gates pass, with hooks enabled on every commit.

Full gmake verify fails at the same unchanged overlay94UpdateController link
step seen before source reconstruction: unresolved func_800254FC,
func_8002565C, func_8002A878 and func_8002A910, plus truncated R_MIPS_26 calls
to func_8005ABA8, func_8005AF14, func_80019AB8 and func_8002B040. The bounded
checkpoint-33 rerun reproduced it. This blocks linked-range/full-ROM proof in
this checkout; the candidate is not object-exact or ROM-exact. The next owner
must preserve this distinction while investigating the remaining structure.

<!-- plateau-handoff:func_800517E0:end -->

#### 2026-09-11, lane `a9-resident`: missing-run map, no source attempt

Offsets are function-relative target offsets with exclusive upper bounds. Missing and surplus counts are alignment operations, not a proof that that many semantic operations are absent; surplus operations are assigned to the target coordinate at their alignment position rather than comparing unrebased offsets. All interpretation is Tier D, Mickey-only structural inference. No external body or name was adopted, and no candidate was attempted -- the lane's source work was blocked by a stale reopen pin and it then ran out of Codex credits.

168 missing words in 99 runs; 87 surplus words in 54 runs; net 81 words short.
Run lengths: 57 singles, 28 doubles, seven triples, two quadruples, four
five-word runs, one six-word run. There is no single missing large loop.

| Target region | Missing | Surplus | Interpretation |
|---|---:|---:|---|
| +0x0000–+0x0764 | 35 | 12 | Entry, path/audio handlers, repeated dispatch-tail state |
| +0x0764–+0x0890 | 11 | 11 | Screen-effect decoding and conversion; net zero |
| +0x0890–+0x0C90 | 15 | 11 | Weather, fog, motion, shake and track handlers |
| +0x0C90–+0x0DA0 | 15 | 0 | Scroll fixed-point division, including register-divisor exception checks |
| +0x0DA0–+0x1504 | 43 | 18 | Later effect/object handlers, decoded-field placement and dispatch tails |
| +0x1504–+0x16C4 | 15 | 15 | Object-byte store and sub-object mode switch; substantial reordering |
| +0x16C4–+0x1C40 | 34 | 20 | Final object/timeline handlers and loop/return state |

The two adjacent longest unpaired runs are +0xCF4 (five words) and +0xD14
(six words), inside command 0x49. They belong to the signed division guard
sequences for the two fixed-point scroll coordinates. Target uses a shared
register divisor; candidate uses constant division there. The target has eight
exception-trap sites against four in the candidate. Do not insert synthetic
checks: recover the source dataflow that keeps this divisor live.

The target materializes the value 6000 at 62 static sites; the candidate at
three. Most target sites are handler exits that restore the shared divisor,
not new divisions. Target-only command reloads, state-address setup and
branches are distributed throughout the handlers. This supplies a concrete
extent for the prior shared-divisor/dispatch-tail observation; raw insertion
count alone overstates absent game behavior.

The +0x1504–+0x16C4 cluster contains conspicuous runs at +0x1524 (five),
+0x153C (four) and +0x1570 (three), but also 15 nearby surplus words under
alignment. The candidate already contains the guarded object-byte write and
sub-object mode switch. These gaps do not prove an absent handler.

**New concrete layout defect:** the local `AnimCommandObject` declaration has
no four-byte member between `animations` and `table70`. `animations` is at
0x68 and ends at 0x6C, making every later field four bytes early. Independently
assembled target accesses and the freshly compiled candidate confirm:

| Field | Candidate offset | Target offset |
|---|---:|---:|
| table70 | 0x6C | 0x70 |
| table74 | 0x70 | 0x74 |
| flags80 | 0x7C | 0x80 |
| soundHandle | 0x80 | 0x84 |
| unk88 | 0x84 | 0x88 |
| unk90 | 0x8C | 0x90 |

For example, command 0x29's sound-handle loads, clear and outgoing handle
address all use 0x80 in the candidate and 0x84 in the target. The command 0x62
flag access uses 0x7C versus 0x80. The 0x6A table lookup uses 0x6C versus
0x70. Correct this local layout first once assignment is reopened; this is a
semantic memory-access defect, not a color preference. No source correction
was attempted while the gate was closed.

Local declarations in this function are scalars and pointers, with four more
scalar/pointer declarations in the scroll block. There is no local array or
aggregate whose unobservable tail can explain the 80-byte frame deficit.
The padded typedefs describe pointed-to memory, not stack allocation. L112
therefore does not directly supply a local array-length variable here.
