<!-- plateau-handoff:func_800498FC:start -->
### `func_800498FC` plateau handoff

- source: `src/main/fx.c`
- score: 91/100 words
- frame: 0x30
- relocations: 5
- first mismatch: +0xD0
- summary: CDX proves a 15/16 pool-population mismatch, not a colour bijection; read-back closes the population but leaves a 13-word UGEN/as1 schedule plateau

The retained configured C remains the best stock object at 91/100 words,
first `+0xD0`, with the exact 100-instruction shape, `0x30` frame, and all five
relocation identities. A prior procedure-scoped UGEN capture mapped it to
procedure 22 and authenticated 25 GP plus six FP births, but could not supply
target lifecycle or source-semantic evidence.

The later instrumented IDO 5.3 CDX pass corrects the residual classification.
The target pool lane has 15 slots and the candidate has 16; the corresponding
temporary lanes have 26 and 25. This is a population difference, not a
four-web colour bijection, so `CDX_FORCE` cannot reach the target assignment.

The extra coloured web is the `flags & 0x80` value. Testing the just-written
`record->value1E` byte instead removes that pool web: pool lanes become equal
at 15/15 and the temporary lanes agree through slot 15. Moving the `status =
0xFF` store above the state branch composes the closest source-authentic UGEN
schedule, improving that experimental form from 23 to 13 differences and
reducing its opcode residual from sixteen to six. It is still worse than the
retained nine-word baseline and was therefore not kept.

What remains after closing the population is a UGEN/as1 line-order conflict:
the target materializes `0xFF` sixteen rows before its status store, but stores
status after state. Neither source order reproduces both. Preserve the 91/100
baseline. Reopen only with a new source-authentic scheduler mechanism that
separates the constant's source line from the store order; do not repeat
global-colour forcing, flag sweeps, direct `flags & 0x80` carrier variants,
the `value1E` read-back alone, or either status-store ordering.
<!-- plateau-handoff:func_800498FC:end -->
