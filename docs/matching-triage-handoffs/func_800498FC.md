<!-- plateau-handoff:func_800498FC:start -->
### `func_800498FC` plateau handoff

- source: `src/main/fx.c`
- score: 91/100 words
- frame: 0x30
- relocations: 5
- first mismatch: +0xD0
- summary: JFG empty flag-field condition is scalar-flat; lvalue forms regress structurally. Next lever: source-authentic UGEN line separation.

The retained configured C remains the best object at 91/100 words, first
`+0xD0`, with the exact 100-word shape, `0x30` frame, and all five relocation
identities. The final workbench verdict is `allocation-mismatch`: all nine
differences are register-only, with no opcode, schedule, structural, frame, or
relocation residual.

The authorized new evidence was re-derived from Jet Force Gemini public commit
`efd5abb1c79636e297b831f7c2d5bf47eac39c0c`. Its `src/fx.c` still guards
`setupClearScreen` with assembly but links exact IDO 5.3 scratch `fDplg`. That
scratch takes one structure pointer and initializes global clear-screen state;
it does not share Mickey's seven-argument ABI or indexed `FxRecord` control
flow. No JFG body, name, or value was adapted. The only source-shaping clue
applicable to Mickey is its empty flag-field condition immediately before the
packed `0x80`, `0x3F`, and `0x40` extractions; this inspection is disclosed at
the candidate's point of use.

Three material donor-derived attempts produced no improvement. A direct empty
condition on Mickey's scalar flag argument compiled object-flat at 91/100. An
address-based lvalue spelling applied to the flag uses grew the candidate to
104 words and regressed to a structural mismatch. Restricting the lvalue to
only the empty condition produced the same regression. The clean scalar
baseline was restored, satisfying the ADR 0018 three-attempt stall rule.

The prior CDX result remains controlling: the baseline has 15 target versus 16
candidate pool slots, and reading back `record->value1E` equalizes that
population only by regressing to a 13-word UGEN/as1 schedule residual. The next
concrete lever is new source-authentic evidence that separates UGEN constant
line ownership from the later status-store order. Do not repeat the JFG empty
condition, address/lvalue wrappers, global-colour forcing, flag sweeps, direct
`flags & 0x80` carrier variants, the `value1E` read-back alone, or either
status-store ordering.
<!-- plateau-handoff:func_800498FC:end -->
