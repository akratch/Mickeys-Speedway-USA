<!-- plateau-handoff:func_8003484C:start -->
### `func_8003484C` plateau handoff

- source: `src/main/textures_35024.c`
- score: 11/21 words
- frame: frameless
- relocations: 4
- first mismatch: +0x8
- summary: Ten natural forms leave the original exact-sized loop best; its texture argument and computed-entry register roles remain reversed.

Fresh configured evidence on 2026-09-04 proves the resident owner at
`0x8003484C..0x800348A0` / ROM `0x3544C..0x354A0`, with no padding, caller,
export, or inbound pointer reference. The candidate and target are both 21
instructions and frameless. V0 matches 11/21 positional words, first `+0x8`,
and has SHA-1 `b7a49009e2a8`. All four candidate static relocation identities
resolve; three of four offset/type/identity sites align, while the cache-base
HI16 moves with the initial schedule.

The target retains the texture argument in `a0`, computes the indexed entry in
`a2`, and lowers the empty-table path with a branch-likely. V0 copies the
texture argument into `a2`, uses `a0` for the indexed entry, and consequently
schedules the cache-base materialization after a non-likely branch. A direct
indexed `for` loop removes the scaled address calculation and shrinks the body
to 19 instructions; an explicit early return grows it to 23. Function-scope
entry declarations, declaration reordering, typed and `register` parameters,
a `register` entry, and a `while` spelling are byte-identical to V0. Naming the
cache base either shrinks the body to 19 instructions or worsens allocation at
the same size.

The retained source is restored. Reopen only with a new natural mechanism that
changes the parameter-versus-indexed-entry web allocation while preserving the
21-instruction loop; do not repeat these ten loop, type, declaration, or base
pointer forms.
<!-- plateau-handoff:func_8003484C:end -->
