<!-- plateau-handoff:func_overlay_101_F000C144_18E7964:start -->
### `func_overlay_101_F000C144_18E7964` plateau handoff

- source: `src/overlays/o101/overlay101TailC144.c`
- score: 183/361 words
- frame: 0x38
- relocations: 39
- first mismatch: +0x10
- summary: 178 masked words, 183 aligned-exact, 47 structural; three source-shape levers paid 31. Identity gate still closed.

Measured 2026-09-11, lane `lane/p2-pres`, with `tools/align_symbol.py` and
`tools/score_symbol.py`, whose positional figure this file quotes. `gmake
verify` printed the expected SHA1 with this source in the tree; the function is
still `#ifdef NON_MATCHING`, so the ROM is unaffected either way.

Before and after, in the aligner's four buckets:

- before: 209 masked, 160 byte-exact, 136 register naming, 5 immediate only,
  64 really different, four insertion sites and four deletion sites, size
  delta 0, first mismatch +0x10.
- after: 178 masked, 183 byte-exact, 131 register naming, 1 immediate only,
  47 really different, ONE insertion site and one deletion site, size delta 0,
  first mismatch unmoved at +0x10.

Three source-shape levers, none costing an instruction.

- The node-24 text store moves forward, in every one of the five text rows. The
  store order becomes length, opacity, text, mode, color0, color1, color2,
  color3, kind, and the row's two root updates become root.child, then the
  counter bump, then root.childType. That is the siblings' "text store belongs
  before the counter bump" lever generalised: here it wants to sit immediately
  after the opacity store rather than merely ahead of the bump. Worth 24 words
  alone, 209 to 185, raising aligned byte-exact from 160 to 176.
- A second physical line inside text rows 1 to 4, and NOT row 5, placed before
  that row's color1 store. L59 again: with the whole row on one line the
  `lineno` key ties for every store in it, and one break re-orders exactly the
  word that was displaced. Worth a further 3 words and it removes three of the
  four insertion and deletion sites. The uniform form, breaking all five rows,
  reads 183 rather than 182, so the asymmetry is measured rather than assumed.
- In the second root header, `height48` is stored before `value4A`. Worth 4
  words, 182 to 178, and it also empties the immediate-only bucket from 5 to 1.

How much of the order space was covered. For a text row: a constrained
single-move greedy plus random-restart hill climbing evaluated 17,481
statement orders of the movable tail under the real dependences, and a
per-row search that allowed the five rows to take DIFFERENT orders converged
on the same order for all five. For the three head groups: 18,996 orders under
their dependences. All 256 whole-group fold subsets were measured, and every
intra-group break position in all eight groups, singly and greedily
accumulated. A final joint greedy over head orders, row orders and break
positions together checked 1,445 neighbours of the adopted form and moved
nothing.

Refuted here, each with its measurement.

- The runtime int-to-float spelling of the opacity multiplier is load-bearing
  and must stay. `* 1.0f` and dropping the multiply both read 289 at a size
  delta of -60, `* 1.0` reads 289 at -40, and hoisting the multiplier into an
  `f32` local reads 358 at -32. `(f32)1` and `(f32)(u32)1` are exactly inert.
- Holding the node-32 pointer in one local across the call reads 330. Giving
  the post-call half its own local is exactly inert. Holding the node-24
  pointer across each row's call reads 306. Dropping the `orderCount` pointer
  local for the global directly is exactly inert, and assigning it only once
  reads 284. A local pointer to the input structure reads 337. `length` as
  `s8` is inert and as `u8` reads 335.
- Compiler flags are settled. `tools/flag_sweep.py` over 119 combinations on
  the sibling TU ranks the configured `-O2 -mips2 -32` first with no override
  improving on it, and the two TUs share the project default.

What the allocator could still reach, and by what force. A greedy forced-colour
census over every phase-one web and every colour, each force verified accepted
in the `p1color` record before its object was scored, ran 209 to 186 to 165 to
159 on the pre-lever source and 178 to 157 to 138 to 132 on the adopted one,
going flat after three rounds both times and naming the same three webs: the
post-call node-32 pointer to `a1`, the class-2 float web that carries the
opacity multiplier, and the pre-call node-32 pointer to `s2`. At 132 the
aligner reads 236 byte-exact against 183, so 46 words of this residual are
allocator-reachable and none of them is reconstruction.

The decision variable, named. The target holds the PRE-call node-32 pointer in
a callee-saved register where this candidate holds it in `v0`; that shows in
the residual as seven naming rows reading `v0` against `s2`. The mechanism is
in this procedure's own records: a caller-saved colour costs 2 for each call a
web spans beyond the first, and once a web's `nocs` reaches 3 its `available0`
mask carries NO caller-saved bit at all -- every `nocs` 3 web here takes `s0`
through `s7` in descending `save`, while the pre-call pointer sits at `nocs` 1,
`save` 9.0 and takes the cheapest colour, `v0`. So the variable is that
pointer's `nocs`, and none of the source forms above moves it.

The promotion gate is unchanged and is not about source shape: the relocation
identity for `gO101TailC144OrderCount` is still unresolved.

<!-- plateau-handoff:func_overlay_101_F000C144_18E7964:end -->
