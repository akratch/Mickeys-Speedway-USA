<!-- plateau-handoff:func_overlay_101_F000C144_18E7964:start -->
### `func_overlay_101_F000C144_18E7964` plateau handoff

- source: `src/overlays/o101/overlay101TailC144.c`
- score: 115/361 words
- frame: 0x38
- relocations: 30
- first mismatch: +0x10
- summary: Exact-size V0 has 246 masked differences; target 30 versus candidate 39 relocations and ambiguous local identities; body untouched.

Fresh maintenance evidence on base
`cf4a0f3a62ba99876282c21ec7d54b6166b3a555`:

- the overlay atlas and adjacent ownership rows bind overlay 101 `.text`
  `+0xC144..+0xC6E8`, ROM `0x18E7964..0x18E7F08`: exactly `0x5A4` /
  1,444 executable bytes with no padding before
  `func_overlay_101_F000C6E8_18E7F08`;
- the ABI is `void (void)`. Overlay 101 exports offset `+0xC144` through ROM
  table index 1997, but the committed C and fallback trees contain no named
  direct caller and the atlas records no resident inbound relocation to this
  entry;
- configured IDO 5.3 `-O2 -mips2 -32 -woff 835` emits an exact-size,
  361-word candidate. Target and candidate frames are both `0x38`; 115
  positional words agree after relocation masking, leaving 246 masked / 249
  raw differences, first `+0x10`;
- workbench classifies 265 aligned structural, 76 register, and eight constant
  residuals, with 129 insertions and 129 deletions in the shift-tolerant
  alignment. The earliest divergence is a register-class consequence of the
  global/order-count carrier, not an isolated scheduling residual;
- the target surface has 30 relocations: seven `R_MIPS_26`, eleven
  `R_MIPS_HI16`/`R_MIPS_LO16` pairs, and one `R_MIPS_32`. The candidate has
  39: seven calls and sixteen HI16/LO16 pairs. `function_preflight.py` remains
  fail-closed because `gO101TailC144OrderCount` and related overlay-local
  symbols do not select unique runtime identities; 38 compared relocation
  sites also carry different target/candidate spellings or layout;
- the current donor scan is exact-negative. Its weak leaders are Conker
  `func_151CFA4C` (1,496 bytes, `0.0696`), `func_1514B364` (1,248 bytes,
  `0.0659`), and `func_151BF340` (1,244 bytes, `0.0648`), with no source
  adoption supported. The earlier pinned DKR/JFG scans remain negative;
- the source guard at `64f10118c15a4f667b001f34de4c6be7bcde7ea0`
  is still current. This pass changed only this exact-symbol evidence marker;
  the function body is untouched.

Next lever: establish non-circular overlay-local data identities and recover
the target's eleven-pair relocation surface before reopening source work. The
prior call-arity, cast, declaration/order, node-builder, flag, and permutation
families remain closed; do not restore synthetic carriers or weaken the
identity gate.
<!-- plateau-handoff:func_overlay_101_F000C144_18E7964:end -->
