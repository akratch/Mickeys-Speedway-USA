<!-- plateau-handoff:func_overlay_101_F000C144_18E7964:start -->
### `func_overlay_101_F000C144_18E7964` plateau handoff

- source: `src/overlays/o101/overlay101TailC144.c`
- score: 152/361 words
- frame: 0x38
- relocations: 39
- first mismatch: +0x10
- summary: 209 masked words; the siblings' L59 fold transfers, their counter partition is refuted here. Identity gate still closed.

Measured 2026-09-11, lane `lane/o11-triplet`. This function is the fourth
relative of the overlay-101 builder family. Of the two levers diagnosed on
`func_overlay_101_F00063F8_18E1C18`, exactly one transfers.

Before: 246 masked words, aligned 126 byte-exact, 141 register naming, 10
immediate only, 88 really different, 4 insertion and 4 deletion words.
After: 209 masked, 160 byte-exact, 136 register naming, 5 immediate only, 64
really different, 4 insertions and 4 deletions. First mismatch unmoved at +0x10.

Transferred. L59, the fold: every per-element assignment group -- the two root
groups and each of the five text rows -- is written as one physical line, so
as1's `lineno` key ties and the raw list order supplies the store reversal the
ROM emits. That is the whole of the 246 to 209 move.

Did NOT transfer. The siblings' counter partition is already correct here and
every change to it regresses. Giving the text rows the order counter's local,
which is what the siblings need, reads 231 masked with 12 insertion sites
against 4. Giving the node32 pre-call read a named index, which is the siblings'
shape, reads 245. Collapsing the two counter locals into one does not compile
as written and was not pursued. So the siblings' second lever is refuted on this
function, and the reason is visible in the source: this reconstruction already
separates `orderIndex` from `nodeIndex`, and its node32 block already reads the
counter global directly for the pre-call half, which is the shape the siblings
had to be moved toward rather than away from.

Promotion remains blocked on the relocation identity for
`gO101TailC144OrderCount` as recorded above, not on source shape.

<!-- plateau-handoff:func_overlay_101_F000C144_18E7964:end -->
