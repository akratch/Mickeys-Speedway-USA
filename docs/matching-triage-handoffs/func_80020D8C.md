<!-- plateau-handoff:func_80020D8C:start -->
### `func_80020D8C` plateau handoff

- source: `src/main/models.c`
- score: 31/48 words
- frame: 0x8
- relocations: 0
- first mismatch: +0x38
- summary: register-field residuals; retained control changes initial reservation; complete dynamic FIFO unproved

Maintenance review confirmed that the source marker and guarded fallback still
describe this exact symbol; this shard now postdates the committed source
plateau.

September 6 two-source control: the actual canonical baseline and authenticated
retained candidate both reproduce their saved 192-byte bodies under the current
full TU, frame 8 and zero owned relocations. Canonical still has 17 differing
words; the retained candidate has 12, both first +0x38. This is retained evidence,
not a new gain or a canonical source adoption. Six stock/trace-off/trace-on
controls pass meaningful-section, relocation and symbol fidelity; all 18 other
nonempty function symbols and all text outside the owned range remain identical.

The retained body stages a later frame product through one used integer carrier
and reuses it as the constant one in the output indexing. It does not reorder
the early index, texture-address or predicate expressions. Their post-UOPT
statement records, excluding source-location records, and emitter instruction
order are unchanged. Both named Ucode streams authenticate procedure 15 of 19.
The first fourteen color assignments agree; one additional assignment appears
in the retained control. The fifth observed GP allocation changes its returned
physical register, and total observed allocation-result events change 14 to 13.
These facts do not establish a changed early expression-demand order.

Important ownership correction: the workbench's static IDO 5.3 profile calls
t0 through t9 temporary-only, while its color decoder already admits t0 through
t5. This function supplies surviving count and next-frame color/value/output
anchors in t0 and t2. Possible colorability and actual per-function ownership
must be separated; the static label cannot establish an uncolorable target or
UGEN-only cause here. No target compiler trace or new forced-color experiment
exists in this packet.

The initial trace review overclaimed complete temporary-pool membership from
the observed return sets. That claim is withdrawn: the five-register and
four-register replay subsets were selected from observed returns, and their
successful FIFO replays are conditional on those selections. Absence from
emitted instructions or allocation events alone is not reservation proof. The
original frozen artifacts remain preserved alongside this correction.

Follow-up independent audit now proves INITIAL membership from a different
evidence chain: both logs seed ten GP registers, and ordered initialization
removals leave five in canonical versus four in retained. The actual compiler's
reservation handler confirms physical-register intervals, rather than color
indices. Binding uses the existing source's DWARF checksum, object/debug-map
association and twelve linked helper non-address-field comparisons; it is not
a fresh rebuild or an independent relink of every destination. The retained
control's extra reservation explains changed initial availability without any
change to the early expression records. This does not retroactively validate
the earlier subset-based proof or establish a complete dynamic FIFO history.

The existing reader drops REMOVE events and treats conditional FREE/FORCE_FREE
entry requests as appends. Source and trace review show that successful frees
may append directly without ADD, while some requests do nothing; six requests
before the first allocation cause no append. MOVE_END concerns the used list,
not the free queue. A sound reader must retain the initial-state evidence while
reporting later queue history incomplete without successful mutation evidence.

The workbench correction is now reviewed and integrated in commits `028a8eaf`
and `1da7f333`. It separates possible colorability from per-input ownership,
preserves initialization evidence, and refuses complete FIFO replay from
conditional request hooks. Scalar comparisons are unchanged.

The subsequent authorized four-case matrix isolates the retained carrier's
product staging and later constant-index reuse. An authenticated archived
attempt already contains the exact product-only body; fresh reproduction adds
the missing full-TU and trace evidence rather than discovering a new candidate.
The formatted baseline body equals the freshly emitted compiler input, so
formatting is controlled. Retain the literal `int` declaration in the three
carrier forms; this matrix does not independently vary that declaration.

All four configured full-TU objects reproduce 192 bytes, frame 8, zero owned
relocations and first target mismatch +0x38. Baseline, product-only and
constant-only each have 17 differing words; retained reproduces its old 12.
Constant-only is byte-identical to baseline. Product-only changes six later
words despite retaining the same mismatch count and mismatch-location set.
All eighteen other function bodies, outside-owned text including padding, full
relocation records and data/rodata remain unchanged.

Twelve stock/trace-off/trace-on compiles pass eight section, symbol and relocation
fidelity comparisons, with all four independently mapped to procedure 15 of 19.
Whole-object hashes differ in debug metadata and are not claimed identical.
Baseline and both isolated uses have the same fourteen color assignments and
five-member initial GP queue. Only the combined control adds a color assignment
and reserves the extra physical register, leaving four initial members. Ordered
initialization and reservation events establish this independently of observed
return subsets; the existing producer source/object/binary hashes still match
the prior binding audit. The early post-UOPT index, address and predicate
records agree across all four after source-coordinate alignment.

Thus neither isolated use produces the added reservation in these exact forms.
Initial membership and coloring are not sufficient to explain all output:
product-only changes actual GP allocation returns from fourteen to twelve,
whereas constant-only retains the baseline sequence and retained has thirteen.
No complete dynamic FIFO history or target compiler ownership is established.
This is a controlled negative result, not a source recipe or a new score gain.

The root independently checked all frozen artifact hashes, owned-output
reproduction, initial ADD/REMOVE readout, and stock/off/on section, symbol and
relocation fidelity. The manual evidence bundle is
`7b749b0b489289a7bfac8026b345c2ef6cc395571d31665a7241b69f714a5d2a`;
it contains the bound preparation, four sources/objects, captures, readout and
independent review. It is not a completed search receipt or seed authority.

Next action: keep the fallback and both old controls parked. Reopen only with
a new source-faithful mechanism for the target's remaining register fields,
including the separate loop-back residual. Do not repeat these ablations,
the exhausted flag lattice, or random searches on the unchanged seed. This
matrix closes without another variant, canonical body adoption or matching
credit.
<!-- plateau-handoff:func_80020D8C:end -->
