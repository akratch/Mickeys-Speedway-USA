<!-- plateau-handoff:func_80020D8C:start -->
### `func_80020D8C` plateau handoff

- source: `src/main/models.c`
- score: 17/48 words
- frame: 0x8
- relocations: 0
- first mismatch: +0x38
- summary: P2 force ceiling reaches 16 only; the unresolved term is a hidden ugen draw in the multiply group.

#### 2026-09-11, lane `f9-small`: the target's loop is `while (remaining--)`, and the temp copy's block is the residual

The instrumented allocator shows the two extra uopt webs that push ugen's
temp ring from `$t3` to `$t5`: web 63 (a one-block temp, save 20) and web 67
(a variable, save 15), both coloured with no instruction. Rewriting the loop
as top-tested `while (remaining--)` (or `if (remaining--) do {} while
(remaining--)`, or `for`) reproduces the target's guard (`move $t1,$t0; beqz
$t0; addiu $t0,-1`) and bottom test exactly, and moves the residual to 23
words of one cause: cfe's post-decrement temp (`move $t1,$t0`) is born at the
loop bottom, after the `if (nextFrame >= 0)`, so as1 duplicates it into a
`bltzl` delay slot; the target's copy sits BEFORE that `if`, in the multiply
block, and the branch is a plain `bltz` with a nop. A plain `remainingCopy =
remaining` before the `if` is dead-code-eliminated in every position (8
slots), with `remaining--` at the top or the bottom (48 forms), with the
guard as a copy or a post-decrement, and with or without a `frameValue`
local (which the target does have: `lh $t1,0($v1)` is a web, not a ring
temp). `remainingCopy = remaining--` before the `if` keeps the copy but puts
the decrement there too (+4). The local-staging lattice (`index`,
`texture`, `frameScale`, `outputValue` types, store-before-increment) is
flat at 17 in the do/while frame. Decision variable: a spelling whose
post-decrement temp is evaluated before the `nextFrame` test while the
decrement itself lands in the bottom delay slot.

#### 2026-09-11, lane p7-res: regions and the loop-test axis are both refuted

Re-measured at 17 masked words against a fresh configured object, size delta 0,
frame 0x8 on both sides, one stack slot each, and the whole residual is register
naming from +0x38. `tools/register_census.py` reads it as **two windows** at 60%
coherence with no closed cycle -- per-iteration consumption, not one ring phase --
so the four-cycle reading that works on some siblings does not apply here.

The one site that is not ring rotation is the loop branch: the target tests the
counter itself, with the decrement in the branch's delay slot, while this
candidate tests the copy. Seven spellings of that were measured and every one is
worse: the post-decrement condition in two forms is 23 and 24, moving the
decrement into the `while` with the copy kept is 24, dropping the copy is 24,
testing the copy with a post-decrement grows the function by a word at 25, and
the two forms that test the counter after decrementing unroll the loop and grow
it by 304 and 308 bytes. The retained body's shape is the best of the family.

**16 L97 region placements** across the loop body, the loop head, the
inner conditional and the tail: ten are byte-identical to the retained body and
six regress, worst at 52. Regions are refuted for this symbol.

Decision variable is unchanged from the `f9-small` note below and is a ugen
question, not an allocator one: the candidate's second window begins at +0x70,
so the pop that differs is inside the multiply group. `DKWB_UGEN_TRACE=1` with a
per-source-line pop count is the instrument.

#### 2026-09-12, lane p15-res: p2 ceiling bounds allocator influence to one word

The configured instrumented compile is text-identical to the stock compile and
maps this leaf to procedure 15. The source-line trace places four GP draws on
the first multiply and two on the conditional multiply. A folded `+ 0` probe at
the first multiply is byte-identical and does not add a draw.

A direct p2 force ceiling covered 95 accepted single-force cells over all 14
recorded decisions. Only web 63 or web 67 forced to colour 12 improves the
masked score, from 17 to 16 at unchanged size. The pair grid for those two webs
does not improve beyond 16; competing assignments to colour 12 are declined.
Thus allocator colour is a one-word term, not the cause of the remaining
per-iteration temp residual. The next discriminator remains an L149 source form
that changes the hidden ugen draw near the multiply group.
<!-- plateau-handoff:func_80020D8C:end -->
