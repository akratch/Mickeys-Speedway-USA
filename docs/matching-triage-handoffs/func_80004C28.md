<!-- plateau-handoff:func_80004C28:start -->
### `func_80004C28` plateau handoff

- source: `src/main/objects.c`
- score: 7 differing words
- frame: 0x48
- relocations: 39
- first mismatch: +0x5C
- summary: Workbench allocation-mismatch: register-role-audit. Next: isolate heap-spill line order and the short-lived record-length web.

The configured full-TU baseline was 239 words against 238 owned words,
230 raw/masked differences, first mismatch +0x0, and a 0x68-byte frame
against 0x48. Attempt 53 is retained: 238 words (952 bytes), 231 exact
words, seven raw/masked differences, first mismatch +0x5C, and the exact
0x48 frame. Workbench classifies three schedule and four register
mismatches, with no opcode or instruction-count deficit. Both objects have
39 relocations; offset/type correspondence is 39/39 and authenticated
identity correspondence is 38/39. This is guarded NON_MATCHING C, not a
linked C match or new matching credit.

The gate returned base-only. PROVENANCE: the weak near-match oracle led to
Diddy Kong Racing public `src/objects.c`, `track_spawn_objects`, at revision
`38d7f9ba39642e2b5311a76e0b83fb3fe2733262`. Its table walk and array-carrier
spelling informed this reconstruction, with disclosure at the source use.
Mickey's own assembly controls every field, condition and call. Rescoring
53 usable objects from the retained TU flag lattice gave no improvement;
no compiler flags were changed.

The baseline table loop skipped the second table entry. A direct sentinel
walk restored the correct boundary and exact word count. Removing redundant
slot pointers, reusing an expired count, and positioning the live ROM offset
restored the frame and its stack homes. The spawn callee consumes only two
arguments: its incoming third and fourth registers are overwritten before
use. Removing the draft's two unsupported arguments repaired most allocation
differences. Putting the one post-store cursor reload in each exclusive
record-remapping arm restored the instruction layout without increasing
runtime memory accesses. Capturing the later ID-slot address in the expired
table pointer reduced the final residual from ten words to seven.

Attempt numbers are global to this lane. Attempts 28–30 repaired the table
walk and frame. Attempts 31–40 investigated array-address spelling, scopes,
types and call carriers. Attempts 41–44 established the two-argument call and
per-arm reload shape. Attempts 45–52 tried lifetime, expression, loop and
line-order variants; attempt 53 reached the retained seven-word result.
Attempt 54 established that a dedicated record-length scalar recovers its
register role but increases the frame to 0x50. The final five attempts
55–59 tested sharing the dead table pointer, sharing the heap pointer,
raw address addition, disjoint lexical scopes and a byte-width length.
None improved the seven-word best or established a way to retain both the
needed register roles and stack layout. Those five probes are this packet's
stall evidence. The larger-frame alternative and every material source,
object and measurement are preserved in ignored build artifacts.

The workbench verdict is `allocation-mismatch`; read `register-role-audit`
before another allocation edit. The next concrete lever is to capture stock
assembler line-order evidence for the heap spill, and separately establish
how to retain the dedicated record-length role without adding a declared
home or joining it to the spawn-decision web. The latter has a controlled
larger-frame witness, not an exact source spelling. Also resolve the one
remaining authenticated relocation identity before promotion. A shared
pointer lifetime can alter an earlier ROM-table return carrier, so review
the whole function after any late-loop change.

Validation: configured `wb_compare.sh --summary-json`, workbench diagnosis
and guides, retained flag-object rescoring, explicit linked assembly-range
comparison with the baserom, `gmake verify` with the expected US ROM hash,
`gmake check-docs`, `gmake cleanroom`, and `tools/finalize_plateau.py`.
The canonical ROM proof covers the fallback; the seven-word C deficit remains.

<!-- plateau-handoff:func_80004C28:end -->
