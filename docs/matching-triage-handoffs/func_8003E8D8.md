<!-- plateau-handoff:func_8003E8D8:start -->
### `func_8003E8D8` plateau handoff

- source: `src/main/particles.c`
- score: 1 differing words
- frame: 0x38
- relocations: 10
- first mismatch: +0x38
- summary: one zero-count guard branch target at +0x38; IDO retargets it past the redundant pool-base reset. The permuter is blind to it (base score 0).

- Residual identity (2026-09-09): configured full-TU C is 140/140 instructions,
  frame `0x38`, all ten relocation tuples exact, and exactly one differing
  word. That word is the zero-trip guard of the first pool scan: the target's
  guard branches to the pool-base reset, the candidate's branches one word
  past it, straight to the second scan's own guard. Nothing else in the
  function differs.
- Causal mechanism, proved: the reset is *redundant on the guard's skip edge*
  because the pre-guard initialiser already gave the carrier the pool base,
  and IDO retargets the branch past instructions that are redundant or dead
  along that edge. Two independent confirmations. (1) Moving the first scan's
  carrier initialiser inside its own guard makes the reset non-redundant on
  that edge and the branch target becomes exact. (2) Writing both of the
  second scan's initialisers as the loop's own preheader (a `for` comma-init)
  makes *two* words redundant on that edge and the branch skips both, landing
  two words past the reset instead of one.
- Cost of the fix, measured: with the initialiser inside the guard the branch
  is exact but the carrier initialiser and the `0xFF` result initialiser
  exchange positions, because a hoisted preheader initialiser is appended
  after the plain pre-guard statements while the target has the carrier first.
  Best clean recombination is 2 differing words (both halves of that one
  exchange); the other seven arrangements of the same three statements
  measured 3 to 8. The 1-word baseline is retained.
- Eliminated this pass, each measured: two separate carriers at all seven
  declaration positions (never coalesced; +1 instruction and 133-135 words
  every time); reusing an existing local as the first scan's carrier
  (+2 instructions); indexing the pool in the first scan instead of walking a
  pointer (+1 instruction); `while`, pre-tested `for`, and a duplicated
  reset in both arms of an explicit `else` (all three byte-identical to the
  baseline -- uopt normalises them); moving the loop index initialiser inside
  or outside either guard (byte-identical); respelling the reset as a cast or
  as the address of element zero (byte-identical); and initialising the result
  inside the first guard, at the declaration, or before the null check (spills
  the result carrier, +3 instructions, 10-116 words).
- Tooling: the permuter **cannot see this residual**. `tools/permute.sh
  func_8003E8D8` reports `base score = 0` and exits immediately, because its
  scorer normalises a branch whose only difference is its destination word.
  No permuter or batch sweep can move this target; do not spend budget there.
  The same scratch, with its `_permuter latedefine` pragmas materialised into
  real `#define`s, reproduces the configured full-TU object exactly, which
  gives a sub-second iteration loop for this TU.
- ADR 0018 stall: the last four attempts (existing-local carrier, indexed
  first scan, declaration-order sweep over the two-carrier form, result
  initialiser placement) each eliminated a hypothesis and none improved on
  1 word.
- Next lever: a spelling that keeps the reset a plain pre-guard statement
  (so it schedules before the guard, as the target has it) while making it
  non-redundant on the skip edge. Statement placement alone cannot do both;
  the two properties are coupled through the same dominance fact.
<!-- plateau-handoff:func_8003E8D8:end -->
