<!-- plateau-handoff:func_8003E8D8:start -->
### `func_8003E8D8` plateau handoff

- source: `src/main/particles.c`
- score: 139/140 words
- frame: 0x38
- relocations: 10
- first mismatch: +0x38
- summary: Branch target and preheader emission order are one coupled choice; the branch-correct arrangement costs the carrier/result init exchange instead. A separate first-scan cursor -- the mechanism that closed func_80041CE4 -- reproduces the known +1-instruction wall in every form, so the coupling is not a cursor question.

- Residual identity (2026-09-09): configured full-TU C is 140/140 instructions,
  frame `0x38`, all ten relocation tuples exact, and exactly one differing
  word. That word is the zero-trip guard of the first pool scan: the target's
  guard branches to the pool-base reset, the candidate's branches one word
  past it, straight to the second scan's own guard. Nothing else in the
  function differs.
- Causal mechanism, proved: the reset is *redundant on the guard's skip edge*
  because the pre-guard initialiser already gave the carrier the pool base,
  and IDO retargets the branch past instructions that are redundant or dead
  along that edge. Two independent confirmations. (1) Moving the first scan's
  carrier initialiser inside its own guard makes the reset non-redundant on
  that edge and the branch target becomes exact. (2) Writing both of the
  second scan's initialisers as the loop's own preheader (a `for` comma-init)
  makes *two* words redundant on that edge and the branch skips both, landing
  two words past the reset instead of one.
- Cost of the fix, measured: with the initialiser inside the guard the branch
  is exact but the carrier initialiser and the `0xFF` result initialiser
  exchange positions, because a hoisted preheader initialiser is appended
  after the plain pre-guard statements while the target has the carrier first.
  Best clean recombination is 2 differing words (both halves of that one
  exchange); the other seven arrangements of the same three statements
  measured 3 to 8. The 1-word baseline is retained.
- Eliminated this pass, each measured: two separate carriers at all seven
  declaration positions (never coalesced; +1 instruction and 133-135 words
  every time); reusing an existing local as the first scan's carrier
  (+2 instructions); indexing the pool in the first scan instead of walking a
  pointer (+1 instruction); `while`, pre-tested `for`, and a duplicated
  reset in both arms of an explicit `else` (all three byte-identical to the
  baseline -- uopt normalises them); moving the loop index initialiser inside
  or outside either guard (byte-identical); respelling the reset as a cast or
  as the address of element zero (byte-identical); and initialising the result
  inside the first guard, at the declaration, or before the null check (spills
  the result carrier, +3 instructions, 10-116 words).
- Tooling: the permuter **cannot see this residual**. `tools/permute.sh
  func_8003E8D8` reports `base score = 0` and exits immediately, because its
  scorer normalises a branch whose only difference is its destination word.
  No permuter or batch sweep can move this target; do not spend budget there.
  The same scratch, with its `_permuter latedefine` pragmas materialised into
  real `#define`s, reproduces the configured full-TU object exactly, which
  gives a sub-second iteration loop for this TU.
- ADR 0018 stall: the last four attempts (existing-local carrier, indexed
  first scan, declaration-order sweep over the two-carrier form, result
  initialiser placement) each eliminated a hypothesis and none improved on
  1 word.
- Next lever, as of the 2026-09-08 pass: a spelling that keeps the reset a
  plain pre-guard statement (so it schedules before the guard, as the target
  has it) while making it non-redundant on the skip edge. Statement placement
  alone cannot do both; the two properties are coupled through the same
  dominance fact.

- 2026-09-09 pass, the coupling localised to two words and one emission rule.
  The branch is reachable and cheap. With the first scan's carrier initialiser
  inside its own guard *and* the second scan's index initialiser lifted out of
  its guard, the guard branch lands on the pool-base reset exactly and the
  whole residual becomes one adjacent pair: the target emits the carrier
  initialiser before the result initialiser, the candidate the other way round.
  Nothing else in the function differs, the instruction count and frame stay
  exact, and the relocations stay exact. Two words, branch correct, against one
  word with the branch wrong.
- The rule behind the exchange: uopt emits every *plain* pre-guard statement
  before every *hoisted loop-preheader* initialiser, whatever their source
  order. Making the carrier non-redundant on the skip edge requires it to be
  guard-local, which makes it a preheader, which puts it last. The result
  initialiser cannot join it there without going uninitialised on the skip
  path, and every arrangement that tries costs two instructions.
- Eliminated this pass, each measured against the two-word form: all six
  orderings of the three initialisers inside the first guard (113-118 words,
  +2 instructions every time); five physical line groupings of the result
  initialiser and the guard, including joining it onto the `if` line, which is
  the lever that moved a schedule on fx.c and is flat here; the result
  initialiser before the null check with and without returning it (10 words);
  the result initialiser after the whole first scan (17); the first scan as a
  pre-tested `for` with a comma init, as a `while`, and as a guarded `for`
  (115-131); and the second scan's index initialiser inside versus outside its
  own guard, which is what selects 2 from 4.
- The 16-cell cross product of {first scan: both inits out, both in, carrier
  in, carrier in with the result after} x {second scan: reset out with index
  in, reset out with index out, both in, index out with reset in} is
  exhaustive at about four candidates a second and its floor is the 1-word
  baseline; the branch-correct floor is 2.
- Still open, and the only lever left that is not statement placement: a way to
  make uopt emit one preheader initialiser ahead of a plain statement, or a
  spelling of the result initialiser that costs no instruction before the
  guard at all.

- 2026-09-09 pass, one more mechanism tried and eliminated. The lever that
  closed `func_80041CE4` in this TU on the same day -- a hand-written pointer
  cursor that was never in the source -- does not apply here. Giving the first
  scan its own cursor so the second scan's `entry = D_8007C898` is a genuine
  first definition (and therefore cannot be redundant on the guard's skip
  edge) reaches the branch, but every form of it costs the same extra
  instruction: a fresh `ParticleModelEntry *` local at all eight declaration
  positions (141 instructions, 133-134 words), the first scan reusing the
  `f32 *pointData` local through a cast (141, 133), indexing `D_8007C898[i]`
  in the first scan instead of walking a cursor (141, 132), and giving the
  *second* scan the new local instead (142, 136). The wall is that both
  spellings of the base materialise separately; uopt never coalesces them.
- Also flat at the 1-word baseline this pass: duplicating the reset into both
  arms of an explicit `else` (byte-identical, re-confirming the earlier
  measurement), and folding the carrier and the result initialiser into one
  comma expression `result = (entry = D_8007C898, 0xFF);` (byte-identical).
  The branch-correct arrangement still floors at 2.
- The open question is unchanged and is now better bounded: it is not a cursor
  question and not a statement-placement question. It needs a spelling in
  which the pool base reaching the second scan is the *same* value as the
  first scan's carrier (so no instruction is added) while not being provably
  equal to it along the skip edge -- or an emission-order lever that puts one
  hoisted preheader initialiser ahead of a plain pre-guard statement.

#### 2026-09-09, lane fin-near: the retarget is uopt's copy placement, and the named-base form costs the same instruction

The ugen listing settles which phase owns the word: the loop-exit fallthrough
already carries the pool-base copy before the join label, so uopt sank the
copy into the one edge where it is not redundant; `as1` is not involved and a
listing replay cannot probe it. The untried spelling this pass was a named
base local shared by the null check, a distinct first-scan cursor, and the
second scan's `entry = base` (so the join copy has no prior definition of
`entry` to be redundant against, and the load is spelled once). Twelve forms
-- base assigned before or after the null check, the cursor initialised
outside or inside its guard, four declaration positions -- are all
+1 instruction (0x234) at 132-137 words, and two of the positions also grow
the frame. The 1-word baseline is retained.
<!-- plateau-handoff:func_8003E8D8:end -->
