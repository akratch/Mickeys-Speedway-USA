<!-- plateau-handoff:func_overlay_046_F0000120_188E518:start -->
### `func_overlay_046_F0000120_188E518` plateau handoff

- source: `src/overlays/o046/overlay46UpdateSequence.c`
- score: 54 differing words
- frame: 0x30
- relocations: 118
- first mismatch: +0x78
- summary: Index-generated pointer forms retain the frame but add instructions; zero-size web partition remains open. First mismatch is relocation-masked.

Added to the flat list this lane, all against the full 317-word target:

- the [L149] double-mask family at the tail alpha site, which is a textbook
  instance of the generator and is already at its floor: dropping the macro's
  second mask 56, dropping the first 87, dropping both 85, and dropping only
  the `(u32)` cast 87. The cast is load-bearing.
- the compare-loop carrier family: deleting `expected` by comparing `*right`
  before the increments 70, by `right[-1]` after them 65, by `*right++` 65;
  `expected` as `s32` 54; `current` as `s32` 59; and the recorded
  adopted-in-waiting `expected != current` still 54.
- a `while` rewrite of the name compare: 54, and its p1color assignment is
  bit-identical to the goto form, so the loop shape is inert here as well.

Instrument note for the next lane: `CDX_PROC=0 CDX_LOG=1 CDX_OUT=<path>` on the
instrumented toolchain prints the `p1dec`, `p1color` and `intf` records for this
procedure, and reading the `intf` rows against the `p1color` list is what shows
the forbidden mask is the interference list. Do that before believing any
statement about why a colour is unavailable.

#### 2026-09-12 colour landscape and lattice

`tools/web_footprint.py func_overlay_046_F0000120_188E518 --proc 0
--every-colour` sampled all 180 accepted legal colour probes across all 21
coloured p1 webs and 9 residual windows. No probe beat the unforced 54/317
baseline, so the measured single-force colour floor is 54/317 over the complete
accepted landscape. The closest legal probes were `p1:w58=c5` and
`p1:w103=c6` at 56, followed by `p1:w166=c6` at 57; web 51's desired a2
colour was not offered and therefore was not falsely counted as a declined
force.

`tools/force_lattice.py --proc 0` measured those three near-misses. All pairs
were additive and worse: `w58=c5 + w103=c6` scored 58, `w58=c5 + w166=c6`
scored 59, and `w103=c6 + w166=c6` scored 59. Blast radii were `w58=c5`:
`+2` at 0x300; `w103=c6`: `+1` at 0x380 and `+1` at 0x400; and `w166=c6`:
`+3` at 0x480. Thus the lattice floor remains 54/317; no winner existed to
feed into it, and every measured force preserved or worsened the baseline.

The source question is an interference-set change, not a colour ratio: web 51
must lose enough live consumers for a2 to enter its legal offer list. The
instrument says the current forbidden mask is exact interference, while the
tested discarded-expression probes and loop spellings leave the p1 decisions
unchanged. A credible source form must alter that web partition at zero size
cost; forcing a2 itself is not legal evidence.
#### 2026-09-12, lane p24-second-mid: index-generated pointer webs cost instructions

The base-only assignment gate passes. Configured baseline: 317 words, zero
size delta, frame 0x30, 70 raw and 54 relocation-masked differences. Raw
first mismatch is +0x4; the first masked difference is +0x78. The header now
uses the latter. Aligned rows are 263 exact and 54 naming, with no immediate,
structural or one-sided words. The candidate and owned runtime table each
contain 118 relocation records; the literal-bearing assembled target has
56 static sites. No new complete identity alignment is claimed. The existing
complete colour landscape was read and not repeated.

L154 opens a different source axis from the previously tested pointer-loop
spellings: declare an index and let the compiler form the walking addresses.
Three controls preserve the compare's byte reads and prefix-match outcome.
An index carried in the existing value local, with both base pointers kept,
adds eight bytes and scores 181 masked. A fresh unsigned index replacing
the declared left pointer also adds eight bytes and scores 181. A for-loop
using the existing index and deriving the left base from its global adds
twelve bytes and scores 185. All three retain frame 0x30. Their aligned maps
show extra instructions and new immediate/structural rows, so the positional
counts are not treated as evidence for a colour improvement.

The external-source baseline was first proved full-TU text-identical to the
configured original path. Every source, object, score, frame census and
aligned per-window comparison is retained under
build/p24/func_overlay_046_F0000120_188E518/. No source body is adopted.
These three controls do not supply a zero-size partition and reproduce the
same added-index-work failure class. Stop on that evidence and retain the
54-word candidate. The next source question remains a web partition that
frees the desired argument-register colour without additional instructions;
this index-generation family does not provide it.

Commands: lane_status, configured compilation, residual_map --object/--against,
frame census, runtime-table count, finalize_plateau and tools/gates.sh.
The candidate remains guarded and earns no new executable credit.

<!-- plateau-handoff:func_overlay_046_F0000120_188E518:end -->
