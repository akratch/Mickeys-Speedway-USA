<!-- plateau-handoff:func_overlay_046_F0000120_188E518:start -->
### `func_overlay_046_F0000120_188E518` plateau handoff

- source: `src/overlays/o046/overlay46UpdateSequence.c`
- score: 54 differing words
- frame: -0x30
- relocations: 118
- first mismatch: +0x4
- summary: Exact 317-word geometry and 118 aligned relocation records; the aligner puts all 54 residual words in the register-naming bucket with zero immediate, zero structural and zero displacement tax, so this is purely a colouring residual. Read off the instrumented allocator on 2026-09-12: the procedure is p1-only, globalcolor consumes colours 1 to 7, and colour 7 is taken by exactly one web, the case-5 `matched` flag, which pushes ugen's expression ring from t0 to t1 and re-phases the whole function. That web is forbidden colours 1 to 6 and its own p1cost list starts at colour 7, so the six argument and return registers are never priced for it; forcing the a2 holder off its colour leaves the flag on t1 and costs 90 words, so freeing an incumbent is not the route. Flat at 54: sixteen region placements, five symbol-boundary splits and merges, four flag spellings, an inverted flag, a while-loop compare, a hoisted flag address and 24 declaration orders. Writing the name compare as `expected != current` restores the target's own operand order at that site and is flat today, and is recorded for the lane that lands the ring. The next lever is to shorten the flag's span until it stops crossing a call that loads a2 or a3, or to find a form in which it is not a distinct symbol; raising its save cannot work, because a colour absent from a web's cost list is not for sale at any ratio.

Fresh evidence on base `a4bf95c18049fe173aa62f2b9349682afd0aa8b8`:

- authenticated owner: overlay 46 `.text` offset `0x120..0x614` (1,268
  bytes), with the next owner beginning at `0x614`;
- ABI: `void (s32)`; one resident `R_MIPS_26` caller was found at runtime
  address `0x80039038`;
- configured V0: IDO `-O2 -mips2 -32`, exact 317-word size and `0x30`-byte
  frame, but 54 masked / 70 raw differing words. Workbench reproduced the
  allocation mismatch at temp slot 7 and pool slot 58, with 23 aligned
  schedule and 54 aligned register residuals;
- relocation surface: target and candidate each have 118 records and every
  offset/type site aligns. Candidate proof is partial because 83 static
  identities remain unresolved, so exact identity is not established;
- donor scan: the best DKR/BK masked 4-gram similarity was `0.067`, with no
  credible donor or sibling mechanism;
- prior constant/relocation audit, packet-carrier and materialization forms,
  structured name-loop variants, full flag lattice, and bounded `-mips2`
  permutation remain exhausted. They were not repeated without a new gain.

The TU-owned switch-table range already clears module growth. Next lever:
derive a new source-level lifetime boundary for the display-list packet carrier
or resolve the static relocation aliases before revisiting allocation.

#### 2026-09-12, lane p8-arity: the arity lever is real, moves the flag one colour, and stops a colour short

Identity gate passed first: the instrumented toolchain's `.text` is byte-identical
to the tree's stock object, and the records reproduce the closure above exactly --
web 51 at save 1.5, nocs 4, totalsave 6, `forbidden0=0x7e000000`, a `p1cost` list
starting at colour 7, `decision=color bestreg=t0`.

**The a2 denial is not interference.** The closure reads web 51's interference
list as holding all six low colours and treats freeing an incumbent as the
route. It is not: forcing web 70 off a2 leaves a2 unassigned at web 51's
decision and web 51's mask still carries colour 5, becoming `0x7e800000`
(colours 1 to 6 plus t1). A direct `CDX_FORCE=p1:w51=c5` is declined
byte-identically, as are c1 through c4 and c6, which is L101's
already-forbidden case.

**The denial is a count keyed to argument arity, and one argument is worth
exactly one colour.** Varying `func_80036F08`'s argument count with everything
else fixed:

- 0, 1 or 2 arguments: `forbidden0=0x7c000000`, colours 1 to 5, web 51 takes a3
- 3 arguments, the tracked form: `0x7e000000`, colours 1 to 6, web 51 takes t0
- 4 arguments: `0x7f000000`, colours 1 to 7, web 51 takes t1

`func_80028374` with six arguments and both `func_8004B0` calls move the mask by
nothing, so they are outside web 51's range; `func_80036F08` and the tail's
reuse of `matched` are inside it. Inlining the tail use of `matched` on its own
drops totalsave from 6 to 4 and the mask to `0x7c000000`.

**The floor is a3.** With every tail call cut to one argument and the tail use of
`matched` inlined away as well, the mask stays at `0x7c000000` and the flag takes
a3, never the target's a2. Colours 1 to 5 survive every arity reduction the
function admits. Each of those probes also scores worse than the base, 108 to 124
against 54. So this lever is closed rather than unexplored: it is worth one
colour on this function and the target needs two.

**The single-decision axis is closed as well, and this is the more useful
number.** A full greedy force ceiling was run -- all 24 p1 decisions against
colours 1 to 22 and the split path, 552 cells, 438 of them accepted and 433
distinct objects. **Not one scores better than 54.** The best accepted cell ties
at 54 and the next is 56. Forcing web 51 itself gives 70 at t3, 74 at t4, 86 at
t1 and t2, 169 at the split path, and 223 to 327 in the saved-register range.

So the residual is not one globalcolor decision. The next lane should not re-run
a force sweep, a colour lattice, or the arity lever: it needs the web *set* to
change. The two shapes that would do that and have not been measured are a source
form in which the case-5 flag is not a symbol at all (a control-flow rewrite of
the name compare that yields its result without a carrier), and one that removes
`func_80036F08` from the flag's range without removing the flag, which the
inline-tail probe did not achieve on its own.

<!-- plateau-handoff:func_overlay_046_F0000120_188E518:end -->
