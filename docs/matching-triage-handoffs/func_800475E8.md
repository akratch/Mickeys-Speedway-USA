<!-- plateau-handoff:func_800475E8:start -->
### `func_800475E8` plateau handoff

- source: `src/main/fx.c`
- score: 390 differing words
- frame: 0x108
- relocations: 6
- first mismatch: 0x0
- summary: JFG efd5abb remains assembly-only; zero source attempts. Need new vertex-loop source or proved build boundary evidence.


Reopening audit (2026-09-08), evidence D: PROVENANCE inspection of Jet Force
Gemini public decomp `src/fx.c` and `src/fx.h` at
`efd5abb1c79636e297b831f7c2d5bf47eac39c0c` found no new target C body.
No donor source, names or values were adopted. Mickey remains authoritative.

Configured full-TU measurement: candidate 1784 bytes / 446 words,
target 1004 bytes / 251 words; size delta +780 bytes.
Raw and relocation-masked differences are 390 and 390, respectively,
first mismatch +0x0; candidate/target frames are
0x108/0xF8. Candidate/target static relocation counts are
6/6; 4 tuples agree in function-relative offset, type and symbol.
These are fallback-object comparisons, not linked-C promotion evidence.

Workbench comparison: `structure-mismatch`. Diagnosis: `mixed(constant:13, structural:248, register:78)`;
playbook `constant-audit`, lever `stack-home`. The named guides were read.
This heuristic routing supplies no new donor evidence to reopen exhausted forms.
Next concrete lever requires new fxMakeConeTextureCoords donor C or measured function boundary that justifies the already-tested loop-unroll flag,
then a fresh gate and configured full-TU comparison.

Stopping evidence: zero source attempts; the authorized donor mechanism supplies
no new implementation. This is the assignment's early-exhaustion stop, not a
five-attempt stall. The prior TU lattice is not repeated: since its recorded
`func_8004ACC4` audit at `4be95a3d`, this TU differs only in EOF handoff comments.
See [the anchor donor audit](func_80049E4C.md) for the full delta and batch disposition.

Ignored evidence is retained in `build/tu-fx-audit/` and `build/wb/`: source and
full-TU object, gate receipts, summaries, diagnoses and relocation tuples.
Commands: `lane_status.py --symbol`, `wb_compare.sh --summary-json`, workbench
`diagnose` and `guide`, and `finalize_plateau.py`. The unchanged guarded C stays
NON_MATCHING and receives zero new exact bytes.

Prior plateau evidence retained (historical measurements and exhausted forms):

- geometry: Target is 1,004 bytes / 251 words with frame `0xF8`; configured candidate is 1,784 bytes / 446 words with frame `0x108`.
- relocation identity: Candidate and target each have 6 static relocations; 4 offsets, types, and identities align in fallback-static evidence.
- flag lattice: All 119 combinations were nonexact. `-O2 -mips2 -32 -Wo,-loopunroll,0` was best at 247 words, 178 differing words, first mismatch `+0x0`, and frame `0x100`; applying it to all of `fx.c` is not justified.
- donor result: JFG's assembly-only `fxMakeConeTextureCoords` is the strongest skeleton at 0.282 and corroborates the control-flow role, but supplies no adaptable C body.

<!-- plateau-handoff:func_800475E8:end -->
