<!-- plateau-handoff:func_80006448:start -->
### `func_80006448` plateau handoff

- source: `src/main/objects.c`
- score: 50/59 words
- frame: 0x28
- relocations: 4
- first mismatch: +0x50
- summary: Nine words, one web. The instrumented capture the earlier note asked for was run: the resource web prices a0 at 0.0 and v0 at infinity, so v0 is not a priced candidate in this procedure and no source form that keeps the value as a globalcolor web can reach the target. Forcing that web to v0 is byte-exact. Regions and twelve further spellings are flat.


#### 27 -> 9: the initializers belong inside the guard

Two of the three webs that differed were `arg0`'s saved copy and the byte
offset trading `s0` and `s1`, and the cause is L87 read literally: uopt colours
pool webs in the order of each web's *first surviving definition in source
statement order*, and `s32 offset = 0; s32 index = 0;` at the declaration puts
those definitions ahead of the parameter copy. Moving both initializers inside
the `unk22 > 0` guard, as ordinary statements, puts the parameter copy first,
gives it `s0`, and takes 24 register words with it. Declaration order is not the
handle here and never was -- the initializer *is* a statement.

The remaining nine words are three sites x three words. The target loads the
resource into `v0`, tests `v0`, and fills the `jal` delay slot with
`move a0,v0`; the candidate loads straight into `a0` and emits `nop`. So the
target's resource web is not coalesced with the call-argument web and ours is,
which is L66 running the wrong way. Measured flat against it: repeating the
field read at the call (lever 45), repeating it at the test, removing the
`resource` local entirely so all three branches spell the load twice, removing
its declaration as well, `!= NULL` and bare-truth tests, an inverted test with
an empty then-arm, a redundant `(void *)` cast, `s8`/`s32`/`u8` for `type`,
`void **` for `resource`, and four declaration orders. All of those compile to
the same object.

Next lever: something that makes the resource web interfere with the `a0`
argument web that carries `arg0` into `func_800058C0` at the top of the loop --
a live range that crosses the loop head, not another spelling of the call.

#### Owned pass, 2026-09-10 (lane/o7-small): eight spellings, still flat

Reconfirmed at nine masked words, delta 0, first mismatch +0x50, against a
fresh configured object. The residual is unchanged: three sites where the
target loads the resource into `v0` and fills the `jal` delay slot with
`move a0,v0`, and the candidate loads straight into `a0` and emits `nop`.

Newly measured, every one byte-identical to the retained body:

- A named `self` carrier for the loop-head call's own argument -- the shape
  L100 predicts would put a high-`save` web on `a0` and push the resource down
  to `v0` -- declared first, declared last, assigned inside the loop, assigned
  in the preheader, and typed both `Objects58C0Arg *` and `void *`.
- `(void *)`, `(void *)(s32)` and `(void *)(u8 *)` casts on the argument at all
  three call sites, testing whether a cast node breaks the copy relation that
  L66 gives the argument register. It does not.
- One `s32` local carrying both the dispatch type and the resource, declared
  first and last (the target does keep both values in `v0`).
- The resource typed `s32` with `(void *)` casts at the calls.
- Three separately named resource locals, one per arm.
- The resource block-scoped inside each arm.

So the "a0 is free, therefore a0 wins" reading stands: a value whose only uses
are a null test and a one-argument call takes `a0` whenever `a0` is free, and
nothing reachable from this source occupies `a0` across the loop body. The
stock `uopt -Wo,-zdbug:2` listing confirms the shape of the web population
(`resource` is one variable with six versions, class M at -12; `type` is class
M at -13) but stock uopt prints no `p1cost` row, so it cannot show whether
`a0` is preferred at negative cost or `v0` at a tie.

**Next lever, sharpened.** An instrumented `p1cost`/`p1dec` capture on this
one web. If `a0` is winning by a copy-relation bonus rather than by the
ascending-colour tie-break, the lever is to remove the bonus, not to occupy
`a0`; if `v0` is simply not offered, the symbol is closed. Either answer is one
build with the instrumented compiler and is worth more than a ninth spelling.

#### 2026-09-11, lane p7-res: the instrumented capture the note above asked for, and it closes the a0 question

The previous section's "next lever, sharpened" was an instrumented
`p1cost`/`p1dec` capture on the resource web. It was run. The answer is that
**`v0` is not a priced candidate for this web in this procedure**, so no source
form that keeps the value as a globalcolor web can reach the target.

The procedure is uopt ordinal 22 in `objects.c` and has exactly five phase-one
webs. The resource web is web 19, class 1, `save` 30.0 with `nocs` 3 and
`totalsave` 90.0, and its evaluated colour costs are: `v0` and `v1` infinite,
`a0` **0.0**, `a1`/`a2`/`a3` 3.0, `t0` through `t5` and the unnamed c13
infinite, `s0` through `s8` 7.75. Nothing is in its forbidden mask -- which is what makes this cost list a
reading of the *table* rather than of the mask, the distinction that made an
earlier float-bank decode wrong. So `a0` is
the unique zero-cost colour, it wins on cost rather than on a tie-break, and the
"remove the copy-relation bonus" branch of the old hypothesis is the live one
while the "v0 is simply not offered" branch is now confirmed as well -- it is
offered at an infinite price, which is the same thing for a source lever.

`CDX_FORCE=p1:w19=c1` compiles to a **byte-exact** function, 0 masked words, so
the target's value really is in `v0` and nothing else about this body is wrong.
`p1:w19=c4` (a1) reproduces the target's shape -- the delay slot gains the copy
-- and still scores 9 as pure register naming, which is the cleanest possible
statement that the residual is one colour and only one colour. The split path
regresses to 24.

Splitting the web does not help and now has a reason. Three separately named
resource locals give three webs with `nocs` 2 apiece, and every one of them
still prices `v0` infinite and `a0` at 0.0; the object is byte-identical to the
one-local form. So the infinite `v0` is a property of the procedure, not of the
web's hull.

Also measured flat this pass, each against the full TU: an embedded assignment
in the condition, an `s32` carrier with a cast at the call, a block-scoped local
per arm, a separate tested copy, an inverted test with an empty then-arm, a
byte-offset cursor advanced in place, and the loop's two increments swapped.
Regressions: hoisting the load ahead of the dispatch (49), a `void **slot`
cursor (23), an index-only rewrite (11). **Six L97 region placements are flat**,
including a region around each arm body, around the call alone, and in the loop
tail; a region wrapping the whole dispatch regresses to 27. So the region lever
does not reach this residual.

Decision variable, sharpened again: what makes a phase-one web's `v0` cost
finite in a procedure that already contains calls. The one contrast measured
here is a diagnostic where the loaded value came from a plain loop-invariant
field read; that web priced **every** caller-saved colour at 0.0 and took `v0`
by lowest-colour tie-break, with `a0` absent from its cost list. Find the
property that separates those two cost lists and this symbol is either closed or
proved unreachable.
<!-- plateau-handoff:func_80006448:end -->
