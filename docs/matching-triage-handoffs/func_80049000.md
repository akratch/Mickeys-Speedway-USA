<!-- plateau-handoff:func_80049000:start -->
### `func_80049000` plateau handoff

- source: `src/main/fx.c`
- score: 5 differing words
- frame: 0x30
- relocations: 2
- first mismatch: +0x108
- summary: addressing all twelve vertex stores from the post-increment cursor took 69 -> 5. Residual is one pool colour: the truncated height carrier is v1 in the target, a0 here.

Reopen audit (2026-09-08), assignment base
`aaae09a635367cd68f10a1f6150a04d1ca69e055`:

- Configured full-TU baseline reproduced before source edits with
  `tools/wb_compare.sh --summary-json func_80049000`: 596 bytes, 149 words,
  zero size delta, 80 identical words, 69 raw and relocation-masked differences,
  first mismatch `+0x0`. Candidate frame is 0x30 versus target 0x38; both
  reserve eight bytes for saved registers. Both call relocations agree in
  relative offset, type and identity (`Arctanf` and `wakeUpdate`).
- Workbench `diagnose` on `build_non_matching/src/main/fx.c.o` reports
  `structure-mismatch`, mechanism `mixed(constant:5, structural:30)`, and
  routes to `constant-audit`. The requested guide audits the earliest
  immediate; here the first mismatch is frame size. Diagnosis proposes
  `stack-home`: recover the declaration/home responsible for the missing
  eight-byte frame quantum. This is a heuristic lever, not proof of an
  original declaration or authorization to repeat exhausted source shaping.
- PROVENANCE: Jet Force Gemini public decomp, `src/fx.c` at
  `efd5abb1c79636e297b831f7c2d5bf47eac39c0c`, still leaves the corresponding
  `wakeUpdateRipple` routine as `GLOBAL_ASM`. Compared with the previously
  pinned `c82affffe8f11cb5b440cfa918f4582ad8573279`, its placeholder is
  unchanged. The new `src/fx.h` supplies no ripple declarations or types;
  the added scratch reference concerns `setupClearScreen`. No donor body
  was available or adopted. Mickey remains authoritative.
- Stopping evidence: zero material source attempts. The sole authorized
  new-donor mechanism supplies no new target implementation or relevant
  source context, satisfying the assignment's early-exhaustion stop.
  This is not a five-attempt stall. Prior mechanisms remain closed.
- Next concrete action: obtain authenticated ripple local declarations or
  a published matching donor body that supports the stack-home lever, then
  obtain a fresh assignment and re-derive the configured comparison.
- The candidate body and its `NON_MATCHING` guard remain unchanged.
  State is compiled nonexact; no linked-C proof or new matching credit is
  claimed. Baseline source/object, summary and relocation receipt are
  retained under ignored `build/m16-fx2-baseline/`; donor source, diagnosis,
  guide and gate logs remain under ignored `build/wb/`.
- Validation commands: `wb_compare.sh --summary-json`, workbench `diagnose`
  and `guide constant-audit`, object symbol/relocation inspection, pinned
  donor `git show`/`git diff`, `tools/finalize_plateau.py`, `gmake cleanroom`,
  `gmake check-docs` and `git diff --check`. No matching-status change was
  made and no full-ROM verification is claimed for this report-only update.
  An inherited summary-punctuation mismatch for `func_8000FAE0` blocked
  `check-docs`; separate commit `cac55578` synchronizes only that punctuation.

<!-- plateau-handoff:func_80049000:end -->
