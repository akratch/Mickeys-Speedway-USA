<!-- plateau-handoff:func_80012574:start -->
### `func_80012574` plateau handoff

- source: `src/main/track.c`
- score: 13 differing words
- frame: 0x40
- relocations: 1
- first mismatch: +0x0
- summary: Globalcolor maps projection web 25 to f18 and square web 40 to f14; target swaps them at frame 0x48, while five natural forms and 119 flag cells stay flat.

The configured candidate and target are both 228 bytes and 57 instructions.
Forty-four words are exact. The only relocation is an `R_MIPS_26` call at
`+0xA4`; its count, type, offset, and resident `sqrtf` identity are exact.
There is no target padding between this function and `func_80012658`.

A fidelity-gated IDO 5.3 globalcolor capture identifies the first mechanism.
The projection is allocator web 25 with save value 1.333333 and colors to
`f18`; its square is web 40 with save value 1.5 and colors to `f14`. The target
uses those two registers in the opposite roles and has frame `0x48`. Integer
allocation, opcode schedule, temporary FP lane, instruction count, and
relocation surface are otherwise unchanged.

The configured flag won a 119-cell lattice; no alternate cell matched.
Declaration order, repeating the projection square in the comparison,
compound square assignment, source-order reversal, and direct repeated
negated-projection outputs all returned the same configured object basin.
The public JFG context retains its analogous routine as assembly, and donor
lookup failed closed because no reference corpus was configured. Do not rerun
generic flags or these five source forms. Reopen only with new evidence that
naturally reverses these two web priorities while also accounting for the
eight-byte frame difference.

`tools/finalize_plateau.py` was attempted but refused because its definition
pattern mistakes an earlier multiline call to this symbol inside another
NON_MATCHING guard for a second definition. This shard and the keyed source
record use the finalizer's schema and are validated by the handoff gates.
<!-- plateau-handoff:func_80012574:end -->
