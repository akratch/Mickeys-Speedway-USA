<!-- plateau-handoff:func_80034E54:start -->
### `func_80034E54` plateau handoff

- source: `src/main/textures_354C8.c`
- score: 461 differing target-offset words
- frame: 0x80 (target 0xB0)
- relocations: 43
- first mismatch: +0x0
- summary: Complete JFG-guided semantic C emits 459 versus 467 instructions; the retained tree improves the placeholder by five target-offset words, but it lacks the target's s1 carrier and 0x30 non-save frame bytes.

Fresh configured evidence on base `bb8858e1012c08f3dbbf507da6c2dfe29413662f`:

- the unique resident owner is `0x80034E54..0x800355A0` / ROM
  `0x35A54..0x361A0`, exactly `0x74C` bytes and 467 instructions with no
  target padding or inbound labels. No resident caller or export supplies a
  stronger identity, so the address name remains the canonical fallback;
- the old guarded placeholder emitted 21 instructions, carried no
  relocations, and differed at 466 target-offset words. The retained guarded
  body reconstructs frame wrapping, draw-mode selection, fractional opacity,
  primitive/environment color commands, render-state caching, geometry-mode
  changes, command-table copies, the CPU texture helper call, interpolated
  current/next texture lists, the ordinary display-list path, and color
  restoration. The assembly fallback remains canonical;
- Jet Force Gemini's permitted public
  `asm/nonmatchings/textures/sprDPset.s` supplied the broad control-flow
  shape. Its 458-instruction function has the same five-argument ABI and
  `0xB0` frame/prologue class, but it has no source body to transplant;
  Mickey's own fields, globals, constants, assembly, and ROM bytes determine
  this candidate;
- configured IDO `-O2 -mips2 -32` emits 459 instructions (`0x72C` bytes) and
  a natural `0x80` frame. It differs at 461 target-offset words from `+0x0`,
  with 411 opcode mismatches and shift-tolerant normalized distance 597. The
  target saves `s0` and `s1`; the candidate saves only `s0`, accounting for
  four of the 48 missing frame bytes, while its non-save area is 44 bytes
  smaller;
- both objects carry exactly 43 relocations with identical type and symbol
  populations: 42 `HI16`/`LO16` global-address records and one `R_MIPS_26`
  call to `func_8004ADE8`. Their offsets and instruction schedule differ, so
  the relocation surface is not exact and supplies no linked-match evidence;
- ten bounded source forms covered raw m2c output, a clean typed rewrite,
  direct and aliased sprite/texture carriers, in-place draw flags, register
  declarations, assignment timing, render-state locals, and authentic GBI
  macros. In-place flags plus the sprite/texture aliases produced a transient
  461-instruction artifact with 453 positional differences; its source was
  superseded during the final macro test and is recorded as historical only,
  not claimed as the reproducible handoff score. The GBI-macro form regressed
  to 475 instructions and a `0xC0` frame;
- all 119 supported flag identities are nonexact. MIPS-III alone happens to
  reproduce the target extent, but the canonical translation unit and target
  are MIPS-II, so that result is diagnostic rather than a viable flag change.

Preserve the fallback. Reopen only with new source-authentic declaration or
lifetime evidence that naturally keeps the sprite in `s1`, the active texture
in `s0`, accounts for the target's extra 44 non-save frame bytes, and delays
the `D_8007BA80` base load to the target schedule. Do not repeat the 119-way
flag lattice, m2c rewrite, GBI-macro conversion, or the ten bounded source
forms above.
<!-- plateau-handoff:func_80034E54:end -->
