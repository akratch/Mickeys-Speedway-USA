<!-- plateau-handoff:func_80002134:start -->
### `func_80002134` plateau handoff

- source: `src/main/audiomgr.c`
- score: 9 differing words
- frame: -0x18
- relocations: 5
- first mismatch: +0x20
- summary: JFG donor rederivation leaves a structure mismatch with exact frame and instruction count; next lever is the TU declaration that makes IDO reload D_80078EFC

#### 2026-09-10, lane `astra-small`: address precoloring, no gain

The configured full-TU baseline and final `tools/score_symbol.py` measurement
both give 84 bytes, zero size delta, nine raw and nine relocation-masked
differing words, and first mismatch `+0x18`. This corrects the older `+0x20`
header. The frame is 24 bytes. The assembled target has seven relocations;
the candidate has five, with three matching positional type/identity records.
Zero relocation-artifact words does not mean the relocation surfaces match.
The original guarded body is retained; no matching bytes are claimed.

A direct IDO compile of the full TU reproduces the configured function bytes
and relocation records exactly. An isolated diagnostic with the same signature
and globals reproduces them too, so the residual is not dependent on another
function in this TU. The existing JFG provenance remains unchanged. The fresh
near-match oracle invocation refused because the symbol table lacks the size
entry that `skeleton_scan.py similar` requires; no new donor was adopted.

The untouched `cc -S` listing already materializes and retains the EFC address
through both accesses. Thus the extra shared address carrier precedes as1's
scheduling. The partial stock `-Wo,-zdbug:2` record for this exact function
reports two integer entries already colored and an empty ordinary color
candidate set. This narrows the decision to address precoloring, rather than a
tie in the ordinary coloring worklist. **Trace limit:** both the full-TU and
isolated debug runs abort in the wrapper's `wrapper_ecvt` assertion and produce
no usable traced object. The partial record is supporting evidence only;
there is no claimed instrumented-output fidelity receipt or forced closure.

Eight checkpointed experiment batches covered 169 source configurations and
three flag diagnostics:

- Region boundaries around both stores, the final store, and at the inner join
  are byte-identical to baseline. An explicit read carrier gives 20 words and
  16 differences.
- `-Wo,-nogb` and `-g3` are byte-identical. `-O1` gives 22 words and 15
  differences. None of these diagnostic flags was adopted.
- Ten length/state-carrier combinations reach only the original nine-word
  residual, an exact-sized eleven-word residual, or the 20-word regression.
- Switch and goto-join forms are byte-identical; a single-iteration break form
  grows to 23 words and 17 differences.
- Eight diagnostic address expressions use an index proved zero by the existing
  length guard. They retain all volatile access counts and order, but grow to
  22 or 23 words with 18 or 19 differences. They remain ignored diagnostics.
- The last three batches each cross all six declaration orders with eight
  direct/local carrier choices, under no region marker, an inner-store region,
  or an empty join marker. Each 48-case batch has the same distribution:
  12 cases at 21 words/nine differences, 24 at 21/eleven, and 12 at 20/sixteen.
  These reproduce the already-known outcomes without changing the address
  decision. They are the three consecutive stalled batches under ADR 0018.

No residual improved in this pass. Attempt 5 last eliminated a new concrete
hypothesis: the existing zero guard does not remove the diagnostic address
arithmetic at zero cost. Attempts 6--8 supplied no new mechanism or identity.
This is a bounded source plateau, **not** a proof that every source form is
unreachable. The next useful experiment must selectively prevent precoloring
of EFC while retaining the DF0 address carrier, or provide a fidelity-proved
trace of that address eligibility decision. Repeating declaration, carrier,
region, or flag forms above does not supply that evidence.

Sources, compiler outputs, the assembled target, scores, and partial traces
remain in ignored `build/astra-small/`; no instruction listings are tracked.

<!-- plateau-handoff:func_80002134:end -->
