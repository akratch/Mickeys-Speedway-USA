<!-- plateau-handoff:func_80007E40:start -->
### `func_80007E40` plateau handoff

- source: `src/main/objects.c`
- score: 92 differing words
- frame: 0x18
- relocations: 0
- first mismatch: 0x18
- summary: Generated outer-model index controls leave 39 draws and the model-load line unchanged; both regress to 95 masked; retained 92.


#### Ownership and measured result

Assignment gate: `base-only` at `6955143930a57e346178b5c245e1d49ffc43c11d`.
Resident text ownership is VRAM `0x80007E40..0x80008028`, ROM
`0x8A40..0x8C28`: 488 executable bytes with no padding or relocations.
The configured full-TU baseline had 131 candidate words against 122 target
words, 131 raw/masked differing words, first mismatch `+0x0`, and a 16-byte
frame against the target's 24. The retained candidate has 122 words,
92 raw/masked differences, first mismatch `+0x18`, and a 24-byte frame.
All zero relocation identities are accounted for. It remains guarded C;
no matching bytes are claimed.

Workbench verdict is `structure-mismatch`, with only two positional opcode
mismatches. Its mixed residual routes through `constant-audit`: audit constants
first, structure next, register classes last. The constants were confirmed;
the remaining opcode positions exchange dimension loads and the batch-offset
initialization. Register differences are widespread and are not proved to be
one allocator bijection. The `forced-color-oracle` guide was read; no forced
compiler result or allocation-exhaustion claim is being made.

#### Preserved progress and stopping evidence

Attempts 30 through 38 reconstructed full-width loop counters, captured
texture dimensions before traversal, captured the two full-width edge
differences before each first-corner store, reused the consumed corner local,
removed a redundant byte-offset/index conversion, restored the explicit
range-end if/else, used the proved triangle-array element type, and named the
loaded model wrapper. Typed triangle indexing removed an extra strength-reduced
induction carrier; the model-wrapper local restored the object-copy instruction.
The width/height narrowing, per-axis guards, first-corner reload, and signed
record-field semantics remain those of Mickey's target.

Attempts 39, 40, and 41 were consecutive flat results: signed-halfword selector
locals, moving the batch-index initialization before the dimension captures,
and reversing the independent edge-capture order all retained the same
92-word residual, exact extent/frame, two opcode mismatches, and zero
relocations. They provided no better residual or new mechanism evidence.
This is the ADR 0018 stall, not an attempt-count ceiling. The retained source
is attempt 38; the flat forms were reverted while their artifacts were kept.

The near-match oracle ranked JFG's assembly-only `objScrollTexture` first
(0.314 Jaccard) and DKR's `func_80014090` second (0.1804). The DKR
`src/objects.c` body was consulted for attempt 39's disclosed selector-type
probe. That probe and its source disclosure were removed when restoring the
Mickey-only attempt 38. DKR's absent per-axis guards and narrowed edge/mask
arithmetic were never adopted. All 53 usable objects from the previously run
content-addressed TU flag lattice were rescored; none improved this function's
original baseline. No TU flags changed.

#### Reproduction and next action

Evidence is retained under ignored `build/m6-objects/func_80007E40/`, including
full source and configured object for each attempt, summaries, first mismatches,
preflight boundaries, oracle results, and cached-flag scores. The baseline
prepared full-TU compiler input passed workbench fidelity and self-context
validation. The restored best was recompiled with `tools/wb_compare.sh
--summary-json func_80007E40`; `tools/finalize_plateau.py` recorded this handoff.
Validation includes `gmake verify`, `gmake check-nonmatching-builds`,
`gmake check-docs`, and `gmake cleanroom`.

Next concrete lever: capture a natural, fidelity-proved allocator trace of
this exact best full TU and map both p1/p2 procedure and web identities before
changing declaration/carrier lifetimes. Separately identify the dimension-load
and offset-zero scheduling dependency. Do not repeat the three flat source
forms without changed input evidence, or call the current mixed residual a
proved register-only wall. Any reopening still requires the assignment gate.

#### 2026-09-13, lane h1: dimension and offset source-line differential

The configured baseline reproduces 122 words, 488 bytes, frame 0x18,
92 raw/masked differences and first +0x18. All five stack slots and traffic
agree; there are no relocations. Paired aligned buckets are 30 exact,
87 naming, zero immediate and four structural rows, plus one candidate-only
and one target-only word. Named Ucode maps procedure 37 of 65. The census
records 39 draws and 223 emission records. Stock/traced full-TU section,
symbol and relocation fidelity passes.

After reading the previous dimension-order and carrier receipts, one source
probe folds the width, height and two offset initializations onto one physical
line, preserving statement order and all subsequent physical line numbers.
It targets the source-line tie rather than repeating a statement permutation.
The census moves three draws and 12 emission records onto the first line,
while total counts and the draw sequence remain fixed. Two owned object words
change at +0xDC and +0xE4, but the raw/masked score, aligned bucket totals and
per-window counts stay unchanged. Attribution movement is not a draw-order
change, and the changed schedule still supplies no residual gain.

Restore the guarded baseline. ADR 0018 early stop combines this source-line
negative with the existing initialization-order and carrier evidence; the
remaining pool allocation and dimension schedule are not generally disproved.
No colour sweep was repeated and no matching bytes are claimed. Source/object
pairs, census/fidelity receipts and aligned deltas remain ignored under
build/h1/func_80007E40. Commands: configured compilation, allocator_trace_receipt.py
--map-only, draw_census.py --save/--compare, residual_map.py --object/--against,
workbench fidelity, finalize_plateau.py and tools/gates.sh.

#### 2026-09-13, lane k1: authenticated draw-census follow-up

Fresh configured stock compilation reproduces 488 target bytes,
size delta 0, 92 raw and
92 masked differences, first +0x18.
Candidate frame is 0x18; the procedure-37 census records
39 draws and 223 emission records. Stock and traced full-TU text
compare identically. Static relocation counts are 0 candidate and
0 extracted target, with 0 identical offset/type/symbol tuples.
These are static measurements; overlay runtime identity proof remains separate.

The baseline has 30 exact, 87 naming, zero immediate and four structural
paired rows, with one gap pair. Two new source controls isolate the OUTER
model-address induction; the earlier typed-triangle and dimension-line
experiments were read and were not repeated.

First, replace the explicitly scaled byte offset with the existing model
counter minus one, because that counter advances before the model load.
Remove only the now-redundant offset initialization and increment, preserving
all memory accesses, bounds and physical line count. The model-load line's
draw and emission counts do not change. Loop attribution moves, total draws
remain 39 in the same order, and emission records fall from 223 to 221.
The result regresses to 95 masked words, with two fewer naming rows but five
more paired structural rows at unchanged frame and extent. The original gap
pair remains.

Second, move the counter increment to the loop tail and index by its current
value. The counter has no intervening use. All 39 draws and their sequence
remain fixed. Three lines change emission attribution, but the result still
has 95 masked differences and adds another candidate/target gap pair. The
one-row improvement in the paired-only delta therefore is not a geometry
improvement. Both probes pass fresh named-Ucode mapping and full-TU
stock/traced fidelity, with all source/object and aligned/draw deltas saved.

Restore the guarded 92-word baseline. ADR 0018 early stop applies to this
model-induction route: neither counter form moves the intended model-load
line, and the existing dimension-order, carrier and physical-line controls
cover the other identified scheduling inputs. No colour sweep was repeated.
This is a measured negative for these source forms, not a general closure
of pool allocation or the dimension schedule.

The retained C remains guarded by its original assembly fallback. Sources, stock
and traced objects, frame and scalar measurements, draw profiles and aligned
maps remain ignored under build/k1/func_80007E40.
Commands: lane_status.py, configured stock compilation, draw_census.py,
residual_map.py --object/--against where compared, finalize_plateau.py and
tools/gates.sh. No executable bytes are newly credited.

<!-- plateau-handoff:func_80007E40:end -->
