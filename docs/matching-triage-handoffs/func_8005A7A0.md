<!-- plateau-handoff:func_8005A7A0:start -->
### `func_8005A7A0` plateau handoff

- source: `src/main/models_5B300.c`
- score: 96/106 words
- frame: 0x50
- relocations: 10
- first mismatch: +0x0
- summary: structure-mismatch; bounds de-declaration fixes sp+0x34, reduced-local carriers stall; next capture target-correlated CFE temp birth-site evidence
#### Owned pass, 2026-09-10 (lane/nm-resident2)

Still 10. What this pass adds is a decomposition of the residual and a
calibration of the frame, not a match.

The ten words are three independent facts:

- **Two words are the frame**, 0x38 against 0x50, at the prologue and the
  epilogue. Both sides address *identical* stack slots -- the same six
  saved-register homes and the same single spill home. The 24-byte difference
  is unaddressed space above them: the target reserves two carrier words there
  and the candidate reserves eight.
- **Four words are one colour.** The `firstAnimation & 3` value lives in a
  callee-saved register in the target and in a caller-saved one here, which
  changes the register at its definition, at its test, at its spill and at the
  reload's consumer.
- **Four words are one schedule**, the argument load and the spill exchanging
  places across the second call, which follows from the colour above: a
  callee-saved carrier does not need to be spilled before the first call.

**Frame calibration (new).** Each additional *live scalar carrier* costs four
bytes, rounded up to eight: adding two used locals moves the frame 0x50 ->
0x58 with the score unchanged at 10. An *unused* declared local is frame-inert
-- it never becomes a carrier. And removing a used local does not necessarily
remove a carrier: dropping `lastAnimation` into its single expression left the
frame at 0x50 and regressed the score to 27, because the expression still
needs the same home.

So the routed lever `drop-a-declared-local` is not sufficient as stated. The
requirement is **six fewer live scalar carriers at the same 106 words**, which
declaration count only sometimes controls; the earlier pass's 0x48 and 0x30
frames are the two- and eight-carrier points either side of the target's two.

#### 2026-09-10, lane `o7-mid`: reproduced, and the routed lever gets its mechanism

Reproduced at **10 relocation-masked words, size delta 0**, at the ten recorded
offsets (`+0x000`, `+0x08C`, `+0x0A0`, `+0x0BC`, `+0x0C0`, `+0x0C4`, `+0x0C8`,
`+0x0DC`, `+0x0F4`, `+0x1A4`). The three-fact decomposition in the pass above
stands unchanged. No edit was made.

**What this pass adds** is the mechanism behind the routed lever
`drop-a-declared-local`, measured in a controlled sweep on `func_8004BA8C` in
`src/main/font.c` (see that shard):

> IDO gives a four-byte frame home only to a local it leaves **memory-class**,
> and assigns those homes **descending from the top of the local block, in
> declaration order**. A local uopt colours owns no slot at all.

That makes the lever precise, and explains the earlier misses. The frame gap
here is `0x50` against `0x38` — six words — and the `-Wo,-zdbug:2` listing
already quoted in the source says all six declared scalars are memory-class in
this candidate. So the requirement is **not** "declare fewer locals" but
"leave six fewer locals memory-class at the same 106 words", which is a
*colouring* outcome and only indirectly a declaration-count one. The earlier
reduced-local carrier maps reached frames `0x48` and `0x30` without passing
through `0x38` because they were moving the count, not the class.

**Next lever.** Read which of the six locals uopt strikes at [L55]'s
`save <= 0` eligibility gate, and attack the save of those, rather than
permuting carriers again. The reopen condition recorded above — target-correlated
CFE temp birth-site evidence — is unchanged and is what a CDX capture supplies.
<!-- plateau-handoff:func_8005A7A0:end -->
