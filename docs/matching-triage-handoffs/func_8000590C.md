<!-- plateau-handoff:func_8000590C:start -->
### `func_8000590C` plateau handoff

- source: `src/main/objects.c`
- score: 538 differing words
- frame: 0x90
- relocations: 99
- first mismatch: +0x1B4
- summary: Workbench structure-mismatch, lever none-known. Pinned JFG donor supplies no C body; next: a source-line/UGEN trace for the header hunk.

2026-09-09 pinned-donor reopening audit:

The fresh assignment gate returned base-only for the explicitly authorized
JFG donor mechanism. A configured full-TU baseline independently reproduced
719/719 words, 538 raw/masked differences, first +0x1B4, frame 0x90,
and 99 relocations per side with 47 authenticated identities. Baseline 240
and documentation-only check 241 are lane checkpoint numbers, not new
matching attempts. The candidate body is unchanged.

PROVENANCE: the public Jet Force Gemini src/objects.c at revision
efd5abb1c79636e297b831f7c2d5bf47eac39c0c was inspected through its pinned Git
object. objSetupObject remains GLOBAL_ASM-backed, and that source file is
identical to its parent revision. No donor C was adopted. The pinned
StaticInstanceSpawn declaration uses a signed halfword ID and signed
halfword coordinates already represented by Mickey's retained packet view;
it does not provide a header-scheduling trace. This audit relies on the
pinned files directly, rather than carrying forward the earlier donor-screen
wording about another entry declaration.

The committed donor-screen handoff at 77d4281e already records the missing
counterpart body and lack of the required scheduling lever. The fresh
workbench diagnosis still reports structure-mismatch and lever none-known:
equal frames alone provide no home, temporary-ring or source-line evidence.
Its structure-buckets route therefore does not authorize another blind
allocation search. The cached 53 usable flag objects were rescored without
new compilation; configured flags remain best and nonexact. The fresh
oracle again supplies only weak similarities. The already exhausted plain
copy-loop and flag hypotheses were not repeated.

This closes the one authorized donor-evidence mechanism with zero new C
attempts. It does not claim that all possible reconstruction mechanisms are
exhausted. The next concrete lever remains a source-line/UGEN scheduling
trace for the first header hunk, followed by normal exact byte, relocation,
linked-range and ROM proof. A new packet must pass the assignment gate.

The existing candidate remains guarded and was refreshed with
finalize_plateau.py. Source provenance and handoff annotations are the only
changes for this audit. Validation includes configured full-TU comparison,
diagnosis and guide, cached flag rescore, donor Git-object inspection,
check-docs, cleanroom, full gmake verify and check-nonmatching-builds.
The original reconstruction evidence is retained below.

The retained C is **NON_MATCHING**, compiled in the configured full translation
unit. Target and candidate each contain 719 words (2,876 bytes), with 538 raw
and relocation-masked positional differences and first mismatch `+0x1B4`.
Both frames are `0x90`; all observed argument/local homes agree. The static
relocation comparison has 99 records on each side, with 47 identities agreeing
at the exact offset and type. These are diagnostic measurements, not a linked
C match. The guarded assembly remains canonical and no bytes are credited.

Boundary and call-graph evidence establishes resident
`0x8000590C..0x80006448`, ROM `0x650C..0x7048`, with no padding. The target
saves four integer saved registers and the return address; outbound stack
arguments reach `+0x2C`. Eleven resident call sites and 23 overlay runtime
call sites corroborate this identity, with no ORT export. Both outbound
runtime overlay calls are authenticated: overlay 19 at its entry and overlay
43 at its entry. The direct callees include the exact object-data sizing and
initialization siblings in this TU.

The target's 43 calls, 85 branch instructions, four backedges and common
return describe four phases: selection/zeroing/header setup; the three-kind
resource loop and its cleanup; optional component construction and allocation;
and copying, pointer fixups, registration and final initialization/rollback.
The baseline already had the same call order and broad control-flow shape.
Its shortfall was **104 bytes, or 26 words**, rather than 104 words or an
absent call subtree. It entered the copy/fixup region two words long and lost
28 words there. The draft cached the staging-base global once; the target
reloads it across fifteen fixups. Restoring those reads and their address
arithmetic accounted for the net shortfall without adding padding or guards.

The baseline was 693 words with 692 differences, a `0xA8` frame, and 99
relocations, of which five identities aligned. The original and post-sibling-
promotion baseline function bytes were identical. Reusing the authenticated TU
flag cache scored 53 rows and retained 66 compiler rejections; the configured
O2/MIPS-II preset remained best. No shared flag was changed. The weak
near-match oracle result supplied no useful exact sibling, and this packet
adopted no external source.

Ten source attempts tested, in order:

1. Repeated staging-base reads in the fixup chain: restored the 719-word extent.
2. Original packet-ID lifetime across calls: retained the original value.
3. Three-way resource switch: restored test and body order.
4. Existing carrier reuse: removed six surplus homes and restored the frame.
5. Declaration ordering: restored the observed flag, selected-ID and failure homes.
6. Separate zeroing/resource counters: restored the early global-address carrier.
7. Correct texture-call ABI and memory accesses: removed an extra argument and
   self-store, and restored the particle-list reload after alignment.
8. Early allocation-result assignment and released resource-index lifetime:
   removed result spills and restored a non-saved copy counter.
9. Explicit header and main-object pointer lifetimes: the retained best,
   719 words, 538 differences, `0x90` frame, 99 relocations and 47 exact identities.
10. Ordinary word-copy loop: did not expand under the configured preset;
    regressed to 697 words, 702 differences and 107 relocations. Restored attempt 9.

The remaining early header hunk begins `+0x1B4`: field-load/store scheduling
and the floating-point latency gap differ. Later residuals include the cursor
and allocation-size saved-register roles, the explicit copy tail, and nested
pointer-fixup carriers. Workbench reports `structure-mismatch`, heuristic owner
`cfe-spelling`, and **`lever: none-known`** because matching frames alone do
not provide home, temporary-ring or line-order evidence. Reopen with a source-
line/UGEN scheduling trace for that first header hunk, audit its load/store
ordering, and stabilize its opcode schedule before further allocator edits.
Do not repeat the exhausted flag lattice or plain-copy-loop hypothesis.

All meaningful source, object, score, first-mismatch and rationale artifacts
are retained locally under ignored `build/objects-big/590c/`, with the ten
attempts separated and the original fallback target preserved. The handoff
was created with `tools/finalize_plateau.py`; final proof and validation receipts
are retained in the same ignored directory.
This unit changes the guarded candidate and its texture-loader declaration in
`src/main/objects.c`, its tier-D boundary line in `symbol_addrs.us.txt`, and
this handoff shard. The preceding exact control-dispatch promotion is committed
as `c6fba089e789b5446724f657724546ee0f1b0282` and remains independently
ROM-proved. No toolchain flag or canonical matching status changes here.

Commands used for the retained result and handoff include:

- `MICKEY_BUILD_JOBS=3 nice -n 15 tools/wb_compare.sh --summary-json func_8000590C`
- `tools/finalize_plateau.py` with the metrics recorded above
- `nice -n 15 gmake -j3 verify`
- `MICKEY_BUILD_JOBS=3 nice -n 15 gmake -j3 check-nonmatching-builds`
- `gmake cleanroom`, `gmake check-docs`, and `gmake check-scoreboard`


<!-- plateau-handoff:func_8000590C:end -->
