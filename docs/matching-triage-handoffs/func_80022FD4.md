<!-- plateau-handoff:func_80022FD4:start -->
### `func_80022FD4` plateau handoff

- source: `src/main/camera.c`
- score: 157/369 words
- frame: 0xB0
- relocations: 55
- first mismatch: +0x9C
- summary: All 9 coloured webs and 61 legal forces are flat; next source question is the L149 folded GP draw.

Swept and flat on this base, all with tools/score_symbol.py's masked number:

- the five statements from `divisor = sprite->divisor` through the volatile
  `angleProduct` store admit 30 orders once `divisor` precedes `frameStep` and
  `quadrant` precedes the `angle &= 0x3FFF`; ten of them tie the incumbent at
  157 and the other twenty are 167. Statement order does not reach this block.
- moving `horizontal = sqrtf(...)` above the quadrant test, above the
  angleProduct store, above the divisor read, above the divisor-ge-2 test,
  above the quadrant computation and above the pitch Arctanf: 265, 272, 267,
  263, 267 and 277, at size deltas +20, +36, +12, +16, +20 and +4. The target
  does schedule that pair of multiplies before the quadrant branch where we
  schedule them after, but every source position that moves them also spills
  localX and localZ.
- head spellings: interleaving each scale with its own subtraction 330 at
  delta -4, compound `*=` 157, reversing the three local subtractions 287 at
  -4, swapping the multiply operands 330 at +8, and deleting the `register`
  spriteEarly carrier 215 at delta 0.

The float side is already one clean fp-ring fact: f8 to f0 to f8, 100%
coherent, one window, two sites.

## 2026-09-12 (lane `p23-bigger`): exhaustive colour landscape is flat

`tools/web_footprint.py --every-colour` covered every coloured web in the
procedure: 61 legal same-kind single forces over 9 coloured webs. All accepted
same-size forces scored exactly the 157-word baseline. Thus the forced-colour
lattice floor is 157 and the sample is 9 of 9 coloured webs; there is no
winners list and no lattice combination to measure.

The aligned split remains byte-exact 219, naming 109, immediate 1 and
structural 35, with five candidate-only and five target-only words. The source
question is consequently the L149 one already identified by the free-list
trace: which semantically defined expression in the transformed-coordinate
block spends the target's extra GP draw before +0x9C and folds away before
emission? No source candidate was adopted.
<!-- plateau-handoff:func_80022FD4:end -->
