<!-- plateau-handoff:func_overlay_050_F0000000_1896970:start -->
### `func_overlay_050_F0000000_1896970` plateau handoff

- source: `src/overlays/o050/overlay50Initialize.c`
- score: 104/185 words
- frame: 0x20
- relocations: 98
- first mismatch: +0xC0
- summary: Fresh proc-0 census: 35 draws/324 emissions. Record-copy schedule and saved-flag traffic remain unresolved; physical-line control is inert.
#### 2026-09-13, lane h1: record-copy physical-line differential

The configured baseline reproduces 185 words, 740 bytes, frame 0x20,
112 raw and 104 masked differences. First raw mismatch is +0x3C; first
masked mismatch is +0xC0. Paired aligned buckets are 101 exact, 23 naming,
six immediate and 48 structural rows, plus seven candidate-only and seven
target-only words. The saved-flag slot is read once by the candidate and
twice by the target. Static relocations are 98 candidate and 58 extracted
target records, with 22 matching tuples; the header now records the measured
candidate count, and no complete runtime relocation identity is claimed.
The census has 35 draws and 324 emission records. Stock/traced full-TU
section, symbol and relocation fidelity passes.

The source and shard's config/order/copy notes were read. One source probe
folds all twelve record-copy assignments onto one physical line, leaving their
order, memory accesses and subsequent line numbers unchanged. It removes the
physical-line distinctions without repeating a transfer-order permutation.
The census reattributes eleven draws and 33 emission records onto the first
line. Total counts and the draw sequence are unchanged; direct owned-range
comparison also proves byte identity, and the aligned map is unchanged.
The line-key route does not change the copy schedule in this source shape.

Restore the guarded baseline and stop this hypothesis under ADR 0018 alongside
the prior config/copy/order negatives. The config-base and copy dependencies
remain unresolved, as does the saved-flag traffic difference. No colour sweep
was repeated and no matching bytes are claimed. Private evidence is under
build/h1/func_overlay_050_F0000000_1896970. Commands: configured compilation,
draw_census.py --save/--compare, residual_map.py --object/--against, workbench
fidelity, direct owned-range comparison, finalize_plateau.py and tools/gates.sh.

<!-- plateau-handoff:func_overlay_050_F0000000_1896970:end -->
