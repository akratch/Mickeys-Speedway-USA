<!-- plateau-handoff:func_overlay_101_F00078F4_18E3114:start -->
### `func_overlay_101_F00078F4_18E3114` plateau handoff

- source: `src/overlays/o101/func_overlay_101_F00078F4_18E3114.c`
- score: 136/525 words
- frame: 0x40
- relocations: 59
- first mismatch: +0xA8
- summary: 136 masked from 145; frame and ladder exact. The pre-call node pointer must be an expression, not a local -- one uopt live range is formed per IR name, and an expression is a name.

Measured 2026-09-11, lane `lane/p5-quad`, on the four-function overlay-101
builder quadruplet. All four carry the same shape over different data, all four
were measured separately, and all four moved 145 to 136 with identical buckets.

Before: 145 masked, aligned 410 byte-exact, 29 register naming, 1 immediate
only, 102 really different, size delta 0, frame 0x40 exact, displacement tax 13.
After: 136 masked, aligned 422 byte-exact, 23 register naming, 1 immediate
only, 95 really different, size delta 0, frame 0x40 with the nine-slot ladder
exact, displacement tax 17. Register mapping 100% coherent, no closed cycle.

**The blocker the previous lane named is answered, and its closure is refuted.**
That lane asked why a range whose only uses are the two pre-call stores
interferes with thirteen webs, and concluded from `available0=0x00020000` that
"every caller-saved colour is forbidden to it" and therefore "the lever is that
interference set, not the save ratio". Both halves are wrong, and the record
that breaks them is the web's own cost list under `CDX_DETAIL_WEB`.

- The range's uses are not two. It is the pre-call pointer of **all three rows**,
  because every occurrence of one address expression joins one uopt live range.
  Its lineage members sit in three different blocks, so its hull covers the two
  calls between them.
- Caller-saved colours are **offered** to it, at 4.0 each, against 0.0 for
  reusing the already-saved s0. They were outbid, not forbidden. `available0` is
  the post-decision consistency mask, not the offer set. Only v0, v1, a0 and s1
  are genuinely forbidden, by the four webs holding them that overlap the hull.
- So the save ratio is exactly the lever. The pointer range's save of 4.5 beats
  each index local's 3.0, it is coloured first, and it takes the s0 that the ROM
  gives the index.

**The law this generalises.** L115 says a live range is formed per symbol. The
stronger statement, read off `lineage_range`/`lineage_member`, is that the same
holds for an expression: one IR name, one range, however many blocks and however
many distinct values its occurrences carry. A consequence that cost this lane a
sweep: deleting the pointer local does **not** split the range, because the
expression is still one name.

**What moved, separately measured.**

- Pre-call stores spelled through the array rather than a pointer local
  (`D_540[D_1D0].x = 0x80`), 8 words. A pointer local is a symbol and a symbol is
  never a ring temporary; the ROM's pre-call base is one. The post-call pointer
  is inert either way, 136 both ways, because uopt forward-substitutes it.
- Which declared local each row reuses for its post-call index, 4 words, pure
  L115. Three fresh locals read 145; `ownerIndex` in row 1 and `nodeIndex` in
  row 2 read 141. Exhaustive over 343 assignments from a seven-local pool,
  re-run after the tails were re-climbed; five tie at the optimum and all five
  put ownerIndex in row 1.
- The three rows' tail orders re-climbed, 5 words. The incumbent orders were an
  optimum of the 149 shape. Rows 2 and 3 want plain ascending field order; row 1
  wants color2 above mode.

**Closed here, so nobody re-runs it.**

- The image macro's post-call statement order: all 630 orders of its seven
  statements that respect the three read-before-write dependences, measured on
  the 136 shape. The incumbent is the unique optimum, second best +8. That is
  the second exhaustion of this lattice on a second shape.
- L59 inside a macro. Folding the image macro's post-call stores onto one
  continuation line, folding its pre-call stores, and folding both, each leave
  the object byte-identical. A multi-line macro expansion already carries the
  invocation's line for every statement, so there is no `lineno` key to retire.
- Carriers in the image groups stay refuted, but the previous note's reason no
  longer describes the residual. On the 136 shape the groups' link loads already
  land in the ROM's own temporaries; what is left there is ordering.

**Next, two named questions.**

(a) The 23 naming rows are one colour rotation: ours takes s0 where the ROM has
v1 seven times, a1 where it has s0 seven, a3 where it has a2 five, a2 where it
has s0 three. The ROM's layout is the index in s0 and the pointer in v1; ours is
the inverse. A source shape reaching the ROM's layout exactly is already known:
read the counter into one shared index local before the call as well as after
it, the same local in all three rows, every store through that local's
subscript. It reads 183 and carries the ROM's colouring **and** the ROM's
schedule -- the whole 20-word schedule residual in the text rows disappears --
one temp-ring position behind, because the pre-call counter read then lands in a
coloured symbol and draws no ring position (L129). Recovering one ring draw at
the head of the text region without re-splitting those ranges is the entire
remaining question, worth about 37 words. Nearest measured point: that shape with
row 1 alone reverted to the direct read reads 160 and is ROM-exact through row
1's first store group, except that row 1's pre-call pointer then becomes its own
range and takes v0 rather than v1 -- L101 withholds v0 only from a range that
reaches a call result.

(b) The eight image node groups, 56 words, are a schedule residual with every
register already agreeing. The ROM issues both old-link loads back to back
before computing the node address; ours puts the first link store between them.
Statement order cannot reach it and L59 cannot reach it. Untried: writing the
eight groups out longhand instead of through a macro, the only way to give their
statements distinct physical line numbers and put L59's key back in play.

<!-- plateau-handoff:func_overlay_101_F00078F4_18E3114:end -->
