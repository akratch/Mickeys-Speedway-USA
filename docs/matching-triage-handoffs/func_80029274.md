<!-- plateau-handoff:func_80029274:start -->
### `func_80029274` plateau handoff

- source: `src/main/main.c`
- score: 39/87 words
- frame: 0x10
- relocations: 0
- first mismatch: +0x8
- summary: Every differing word is a float register: one coherent f12/f14 cycle at 93% over 24 sites, so the axis is the float pool's c26/c27, not structure

PROVENANCE audit (2026-09-08): Jet Force Gemini's public decomp at
`efd5abb1c79636e297b831f7c2d5bf47eac39c0c` still represents
`GetSmoothAcceleration` with `GLOBAL_ASM` in both
`src/overlays/o3/overlay_3.c` and its kiosk counterpart. Git attribution pins
both directives to `e5276f760d0dde062e1c054080bdd0ba84d0656e`, and the older
`c82affffe8f11cb5b440cfa918f4582ad8573279` reference also has no C body.
The upstream advance therefore provides no new declaration, lifetime, or
control-flow spelling to test against Mickey; Mickey's ROM remains decisive.

The configured full-TU baseline remains 87 words with 39 raw and
relocation-masked differences, an exact `0x10` frame, zero relocations, and
first mismatch `+0x8`. Workbench verdict: `structure-mismatch`, with the
dominant ownership routed to C/front-end spelling. The prior 119 flag rows and
17 source forms remain exhausted. No new source attempt was made because the
only authorized mechanism supplied no candidate; this is the ADR 0018 early
stop evidence. Resume with a matched permitted donor C body or authenticated
original source-lifetime evidence for the delayed argument-to-FP move.
#### 2026-09-12 (lane `lane/p7-res2`): the residual is 100% float pool, one coherent f12/f14 cycle, and 126 forms do not move it

Still 39, and the verdict in the 2026-09-08 shard -- `structure-mismatch`, with
"the dominant ownership routed to C/front-end spelling" -- is refuted by
measurement. The aligner splits the residual 54 byte-exact / 24 register-naming
/ 2 immediate-only / 9 really-different, and `register_census` reports **no
integer-register differences at all**: every one of the 24 substitutions is a
float register, 93% of them follow their source's dominant target over five
source registers, and the dominant mapping is a single closed f12/f14 cycle
(f12 to f14 twelve times, f14 to f12 eleven). That is one fact about the float
pool, not a structure question, and it is why no spelling lattice has ever
moved it.

**The decision variable, named.** L133 decodes the float colour table as c26
`f12`, c27 `f14`. The candidate gives c26 to the carrier that holds the
negatable first float argument and c27 to the integer-to-float conversion of the
counter; the target gives them the other way round. The nine really-different
words are the visible consequence: the candidate pays a copy after moving the
argument out of its integer arrival register (its carrier is not the register
the move lands in) and a second copy after the conversion, and the target pays
neither because in each case the destination is already the carrier's colour.

**This function is a leaf**, so by the brief's call test it emits p2 records
only and statement position is a lever (L106). It was swept and it is not this
one:

- nine declaration positions for the float carrier, crossed with `register`
  present and absent and five orderings of the four initialising statements
  (90 cells): all 39, byte-flat.
- fifteen orderings of the seven head statements, crossed with `register`
  (30 cells): 39 where the conversion stays below the early-return block, 42
  for one ordering, and 82 with three instructions lost wherever the conversion
  is hoisted above the early return. So the conversion's *position* is fixed by
  the early return, and with it the order in which the two webs are defined.
- five L97 regions -- `if (1)` around the loop, the early-return block and the
  post-loop chain, and a `do { } while (0)` around the negation -- all 39,
  byte-flat. L136's lever does not reach a float-pool colour, which is
  consistent with its own scope note.
- six carrier-identity forms reusing the incoming argument itself in place of
  the declared carrier: 88 with four instructions added, because the argument
  arrives in an integer register and reusing it forces a reload. Inlining the
  conversion instead of naming it is 82 and three instructions short, so the
  target does have both locals.

Resume on the save ratio (L100) between exactly those two float webs, not on
statement order: the conversion is referenced once inside the do-while, which
should weigh ten, and the candidate still gives the other web the lower colour.
Either the loop reference is not being counted at the depth L100 predicts, or
the tie is being taken on ascending web number and the web order is fixed by the
definition order the early return pins. An instrumented p2 capture on this
procedure would separate those two in one build; nothing in the source lattice
can.
<!-- plateau-handoff:func_80029274:end -->
