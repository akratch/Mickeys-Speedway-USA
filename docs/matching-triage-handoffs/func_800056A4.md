<!-- plateau-handoff:func_800056A4:start -->
### `func_800056A4` plateau handoff

- source: `src/main/objects.c`
- score: 17/19 words
- frame: frameless
- relocations: 6
- first mismatch: +0x24
- summary: JFG efd5abb leaves objGetTable body/types unchanged; zero new attempts. Next: authenticated UGEN reservation and temp-demand trace.

#### Pinned donor reopening

PROVENANCE (review only): Jet Force Gemini's public `src/objects.c`,
`objGetTable`, at `efd5abb1c79636e297b831f7c2d5bf47eac39c0c` was compared
with the reference revision `c82affffe8f11cb5b440cfa918f4582ad8573279`.
Its function body is unchanged. The relevant declarations remain two `s32 *`
table pointers and one `s32` count; they moved from `include/variables.h` to
`src/objects.h`. Their US symbol addresses are also unchanged. No donor body,
name, or declaration was adopted in this reopening.

The masked four-gram oracle over the retained JFG objects TU ranks
`objGetTable` first at 0.3200 Jaccard. This is semantic donor context, not
byte identity or a new source input. The donor object's symbol-to-next-symbol
slice includes alignment and is not used as Mickey's executable boundary.
The only newly implemented C functions in this donor TU between those revisions
are `objGetObjList` and `GetRomlistInfo`. Their corresponding Mickey routines,
`func_8000572C` and `GetRomlistInfo`, are already unguarded C in the assignment
base. The update supplies no new body for this target.

The assignment gate was `base-only` at
`af22d0084eb630d5392c11e73df66c0176e73c14`. The authorized newer-donor
hypothesis is eliminated without a source attempt. This is the assignment's
explicit zero-attempt stopping case, not a five-attempt stall or a wall-clock
expiry. The earlier direct-pointer/carrier, forced-color, and flat permuter
results remain closed. No flag lattice or source sweep was repeated, and no
compiler flag changed. The committed TU handoffs already record one lattice
with 53 usable rows and 66 preparation failures; those are prior evidence,
not newly executed coverage.

#### Re-derived evidence

The configured full-TU `NON_MATCHING` build emits 76 bytes / 19 words,
with two raw and relocation-masked differences, first at `+0x24`, and no
stack frame. Both objects have six executable relocations, with every
function-relative offset, type, symbol identity, and addend agreeing.
The owned resident range is `0x800056A4..0x800056F0`, ROM
`0x62A4..0x62F0`, with no padding credit. Linking the extracted fallback
at its real address and resolving its two global identities reproduces those
76 ROM bytes. This authenticates the comparison target only; it is not a
linked C match.

The current workbench comparison reports `allocation-mismatch`; diagnosis
refines this to `register-ring-only`, playbook `register-role-audit`,
`owning_pass=unknown`, and `lever: none-known`. Both that guide and the
assignment's `forced-color-oracle` guide were read. The two differences are
one visible table-pointer substitution. A register name alone does not prove
its allocator role. The previous CDX p2-web and forced-split observation is
historical evidence, not a trace re-derived in this run.

Next concrete lever, requiring a new assignment authorization: authenticate
the current full-TU procedure mapping, stock-output fidelity, UOPT reservation
state, and complete UGEN temporary demand/lifetime events for the loaded
table pointer. Establish which pass owns the difference before proposing a
natural C change. An updated matched donor body or relevant declaration change
would also constitute new evidence; unchanged donor source does not.

#### Preservation and validation

Ignored evidence is retained under `build/m13-objects2/func_800056A4/`:
the unchanged full source and configured object, target object and ROM-range
receipt, relocation comparison, workbench summary and diagnosis, donor audit,
and finalization log. Pinned donor source snapshots and the oracle are under
`build/wb/`. The shared checkpoint records zero attempts and the current
17/19-word result with six exact static relocation identities.

Commands include `tools/wb_compare.sh --summary-json func_800056A4`,
`decomp-workbench diagnose`, both field guides, the existing skeleton and
relocation parsers, and `tools/finalize_plateau.py`. The finalizer ran
`gmake check-docs` and `gmake cleanroom`.

`gmake verify` was attempted on the unchanged base and failed while linking
`overlay94UpdateController`: unresolved resident call names and out-of-range
call relocations remain in that unrelated object. Rebuilding the canonical
candidate objects did not resolve it. The failure is preserved in
`build/wb/verify.log` and `build/verify/rom-build.log`; the full preflight also
stops because there is no canonical linked ELF. No unrelated source or build
rule was changed. This handoff preserves guarded C and its fallback, adds zero
matching bytes, and makes no canonical linked or full-ROM success claim.

#### TU follow-on screen

The assignment-base ranking re-derives to 28 queued functions and 22,720
bytes. Running the assignment gate for each yields 13 `base-only` entries
including this anchor, four `already-integrated/exhausted` entries, and 11
`stale-ledger` entries. The closed entries are `func_80004454`,
`func_8000471C`, `func_80007E40`, and `func_8000A39C`. The 11 stale entries
require the gate's `prose-needs-remeasurement` maintenance before assignment.
These are scheduling verdicts at the supplied base, not match proof.

The other 12 eligible entries were measured in ranking order using the same
configured full-TU candidate and freshly assembled fallback targets. Their
raw/masked differing words and target/candidate word counts are:

- `func_8000590C`: 538/538 differences; 719/719 words.
- `func_80009F74`: 149/149 differences; 180/178 words.
- `func_80006534`: 198/198 differences; 205/207 words.
- `func_80004C28`: 230/230 differences; 238/239 words.
- `func_80009AA8`: 268/268 differences; 244/269 words.
- `func_8000784C`: 273/273 differences; 263/277 words.
- `func_800084C4`: 314/314 differences; 343/348 words.
- `func_80004FE0`: 337/337 differences; 346/347 words.
- `func_80008B94`: 397/397 differences; 419/420 words.
- `func_80009414`: 411/411 differences; 421/378 words.
- `func_80007118`: 445/443 differences; 459/362 words.
- `func_8000B3CC`: 489/489 differences; 494/427 words.

This is a read-only baseline and donor screen, with no secondary source
attempts or matching promotions. No newly implemented donor body corresponds
to these entries. JFG's `objSetupObject` is still assembly-backed. Its new
`LevelObjectEntryCommon` declaration supplies an unsigned ID, while Mickey's
retained packet and target load require a signed halfword; its coordinate
layout is already represented in the Mickey candidate. That header does not
supply the recorded header-scheduling lever for `func_8000590C`. The larger
donor header diff is not proof of a usable allocation or scheduling change.

Per-symbol gate receipts are retained under `build/m13-objects2/queue-gates/`;
the source, object, fresh baseline summaries, and heuristic donor rankings are
under `build/m13-objects2/batch-screen/`. Weak oracle similarities do not
establish counterpart identities. Follow-on work needs a target-specific
source or trace hypothesis within its authorization, plus ledger maintenance
where the gate requires it. The independent canonical link failure must be
resolved before any future promotion can pass full-ROM verification.

<!-- plateau-handoff:func_800056A4:end -->
