<!-- plateau-handoff:func_overlay_101_F0008128_18E3948:start -->
### `func_overlay_101_F0008128_18E3948` plateau handoff

- source: `src/overlays/o101/func_overlay_101_F0008128_18E3948.c`
- score: 311/525 words
- frame: 0x40
- relocations: 59
- first mismatch: +0xA8
- summary: 311 masked words from 461; size, frame and instruction multiset all exact. Residual is 215 register naming and 111 schedule order.

Measured 2026-09-11, lane `lane/o12-quad`, on the four-function overlay-101
builder quadruplet. All four carry the same shape over different data, and
every number below is `tools/align_symbol.py`, whose positional figure agrees
with `tools/score_symbol.py` by construction. The four measured separately and
came out identical in every bucket.

Before: 461 masked words at a size delta of -4, aligned 113 byte-exact, 285
register naming, 7 immediate only, 142 really different, 17 insertion and 18
deletion sites, and a frame ladder of eight slots against the target's nine.
After: 311 masked at size delta 0, 221 byte-exact, 215 register naming, 0
immediate only, 111 really different, 12 insertions and 11 deletions, and a
frame ladder identical to the target's at all nine offsets. The candidate's
instruction multiset now equals the target's exactly, opcode for opcode.

Four levers, in the order they were found.

- L59. The three non-macro assignment groups are folded onto ONE physical line
  each. as1 minimises `(start_time, -aftercycles, -latency, addr, lineno,
  ready-list position)`; with a group's stores on separate lines `lineno`
  decides and emits them in source order, while the ROM emits each group
  reversed. Folding retires the key. Worth 461 to 456 here, much less than on
  the F63F8 relative, because this family's gate is elsewhere. The same reading
  fixes the root header: the ROM emits height, width, kind, so the source
  writes kind, width, height, worth a further four words.
- L115 and L101, the missing callee-saved web, which is the gate every earlier
  note on this family named and none reached. The node-32 pointer must be USED
  after the call and before the counter is re-read. Storing `handle`,
  `previousType` and `previous` through the pre-call pointer -- the same
  address the re-read recomputes -- puts the symbol's live range across the
  call, so a caller-saved colour stops being free and the web takes s0. This
  was confirmed on the instrumented uopt before it was found in source: the
  node-pointer web decides `bestcost=0.000000 bestcolor=1 bestreg=v0` on the
  old shape and `color=14 reg=s0` on the new one, and forcing `p1:w21=c14` on
  the old shape reproduced most of the gain on its own. It closes the whole
  -4 size delta and moves the two float homes from 0x18/0x20 to the ROM's
  0x10/0x18. Worth 456 to 326, the largest single step.
- The pointer spelling. Plain `Node32 *` beats `register volatile Node32 *` by
  19 words on the corrected shape. See the refutation below.
- L110, the input block. The three text pointers are fields of the shared
  overlay input struct, not separate externs. The ROM materialises one base
  with `lui`+`addiu` and then reaches two of the three loads by displacement;
  three independent externs give three `lui`/`lw` pairs. Worth 326 to 315, and
  it removed the last difference in the mnemonic census.

Refuted, with the measurement. The previous handoff recorded `register
volatile Node32 *` as "worth eight masked words over every other spelling and
the only spelling that reaches 461". That is true of the old shape and false of
the corrected one: on the fixed post-call order it reads 405 against 386 for
plain `Node32 *`, so the qualifier is now the worst of the four spellings.
`register` alone is inert in both directions.

Also refuted here: the L100 counter partition that paid 57 words on the F63F8
relative. This family already separates its three counters, and every merge
regresses -- owner with node 461, owner with text 513 at a size delta of -12,
all three in one local 483 at +4, against 456 for three separate locals.

Measured inert, all within 311 to 316: statement order inside either macro, the
order of the twelve stores in the root's second group over six orders including
the full reversal, and the order of the three pre-re-read stores in the
post-call sequence. That is the call test holding -- the procedure issues
calls, so it is p1-only and statement order decides nothing.

Next lever, with the decision variable named. Size, frame and the instruction
multiset are exact, so nothing structural is left: the residual is 215 register
naming and 111 words of schedule order over the same instructions. The naming
rows are ring registers, which by L114 is ring phase rather than colour, so no
save edit or force reaches them. The one place the streams still disagree in
kind is the post-call block of each node group: the ROM performs the counter
re-read first and stores the handle through the recomputed pointer, while this
candidate must store through the pre-call pointer to hold the web across the
call. Whatever source form gives the ROM both -- the web across the call and
the handle store after the recompute -- is the next thing to find, and it was
not reached here. Six post-call orders were measured; moving the re-read before
any of the three stores loses the web and returns to the 386-396 basin, and
moving the chain store before the re-read lets uopt delete the recompute
entirely (size delta -64).

<!-- plateau-handoff:func_overlay_101_F0008128_18E3948:end -->
