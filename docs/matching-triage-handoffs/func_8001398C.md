<!-- plateau-handoff:func_8001398C:start -->
### `func_8001398C` plateau handoff

- source: `src/main/track.c`
- score: 163 differing words
- frame: 0x140
- relocations: 21
- first mismatch: +0x60
- summary: Mickey m2c audit retains defined flag/material types; hoisted sorting bound suppresses required unrolling. Next: source-proved query lifetimes.

#### Mickey m2c structural reconstruction, 2026-09-09

- The fresh configured baseline is 330 candidate and target words, 162 differences, exact 0x140 frame, and 21 candidate/target relocations with 19 exact offset/type/identity records. Workbench verdict `structure-mismatch`, playbook `constant-audit`; the m2c draft was audited against Mickey's own extracted assembly before editing.
- Global attempts 79–83 tested defined flag extraction, the signed material-byte carrier, equivalent unsigned shifting, and the draft's placement of the invariant sorting bound and index initialization. The retained attempt 80 uses an unsigned batch flag and literal bit mask, and an `s8` material carrier consistent with the target's signed byte load. It has 163 differing words, first +0x60, unchanged geometry/frame and 21/21 relocations (19 aligned identities).
- The one additional differing word is an operand exchange in a commutative integer AND. A baseline-versus-retained workbench comparison finds no opcode changes or gaps. The previous signed-left-shift spelling is undefined for the high-bit values being tested and is not an admissible better candidate.
- Moving the invariant sort bound outside the repeated passes, as rendered by m2c, suppresses target-required unrolling: 272 candidate versus 330 target words, 224 differences, a 0x130 frame and 19 candidate relocations. Moving the index initializer does not restore it. These trials establish that the hoisted draft assignment describes generated data flow but does not prove the original source placement. Both are discarded; the best meaningful source and every measured trial are retained under ignored `build/wb/tu-track/func_8001398C/`.
- Stop evidence: all five source trials fail to improve the admissible residual; the only newly exposed loop-placement hypothesis is eliminated by the last two. Call identities, signed material load/store, record stride and flag bit are accounted for. No further missing structural identity is present in the m2c draft, and the remaining lifetime/allocation family is closed by prior evidence. This is mechanism exhaustion, not an exact result or new matching credit.
- Next concrete lever: source-attributed lifetimes around the surface query and sorting temporaries, with a newly authorized allocation mechanism. Preserve the per-pass bound assignment and required unrolling; do not repeat the hoisted-bound family without changed evidence.
- Validation: configured full-TU compilation and relocation comparison, workbench `diagnose`, `gmake check-nonmatching-builds`, `tools/finalize_plateau.py`, and `gmake verify cleanroom check-docs`. The function remains guarded by `NON_MATCHING` over its original assembly fallback.

#### Pinned donor audit, 2026-09-08

- Assignment gate: `base-only` at `32a75d648e8954f7455897fb8f16a8ef5f05df11` for this exact symbol and source path.
- Jet Force Gemini `efd5abb1c79636e297b831f7c2d5bf47eac39c0c` has 12 C implementations and 53 assembly placeholders in `src/track.c`. Its implemented routines do not supply this target's body. The closest masked track-object hit, `trackGetHeights` (Jaccard 0.1942), corresponds by verified TU order to the still-assembly `trackGetHeights` in that pinned source. This is diagnostic structural context, not an adopted name, ABI identity, or exact donor match.
- Fresh configured full-TU measurement: 330 target words and 330 candidate words; 162 raw and 162 relocation-masked differences; first +0x60; target frame 0x140, candidate frame 0x140. There are 21 candidate versus 21 target relocation records, with 19 stable identities at matching offsets/types.
- Workbench verdict `structure-mismatch`, playbook `constant-audit`. The existing diagnosis still requires source-shape/lifetime evidence; the donor-only reopen does not authorize substituting a flag, allocator, or permuter mechanism.
- ADR 0018 stop: zero new source attempts for this target. The pinned source disproves the new matched-donor-C hypothesis. No unchanged flag lattice or previously exhausted source family was repeated. The compiled candidate is unchanged and remains behind `NON_MATCHING` and its original `GLOBAL_ASM` fallback; zero new matching bytes are credited.
- Next concrete lever: a published matched counterpart with an authenticated ABI and useful source lifetimes, followed by Mickey-specific field, branch and call proof. The remaining general levers below require separately authorized changed evidence.
- Commands: `tools/wb_compare.sh --summary-json func_8001398C`, workbench `diagnose` on the configured full-TU object, the track-object masked skeleton audit, `tools/finalize_plateau.py`, `gmake cleanroom`, and `gmake check-docs`. Source, object, scores, first mismatch and previous handoff are retained under ignored `build/wb/`; no instruction rows are included here.

#### Prior committed plateau evidence (historical)


- source: `src/main/track.c`
- score: 168/330 words
- frame: 0x140
- relocations: 21
- first mismatch: +0x60
- summary: Exact geometry/frame and 21/21 relocation count; 19 identities align. Remaining allocator schedule needs procedure-scoped lifetime evidence.
<!-- plateau-handoff:func_8001398C:end -->
