<!-- plateau-handoff:func_8002F618:start -->
### `func_8002F618` plateau handoff

- source: `src/main/rcpFast3d.c`
- score: 91/327 words
- frame: 0x40
- relocations: 6
- first mismatch: +0x34
- summary: Indexing arg1[i] instead of carrying a node cursor removed a whole-function t6-t9 ring-phase rotation: 279 to 91 words at the target frame.

## 2026-09-11 (lane `lane/w3-mid`): the ring phase was a cursor-versus-index choice

Before: 279 positional masked words, size delta +4; aligned 137 byte-exact,
162 register naming, 5 immediate only, 26 really different; first mismatch
+0x34.
After: 91 positional masked words, size delta +8; aligned 267 byte-exact,
31 register naming, 8 immediate only, 24 really different; first mismatch
+0x34. Six relocations on each side, unchanged.

The register-substitution census of the 279-word residual was one four-cycle
over the integer temp ring: t8 becoming t9 on 52 sites, t6 becoming t8 on 51,
t9 becoming t7 on 49 and t7 becoming t6 on 43. That is 195 of the 279 words and
it is a single whole-function ring-phase fact, not 195 independent ones.

`tools/frame_census.py` shows seventeen stack slots on each side at identical
offsets and frame 0x40 on both, so nothing here was ever a frame problem.

Replacing the `element` node cursor with direct `arg1[i]` indexing, and
`element++` with `i++`, removes the phase entirely. The earlier note that
"natural indexed forms grow the frame to 0x58" is refuted by this spelling: the
frame stays 0x40, and the strength-reduced cursor still appears as the
sixteen-byte-stride pointer the target advances.

The `element` declaration line and its initialiser line are replaced in place
rather than deleted, so the physical line numbers of everything after them in
the translation unit are unchanged (L59).

What is left, in three named pieces:

  - The prologue at +0x34 carries one extra instruction that copies the second
    parameter into a callee-saved register; the target has no such copy and
    reaches its early exit through a branch-likely instead. That one word
    displaces the rest of the prologue and is the +4 of the size delta.
  - A caller-saved naming layer of twelve words: a1 becoming a0 on seven sites
    and v1 becoming a0 on five.
  - The second command pair of the non-alternate branch is written through a
    merged cursor here and through a separate one in the target. Spelling that
    pair through an explicit cursor was measured at 120 words and spelling both
    pairs that way at 248, so the cursor spelling is not the lever; the two
    increments have to separate some other way.
<!-- plateau-handoff:func_8002F618:end -->
