<!-- plateau-handoff:func_8001EC44:start -->
### `func_8001EC44` plateau handoff

- source: `src/main/charControl.c`
- score: 231 differing words
- frame: 0xE0
- relocations: 45
- first mismatch: +0x0
- summary: Target 238w, frame 0xA0, 47 relocs; scalar identities and X/Z normalization are correct, but common-prefix sinking and FP/integer allocation remain.
- ownership/ABI: resident `0x8001EC44..0x8001EFFC` (ROM `0x1F844..0x1FBFC`), no padding; five-argument collision callback `void (s32, ControlVector3 *, ControlVector3 *, f32, ControlCollisionPlane *)`, passed to `func_80010900` from the owning TU.
- baseline: fresh configured V0 reproduced 238 target words versus 240 candidate words, 236 positional differences, frame `0xA0` versus `0xD0`, and 33 versus 47 static relocations with two exact identities.
- retained candidate: 238 target words versus 241 candidate words, 231 raw and relocation-masked differences, first `+0x0`, frame `0xA0` versus `0xE0`; candidate/target relocation counts are 45/47, eight offsets/types align, and two identities are exact.
- semantic repair: adjacent collision-state floats now retain their nine separate relocation identities; the third branch preserves the original plane X/Z normal for normalization and result-state stores instead of using Y/Z or angle-delta values.
- bounded attempts: ten configured forms covered scalar relocation identities, point-coordinate lifetimes, separate and reused angle webs, a full distinct-web m2c draft, direct plane reloads, block-scoped original normals, `register` qualifiers, raw `f32 *` vector spelling, and a register-qualified distance web. Attempts 8-10 were byte-flat; no exact result emerged.
- donor/oracle evidence: JFG's assembly-only `controlSquashCheckPrior` is a different high-level routine. A direct five-reference masked four-gram scan over the proved Mickey range tops out at 0.0648 on BK `guLookAtStereoF`; no JFG function enters the top 15, so there is no credible code donor.
- flag/permutation disposition: `flag_sweep.py` refuses this unsymbolized resident fallback before compiling because no unique `symbol_addrs` owner exists. No generic permutation was run from the broad structural basin.
- finalizer disposition: `finalize_plateau.py` refused because this shared TU contains the unrelated legacy inline `func_8001D880` handoff. The target marker was appended without moving measured source lines, and `plateau_handoff_audit.py --write` generated this fixed shard.
- next lever: find a source-authentic macro/aggregate or alias shape that keeps the plane-dot calculation in the common prefix while assigning original X/Y/Z normals to the target saved-FP pool and leaving both pointer parameters in caller-saved registers; do not repeat flags, qualifiers, raw vector types, or these ten web forms.
<!-- plateau-handoff:func_8001EC44:end -->
