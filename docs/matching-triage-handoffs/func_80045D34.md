<!-- plateau-handoff:func_80045D34:start -->
### `func_80045D34` plateau handoff

- source: `src/main/diCpu.c`
- score: 254/459 words
- frame: 0xA8
- relocations: 91
- first mismatch: +0xC
- summary: Control-flow and lifetime corrections recover 459 words and exact frame/home traffic; 298 aligned exact, with type and emission residuals remaining.
#### 2026-09-13, lane c2: recover control flow, lifetimes and every stack home

Incoming configured baseline: 451/459 words, delta -32, 440 raw and 439
masked differences, first +0; frame 0xB0 against 0xA8. Alignment is 176 exact,
89 naming, 45 immediate and 157 structural rows. Procedure 5 of 14 is mapped
from captured named Ucode; its stock/instrumented full-TU text agrees, with
62 draws and 712 emission records.

The retained candidate is 459/459 words with frame 0xA8 and the target's exact
19-slot ladder, including every load/store count, access width and address-take
count. It has 270 raw and 254 masked differences, first +0xC. Alignment is
298 exact, 98 naming, 20 immediate and 49 structural rows; the last count
includes six candidate-only and six target-only words. The final census has
59 draws and 720 emissions. This remains NON_MATCHING C, not object-exact or
ROM-exact C. Matching credits remain unchanged.

Adopted changes, each compiled and preserved independently:

- Explicit video-mode branches replace the Boolean assignment. They remove
  ten aligned residual rows even where the positional count initially worsens.
- The page-count fallback belongs to the logging-mode else branch. This
  reproduces its duplicate store and reaches the full target word geometry.
- The row loop tests its pre-decrement value. The incoming decrement-before-
  test form also had the wrong exit behavior when page one sets row to zero.
  Testing row-- directly subsequently removes the unnecessary Boolean
  materialisation emitted for an explicit comparison with zero.
- Redraw storage holds the pool base only while the flag is dead, and is
  reset at the original loop exit before any flag use. Row storage serves the
  later digit index and page label; column storage serves the later digit word.
  The page-limit carrier is separate from the address-edit candidate, and the
  redundant log-count carrier is merged into printedValue. All values retain
  their required 32-bit representation and every call/store order is reviewed.
- Declaration order is solved from the observed home offsets. No dummy
  locals, synthetic padding or shared compiler/header changes are introduced.
- Capture the colour tag before the address-taken printedValue store. This
  removes the alias-sensitive second field load, reduces two draws, and moves
  the intended pair of source lines. A nested page switch recovers the flat
  dispatch shape and removes six emission records.

The exact frame and all home traffic are independently re-censused on the
retained source. Every adoption has an aligned per-window comparison, not just
a positional score. Census counts alone are not an opcode identity proof:
the direct row-value test changes the emitted operation at unchanged counts.

Negative controls are preserved separately. Old-page address-taking creates an
extra home without removing its register; volatile old-page storage adds eight
words on the new shape. Moving the memory-index definition improves positional
count but loses aligned agreement; folding the initial flags is no improvement.
Using the mask as the digit carrier leaves every aligned window and draw
unchanged. A named row intermediate is full-text inert. Signed old-page state
and a signed cast at only its equality-to-one test remove the surplus constant
but regress to 389 masked and 241/240 aligned exact, respectively. The isolated
cast reproduces the broad regression instead of preserving the earlier windows.
The last three controls offer no improved retained residual or width-preserving
source route; restore the best rather than carrying their regressions forward.

The remaining blocker is the type/lifetime and emission schedule around the
page-one comparison, address edits and mode reset, plus the saved-register
ordering and call-argument schedules. A future causal packet must preserve
the now-proved frame, homes and control-flow fixes while moving those regions.
Neither a general source impossibility nor an exhaustive new colour closure
is claimed, and no colour sweep was repeated in this lane.

The public JFG checkout at efd5abb was inspected read-only. Its corresponding
src/diCpu.c::func_80066EB0_67AB0 remains an incomplete NON_EQUIVALENT body;
the older func_80067880 name is stale. No complete donor body exists there to
transfer. The in-source PROVENANCE note records this context; Mickey's own
target establishes the adopted changes.

Static relocation counts are 91 candidate against 89 assembled target, with
27 literal tuple identities at matching positions. No relocated owned-range
or canonical C promotion is claimed. Source/object pairs, stock/capture
fidelity receipts, Ucode, frame/draw profiles and aligned maps stay ignored
under build/c2. The lane activity counter reaches 32, including the explicitly
invalid overlay-A preparation, which is not a valid source attempt.
Commands: assignment gate, configured stock and traced full-TU compilation,
draw_census.py, frame_census.py, residual_map.py with retained objects and
paired deltas, finalize_plateau.py and tools/gates.sh. The full-ROM proof
covers the unchanged assembly fallback for this guarded function.
<!-- plateau-handoff:func_80045D34:end -->
