<!-- plateau-handoff:func_8003F154:start -->
### `func_8003F154` plateau handoff

- source: `src/main/particles.c`
- score: 17 differing words
- frame: 0x58
- relocations: 16
- first mismatch: +0x20C
- summary: 17 FP-allocation words, all one fp-pool position from the emission-direction zero. The candidate spends two fp-pool colours the target spends as ring temps, and the direction is not reversible from source: every naming and de-naming form measured is flat or worse.

- 2026-09-09 pass, the residual localised to a register *class*, not a colour.
  The workbench's fp lanes say it exactly: the candidate spends two fp-pool
  colours the target spends as ugen ring temps, first at aligned row 135. Both
  sites are named locals -- `speed = config->value3C` and the sum of squares
  assigned to `scale` -- and the target's own zeros at rows 131 and 253 are
  pool colours where the candidate's are ring temps. So the target names its
  two zeros and does not name the two values the candidate names.
- Neither half is reachable. Naming a zero costs an instruction every way it
  can be written: `X = 0.0f; offset[0] = X; offset[1] = X;` re-materialises the
  constant instead of copying it (298 versus 297 instructions) for `magnitude`,
  `scale`, `speed` and a fresh local alike, and a fresh `f32` local also moves
  the frame `0x58` -> `0x60` because the declaration census is saturated.
  Un-naming the two candidate webs keeps 297 instructions but scores 33 to 36:
  `offset[2] = -config->value3C` alone is 33, inlining the sum of squares alone
  is 22, both together 34 to 36.
- Inlining the sum of squares into `sqrtf` *does* close rows 249 and 250
  exactly -- the target's ring registers for the two products -- and then turns
  row 252 into a commutative-order word and costs eleven more downstream. Every
  parenthesisation of the three squares was measured; z-first is the best of
  them at 22.
- Flat this pass, each measured: eight spellings of the emission-direction head
  (the comma-operator `flags` assignment is *not* load-bearing -- writing it as
  its own statement before the zero is byte-identical, and that cleaner form is
  now the retained candidate -- but the `offset[1] = offset[0]` copy is: any
  form that stores two independent zeros costs an instruction, and `flags`
  after the zero costs 151); a 288-cell cross product over the random-scale
  constant, the magnitude-zero branch shape, the three scale multiplications,
  the three final adds, the `speed = -offset[2]` placement and the
  `D_800D413x` publication order (floor 17, and the whole product is flat at
  17 or worse); nine inert probes that try to hand the zero a colour by naming
  its result afterwards (`magnitude = offset[0]` and friends -- all exactly 17,
  so the class is not source-reachable at the definition either); and the
  chained `offset[0] = offset[1] = 0.0f` forms (65 and worse, 296 instructions).
- Next lever: nothing in the naming lattice. This needs either an
  instrumented ugen ring trace, or evidence about the *declaration set* -- a
  form with the same eleven-slot census that gives the two zeros webs without
  spending an instruction on them.
<!-- plateau-handoff:func_8003F154:end -->
