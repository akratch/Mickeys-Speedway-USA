<!-- plateau-handoff:func_8003F154:start -->
### `func_8003F154` plateau handoff

- source: `src/main/particles.c`
- score: 13 differing words
- frame: 0x58
- relocations: 16
- first mismatch: +0x21C
- summary: 13 words in three clusters of one class: at each site the target leaves a value on the ugen fp ring where the candidate holds it on a uopt pool colour. This pass names the sum-of-squares site exactly and closes its arithmetic. Writing that sum inlined into sqrtf with the x square first -- sqrtf(((offset[0] * offset[0]) + (offset[1] * offset[1])) + (offset[2] * offset[2])) -- makes the whole sqrt argument region exact through +0x03F0, including the three words the named scale carrier gets wrong, and it is the only order that does: the same inlining y-first scores 23 and z-first 22 against this form's 19. So the target does not carry that sum through scale, and the earlier reading that inlining is simply worse was measuring the wrong operand order. What the inlined form costs is one ring slot: the magnitude test's zero moves one step further along the ring and the tail follows, 12 tail words instead of 6. The target spends no ring slot there at all -- it materialises that zero on the same pool colour scale takes afterwards -- so the one remaining decision variable is a uopt fp web for the comparison constant, which would return the ring slot the inlined form spends; that the tail then realigns is ring accounting, not measurement. Eliminated at this base, all delta 0 unless noted: a 900-cell product over five emission heads, three zero-fill shapes, five sum forms, four branch shapes and three Z-accumulate forms has 13 as its floor and only the untouched cluster-2 spelling reaches it; a 65-cell carrier lattice over the three final accumulates is flat or worse; every zero-carrier identity at the magnitude test is byte-identical (scale) or costs 30 words or more (speed, magnitude, offset elements); an fp force sweep over the inlined form, 41 webs times the six pool colours with 59 forces applied, has nothing under 19; and widening offset to four elements moves the frame, so the declaration census stays saturated at 0x58.

- 2026-09-09 pass, the residual localised to a register *class*, not a colour.
  The workbench's fp lanes say it exactly: the candidate spends two fp-pool
  colours the target spends as ugen ring temps, first at aligned row 135. Both
  sites are named locals -- `speed = config->value3C` and the sum of squares
  assigned to `scale` -- and the target's own zeros at rows 131 and 253 are
  pool colours where the candidate's are ring temps. So the target names its
  two zeros and does not name the two values the candidate names.
- Neither half is reachable. Naming a zero costs an instruction every way it
  can be written: `X = 0.0f; offset[0] = X; offset[1] = X;` re-materialises the
  constant instead of copying it (298 versus 297 instructions) for `magnitude`,
  `scale`, `speed` and a fresh local alike, and a fresh `f32` local also moves
  the frame `0x58` -> `0x60` because the declaration census is saturated.
  Un-naming the two candidate webs keeps 297 instructions but scores 33 to 36:
  `offset[2] = -config->value3C` alone is 33, inlining the sum of squares alone
  is 22, both together 34 to 36.
- Inlining the sum of squares into `sqrtf` *does* close rows 249 and 250
  exactly -- the target's ring registers for the two products -- and then turns
  row 252 into a commutative-order word and costs eleven more downstream. Every
  parenthesisation of the three squares was measured; z-first is the best of
  them at 22.
- Flat this pass, each measured: eight spellings of the emission-direction head
  (the comma-operator `flags` assignment is *not* load-bearing -- writing it as
  its own statement before the zero is byte-identical, and that cleaner form is
  now the retained candidate -- but the `offset[1] = offset[0]` copy is: any
  form that stores two independent zeros costs an instruction, and `flags`
  after the zero costs 151); a 288-cell cross product over the random-scale
  constant, the magnitude-zero branch shape, the three scale multiplications,
  the three final adds, the `speed = -offset[2]` placement and the
  `D_800D413x` publication order (floor 17, and the whole product is flat at
  17 or worse); nine inert probes that try to hand the zero a colour by naming
  its result afterwards (`magnitude = offset[0]` and friends -- all exactly 17,
  so the class is not source-reachable at the definition either); and the
  chained `offset[0] = offset[1] = 0.0f` forms (65 and worse, 296 instructions).
- Next lever: nothing in the naming lattice. This needs either an
  instrumented ugen ring trace, or evidence about the *declaration set* -- a
  form with the same eleven-slot census that gives the two zeros webs without
  spending an instruction on them.

#### 2026-09-10, lane c2-resident: composition tried, and the class re-read

Still 17. Confirmed against the current tree, and the residual re-read off the
objects rather than the aligned rows, which sharpens the description: the
seventeen words are **three clusters**, and every one of them is the same
question of which value gets a low register.

- Five words where the emission-direction zero and the `value3C` read trade
  the two lowest fp registers between them.
- Five words in the magnitude block where the sum of squares, the third
  square and the comparison's zero rotate through three registers.
- Seven words in the three final adds, where the second component's load and
  the first sum trade one lane and the rotation carries to the last store.

In every cluster the target puts a **zero on the low register and the computed
value on the higher one**, and the candidate does the reverse -- one statement
of the class that fits all three sites, where the previous handoff described
two.

Composition was the obvious untried move, since the previous pass established
that naming a zero costs an instruction alone and un-naming a candidate web
regresses alone. A **210-cell cross product** of six emission-direction heads
(inlining `speed`, and hosting the zero on `magnitude`, `scale`, `speed` or a
fresh local, with and without keeping `speed` named) x seven sum-of-squares
forms (named, four inlined parenthesisations, two casts) x five final-add forms
(compound assignment, explicit, reversed, and a cast on either operand) is flat
or worse at every cell; 17 is the floor and only the untouched baseline reaches
it.

Falsified specifically, because it is the lever that closed `func_8005716C` the
same day: the **`(f32)` weight cast does reach these adds** -- it is not inert,
it moves five words -- but it moves them the *wrong way*, from 17 to 22, and so
does the reversed written order. So the final-add cluster is not a
commutative-order residual at all; the operand order already agrees and only
the destination register differs, which is a ring-position question the weight
rule does not govern.

Next lever unchanged in kind but narrower: the declaration census is saturated
at frame `0x58`, so any route runs through trading a declaration for a compiler
temporary rather than adding one. `cc -Wo,-zdbug:2` prints the census directly
(one `isvar M` row per declared local) and is the instrument to use before
guessing at the trade.

#### 2026-09-10, lane c4-resident: the sum of squares is inlined, x-square first

The 13 words are still three clusters and still one class. What this pass adds is
a source form for the middle one, and the ring accounting that explains the rest.

**The receipt.** Replacing

```c
scale = (offset[1] * offset[1]) + (offset[0] * offset[0]);
magnitude = sqrtf(scale + (offset[2] * offset[2]));
```

with

```c
magnitude = sqrtf(((offset[0] * offset[0]) + (offset[1] * offset[1])) + (offset[2] * offset[2]));
```

makes the sqrt argument region exact from `+0x03CC` through `+0x03F0` -- the two
component loads, the three products, both sums, all of it -- where the retained
`scale` form gets three of those words wrong. The operand order is the whole
result: the same inlining written y-square first scores 23, z-first 22, x-first
**19**. The earlier note here recorded "z-first is the best of them at 22" and
concluded inlining was worse than the carrier; it was measuring the wrong order.
So the evidence now says the target does **not** carry that sum through `scale`.

**What it costs, and why that is the last question.** Inlining hands the sum a
ugen ring temp instead of `scale`'s pool colour, which advances the ring one step
for the rest of that region: the magnitude test's zero moves one slot along and
the final-accumulate cluster follows, 12 tail words instead of 6. The target
spends no ring slot on that zero at all -- it materialises the comparison
constant on the same pool colour `scale` takes immediately afterwards. So the one
remaining decision variable is a uopt fp web for the comparison constant. Give it
one and the inlined form returns the slot it spends; that the tail then realigns
is ring accounting rather than measurement, so treat it as the prediction to
falsify first.

Eliminated this pass, all delta 0 unless noted, against the 13-word base:

- A **900-cell product**: five emission-direction heads x three zero-fill shapes
  x five sum forms x four branch shapes x three Z-accumulate forms. 13 is the
  floor and only the untouched sum spelling reaches it. The composition that
  looked most likely -- un-naming all three sites at once -- is 33.
- The three un-namings measured alone and in every combination: the `value3C`
  load direct is 31, the inlined sum 23, the Z accumulate direct 14; pairs and
  the triple are 24 to 34. Each is a regression alone and composition does not
  rescue them.
- A **65-cell carrier lattice** over the three final accumulates (each component
  staged through `magnitude`, `scale`, `speed`, or not at all). `scale` and
  `magnitude` are byte-identical to each other at every position, `speed` is
  worse everywhere, and nothing is under 13.
- Every zero-carrier identity at the magnitude test, before and after the sqrt
  call: through `scale` it is byte-identical to the bare constant (the assignment
  is propagated away), through the offset elements it costs 41 to 57 words, and a
  fresh `f32` local moves the frame.
- Nine branch shapes for the magnitude test (`!=` with the arms swapped, the
  guard hoisted out of the `if`, both ternaries, the constant on the left, a
  negated `>` test) on both sum forms: flat or worse.
- An **fp force sweep over the inlined form**: 41 webs x the six pool colours,
  59 forces applied, nothing under 19. So on that form the remaining words are
  not reachable by moving any existing web's colour either -- the zero simply is
  not a web.
- `f32 offset[4]`, to buy a spare pool competitor out of the tail padding, moves
  the frame. The declaration census is still saturated at `0x58`.

Instrument note for whoever picks this up: `DKWB_UGEN_TRACE=1` on the
instrumented `cc` prints one free-list record per fp allocation with ugen's own
source line, and it reads this function cleanly at proc 22. It shows the ring
here rotating five wide, not four, and it is what makes the ring accounting above
checkable rather than inferred.

**Corroboration for the inlined form, from matched code in this tree.** Every
matched three-component magnitude in the resident segment writes the sum flat
and inlined, never through a carrier: `src/main/anim.c` does
`distance = sqrtf((deltaX * deltaX) + (deltaY * deltaY) + (deltaZ * deltaZ));`
at seven sites and `src/main/audio_manager_36D0.c` at four more. Written that
way here -- flat, x-square first, which is the same tree as the parenthesised
x-first form by left associativity -- it scores the same 19 and closes the same
region, while the y-first flat form scores 23. So the source shape is not in
doubt; only the comparison constant's web is.

**Also measured and flat, this pass.** No free fp ring pop exists to pay for the
slot the inlined form spends: `scale * 1.0f`, `(f32) scale`, `speed / magnitude`
cast on either operand, the explicit and reversed multiply forms, and
`-(-scale)` are either byte-identical or cost instructions, which reconfirms
[L89] on this function -- the fp ring has no analogue of the integer ring's
redundant-mask lever. Semantic rewrites of the guard (testing the sum instead of
its root, dividing before the test, naming the zero through `scale` around the
call) are 22 to 121.
<!-- plateau-handoff:func_8003F154:end -->
