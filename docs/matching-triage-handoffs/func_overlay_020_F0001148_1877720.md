<!-- plateau-handoff:func_overlay_020_F0001148_1877720:start -->
### `func_overlay_020_F0001148_1877720` plateau handoff

- source: `src/overlays/o020/func_overlay_020_F0001148_1877720.c`
- score: 178 differing words
- frame: -0x90
- relocations: 5
- first mismatch: +0x20
- summary: Target is 207 words versus 205; four of five relocation offset/type sites align but static identities remain unresolved.

Fresh evidence on base `8b29adc5bdc820682bd3df4dabe6e0e8ce9a491f`:

- authenticated owner: overlay 20 `.text` offset `0x1148..0x1490`
  (`0x348` bytes), comprising the 828-byte / 207-word executable target and
  12 bytes of target padding at the end of the overlay;
- ABI: `f32 (Overlay20TailGrid *, f32, f32, Overlay20TailVector *)`; the
  runtime export is ORT 1611 and one `R_MIPS_26` inbound reference was found
  in overlay 8 at `+0x4E64`;
- configured V0: IDO `-O2 -mips3 -32 -Wab,-r4300_mul`, 205 candidate words
  against 207 target words (`-8` bytes), with matching `0x90`-byte frames and
  178 masked / 180 raw differing words from `+0x20`. This freshly reproduces
  the retained ranking rather than relying on it;
- workbench classified 17 aligned structural, 114 register, and 10 constant
  residuals, with 16 alignment gaps. The first divergence remains in the
  grid-field load web and affects both integer and floating-point allocator
  populations;
- relocation surface: target and candidate each have five records, but only
  four offset/type sites align. Four candidate static identities are
  unresolved, the candidate call is shifted from `+0x294` to `+0x290`, and
  no effective identity aligns, so relocation proof is nonexact;
- donor scan: the best available DKR/BK masked 4-gram similarity was `0.046`
  (`guLookAtF`), with no credible donor or sibling mechanism;
- the historical ten-form cap, full flag lattice, initialization/type/
  comparison/volatile/neighbor/lifetime probes, and bounded permutation are
  already exhausted. No route was repeated without new natural evidence.

Next lever: recover a new source-authentic grid-field initialization/lifetime
form that adds the missing two words while reproducing the target's first
integer and floating-point allocator choices; only then revisit relocation
identity resolution and a bounded batch.
<!-- plateau-handoff:func_overlay_020_F0001148_1877720:end -->
