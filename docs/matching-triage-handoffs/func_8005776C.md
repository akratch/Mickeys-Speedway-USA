<!-- plateau-handoff:func_8005776C:start -->
### `func_8005776C` plateau handoff

- source: `src/main/anim.c`
- score: 47 differing words
- frame: 0xC0
- relocations: 2
- first mismatch: +0x24
- summary: Donor audit improved the residual to 47 words; next lever is authenticated outer sort cursor scheduling evidence.
#### 2026-09-11, lane `p6-small`: the sort loop's structural difference is now named

Unchanged at 14 relocation-masked words, size delta 0. The aligner splits them
12 register-naming, 1 immediate-only, 1 really-different, and the two
non-naming words are one fact with a source-level cause that had not been
identified:

**The target re-initialises the distance cursor on every outer sort pass; the
retained C does not.** The target's outer back edge lands on the shift that
recomputes the pass limit, and its branch-likely delay slot re-materialises the
array address, so the cursor restarts at `distances` each pass. The retained C
lets the cursor keep advancing across passes, which makes the loop a different
(and incorrect) sort; the two are the same instruction COUNT, which is why the
size delta is zero and why this went unnoticed. The ROM is the authority here,
so the retained body is semantically wrong and the target's is a plain bubble
sort.

**Writing the reset costs one word, and that is the open question.** Ten
placements of `distance = distances` inside the outer loop -- first, after the
limit, after the offset reset, at the bottom before and after the decrement --
times six spellings of the address (`distances`, `&distances[0]`,
`distances + 0`, a cast, `&distances[remaining - remaining]`, a cast of
`&distances[0]`), times three region wrappers, 162 points in all: every one is
size delta +4 and 52 words or worse. The extra word is always the same
instruction, a register copy: uopt hoists the loop-invariant array address into
its own register and copies it into the cursor each pass, where the target
re-materialises the address from the stack pointer in the delay slot. The limit
computation then reads the hoisted register rather than the cursor, which is
the second difference.

Also measured and not levers: an index-based inner loop with the cursor and
limit left to strength reduction, in four shapes and four declaration slots for
the index (16 and 18 words, never below); `&distances[remaining]` for the limit
instead of `&distance[remaining]` (worse by 12).

**Decision variable:** a form in which the array address is re-materialised per
outer pass rather than hoisted into a register and copied. L110 says uopt never
merges an address constant across a basic-block boundary, yet here it merges a
preheader occurrence with a loop-body occurrence written as two separate source
statements in two blocks, so either L110 is narrower than its statement or the
merge happens in a later phase; settling that is the next instrument.

The twelve naming words are a separate question and are one coherent mapping
over the integer bank (100% coherence, one window, no closed cycle), chained
`t0 -> t9 -> t8 -> t7 -> v0 -> v1`. Re-read after the 2026-09-11 float-bank fix
to `register_census.py`; the reading is unchanged because this residual carries
no float substitutions.

<!-- plateau-handoff:func_8005776C:end -->
