<!-- plateau-handoff:func_8005776C:start -->
### `func_8005776C` plateau handoff

- source: `src/main/anim.c`
- score: 54/105 words
- frame: 0xC0
- relocations: 2
- first mismatch: +0x24
- summary: Fidelity trace proves -Wab,-r4300_mul fixes the first 28 words and sqrtf site, but the sort cursor then hoists a stack base and leaves 106 words.
- assignment base: `8a8655c0440715d5def4d63d4ad5dbf2fd19ee85`
- owned range: resident `0x8005776C..0x80057910`, ROM `0x5836C..0x58510`, 420 bytes / 105 words, with no trailing padding before `fmvInit`
- ABI and callers: `s32 (f32, f32, f32, f32, s32, HitCopyState **)`; ten authenticated overlay callers use `R_MIPS_26` relocations to the resident symbol
- configured baseline: the full-TU `-O2 -mips2 -32 -Wo,-loopunroll,0` candidate is exact-sized with frame `0xC0`; 54/105 words match and 51 differ, first at `+0x24`
- relocation proof: target and candidate each have two static `R_MIPS_26` records for `func_80005750` and `sqrtf`; the first site aligns, while configured V0 emits the `sqrtf` site four bytes early
- new scheduler evidence: the stock `-Wa,-R` full-TU capture passes workbench fidelity for text, data, rodata, relocations, and symbols; it shows the radius multiply ready early but delayed into the first call slot by the scheduler's cycle key
- focused flag result: adding `-Wab,-r4300_mul` makes the 28-word prefix exact and moves `sqrtf` to the target offset, but the candidate is 106 words because its sort loop hoists the distances base and emits one extra cursor move
- attempts: seven coherent source probes covered radius statement order, an explicit squared-radius carrier, cursor scope/initializers, an explicit target-kind carrier, next-distance load order, and an explicit reset edge; none strictly improved V0, with measured residuals of 51, 68, 52, 51, 52, 53, and 68 differing words respectively
- prior work respected: the earlier ten-form, flag-lattice, and bounded-permuter campaign was not repeated; no new permutation ran because no natural source form produced a strict gain
- donor evidence: the retained body remains the permitted JFG `hitPlayer` tier-B reconstruction; local coddog is unavailable and `skeleton_scan.py similar` fails closed because this workstation has no configured reference directories
- flag-impact limitation: `tu_flag_impact.py` refuses this TU because its matching build has a post-compile policy the diagnostic cannot reproduce, so the R4300 flag result is mechanism evidence rather than a safe TU-wide flag proposal
- next action: reopen only with authenticated source for the sort cursor lifetime or a postprocess-aware TU flag-impact path; do not repeat declaration, load-order, reset-edge, broad flag, or generic permutation probes
<!-- plateau-handoff:func_8005776C:end -->
