<!-- plateau-handoff:func_overlay_008_F000291C_1860674:start -->
### `func_overlay_008_F000291C_1860674` plateau handoff

- source: `src/overlays/o008/overlay_008.c`
- score: 56 differing words
- frame: -0x70
- relocations: 25
- first mismatch: +0x0
- summary: Fresh exact geometry retains the 0x70/0x68 frame split and divergent FP pool; prior declaration, lifetime, constant, and flag probes are exhausted.

Fresh maintenance evidence on base `0a6b1312ef522592a8780d8e2e1cf810e6b23ddc`:

- The owned overlay-8 text range is `+0x291C..+0x2EC0` (1,444 bytes, 361 words). The configured `NON_MATCHING` C also emits 361 words, with 305 exact words, 56 relocation-masked differences, and 60 raw differences.
- The ABI is `void (O8P291CMotion *, O8P291CState *, f32)`. Its local overlay-8 caller has one `R_MIPS_26` reference at caller offset `+0x6BC`. The shared TU compiles with its configured `-Wab,-r4300_mul` flag.
- Workbench classifies V0 as `allocation-mismatch`: target frame `0x68`, candidate frame `0x70`; integer pool/temp and FP temp lanes agree, while the first FP-pool divergence is slot 2. The candidate keeps eight extra non-save bytes.
- Target and candidate each have 25 relocations with exact offset/type alignment. Candidate static identities remain unresolved, so relocation identity is not proven and the body cannot be promoted.
- The best external skeleton similarity was only 0.062 (`bk:chZubbaDocile_update`); no credible Mickey sibling or external donor was found.
- Historical attempts already exhausted declaration-block/order, volatility, scalar-versus-aggregate angle forms, constant audit, configured flag lattice, and gain-gated bounded permutation without improving this basin. These mechanisms should not be repeated without new evidence.
- A genuinely new lever must naturally shorten the angle/contact lifetime enough to remove eight bytes of frame while reproducing the target FP pool. Re-run preflight and linked relocation identity proof after such a gain; otherwise retain this candidate.
2026-09-09 lane `lane/tu2-o008` maintenance evidence (base `304d363e`, score unchanged at 56
relocation-masked words / 60 raw, 361 of 361 instructions):

- This is the closest-shaped target in the unit and its residual is now decomposed.
  All three integer lanes are identical (pool 31/31, temp 14/14, shared 13/13) and
  the FP temp lane is identical 96/96. Only the FP pool diverges, at slot 2. Of the
  56 differing words, 33 are constant-class stack displacements that follow from the
  eight-byte frame excess, so the frame is worth more than every other lever here
  combined: closing it should resolve well over half the residual in one step.
- The frame is not a declaration-census artifact, contrary to the usual lever.
  Fifteen source forms were measured and the candidate frame stayed at `0x70`
  against the target's `0x68` in every one: six whole-list reorders, removal of the
  dead local, hoisting the inner-scope block declarations to function scope, four
  register-storage-class probes, and inlining each of the two single-assignment
  scalars at their use sites. Dropping a declared local does not shed the eight
  bytes, so the excess is not in the declared block.
- Declaration order in this unit is a colour lever, not a frame lever. Across
  twenty-one orders on this function and `+0x4CF0` the frame never moved once, while
  the score ranged from 56 to 80. The order is also a local optimum with real
  structure: exchanging the first two declarations is byte-inert, one adjacent
  exchange further in costs five words, and larger permutations settle into a single
  80-word basin. Order search is therefore exhausted as a route to this frame.
- The `register` storage class is byte-inert in this translation unit; four
  placements left the object unchanged at 56 words.
- Home census for the next lane: the candidate places the angle, contact and
  saved-rate scalars at the top of its local region and leaves roughly twenty-four
  bytes unreferenced in the middle of the frame, while the target packs the same
  values low and has no such hole. The eight-byte excess and that hole are the same
  fact. Resume by explaining the hole, not by respelling the declarations.
<!-- plateau-handoff:func_overlay_008_F000291C_1860674:end -->
