<!-- plateau-handoff:func_80053868:start -->
### `func_80053868` plateau handoff

- source: `src/main/anim.c`
- score: 1197 differing words
- frame: 0x240
- relocations: 61
- first mismatch: +0x0
- summary: Corrected two-pointer call ABI; candidate is 60 instructions short with a 0x148 frame excess and displaced collision-update CFG.

#### Narrow ABI correction

The reviewed correction against `5b4bfc57` restores the two-pointer
`mathOneFloatRPY(ControlTransform *, f32 *)` declaration and removes the
unsupported third and fourth arguments from this function's call. Physical
source lines are preserved. Mickey's callee at `0x80029E74` consumes the two
pointers, overwrites incoming `a2`, and does not consume incoming `a3`.
The target call at function offset `+0x1A8` sets only the two pointer arguments;
the constants previously passed as extra arguments are materialized after
the target call. Existing declarations in `charControl.c` and the other
two calls in `func_80053550` corroborate this signature. This is a per-callee
ABI correction, not authority to retarget or retype runtime trap calls.

The configured full-TU candidate changes from 4,588 to 4,580 bytes against
the 4,820-byte target. Its frame remains `0x240` against target `0xF8`.
The raw and relocation-masked positional difference both worsen from 1,179
to 1,197 words; the first mismatch remains `+0x0`. The candidate has 61
relocations against 59 target relocations. All 61 candidate identities resolve,
but only one offset/type and effective-identity pair aligns; this is partial
static evidence, not exact relocation proof. The structural edit distance
decreases from 1,356 to 1,318 and opcode edit distance from 613 to 611, which
does not override the remaining size, frame, instruction, and relocation gaps.

Only this function changes among the full-TU candidate's 55 emitted functions.
The other 54 retain identical function bytes and function-relative relocation
tuples, including the other emitted consumers of the restored declaration.
Six changed `.rodata` words are existing `R_MIPS_32` relocation addends into
the later, unchanged `func_800563B4`; each retains the same target-function
relative offset after the eight-byte layout shift. The normal fallback TU's
`.text`, `.rodata`, `.data`, and all relocation tuples remain identical to the
preserved baseline, and the normal ROM rebuild retains the expected hash.

Baseline and corrected source/object/preflight/comparison artifacts are
preserved in the owning lane's ignored `build/function-probe/` directories.
The `NON_MATCHING` fallback remains in place: this correction earns no matched
bytes and does not reopen manual frame/CFG work. The next matching action is
a separately leader-assigned bounded standing sweep or an independently
reviewed new-evidence experiment, not additional hand permutation here.
<!-- plateau-handoff:func_80053868:end -->
