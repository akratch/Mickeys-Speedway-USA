<!-- plateau-handoff:func_80053868:start -->
### `func_80053868` plateau handoff

- source: `src/main/anim.c`
- score: 1169 differing words
- frame: 0xF8
- relocations: 59
- first mismatch: +0xC
- summary: 457 aligned exact words; exact frame; 60 words short. Callback constant force recovers 48 words; vector region remains allocation/peeling.

#### Decompiler-assisted reconstruction, 2026-09-08

Assignment base: `a4431db5ba447021d43bd780f8dfb1a1e0bc1db0`.
The authorized mechanism was decompiler-assisted structural reconstruction
from Mickey's own extracted assembly, recovering identities and types before
control flow. No flag sweep, permuter, allocator instrumentation, shared-header
change, or toolchain change was performed.

The owned executable range is VRAM `0x80053868..0x80054B3C`, ROM
`0x54468..0x5573C`: 4,820 bytes / 1,205 words, ending with the return's delay
slot. There is no post-return padding in that range. The configured candidate
uses the full `main/anim` TU with its existing `-O2 -mips2 -32
-Wo,-loopunroll,0` flags and `NON_MATCHING` enabled.

`generate_ctx.sh` and `mips_to_c.sh` were run. The all-header context failed
on an unrelated flexible array; the full-TU context failed on an existing K&R
header. A context containing only this TU's relevant headers, local layouts,
and declarations produced a draft successfully. The draft served as a
control-flow audit and remains ignored in `m2cfiles/`; it was not imported
into tracked source. These context failures are not source attempts.
The near-match scanner had no useful donor in its 30-percent size window.
Its resident CLI lacked a symbol-size entry, so its existing scanner API was
used with the independently verified 4,820-byte extraction boundary.

Two prerequisite commits bank the identity/type corrections:

- `b462a78f956c23863b4419c19a8416da929b5a6e`: the matched Mickey
  `func_8000572C` establishes two output pointers; the local prototype and call
  now agree. This changes no configured function bytes.
- `c7a77997ae7876e30d79693e8f94b8f49b2cc714`: the flag-width, vertex-stride, and contiguous-vector
  corrections, independently checked against Mickey's accesses.

The type correction restores the halfword collision flags, one 12-byte stride
per float vertex, and a contiguous three-float transform buffer. The later
reconstruction uses typed previous/current/displacement/bounds triples and a
24-byte result record containing two object references, a fraction, and a
normal. It replaces decompiler-only temporaries with phase-local variables,
retains the observed peeled four-entry batches, and recovers bounded index
loops. Callee-specific structure views are explicit at each typed call.

#### Measurements and stopping evidence

All rows come from retained configured full-TU comparisons. Differences are
positional, not an aligned score. The source retained for handoff is attempt
22, which ties attempt 18 on the checkpoint's complete comparison key and
contains the reviewed callee casts and typed pointer-table indices.

- Attempt 0, Re-derived baseline: 1145 words; raw/masked 1197 / 1197;
  frame `0x240`; 61 relocations.
- Attempt 1, Object-list ABI: 1145 words; raw/masked 1197 / 1197;
  frame `0x240`; 61 relocations.
- Attempt 2, Flags, vertex stride, transform vector: 1146 words; raw/masked 1178 / 1178;
  frame `0x240`; 61 relocations.
- Attempt 4–5, Reviewed local lifetimes and result layout: 1163 words; raw/masked 1167 / 1167;
  frame `0x138`; 71 relocations.
- Attempt 6, Vector arrays and axis loops: 1162 words; raw/masked 1180 / 1179;
  frame `0x120`; 71 relocations.
- Attempt 7, Simple counted batches, rejected regression: 975 words; raw/masked 1171 / 1171;
  frame `0x108`; 55 relocations.
- Attempt 8, Restore peeled batches, reduce bookkeeping: 1154 words; raw/masked 1166 / 1166;
  frame `0x110`; 71 relocations.
- Attempt 14, Remainder-to-batch fallthrough: 1131 words; raw/masked 1170 / 1170;
  frame `0x110`; 63 relocations.
- Attempt 15, Live remainder index instead of literal zero: 1127 words; raw/masked 1167 / 1167;
  frame `0x110`; 59 relocations.
- Attempt 17, Ordinary time-step while loop: 1127 words; raw/masked 1167 / 1167;
  frame `0x110`; 59 relocations.
- Attempt 18–22, Relational loops; subsequent reviewed forms tie: 1138 words; raw/masked 1166 / 1165;
  frame `0x110`; 59 relocations.
- Attempt 24, Guarded early-continue traversal, rejected regression: 1143 words; raw/masked 1179 / 1179;
  frame `0x110`; 59 relocations.

Attempts 9–13 and 16 also failed to improve the retained best; their source,
objects, hypotheses, and measurements remain in the ignored attempt archive.
Attempt 3 was excluded after review found that horizontal extent remains live
across the vertical-extent calculation. Attempt 4 corrects that error.
Attempt 23 was excluded because pointer formation moved outside the original
nonempty-range guard; attempt 24 restores that guard but still regresses.
Neither excluded candidate is retained as the handoff source.

Attempt 18 was the last best-residual improvement. Attempts 19–22 (counted
axis loops, compound vector updates, callee views, and typed table indices)
leave the best word geometry, differences, frame, and relocation evidence
unchanged. Attempts 23–24 do not improve the best and establish no new callee
or global identity. This satisfies the assignment's five-consecutive-attempt
stall rule; stopping is not based on an attempt cap or the remaining deadline.

The final candidate has 4,552 bytes: it is 268 bytes / 67 words short, versus
the baseline's 240-byte deficit. Raw differences improve by 31 words and
masked differences by 32; the frame shrinks by 304 bytes to `0x110`, still
24 bytes larger than the target's `0xF8`. Both first mismatches remain `+0x0`.
The size deficit has therefore worsened even though the residual, frame, and
relocation multiplicities improve. This is compiled reconstruction, not an
object-exact, canonical-staged, or ROM-exact candidate, and earns no bytes.

All 29 call sites have the target's callee identities and multiplicities.
All three table identities and their relocation multiplicities also agree:
59 candidate relocations against 59 target relocations, all candidate
identities resolved. Only one offset/type/effective-identity pair aligns
exactly. Equal counts and names are not exact relocation proof. Attempt 24's
three coincident exact sites do not prove a new identity or overcome its
worse residual.

#### Remaining mechanism and next action

The workbench verdict remains `structure-mismatch`. The required
`structure-buckets` guide supplies context parity (lever 4) and the spill-slot
census (lever 6); diagnosis also routes the mixed result through
`constant-audit`, lever 1, "audit the earliest immediate." The concrete wrong
width and stride were corrected. The remaining leading constant difference
is a stack home, and positional comparisons remain displaced by the size gap.

Mickey's callback dispatcher retains three kind constants across the outer
iteration and reloads them after callbacks. The candidate materializes those
constants near comparisons instead. This is a measured code-generation
contrast, not proof that a particular flag or declaration causes it; it must
not be repaired by inserting artificial constant reloads or editing objects.
Normalizing the outer while loop and the later reviewed source forms did not
recover that behavior. The remainder-index reconstruction did explain and
remove the excess table-address relocation pairs.

Next action: request a separately scoped full-TU optimizer/context causal
investigation of callback-constant lifetime and stack homes, starting from
this reviewed baseline. Retain the current flag group and prove any proposed
context/toolchain change across every consumer before considering adoption.
Do not resume the rejected simple batches, literal-zero remainder cursor, or
already-flat source forms without changed evidence. The present assignment
does not authorize replacing structural reconstruction with that investigation.

#### Validation and retained evidence

The final source keeps the `NON_MATCHING` fallback. Only this target changes
among the TU's 55 configured emitted functions; the other 54 retain their
function bytes and function-relative relocation tuples. The normal fallback
sections and relocation tuples are checked against the preserved baseline.
`gmake verify`, `gmake cleanroom`, `gmake check-docs`,
`gmake check-nonmatching-builds`, and `gmake check-scoreboard` are the closing
gates. The ROM proof is for the fallback build, not for the candidate.

Reproduce the candidate comparison with
`tools/wb_compare.sh --summary-json func_80053868`; diagnose the target object
against `build_non_matching/src/main/anim.c.o`. Artifacts are retained under
ignored `build/func53868/attempt-NN/` (source, full-TU object, summary,
hypothesis/compile logs), with the final summary under `build/wb/`.
`tools/finalize_plateau.py` writes the guarded source footer and this shard's
metric header before handoff. No raw draft, instruction listing, object,
ROM slice, or workbench ledger is committed.

#### Prior ABI handoff

The prior handoff at the assignment base remains historical evidence. Its
review of the matched `mathOneFloatRPY` callee establishes exactly two pointer
arguments. The former unsupported third and fourth arguments remain removed;
this reconstruction does not retype or retarget `TrapDanglingJump` calls.

#### 2026-09-11, lane `a9-resident`: missing-run map, no source attempt

Offsets are function-relative target offsets with exclusive upper bounds. Missing and surplus counts are alignment operations, not a proof that that many semantic operations are absent; surplus operations are assigned to the target coordinate at their alignment position rather than comparing unrebased offsets. All interpretation is Tier D, Mickey-only structural inference. No external body or name was adopted, and no candidate was attempted -- the lane's source work was blocked by a stale reopen pin and it then ran out of Codex credits.

105 missing words in 66 runs; 39 surplus words in 32 runs; net 66 words short.
Run lengths: 34 singles, 25 doubles, seven triples. No missing run exceeds
three words.

  - Target region; Missing; Surplus; Interpretation
  - +0x0000–+0x0358: 10 missing, 7 surplus -- Initial object traversal and coordinate setup
  - +0x0358–+0x04B0: 9 missing, 6 surplus -- Iteration setup and moving-shape bounds
  - +0x04B0–+0x07C0: 9 missing, 0 surplus -- Pair traversal, overlap tests and callback state
  - +0x07C0–+0x0930: 5 missing, 4 surplus -- Pair-fraction selection and peeled-loop setup
  - +0x0930–+0x0C94: 20 missing, 20 surplus -- Unrolled coordinate advancement; net-zero schedule cluster
  - +0x0C94–+0x1090: 44 missing, 0 surplus -- Callback dispatch and repeated kind-constant restoration
  - +0x1090–+0x12D4: 8 missing, 2 surplus -- Iteration tail, final coordinate update and return

The callback cluster supplies 44 of the total 66 net missing words. Of its
44 inserted rows, 39 are constant materializations, four are argument moves
and one is address setup. Those inserted rows are only part of the constant
contrast: over the full function the target materializes the three kind
values 57/58/64 at 84 sites, versus seven in the candidate. The missing
triples at +0xC94 and +0x1084, and the later triple at +0x125C, restore all
three constants. Repeated two-word runs between them are pieces of the same
callback-exit pattern, not missing two-statement game effects.

The +0x930–+0xC94 vector cluster has 20 missing and 20 surplus words. Most
insertions are float loads moved earlier within the same repeated coordinate
updates. It must not be filled by duplicating updates or by treating each
missing pair as an absent array element.

Both candidate and target have 29 direct calls with identical callee-name
multiplicities, and both have 59 total relocation records. This rules out a
missing direct-call inventory, not incorrect predicates or arbitrary semantic
errors. Exact offset/type/identity relocation proof remains outstanding.

The only local array is the three-float transform offset. All three elements
are initialized, passed as a contiguous vector to mathOneFloatRPY and read
back. Three is a proved lower bound, not an adjustable guess. The candidate
frame is 0x110 and the target 0xF8: reducing only the array block would require
24 bytes, more than its entire 12-byte allocation. Its tail cannot explain
this frame excess. Increasing it cannot shrink the frame under the simple
frame identity; other allocation/context effects would need a separate proof.

#### 2026-09-12, lane `p17-anim`: first structural checkpoint

This is an intermediate source checkpoint, not a closure. The assignment owns
both large guarded functions in this TU. Current configured flags include
both the loop-unroll limit and the R4300 multiply selection. Re-measurements
therefore supersede older scores without rewriting their historical evidence.

The initial four aligned buckets were 307 exact, 458 naming, 50 immediate,
and 429 structural; positional masked differences were 1168 of 1205 target
words. The current buckets are 412 exact, 418 naming, 45 immediate, and 360
structural. Candidate geometry remains 1139 words, 66 short. Raw differences
are 1177, masked differences 1176, first mismatch at +0xC. These are alignment
and diagnostic improvements, not an object match or new byte credit.

Retained source changes reuse the existing fraction for each vector result,
remove seven transient aliases with non-overlapping uses, and arrange declared
homes according to Mickey's stack accesses. The frame shrinks from 0x110 to
the exact 0xF8. The vector occupies +0xCC through +0xD4; the list bounds and
counts occupy +0xB0, +0xAC, +0xA8 and +0xA4, with pair selection at +0x9C
and the iteration counter at +0x94. This establishes home locations, not equal
traffic: one target-only and two candidate-only spill slots remain, and the
candidate loads the iteration home much more frequently after callbacks.
No dummy storage, volatile access, field-width change or arithmetic
reassociation is introduced. Array bounds and call order are unchanged.

The full-TU instrument identity gate passes for text and relocation tuples.
Procedure ordinal 36 emits phase-one records. On the result-carrier shape,
the three callback kind constants each have total saving 1090 against minimum
caller-register cost 6210, and are split. Accepted forces onto three offered
caller registers recover 56 executable words, reducing the deficit from 66
to 10. The forced output is diagnostic only and is not retained as source.
An accepted force of the proposed vector-address web onto a saved register
was byte-inert; splitting the selected-pair address instead grew the function
by 139 words. Those are bounded observations, not allocator exhaustion.

The result-carrier edit alone adds 33 aligned exact words at unchanged size.
A spelling that separately carries the load, product and sum triggers another
peeled loop and adds 82 words, so it is not retained. Removing the configured
unroll limit in a diagnostic command also changes the loop structure and
regresses; no build flag is changed. The three-float vector lower-bound proof
from the prior handoff survives: the frame repair comes from other locals.

All attempts preserve their source, object, score, first mismatch and compiler
records in private lane scratch. Both candidate and target have 59 relocation
records, with only one exact offset/type/identity pair; relocation parity is
not proved. The larger target's candidate bytes and relocation tuples remain
unchanged. The guarded canonical fallback passes full ROM verification with
expected SHA1 507341c0a40ca3e9a7cee969b396ee53facfb548 after the documented
fresh-worktree overlay-alias regeneration. This verifies the fallback, not the
nonmatching C. Further work will price the callback split and the remaining
address windows before the final plateau handoff.

#### 2026-09-12, lane `p17-anim`: retained partition and causal plateau

This section supersedes the intermediate checkpoint above. All source remains
`NON_MATCHING`; no executable bytes are promoted. Measurements use the complete
configured TU, including `-Wab,-r4300_mul`. The dispatch's large-function delta
of -324 is bytes, not words; sizes below always state their units.

The retained small candidate is attempt 21: 1145 of 1205 words, 60 words short.
Baseline to retained aligned buckets are 307 to 457 exact, 458 to 393 naming,
50 to 34 immediate, and 429 to 347 structural. The structural bucket includes
unpaired rows. Positional raw differences are 1169 to 1170; masked differences
are 1168 to 1169. Alignment improves substantially while positional scoring
remains poor because this is still a size mismatch. First mismatch is +0xC.
Both sides have 59 relocations, but only three offset/type/identity tuples
agree at their current positions. This is not relocation-exact.

The following address partition is Tier D inference from Mickey's own
instructions and measured alignment, not a claim of semantic equivalence.
Bounds are target-relative and upper-exclusive. Each row gives retained
naming/immediate/paired-structural counts, target-only and candidate-only
counts, and total residual before to after. Candidate-only rows are assigned
to their target alignment coordinate; they are not filtered by their original
candidate offsets. Register census reports GPR then FPR coherence and the
number of consistent address windows, using bank-aware `register_census.py`.

- +0x0000–+0x0358: 51/4/31, missing 11, surplus 7; residual 161 to 104.
  Initial list traversal and coordinate setup: recovered declared homes and
  phase-local aliases improve this region. Census 74 percent/6 windows and
  100 percent/1 window. A GPR two-cycle is local, not a function-wide phase.
- +0x0358–+0x04B0: 33/2/19, missing 9, surplus 6; residual 75 to 69.
  Moving bounds: float pool rotation and list-index colouring dominate.
  Census 93 percent/2 and 97 percent/2, with a six-register float cycle.
  This region is a useful isolated rotation lead; the whole-function census
  conceals it. It has not been proved reachable by a source draw lever.
- +0x04B0–+0x07C0: 40/5/29, missing 7, surplus 0; residual 95 to 81.
  Pair traversal and overlap calls: per-web index allocation and branch/call
  setup remain. Census 73 percent/2 and 100 percent/1. Two GPR cycles occur
  within a non-functional mapping and do not establish one ring phase.
- +0x07C0–+0x0930: 33/0/30, missing 7, surplus 6; residual 78 to 76.
  Pair selection and peel setup: selected-pair carrier and float result
  lifetimes differ. Census 97 percent/2 and 54 percent/4; local GPR two-cycle,
  no float cycle. Splitting the selected-pair web increased the earlier shape
  by 139 words; deleting its source carrier on the retained shape added 82.
- +0x0930–+0x0C94: 147/0/29, missing 1, surplus 6; residual 230 to 183.
  This is the largest retained semantic window. Repeated vector products and
  sums have changing float carriers, plus peeling/scheduling differences.
  Census 76 percent/4 and 59 percent/19. Of 158 differing float operand slots,
  two different candidate registers each map to the target result carrier
  26 times. The two detected float cycles are not a coherent rotation.
- +0x0C94–+0x1090: 16/19/74, missing 44, surplus 0; residual 179 to 153.
  Callback exits split three kind constants and reload the iteration home.
  Census 89 percent/3 and 100 percent/1. Several tiny cycles include fixed
  argument/zero registers and must not be interpreted as temporary-ring draws.
- +0x1090–+0x12D4: 73/4/23, missing 7, surplus 1; residual 119 to 108.
  Iteration join and final coordinate copies combine float carrier changes
  with callback constant restoration. Census 61 percent/4 and 89 percent/3;
  neither bank has a closed dominant cycle in this region.

`residual_map.py` was run first, including restricted region measurements.
Its current register-field decoder skips COP1 arithmetic and labels FP memory
operands as GPRs. Its address/bucket counts were retained, but register cycles
above were recomputed with the existing bank-aware census over saved streams.
No tooling change is included in this lane.

The vector result carrier alone bought 33 aligned exact words at unchanged
size on attempt 2. On the recovered-home shape, carrying product and result
separately avoids the large extra peel caused by carrying the fraction load.
A load-plus-result spelling adds 83 words; load/product/result adds 82;
removing the explicit peeled batches loses 107 words versus the original
checkpoint. These retained diagnostic objects price a loop-shape boundary,
not a shortage of game updates. The final vector region has only one missing
word and six surplus, so inserting updates cannot solve its 147 naming rows.

The other priced mechanism is callback constant splitting. In the final
instrumented source, the three outer signed constants each have total saving
90 versus minimum offered caller cost 6210, with 58 counted blocks. Accepted
phase-one forces for procedure ordinal 36, webs 623/624/625, record colours
7/8/9 respectively. The forced object is scored directly: 1193 words,
12 short, aligned 492/404/22/323. It restores 48 words and reduces the callback
region from 153 residual rows to 98, and its missing rows from 44 to three.
This is a decision price, not adopted compiler output. Changing only the
inner comparisons to unsigned constants creates separate IR identities and
adds 12 words to the counted-exit shape, but the outer cost remains 6210.
It therefore improves source allocation without solving the measured split.

Retained source beyond commit `9efc0945` carries the vector product explicitly,
uses equality exits for bounded counted loops, and makes only the three inner
kind constants unsigned. Counters start within bounds and advance to their
known count or remainder; four-entry loops retain their existing peeling and
remainder relationship. Equality comparisons of a signed halfword to these
positive constants retain their truth values after unsigned promotion,
including negative halfwords. Float operation association, object stores,
list order, callbacks, and the three-element transform buffer are preserved.

The exact 0xF8 frame is retained, but matching frame size does not close frame
traffic: candidate slots +0x64 and +0x74 replace target +0x7C, and the +0x94
iteration home is loaded 21 times versus two. The inherited three-element
array lower-bound proof survives. The inherited simple-loop and default
unroll alternatives regress when re-tested. Earlier relational-spelling
closure does not survive the recovered-home shape: attempt 14 changes the
object and improves alignment. The two-pointer callee ABI proof was retained;
no external donor body or new external name was used.

After attempt 21, deleting the pair carrier regressed. Attempts 29, 30 and 31
(callback region wrapper, preincrement join, address-taking iteration reload)
are each byte-identical to the retained shape. They establish no new identity,
eliminate no new mechanism, and improve no residual. This meets ADR 0018's
three-attempt stall rule for the callback-join source line. Attempt 32 then
re-priced the existing constant-force mechanism on the final shape; it does
not supply a new adoptable spelling. No force or inert wrapper is retained.

Next action: investigate the isolated moving-bounds float rotation separately
from the vector region's per-iteration float allocation. For the missing-word
problem, determine why the outer constants still inherit 58 loop blocks after
the inner type split; statement-order or ratio sweeps without that decision
record are not warranted. Retain the exact frame and the three-element array.

The configured candidate compiles. `tools/finalize_plateau.py` refreshed its
footer and this header. `tools/gates.sh` and `gmake check-nonmatching-builds`
are run before committing. The normal `gmake verify` proof checks the guarded
assembly fallback; it does not certify this C candidate. Source/object/metric
snapshots, forced records and address maps remain in the external lane scratch
archive, never in Git.

#### Completed closing validation

The source plateau is banked in `d727307c`. `tools/gates.sh` passed all four
checks: verify, cleanroom, check-docs and check-tooling. `gmake verify` printed
matching expected and built SHA1 `507341c0a40ca3e9a7cee969b396ee53facfb548`.
`gmake check-nonmatching-builds` passed. This ROM proof is for the guarded
fallback, with no claim that the nonmatching candidate is ROM-exact.

<!-- plateau-handoff:func_80053868:end -->
