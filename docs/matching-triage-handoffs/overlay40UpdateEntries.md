<!-- plateau-handoff:overlay40UpdateEntries:start -->
### `overlay40UpdateEntries` plateau handoff

- source: `src/overlays/o040/overlay40UpdateEntries.c`
- score: 44/46 words
- frame: frameless
- relocations: 4
- first mismatch: +0xC
- summary: A fidelity-gated native scheduler DAG proves the count initialization and object-table address completion are independent ready nodes selected by logical source line; no dependency edge authenticates a new source form.

Fresh dependency-edge reproof on canonical base `0df1962d37471fe5d81d38a31b0a65df8b7b7da8`:

- Configured V0 remains the exact 184-byte / 46-word frameless candidate, with 44/46 positional words and the first mismatch at `+0xC`. It emits four relocation records; the object-table low-half record is displaced by the adjacent swap, so strict preflight correctly leaves its runtime identity unresolved while scalar workbench comparison remains available.
- One native IDO 5.3 scheduler capture was made with the assembler's print-only trace option. A stock-versus-traced fidelity gate passed `.text`, `.data`, `.rodata`, relocations, and symbols; only unstable debug metadata prevented whole-file equality.
- The authenticated initial-block DAG contains two independent address-materialization pairs, each with only its own high-half-to-low-half edge. Once the entry-table pair completes, the loop count and object-table low half are simultaneously ready. The scheduler selects the loop count solely because its logical source line precedes the object-table line; there is no data-dependency edge between the two nodes.
- The reopen authorized source mutation only from an authenticated dependency edge. Because the capture proves that edge absent, no source variant was attempted. The completed flag, line, comma, declaration-order, and permutation families were not repeated.
- Next lever: a new producer-authenticated UGEN source/edge mechanism that changes the upstream DAG without fabricated dependencies, or a demonstrably different scheduler/toolchain mechanism. Do not repeat source-line or ordering families.
<!-- plateau-handoff:overlay40UpdateEntries:end -->
