<!-- plateau-handoff:func_overlay_022_F0000D30_1878E38:start -->
### `func_overlay_022_F0000D30_1878E38` plateau handoff

- source: `src/overlays/o022/overlay22RemoveObject.c`
- score: 43/91 words
- frame: 0x28
- relocations: 12
- first mismatch: +0x10
- summary: Indexed shift-loop controls move draws but regress alignment or add a word; retain the guarded pointer-loop baseline and distinct array identities.

#### 2026-09-13, lane `j1`: indexed shift-loop draw ordering

No per-symbol shard existed on arrival; the source's bounded plateau was
read and the assignment gate returned base-only. Fresh configured baseline
has 91 words, delta zero, frame 0x28, masked 43, first +0x10. All 43 aligned
rows are naming differences; 48 words are exact. Procedure 0 has 21 draws
and 215 emissions. Static relocation tables are twelve candidate versus six
extracted target records, with two matching tuples. The source's historical
runtime-identity claim was not independently reauthenticated in this pass.

Two source experiments retain stock/traced full-TU text identity and private
source/object/census pairs. First, replace the shift loop's explicit walking
pointer with an index and indexed array accesses, retaining the separately
named end-array identity. The loop performs the same reads, writes and
pointer-bound test. It stays 91 words and frame 0x28, but naming differences
rise to 46. Total draws remain 21; one draw moves from cursor initialization
to the loop head, the ring sequence changes, and emissions rise to 220.
This demonstrates a movable schedule, but the aligned result regresses.

Second, on the indexed shape, inline the end-pointer expression at its only
use. Two draws move from the removed end definition to the loop head and
the sequence changes again, while total draws/emissions remain 21/220.
The candidate grows one word and scores 75. Alignment is 38 exact, 45
naming, zero immediate and nine structural rows including one extra word.
Neither shape is adopted. The original pointer loop and both distinct array
identities remain; they are not merged on the basis of a shared address guess.

Stop under ADR 0018: the index and generated-bound hypotheses are eliminated
at the required extent, alongside the source's prior lifetime/loop controls.
A new source mechanism must alter the allocation order without these measured
costs. This does not prove all schedules impossible. Commands: assignment
gate, configured stock/traced compilation, draw census before/after,
residual_map.py --object --against, finalize_plateau.py and tools/gates.sh.
The canonical ROM uses the assembly fallback; no new bytes are credited.
<!-- plateau-handoff:func_overlay_022_F0000D30_1878E38:end -->
