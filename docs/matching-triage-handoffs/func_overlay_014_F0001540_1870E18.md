<!-- plateau-handoff:func_overlay_014_F0001540_1870E18:start -->
### `func_overlay_014_F0001540_1870E18` plateau handoff

- source: `src/overlays/o014/func_overlay_014_F0001540_1870E18.c`
- score: 12/188 words
- frame: 0x80
- relocations: 26
- first mismatch: +0xDC
- summary: Outer while/generated stride leaves 12 naming rows at exact geometry; remaining pool cycle and relocation identities are unresolved.

#### h2 outer-loop and generated-stride receipt (2026-09-13)

Reproduced from a sibling lane's unintegrated source result and recompiled at
this lane's configured flags. The guarded candidate is 752 bytes / 188 words,
with zero size delta and an exact 0x80 frame. The aligned residual improves from
129 exact, 33 naming, seven immediate and 17 structural rows to 176 exact and
12 naming rows, with no surplus or missing words. The first masked mismatch is
+0xDC; 14 raw words differ. The 27-draw sequence is unchanged, while the
emission census moves only the outer-entry and stride statements.

The source change replaces the outer goto loop with a guarded while using
`index < count` and `remaining > 0`, removes the declared `entryOffset`, and
uses indexed entry accesses so IDO generates the stride. It also preserves the
existing inner text loop and statement order. The result closes the prior CFG,
immediate, and generated-stride residuals; the remaining 12 rows are one
caller-pool cycle (`v0`/`v1`) in the two body windows. Relocation identities
remain unauthenticated (26 candidate records versus 16 named target sites), so
this is a NON_MATCHING plateau, not a promotion.

Validation: corrected source draw census with `proc=0`, before/after
`draw_census.py --compare`, and `residual_map.py`; no colour sweep was repeated.
<!-- plateau-handoff:func_overlay_014_F0001540_1870E18:end -->
