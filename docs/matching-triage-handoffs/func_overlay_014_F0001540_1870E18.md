<!-- plateau-handoff:func_overlay_014_F0001540_1870E18:start -->
### `func_overlay_014_F0001540_1870E18` plateau handoff

- source: `src/overlays/o014/func_overlay_014_F0001540_1870E18.c`
- score: 12 differing words
- frame: 0x80
- relocations: 26
- first mismatch: +0xDC
- summary: While loop and generated stride improve 82 to 12 naming rows with exact geometry. Three final controls preserve the caller-pool swap and draw sequence.

#### g1 loop and generated-stride receipt (2026-09-13)

The retained candidate improves from 82 to 12 relocation-masked differences at
752 bytes and zero size delta. It has 176 aligned exact and 12 naming rows,
no immediate or structural rows, and no surplus or missing words. The frame is
exact at 0x80. First masked difference is +0xDC; raw differences are 14. The
configured candidate has 26 relocations; the assembled target represents 16
named sites. This packet does not establish exact relocation identities or a
linked C match. The guarded ROM rebuild therefore proves the assembly fallback.

The baseline stock and instrumented complete-TU text agree. Procedure 0 has
27 draws and 258 emission events. The inherited 70-row alignment summary was
stale: the current baseline has 129 exact, 33 naming, seven immediate and 17
structural paired rows plus two words unique to each side. The near-match
oracle nominated a permitted JFG overlay routine at 0.329 masked four-gram
similarity; no donor source was read or incorporated in this packet.

Attempt 54 changed the outer goto into a guarded do-while on the same physical
lines. The draw sequence stayed fixed; alignment improved to 143 exact rows,
but size fell by eight bytes. Attempt 55 used an outer while condition with the
existing index comparison and computed the entry stride inside the loop. It
reached 34 masked differences and exact extent, with 154 aligned exact rows,
but its frame grew to 0x88. The outer-loop source shape corrected the inherited
remaining/y and saved-byte/cursor swaps. Attempt 56 moved the stride initializer
outside that loop; it again lost eight bytes and was rejected. Attempt 57
removed the redundant stride increment and reproduced attempt 55 exactly.

Attempt 58 removed the declared stride carrier and generated the two byte
offsets from the index. This recovered the 0x80 frame and closed every remaining
structural and immediate row, leaving the 12-row caller-pool residual. Attempt
59's typed entry indexing is identical. Relative to the original baseline,
the while-entry, selected-entry and loop-tail line censuses change, but the
complete 27-register draw sequence is unchanged. Relative to attempt 57,
removing the offset carrier removes its definition event without changing the
draw sequence. Equal draws therefore do not imply equal homes or allocation.

An unforced allocator capture reproduces the 12-row candidate: the generated
expression web wins v0 before the entries-base web, which receives v1; the
split index also receives v0. This is a measured p1 conflict, not a remaining
scratch-ring rotation. No colour sweep was repeated. Attempt 60 named the
entry too early, across a dispatch call; it was rejected for load-order semantics
as well as changed size. Attempt 61 restored the post-call load but still lost
20 bytes and regressed alignment. Attempts 62–64 named the loop-header snapshot,
moved that declaration before index, and used an unsigned access index in its
proved nonnegative domain. Each is byte-identical to the 12-row candidate, with
27 draws: three consecutive controls supply no improvement or new mechanism.
Stop on that stall. Attempt 65 removes diagnostic comments and uses ordinary
typed indexing, preserving physical line count and reproducing the same object.

All candidates, objects, censuses, unforced allocator log and residual maps
remain ignored under `build/g1/func_overlay_014_F0001540_1870E18/`. The source
retains the while loop and generated stride. A next packet needs to remove or
change the interfering generated expression web while preserving the now-exact
geometry, then authenticate relocation binding and linked owned bytes.

Validation: configured compilation through `build/g1/measure.py`, per-line
`draw_census.py --compare`, retained-object `residual_map.py --object --against`,
unforced `force_lattice.run_cell`, `tools/gates.sh verify check-nonmatching-builds`,
and `tools/finalize_plateau.py --commit`. No newly matched bytes are claimed.

- score: 12/188 words
- frame: 0x80
- relocations: 26
- first mismatch: +0xDC
- summary: Outer while/generated stride leaves 12 naming rows at exact geometry; remaining pool cycle and relocation identities are unresolved.

#### h2 outer-loop and generated-stride receipt (2026-09-13)

Reproduced from a sibling lane's unintegrated source result and recompiled at
this lane's configured flags. The guarded candidate is 752 bytes / 188 words,
with zero size delta and an exact 0x80 frame. The aligned residual improves from
129 exact, 33 naming, seven immediate and 17 structural rows to 176 exact and
12 naming rows, with no surplus or missing words. The first masked mismatch is
+0xDC; 14 raw words differ. The 27-draw sequence is unchanged, while the
emission census moves only the outer-entry and stride statements.

The source change replaces the outer goto loop with a guarded while using
`index < count` and `remaining > 0`, removes the declared `entryOffset`, and
uses indexed entry accesses so IDO generates the stride. It also preserves the
existing inner text loop and statement order. The result closes the prior CFG,
immediate, and generated-stride residuals; the remaining 12 rows are one
caller-pool cycle (`v0`/`v1`) in the two body windows. Relocation identities
remain unauthenticated (26 candidate records versus 16 named target sites), so
this is a NON_MATCHING plateau, not a promotion.

Validation: corrected source draw census with `proc=0`, before/after
`draw_census.py --compare`, and `residual_map.py`; no colour sweep was repeated.
<!-- plateau-handoff:func_overlay_014_F0001540_1870E18:end -->
