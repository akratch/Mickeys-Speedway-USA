<!-- plateau-handoff:func_80030610:start -->
### `func_80030610` plateau handoff

- source: `src/main/sched.c`
- score: 87 differing words
- frame: -0x90
- relocations: 13
- first mismatch: +0x0
- summary: Size closed at 192/192 by spelling the first 0x80000000 site as (u32) D_80000000. Frame is 0x90 against 0x98 and message is homed 8 too high.
#### 2026-09-09: an address materialised once too few, not a body difference

The candidate is 193 words against 192 and its frame is `0x90` against the
target's `0x98` -- it is *smaller* by eight bytes while being one instruction
longer, so the two are not the same question. The instruction the target has
and the candidate does not is a second `lui` into `s3`: the target materialises
the literal `0x80000000` there at `+0x17C` and then materialises
`%hi(D_80000000)` into the same register four words later, where the candidate
has only the literal in `at`. The source spells the first of those three
`0x80000000` sites as a plain constant (`printStart = commandStart +
0x80000000`) and the other two as `(u32) D_80000000`; making all three the
symbol is the untested lever, and the fallback's unpaired `lui` at the first
site is exactly the false-residual signature in `docs/ido-learnings.md`, so
score that site against the linked ELF rather than the raw word.

#### 2026-09-09: the size is closed; what remains is a home census

The extra instruction was an address materialised once too few. The source
spelled the first of this function's three `0x80000000` sites as a literal
(`printStart = commandStart + 0x80000000`) and the other two as
`(u32) D_80000000`. The target materialises that symbol's high half into `s3`
*twice* -- once where splat could not pair it, so the fallback prints a bare
`lui` of a plausible RAM address, and once as a proper `%hi`, with the matching
`%lo` folded into a later `lb` -- and a literal constant can only ever produce
one. Making all three the symbol gives 192 words against 192 and drops the
positional residual from 113 to 87. Score that first site against the linked
ELF, not the raw word: it is the unpaired-`%hi` false residual.

The remaining question is the declared-home census, and the target's own slots
give it. Target: `savedCommands` at `0x80..0x8F`, `done` at `0x7C`, `message`
at `0x70`, frame `0x98`. Candidate: the same aggregate and the same `done`,
`message` at `0x78`, frame `0x90`. So the target's block has two reserved
slots above the aggregate (`0x94`, `0x90`) and two more between `done` and
`message` (`0x78`, `0x74`).

Measured, and the reason the obvious fix does not work: in this unit only a
*spilled* local reserves a slot. Twelve placements of the ten colour-only
scalars around the aggregate and around `done` are byte-identical at frame
`0x90`; moving `done` itself above the aggregate does move the frame to `0x98`,
but then `done` homes at `0x94` and `message` at `0x7C`, both wrong, for a
worse-in-kind 81-word residual. So the four missing slots are four more
genuinely spilled values, which is a structural difference in the source, not a
declaration-order sweep. Do not repeat the order sweep.

<!-- plateau-handoff:func_80030610:end -->
