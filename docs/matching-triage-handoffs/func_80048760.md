<!-- plateau-handoff:func_80048760:start -->
### `func_80048760` plateau handoff

- source: `src/main/fx.c`
- score: 20 differing words
- frame: 0x48
- relocations: 4
- first mismatch: +0x90
- summary: spelling the alignment pad twice instead of caching it makes the prologue exact, 23 -> 20. Residual is one temp-numbering rotation over the extent shifts and the tail halfword loads.

The donor counterpart `wakeSetupRipple` is still a `GLOBAL_ASM` placeholder,
confirming the prior handoff's finding at this newer pinned revision.
Configured baseline: 484 bytes, 121 words, zero size delta, 23 raw and masked
differences from +0x08, frame 0x48; all four relocations agree in relative
offset, type and symbol identity. The comparator says `structure-mismatch`,
but diagnosis refines this to `mixed(schedule:4, register:19)`, playbook
`g0-schedule-probe`, lever `line-order`. This is a diagnostic routing result,
not new donor evidence authorizing a repeat of exhausted source forms.

The best scored lattice rows retain 23 differences at O2/MIPS2. The O2/MIPS1
row that improves the smaller batch target regresses this function to 47
differing words and 488 bytes. Next lever: a published wakeSetupRipple C body
or newly authenticated source line topology for the initialization schedule,
followed by the configured full-TU line-order check under a fresh assignment.

PROVENANCE audit (2026-09-08): Jet Force Gemini public decomp, `src/fx.c`
at `efd5abb1c79636e297b831f7c2d5bf47eac39c0c`. This TU last changed at
`6ff6177` (2026-08-09), adding an include and a scratch reference for the
unrelated `setupClearScreen`. The companion `src/fx.h` supplies no target
implementation. No new donor body was available or adopted; Mickey remains
the authority.

One requested lattice for the unchanged full TU evaluated 119 rows: 53 scored,
66 rejected by the asm-processor wrapper (30 unsupported MIPS1 optimization
combinations, 36 lacking a supported optimization/debug selector). This
function reused that content-addressed cache with `--rescore`, compiling
zero new rows. Coverage is partial; no scored row is exact. No TU flag
change is proposed.

Stopping evidence: zero source attempts, one requested TU flag experiment.
The pinned donor provides no new implementation for this target, so the
committed source experiments remain exhausted. This is the assignment's
early-exhaustion stop, not a five-attempt stall. Any new source-shaping
mechanism requires fresh evidence and assignment authorization.

Ignored evidence remains in `build/m14-fx-batch/` and `build/wb/`: baseline
source/object, summaries and diagnoses, relocation counts and sweep reports.
Commands include `wb_compare.sh --summary-json`, workbench `diagnose` and
`guide`, compile-free `flag_sweep.py --rescore`, and `finalize_plateau.py`.
The guarded C remains NON_MATCHING; the canonical assembly ROM proof gives
no linked-C or ROM-exact credit to this candidate.

<!-- plateau-handoff:func_80048760:end -->
