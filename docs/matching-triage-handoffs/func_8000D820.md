<!-- plateau-handoff:func_8000D820:start -->
### `func_8000D820` plateau handoff

- source: `src/main/track.c`
- score: 65 differing words
- frame: frameless
- relocations: 6
- first mismatch: +0x3C
- summary: m2c confirms existing identities and CFG; zero new attempts. Next: dirty-flag and shared-count lifetime evidence.

#### Mickey m2c structural audit, TU batch 2026-09-08

- Assignment base: `d0bdcaf16a9b0a7fb7371eb435c0940cb33f5c19`; the fresh assignment gate returned `base-only` for the exact guard and handoff pins before this audit.
- Configured full-TU measurement remains 84 candidate versus 86 target words, 65 raw and relocation-masked differing words, first workbench mismatch `+0x3C`, and frameless on both sides. The owned range remains 344 bytes without padding. The canonical static relocation comparison independently proves all six offsets, types and stable identities exact.
- Workbench verdict `structure-mismatch`, mechanism `mixed(structural:2, register:52)`, playbook `structure-buckets`. Lever 4 (configured flag/context parity) is satisfied by the actual full-TU build and an accepted self-comparison of the IDO-preprocessed compiler input. Lever 24 finds no collapsed preprocessor condition. The other structural levers identify no wrong immediate, spill slot or call-hoisting hypothesis here.
- Fresh context-aware and untyped m2c drafts from Mickey's own extracted function confirm the three existing globals, signed segment/light counts, signed lighting mode, source/mask record and halfword mask accesses. The positive full-colour copy and negative dirty-mask copy have the same blocks, cursor strides and mode updates as the retained candidate. No new call or global identity is present.
- The draft's narrow mask plus separate shifted temporary already occurs in the historical pre-plateau source. Its count and cursor forms fall within the explicitly exhausted width, lifetime, remaining-counter and loop families. The draft is not new original declaration evidence. Its printed pointer increments must be interpreted as target byte strides, and its post-decrement loops checked against the branch delay slots before treating it as compilable source.
- ADR 0018 early stop: zero source attempts. The authorized reconstruction audit produced no changed identity, type or CFG input that reopens an exhausted source family. The remaining deficit is two words plus register allocation; no allocator or permutation mechanism was substituted for structural reconstruction. The TU-requested flag command failed closed before compilation because this symbol has no sized symbol-table owner; the batch uses an eligible sized sibling instead.
- Next concrete lever: authenticated original lifetime evidence for the dirty-flag temporary and the count shared by the full-copy/group paths, sufficient to reopen one specific exhausted family. Preserve the current guarded candidate until that evidence exists.
- Commands and artifacts: configured `wb_compare.sh --summary-json`, workbench `diagnose` and `guide structure-buckets`, both m2c drafts, `candidate_context.py` self-comparison, `context lint`, `function_preflight.py`, `reloc_surface.py compare`, `finalize_plateau.py`, and passing `gmake verify` (US SHA1 `507341c0a40ca3e9a7cee969b396ee53facfb548`). Exact source, object and summaries are retained under ignored `build/wb/tu-track/`; no ROM-derived instruction rows are tracked. No matching credit is added.

#### Pinned donor audit, 2026-09-08

- Assignment gate: `base-only` at `32a75d648e8954f7455897fb8f16a8ef5f05df11` for this exact symbol and source path.
- Jet Force Gemini `efd5abb1c79636e297b831f7c2d5bf47eac39c0c` has 12 C implementations and 53 assembly placeholders in `src/track.c`. Its implemented routines do not supply this target's body. The closest masked track-object hit, `func_80013DCC` (Jaccard 0.3200), corresponds by verified TU order to the still-assembly `func_8001400C_14C0C` in that pinned source. This is diagnostic structural context, not an adopted name, ABI identity, or exact donor match.
- Fresh configured full-TU measurement: 86 target words and 84 candidate words; 65 raw and 65 relocation-masked differences; first +0x3C; target frame frameless, candidate frame frameless. There are 6 candidate versus 6 target relocation records, with 6 stable identities at matching offsets/types.
- Workbench verdict `structure-mismatch`, playbook `structure-buckets`. The existing diagnosis still requires source-shape/lifetime evidence; the donor-only reopen does not authorize substituting a flag, allocator, or permuter mechanism.
- ADR 0018 stop: zero new source attempts for this target. The pinned source disproves the new matched-donor-C hypothesis. No unchanged flag lattice or previously exhausted source family was repeated. The compiled candidate is unchanged and remains behind `NON_MATCHING` and its original `GLOBAL_ASM` fallback; zero new matching bytes are credited.
- Next concrete lever: a published matched counterpart with an authenticated ABI and useful source lifetimes, followed by Mickey-specific field, branch and call proof. The remaining general levers below require separately authorized changed evidence.
- Commands: `tools/wb_compare.sh --summary-json func_8000D820`, workbench `diagnose` on the configured full-TU object, the track-object masked skeleton audit, `tools/finalize_plateau.py`, `gmake cleanroom`, and `gmake check-docs`. Source, object, scores, first mismatch and previous handoff are retained under ignored `build/wb/`; no instruction rows are included here.

#### Prior committed plateau evidence (historical)


- source: `src/main/track.c`
- score: 65/86 words
- frame: frameless
- relocations: 6
- first mismatch: +0x3C
- summary: Two-instruction structural residual; next inspect original declaration lifetimes and dirty/source cursor types.

#### Proven boundary and build context

- Base: `c4796c73e615a24c4368718fc5efd02c59112180`.
- Ownership: resident text at VRAM `0x8000D820`, ROM `0xE420`, through `0x8000D978`; target size `0x158` bytes or 86 instructions.
- ABI: `void func_8000D820(void)`; no arguments, no calls, and no target stack frame.
- Translation-unit flags: `-O2 -mips2 -32 -Wab,-r4300_mul`.
- Target relocations: six static records, comprising HI16/LO16 pairs for `D_800792E8`, `D_80079308`, and `D_800C95B4`.

#### Baseline and retained candidate

- Baseline: 84 raw positional differences and 84 relocation-masked positional differences; 84 candidate instructions against 86 target instructions; candidate frame `0x8`; first mismatch `+0x0`.
- Retained candidate: 65 raw positional differences and 65 relocation-masked positional differences; 84 candidate instructions against 86 target instructions; frameless; first mismatch `+0x3C`; 29 opcode differences and two alignment gaps.
- Measurable gain: 19 fewer positional word differences, removal of the spurious frame, and an exact 15-instruction prefix. The candidate also corrects the dirty-mask cursor semantics by preserving source and destination advancement between 16-light groups and advancing the mask cursor by two bytes.
- Relocation proof: `tools/reloc_surface.py compare` reports all six candidate records exact by offset, type, and stable identity. The workbench's two alias-name cautions are therefore metadata aliases rather than relocation-surface differences.
- Verdict: plateau, not exact. The retained candidate remains behind the existing `NON_MATCHING` fallback and contributes no matching credit.

#### Exhausted bounded attempts and next lever

- Exhausted approximately ten coherent hypotheses covering counter widths, mask widths, cursor types, variable lifetimes, separate remaining counters, pre/post-decrement loop forms, no-unroll diagnostics, and source-expression association.
- A single bounded 119-mode flag lattice found no improvement over the configured `-O2 -mips2 -32` family. No permuter was run.
- The remaining residual is two instructions short and then dominated by register allocation. The next useful lever is original declaration/lifetime evidence for the dirty flag and persistent source cursor after first re-deriving the `+0x3C` control-flow expression; further flag or loop-spelling sweeps are exhausted.

#### Jet Force Gemini utility

- JFG's assembly-only `func_80013DCC` in `src/track.c` is the unique useful near donor, with masked 4-gram Jaccard `0.3200`; the next donor scored only `0.0429`.
- This Mickey reconstruction gives JFG maintainers a concrete source scaffold for that function: a segment loop, positive-mode full packed-colour copy, negative-mode dirty-mask copy, persistent per-group cursors, and the final lighting-mode update. JFG must retain its own byte dirty array, `0x48` segment stride, and fields at `+0x32`, `+0x34`, and `+0x24`; Mickey instead uses 32-bit dirty flags, a `0x40` stride, and fields at `+0x2E`, `+0x30`, and `+0x20`.
- This is structural donor guidance only. The Mickey C is still non-matching and must not be represented to JFG maintainers as exact source.
<!-- plateau-handoff:func_8000D820:end -->
