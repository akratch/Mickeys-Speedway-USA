<!-- plateau-handoff:func_8005807C:start -->
### `func_8005807C` plateau handoff

- source: `src/main/flash_58C10.c`
- score: 27/29 words
- frame: 0x20
- relocations: 16
- first mismatch: +0x6C
- summary: Fresh remeasurement confirms 27/29 words, exact 0x20 frame, and 16 aligned relocation identities; only an unreferenced duplicate-return tail remains.

Configured remeasurement on base `bf24e69b5de499156514282b1668071620b135ed`
confirms the historical scalar evidence against the current source:

- the resident owner is `0x8005807C..0x800580F0` / ROM
  `0x58C7C..0x58CF0`, exactly `0x74` bytes with no boundary ambiguity;
- configured IDO emits 27 instructions versus the target's 29 with the same
  `0x20` frame. The first 27 instructions are byte-identical; the target then
  carries an unreferenced two-instruction duplicate-return tail at `+0x6C`;
- target and candidate each carry 16 relocations. All records align by offset
  and type, and all 16 symbol identities resolve without ambiguity;
- the guarded body remains the small Game Boy Pak ID reader already derived
  from Mickey's fields, globals, and calls. The assembly fallback remains
  canonical, and this maintenance pass changes no function body.

The next bounded mechanism is a permuter sweep over source-equivalent branch
and return spellings that may inhibit IDO's shared-epilogue fold. Preserve the
fallback unless untouched configured IDO output becomes byte-identical and
the linked range plus full ROM verify.
<!-- plateau-handoff:func_8005807C:end -->
