<!-- plateau-handoff:func_80034448:start -->
### `func_80034448` plateau handoff

- source: `src/main/textures_35024.c`
- score: 54 differing words
- frame: 0x48
- relocations: 53
- first mismatch: 0x0
- summary: Consumed loadSize carrier removes surplus word; 54 masked differences remain with frame/home and schedule deficits.

Fresh configured evidence on 2026-09-05 proves the resident owner at
`0x80034448..0x800347A0` / ROM `0x35048..0x353A0`: 856 executable bytes,
214 target instructions, and no padding. The target and retained candidate
both use a `0x50` frame and the same `s0`, `s1`, and `ra` save slots. Eighteen
overlay call relocations corroborate the resident identity.

The original cache-only scaffold was 25 instructions with four relocations and
first differed at `+0x4`. Adapting DKR's public `load_texture` control flow and
the near-identical public JFG object recovered both asset tables, cache lookup,
fixed-address and ordinary allocation, compressed loading, decompression,
cache insertion, display-list initialization, and both overflow returns. A
typed cache produced 209 instructions and a `0x58` frame; using Mickey's proved
flat two-word cache restored the complete 214-instruction topology. An explicit
maximum-size `else` restored the target's otherwise redundant branch, while
letting the repeated aligned address remain a compiler temporary restored the
target frame.

The retained candidate is 215 instructions, has 118 positional differences,
normalized distance 58, a 139/215 aligned-row score, and function SHA-1 prefix
`4ff58b7dd963`. Its first 22 instructions are exact. All 53 candidate static
relocation identities resolve; 37 offsets, types, and identities align with the
53 target records. The single structural surplus is a move into the second
DMA argument before the aligned address is stored. Workbench reports the first
upstream divergence at ugen temporary slot 8 (the asset-table address rotates
from the target carrier), followed by one pool-lane divergence at slot 59.

Ten bounded forms covered the typed and flat caches, direct and named asset
table entries, signed pointer versus integer aligned addresses, explicit and
implicit maximum-size branches, reuse of the dead asset index, repeated
conditional expressions, declaration reordering, and the donor's literal
`+ 0` cache macro. Reusing the asset index gives the exact frame but worsens
normalized distance to 80; repeating the conditional grows to 252
instructions; naming the asset-table entry restores the `0x58` frame. Reopen
only with producer trace evidence for the missing declared web and the ugen
slot-8 pop. Do not repeat those ten families or treat the exact frame and
resolved relocations as a match.

#### Surplus word isolated, 2026-09-09 (no gain)

A shift-tolerant alignment that normalises registers, stack displacements and
branch destinations now leaves exactly **one** structural difference in the
whole function: a copy into the second argument of the first decompression-path
`piRomLoadSection` call. Everything else the positional score reports is a
uniform four-byte shift of the temporary slot pool plus block-order noise. The
target computes the aligned source address directly into that argument register
and spills the argument register to its home; the candidate computes it into a
temporary, copies it into the argument register, and spills the temporary.

The four-byte pool shift is the second, separable question and points at a
halfword temporary the target has and the candidate does not: the target's
temporary homes sit four bytes below the candidate's throughout, which is the
signature of a `Uvreg` slot below the declared block.

Four more forms measured this pass, all flat or worse against the retained
candidate's 215/214 words and 129 differing rows: naming the aligned address in
a local declared last (215, 132); naming the pre-alignment sum and applying the
modulo to it at both sites (215, 132); `& ~0xF` instead of `- (x % 16)`
(210 words, so it is not the same computation); and a `(u32)` cast on the
argument (215, 129). With the ten forms already recorded, the expression's
spelling is closed.

Resume on the argument-register question directly -- what makes IDO write a
spilled common subexpression into the argument register rather than a
temporary -- and on the missing halfword slot, rather than on more rewrites of
the alignment arithmetic.
#### 2026-09-13, lane k1: authenticated draw-census follow-up

Fresh configured stock compilation reproduces 856 target bytes,
size delta 0, 54 raw and
54 masked differences, first +0x0.
Candidate frame is 0x48; the procedure-2 census records
31 draws and 316 emission records. Stock and traced full-TU text
compare identically. Static relocation counts are 53 candidate and
53 extracted target, with 46 identical offset/type/symbol tuples.
These are static measurements; overlay runtime identity proof remains separate.

The fresh entry baseline is 215 candidate words against 214, 118 raw/masked
differences, first +0x58, frame 0x50, and 37 exact static relocation tuples.
Its aligned residual has 44 naming, 19 immediate and 19 structural paired
rows plus one candidate-only word. The baseline draw profile has 31 draws
and 317 emission records. Existing expression/local-carrier controls were
read first; no colour sweep was repeated.

Six source controls use the mapped full-TU procedure 2 of 5. Every control
has saved source, stock object, scalar/frame measurements, line census,
aligned delta, named Ucode mapping and stock/trace text fidelity evidence.

1. Reuse the consumed loadSize local for the signed aligned address by
   assigning it in the DMA argument, then read it for decompression. This
   retains allocation arguments, modulo association, call order and widths;
   the old size has no subsequent use. Both raw and masked differences fall
   from 118 to 54, extent becomes exactly 214 words, and static exact tuples
   rise from 37 to 46. This is the retained source. The draw count stays 31
   but its order changes; emission records fall to 316, with the DMA line
   falling from 18 to 16. Against the baseline, paired residual improves by
   7 in the first window and 29 in the second. The candidate now has 12
   naming, 25 immediate and 9 structural paired rows, with two candidate-only
   and two target-only words. The extra gaps are disclosed, not hidden by
   the better scalar score. Frame shrinks to 0x48 against the target's 0x50;
   the first mismatch is consequently at +0x0.
2. A distinct u16 ID local, preserving the s32 parameter ABI and its masked
   values, tests the previously named halfword-home mechanism. It regresses
   to 228 differences at delta +60 and frame 0x58, with 30 draws and 332
   emission records. A separate narrow local does not recover the target.
3. Changing only that new local to u32 gives 219 differences, delta +56,
   frame 0x50, 30 draws and 330 emission records. The introduced ID lifetime,
   rather than only its narrow width, disrupts the improved schedule.
4. Keeping the original parameter carrier and replacing its low-bit mask
   with a u16 conversion is byte-identical to the retained candidate. All
   draws, their order and every line's emission count are unchanged: this
   edit does not move the targeted entry schedule or create a hidden home.
5. A fresh s32 alignedAddress carrier on the improved inline-assignment form
   gives 121 differences, delta +4 and frame 0x58. It restores the surplus
   word, returns the DMA line to 18 emission records and worsens paired
   residual by 41. Merely declaring a destination is insufficient; the
   consumed carrier's lifetime matters.
6. Moving the retained assignment immediately before the DMA call produces
   identical executable text, unchanged draw order and one extra emission
   record attributed across the assignment/call lines. The improvement is
   therefore carrier-dependent, not a requirement for assignment-in-argument
   syntax. The original inline form is retained to avoid a byte-flat edit.

Early plateau: the measured carrier and conversion controls, together with
previously recorded declaration, local-address and arithmetic controls, leave
no supported source mechanism for the remaining home placement. This is an
explicit evidence-based stop, not a claim that all source forms are exhausted
or that three byte-flat attempts were run. Reopen with a counted Uvreg and
spilltemps slot-allocation comparison for the improved carrier, separating
front-end home creation from draw order before another source edit. Do not
repeat a colour sweep or add unexplained padding to force the frame size.
The comment above the function is updated without moving executable source
lines. No ABI, global layout, flags, symbol entry or matching status changes.

The retained C remains guarded by its original assembly fallback. Sources, stock
and traced objects, frame and scalar measurements, draw profiles and aligned
maps remain ignored under build/k1/func_80034448.
Commands: lane_status.py, configured stock compilation, draw_census.py,
residual_map.py --object/--against where compared, finalize_plateau.py and
tools/gates.sh. No executable bytes are newly credited.

<!-- plateau-handoff:func_80034448:end -->
