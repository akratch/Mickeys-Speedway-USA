<!-- plateau-handoff:func_8001291C:start -->
### `func_8001291C` plateau handoff

- source: `src/main/track.c`
- score: 499 differing words
- frame: 0x2b0
- relocations: 15
- first mismatch: +0x0
- summary: Mickey m2c fixes batch stride and best-hit state; retained 499 diffs, target-count alternative preserved. Next: authenticated source lifetimes.

#### Mickey m2c structural reconstruction, 2026-09-09

- The fresh `base-only` gate and configured full-TU baseline establish 543 candidate versus 548 target words, 527 differing words, first +0x0, frame 0x2b0 versus target 0x288, and 15 candidate versus 13 target relocations with zero exact offset/type/identity records. Workbench verdict `structure-mismatch`, playbook `constant-audit`. Mickey's m2c draft and actual field loads, saved-hit writes and loop latches were audited before editing.
- Attempts 109–113 correct five data-flow errors: the batch pointer had been advanced both directly and by the batch index; current-batch flags overwrote saved-hit flags; later tentative intersections overwrote the selected hit position; endpoint-plane dot products had the wrong floating-point association; and edge checking continued after the first outside edge. These corrections are mandatory regardless of score. The intermediate 548-word/509-difference attempt 110 still has the saved-position error and is not an admissible better candidate.
- Attempt 114 uses the existing segment/batch/plane types and removes a redundant batch alias and unread byte cursor. Attempt 115 reconstructs the three saved hit coordinates as independent scalars, reaching 538 words and 512 differences. Attempt 116 reuses the near-clip local as the insertion-swap carrier written by the target, improving to 499 differences; the redundant time temporary is removed. This is the retained source.
- Retained measurement: 538 candidate versus 548 target words, 499 raw and masked differences, first +0x0, candidate frame 0x2b0 versus target 0x288, and 15 candidate versus 13 target relocations with 2 exact offset/type/identity records. It improves the admissible residual by 28 words from the original baseline, but has a ten-word size deficit and forty-byte frame excess. Zero matching bytes are credited.
- Attempts 117–120 recover correlated insertion-store direction, mask-helper/capture ordering, a typed edge cursor and the segment discovery guard. Capturing the segment pointer after the XZ/Y calls removes the excess global relocation pair: attempt 118 has 13/13 relocation records and 517 differences; its typed edge and discovery follow-ups reach 514 differences at attempt 120 (537/548 words, frame 0x2b8, 3 exact relocation identities). That alternative is retained separately. Both helper implementations were checked: the retained earlier capture does not change their call order or observable data flow.
- Stopping evidence: five further m2c structural/type follow-ups 121–125 do not improve either the lowest residual or the 514-difference alternative with the target relocation count. Parallel hit-list cursors regress to 518 differences/frame 0x2c0; count reload and batch/triangle entry/latch forms stay at 514; halfword triangle bounds/counter give 549 words and 517 differences. The latter's eleven extra words are narrowing/extension cost, not evidence that a missing target loop was recovered. The counter remains wide in the retained source; the separate segment index also stays wide because the hit-cap path sets it to the segment count before incrementing.
- All meaningful source/object/score/first-mismatch evidence is preserved under ignored `build/wb/tu-track/func_8001291C/`. A failed edit assertion before attempt 112 left source unchanged and was rerun in the same attempt slot; the unchanged measurement was not counted as a source attempt. No new flags, permutation family, inert padding or post-compile changes were introduced.
- Next concrete lever: source-attributed declaration/lifetime evidence under the corrected nearest-hit data flow. Compare the retained lowest-residual source with the target-count alternative; preserve the best-hit snapshots and single batch advance. The m2c draft exposes no further missing semantic identity, and the remaining allocation family requires a new reopen authorization.
- Validation: configured `wb_compare.sh`, workbench `diagnose` and `guide constant-audit`, own-assembly state/field audit, `gmake check-nonmatching-builds`, `tools/finalize_plateau.py`, and `gmake verify cleanroom check-docs`. The existing `NON_MATCHING`/`GLOBAL_ASM` guard remains.

#### Pinned donor audit, 2026-09-08

- Assignment gate: `base-only` at `32a75d648e8954f7455897fb8f16a8ef5f05df11` for this exact symbol and source path.
- Jet Force Gemini `efd5abb1c79636e297b831f7c2d5bf47eac39c0c` has 12 C implementations and 53 assembly placeholders in `src/track.c`. Its implemented routines do not supply this target's body. The closest masked track-object hit, `trackNearestIntersection` (Jaccard 0.1399), corresponds by verified TU order to the still-assembly `trackNearestIntersection` in that pinned source. This is diagnostic structural context, not an adopted name, ABI identity, or exact donor match.
- Fresh configured full-TU measurement: 548 target words and 543 candidate words; 527 raw and 527 relocation-masked differences; first +0x0; target frame 0x288, candidate frame 0x2b0. There are 15 candidate versus 13 target relocation records, with 0 stable identities at matching offsets/types.
- Workbench verdict `structure-mismatch`, playbook `constant-audit`. The existing diagnosis still requires source-shape/lifetime evidence; the donor-only reopen does not authorize substituting a flag, allocator, or permuter mechanism.
- ADR 0018 stop: zero new source attempts for this target. The pinned source disproves the new matched-donor-C hypothesis. No unchanged flag lattice or previously exhausted source family was repeated. The compiled candidate is unchanged and remains behind `NON_MATCHING` and its original `GLOBAL_ASM` fallback; zero new matching bytes are credited.
- Next concrete lever: a published matched counterpart with an authenticated ABI and useful source lifetimes, followed by Mickey-specific field, branch and call proof. The remaining general levers below require separately authorized changed evidence.
- Commands: `tools/wb_compare.sh --summary-json func_8001291C`, workbench `diagnose` on the configured full-TU object, the track-object masked skeleton audit, `tools/finalize_plateau.py`, `gmake cleanroom`, and `gmake check-docs`. Source, object, scores, first mismatch and previous handoff are retained under ignored `build/wb/`; no instruction rows are included here.

#### Prior committed plateau evidence (historical)


- source: `src/main/track.c`
- score: 527 differing words
- frame: 0x2B0
- relocations: 15
- first mismatch: +0x0
- summary: Recovered target insertion, hit-cap, early-out, and distance CFG; next lever is declaration/lifetime layout for the 40-byte frame and one global pair.
<!-- plateau-handoff:func_8001291C:end -->
