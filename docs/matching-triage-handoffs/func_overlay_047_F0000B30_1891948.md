<!-- plateau-handoff:func_overlay_047_F0000B30_1891948:start -->
### `func_overlay_047_F0000B30_1891948` plateau handoff

- source: `src/overlays/o047/func_overlay_047_F0000B30_1891948.c`
- score: 1972/2168 words
- frame: 0x280
- relocations: 318
- first mismatch: +0x4
- summary: Size/frame exact; 83 ordered calls, 318/321 text relocations. Five CFG probes stalled. Next: structure-buckets lever 6 spill-slot census.

#### Structural reconstruction packet, 2026-09-08

Outcome: the configured candidate's 2,176-byte overrun is closed, its frame
matches, and all 83 call-site identities occur in target order. This is a
compiled `NON_MATCHING` plateau, with zero new matched bytes. Executable size
agreement is not instruction, relocation, CFG-equivalence, or ROM proof.
The assembly fallback remains selected in the canonical build.

Ownership is overlay 47, text offset 0xB30, extent 0x21E0, ending at 0x2D10;
the corresponding ROM range is [0x1891948, 0x1893B28). Assignment base was
`cbd755d9b2e828b5f4c05a4efe81d70353d205cb`.

Re-derived configured measurements:

- Target: 8,672 bytes, 2,168 words, frame 0x280, 321 runtime text relocation
  sites, comprising 83 calls and 238 HI/LO sites.
- Baseline: 10,848 bytes, 2,712 words, frame 0x320; 2,701 raw and masked
  differing words, first mismatch +0x0. Its 191 text relocations plus six
  jump-table relocations explain the earlier handoff's total of 197.
- Retained attempt 15: 8,672 bytes, 2,168 words, frame 0x280; 1,975 raw and
  1,972 masked differing words, first mismatch +0x4. There are 318 candidate
  text relocations and six further jump-table relocations in `.rodata`.
  The score above counts differing words, not matching words.

The runtime tables supplied identities for all 321 target sites. A diagnostic
source-binding audit resolves 304 of the candidate's 318 text sites, with 49
identities agreeing at the same offset and relocation type. The unresolved
14 sites reference compiler-owned `.rodata`. All 83 calls agree in identity
and order, but most offsets still move. These bindings are an analysis aid,
not an authenticated canonical relocation receipt. The extracted fallback
object has placeholder names and fewer static relocations than the runtime
surface, so workbench symbol-mismatch totals are not a missing-call count.

#### Evidence and source recovery

`generate_ctx.sh` succeeded; the full context initially failed m2c parsing at
an unrelated flexible array in `Overlay8ScaleOutput`. Unannotated extracted
assembly also collapses call and global names. Both runtime relocation tables
were therefore decoded using the project's overlay and relocation tools,
then applied to an ignored diagnostic assembly copy. A focused typed context
and the six-case dispatch table allowed a useful m2c draft. The committed C
was manually reconstructed in ordinary project C; generated drafts, annotated
assembly, objects, and aligned comparison rows remain ignored.

The near-match oracle's best sibling score was only 0.057, providing no useful
body donor. No external function body, flag lattice, permuter, object patch,
dummy executable padding, or additional mechanism was used.

Recovered evidence includes:

- The actual call identities and signatures, including the overlay 45
  field-22 setter, distinct from its field-20 sibling.
- Player stride 0x34, icon stride 0x2C, position and texture-node stride
  0x10; actor field widths corroborated by Mickey's spawn, animation and
  object consumers; three `MtxF` matrices replace untyped scratch storage.
- Signed animation-frame indexing with an unsigned switch selector,
  captured texture-scroll pointer, four correctly ordered halfword updates,
  and ready/unready table row addressing.
- Initial selector movement before the occupied-slot search, ordinary
  update-rate interpolation, and recovered packed-colour channel extraction.
  High colour shifts use unsigned operands; no inert code was inserted.
- Six readonly floating constants agree in value; compiler pool order and
  reference forms still differ. Physical graphics pointers retain the target
  base/addend interpretation.

The typed reconstruction was banked in `99b161f3`; the size/frame-exact
candidate was banked in `1a2232dc`. Both commits change only the owned source.
Earlier attempts and the displaced final probes remain locally recoverable
under ignored `build/whale2/attemptN.c`, `.o`, summary and audit files.

#### Attempts and stopping evidence

Attempts 1-5 recovered the structure, call identities, physical-pointer
addends, selector search and table addressing. Attempts 6-15 recovered literal
constants, frame geometry, signed frame access, scroll-pointer reuse and
colour packing. Attempt 12's lower masked residual of 1,899 was on a shorter
candidate with the incomplete unsigned frame type; it is not the retained
size-exact candidate. Fixed table extents and a separate player-end symbol
were flat; retaining departure speed as an external scalar regressed. These
inputs do not justify repeating those probes.

The assignment's five consecutive no-information attempts were all measured
from the retained attempt-15 source. All remained 2,168 words with frame
0x280 and the same 83 ordered calls:

- 16: guarded equality-terminated interpolation loop; 1,975 raw / 1,972 masked.
- 17: explicit post-test four-player traversal; 1,975 raw / 1,973 masked.
- 18: explicit post-test statistics traversal; 1,975 raw / 1,972 masked.
- 19: explicit guard/post-test icon traversal; 1,975 raw / 1,972 masked.
- 20: explicit post-test player search within icons; 1,975 raw / 1,972 masked.

None improved the residual or established a new identity or causal fact.
The best source was restored and remeasured before this handoff. Baseline,
unchanged reruns and initial tool failures are excluded from the 20 attempts.

#### Validation and next action

Commands run include `tools/wb_compare.sh --summary-json` for each configured
candidate, workbench `diagnose` and `guide structure-buckets`,
`gmake check-nonmatching-builds`, `gmake cleanroom`, `gmake check-docs`,
`gmake extract`, the canonical object build, the full ELF build, and
`gmake verify`. All 243 candidate-bearing TUs compile. Clean-room and
documentation gates pass. `tools/finalize_plateau.py` generated the guarded
source marker and refreshed the structured handoff metrics.

The canonical fallback object's 8,672 text bytes and 165 static relocation
tuples agree exactly with the extracted fallback target. This confirms the
guard preserves that fallback; it is not linked-range or full-ROM proof.
Full `gmake verify` fails at the assignment base's unchanged
`overlay94UpdateController.c`, including unresolved `func_800254FC`,
`func_8002A878`, `func_8002565C`, and `func_8002A910`, plus truncated call
relocations. Fresh extraction and rebuilding reproduce the failure. No
out-of-scope source was changed to address it. See ignored
`build/whale2-fresh-verify.log` and `build/verify/rom-build.log`.

The workbench verdict is `structure-mismatch`; its final diagnostic footer
says "mixed residual: fix constants first (they cascade), structure second,
register classes last." Equal aggregate size has not proved every block.
The next packet should first distinguish remaining literal/reference
mismatches from stack offsets using `constant-audit` lever 1 and authenticated
runtime identities, then apply `structure-buckets` lever 6, the spill-slot
census, with lever 4's full-TU context check. In particular, the target saves
through f30 while this candidate saves through f28; matrix/local homes differ,
the first two readonly pool values are reversed, and unready-table reference
multiplicity differs. Establish which discrepancy causes a local residual
before changing declarations or allocation. Do not repeat the five flat CFG
spellings or substitute a flag/permutation search under this packet.

Final concise measurements and diagnostics are retained in ignored
`build/whale2/final-summary.json`, `final-audit.txt`, `final-diagnose.txt`,
`final-nonmatching-builds.log`, and `notes.md`. All target-derived detailed
evidence stays outside Git.

<!-- plateau-handoff:func_overlay_047_F0000B30_1891948:end -->
