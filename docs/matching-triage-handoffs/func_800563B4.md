<!-- plateau-handoff:func_800563B4:start -->
### `func_800563B4` plateau handoff

- source: `src/main/anim.c`
- score: 12/649 words
- frame: 0xD8
- relocations: 11
- first mismatch: +0x1C
- summary: Candidate is 40 words short with 637 differences; frame and 11 relocation types agree; preserve semantic reconstruction for transplant.

Orphan-lane reconciliation evidence on branch base
`5b92baa2a2dec969bd44f8363aab30e44ef9f44c`:

- the committed fallback boundaries are resident VMA
  `0x800563B4..0x80056DD8`: `0xA24` / 2,596 bytes / 649 words, directly
  followed by `func_80056DD8` with no owned padding;
- the recovered ABI is
  `s32 (s32, AnimCollisionShape *, s32, AnimCollisionShape *,
  AnimCollisionResult *)`. The orphaned work also records the shape byte,
  height field, result normal, and typed declarations for the three resident
  intersection helpers used by the candidate;
- configured IDO 5.3 `-O2 -mips2 -32` compiles the preserved candidate to
  `0x984` / 2,436 bytes / 609 words, 40 words short. Target and candidate
  frames are both `0xD8`; positional comparison leaves 637 raw differences,
  first `+0x1C`;
- workbench classifies the candidate as a broad structural mismatch: 288
  aligned structural, 166 register, and 56 constant residuals, with 116
  insertions and 156 deletions. The shift-tolerant view has 227 aligned
  matches, but that is diagnostic only while geometry differs;
- target and candidate each carry eleven static relocations with the same type
  counts: seven `R_MIPS_26`, two `R_MIPS_HI16`, and two `R_MIPS_LO16`.
  Their sites are shifted by the structural deficit, so no exact relocation
  or linked-owner claim is made;
- the uncommitted source was a substantive semantic reconstruction, not build
  residue. It is now protected by `NON_MATCHING` with the original fallback;
  the default build and scoreboard therefore retain zero match credit;
- provenance text from the orphaned work attributes only the collision role
  and broad case order to JFG's public decompilation. Mickey's own fallback,
  helper calls, struct offsets, and compiler output remain the authority.

Next lever: transplant this guarded reconstruction onto current canonical
`anim.c`, reconcile its shared collision types against the newer
`func_80054B3C` plateau, and reproduce a current V0 before source iteration.
Then audit the earliest missing radius/vector operations and case constants;
do not infer progress from the aligned score or promote without exact geometry,
relocations, linked ownership, and the full-ROM proof.
<!-- plateau-handoff:func_800563B4:end -->
