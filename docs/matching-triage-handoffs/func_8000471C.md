<!-- plateau-handoff:func_8000471C:start -->
### `func_8000471C` plateau handoff

- source: `src/main/objects.c`
- score: 77/80 words
- frame: 0x88
- relocations: 6
- first mismatch: +0x50
- summary: Twin of func_80004454: the same p1 web 22 takes a0 where the target takes a1. Same requirement, same measured-flat search space.
The current configured stock-IDO result owns 320 bytes / 80 words at resident
`0x8000471C..0x8000485C`, ROM `0x531C..0x545C`. Three raw and relocation-masked
words differ from `+0x50`; the frame is `0x88`, with no opcode or floating-point
residual. All six relocation offsets, types, and identities align. The earlier
six-word baseline had only five aligned sites: its global-address low
relocation moved with the schedule. The previous handoff's all-six claim was
incorrect. This remains guarded C with its canonical assembly fallback.

The measured sibling mechanism transfers from `func_80004454`: two used
coordinate locals replace the unused volatile reserve, the loop preheader
assignments share a physical line with the loop header, and the pure class
comparison has the object operand first. This reduces the configured six-word
baseline to three words while preserving calls, data widths, and operation
order. No donor source was adopted; JFG upstream `efd5abb` still supplies
assembly-backed closest-object functions.

Workbench now reports `register-permutation`; the applicable playbook is
forced-color-oracle lever 19. The exact named Ucode stream identifies isolated
procedure zero. Stock-versus-instrumented fidelity passes for sections,
relocations, and symbols. Its 13 coloring decisions are all in p1, with no p2
decisions. The initial-index decision has the same measured saving, occurrence
divisor, costs, interference mask, and five interferences as the sibling.
Forcing that decision to the target color gives zero differing words. This is
diagnostic evidence only; no forced output enters the source or canonical ROM.

The remaining source search used the calibrated, context-guarded stock
compiler input and only declaration, operand, condition, struct-reference,
compound-assignment, and line-grouping mutations. It stopped at its declared
three-minute no-improvement threshold after 180.5 seconds. All 21,579 evaluated
iterations retained a best strict score of 15; 15 reported errors are excluded
from matching evidence. The final three scores were 53, 53, and 15, buying no
better residual, identity, or new hypothesis elimination. This is the measured
stall, not an attempt-cap stop. The longer cap was ten minutes and was not hit.

The one TU flag lattice was reused without recompilation: none of its 53
usable cached objects improved the original six-word baseline. Do not repeat
those flags, this source-equivalent sweep, or the sibling's typed-return,
start-address, initial-induction, and bounds-array forms without changed
inputs. Next lever: new source/def-use evidence that changes the initial-index
color preference or interference, followed by a stock-source proof of the
forced-color-oracle result. The extra class-field load has not supplied that
handle; the measured initial-index decision is unchanged.

All sources, objects, context checks, fidelity and procedure-map receipts,
search status counts, scores, and hypotheses are retained under ignored
`build/m6-objects/func_8000471C/`. The plateau was recorded with
`tools/finalize_plateau.py`; validation includes configured workbench comparison,
`gmake -j14 verify`, `gmake check-nonmatching-builds`, `gmake check-docs`, and
`gmake cleanroom`. Canonical ROM equality comes through the fallback and adds
no new matching credit.

#### The a0-versus-a1 residual, and what does not move it

The whole remaining deficit is one caller-saved colour on one web: the target
takes `a1` at every site, the candidate `a0`, with identical opcodes,
schedule, frame, constants and relocation identities. `func_80009AA8` in the
same translation unit shows the same thing on an unrelated web, so this is a
property of the compilation rather than of any one source shape. Measured
against the real full-TU object in this lane and eliminated:

- Comparison spelling at the use, including L87's `field == (group ^ 0)` on
  both operands, the reversed pure comparison, `!(a >= b)`, `!= 0` versus a
  bare truth test, splitting the `&&` into nested `if`s, and swapping the two
  `&&` terms. Best result unchanged; the reversed pure comparison and the
  swapped `&&` both regress, consistent with L67.
- Dead-store colour reservation (L87's `value = 0` clause), assigned to each
  existing local before the call and to a fresh local. The existing locals
  merge into their own later webs and change nothing; a fresh local moves the
  frame instead.
- Naming the loaded bound, the loaded start, or both as extra locals; each
  costs the exact frame.
- Dropping the m2c `temp_v0` return carrier, retyping it as `void **`,
  parenthesising the pointer sum differently, hoisting the initial index
  multiply out of the guard, and writing the shift instead of `* 4`.
- The floating-point argument copies: dropping `queryX`, `queryY` or both in
  favour of the parameters, swapping their assignment order, swapping their
  declaration order, moving them after the call, and adding a third for
  `arg2`. Only the base and "assignments after the call" hold at three words.
- Splitting the preheader's two assignments off the loop-header line, which
  costs two words and confirms the recorded line-grouping result is at its
  optimum rather than merely at a local one.

Next lever is unchanged in kind but sharper in scope: find what reserves `$a0`
in the retail build. It is not a source-level web this lane can create.
#### Confirmed exhausted on 2026-09-09; lever 50 does not apply

Remeasured unchanged: 80 words, frame `0x88`, 6/6 relocation identities,
three register words from `+0x50`. The single web is the initial list index
read back from `124(sp)` after `func_8000572C`, taking `a0` here and `a1` in
the target at the load, the `slt` against the bound, and the `sll` that forms
the byte offset.

Lever 50 (an unrelocated literal address pair scoring as a false residual) was
checked first and does not apply: the workbench reports `aligned_constant = 0`,
`register_mismatches = 3`, and all six relocation offsets, types and identities
resolved, which is the opposite of that signature. Lever 51 does not apply
either -- the hand-written `u8 *` cursor beside the byte index is exactly the
pair of induction variables the target emits, and both registers already match.

The sibling `func_80009AA8`'s refreshed shard carries the dead-local carrier
family that was newly eliminated today; the mechanism is shared, so treat that
result as covering all three of this TU's register-only rows.
#### 2026-09-09, lane win-a: identical to func_80004454, measured

Procedure ordinal 4 of 65; the residual is the same p1 web 22 with the same
profile, and `CDX_FORCE=p1:w22=c4` gives zero differing words here too
(reachability proof, never a match). Everything in `func_80004454`'s shard
applies unchanged, including the search-space theorem -- `a0` is the lowest
zero-cost free colour and this web is visited last, so only an extra
interfering `a0` web can move it -- and the roughly 1,900 measured-flat
candidates. Do not search this one separately; close the twin first.
<!-- plateau-handoff:func_8000471C:end -->
