<!-- plateau-handoff:func_80009AA8:start -->
### `func_80009AA8` plateau handoff

- source: `src/main/objects.c`
- score: 3 differing words
- frame: 0x60
- relocations: 17
- first mismatch: +0x54
- summary: One uopt caller-saved colour: target a1 where the candidate takes a0. Next: what reserves a0 in the target.

The configured full-TU baseline had 269 words against 244 owned words,
268 raw/masked differences, first mismatch +0x0, a 0x68 frame against
0x60, and 60 candidate relocations against 17 target relocations. The
retained candidate has 244 words (976 bytes), 240 exact words, four
raw/masked differences, first mismatch +0x54, and the exact 0x60 frame.
All 17 relocation offsets, types and authenticated identities agree.
Workbench reports allocation-mismatch: four register fields, no opcode,
schedule, constant or instruction-count deficit. This remains guarded
NON_MATCHING C; the linked proof covers the assembly fallback.

The assignment gate returned base-only. The near-match oracle supplied
only weak structural neighbors, without an established counterpart; no
external decomp body was adopted. The command layout and RGBA packing use
Mickey's own SDK headers. Rescoring all 53 usable cached TU flag objects
found a debug-mode baseline improvement of one word, still 25 words too
large; no shared flags were changed.

The baseline mistook a physical static address for a load through an
unrelated object-pointer global. The retained spelling uses the existing
physical-address symbol. The material's command-list pointer needed its
proved padding, and the object tier is an unsigned byte. Removing redundant
spill-copy locals recovered the frame. Advancing the scalar display-list
global directly by one command allowed IDO to retain its address across
calls: the candidate dropped from 60 relocation records to 17. An unsigned
selected display-list address then removed the final extra instruction.
Repeating the selected table expression in its exclusive branch preserved
common-subexpression sharing while restoring the register-move shape.
Finally, the SDK's complete unsigned-int RGB shift-and-mask expression
repaired the long register residual after the first primitive command.

Attempt numbers are global to this lane. Attempts 60–70 established the
address, field and frame corrections; 71 established the direct global
update. Attempts 72–77 explored pointer homes and command stores; 78
established unsigned address parity. Attempts 79–82 changed selection and
carrier spelling; 83 recovered the last opcode difference. Attempts 84–86
cleaned and reordered command expressions. Attempt 87 reached four words.
The final five probes 88–92 tested an expired projection-flag carrier,
its combination with SDK packing, a shared model-index scalar, ordinary
signed-byte promotion and a native-int boolean. None improved the four-word
best or resolved the remaining carrier/spill mechanism. Those five probes
are the stall evidence. Cleanup attempt 93 retained the four-word result.
All meaningful sources, objects and measurements remain in ignored build
artifacts. Shared-global pointer and array declaration experiments changed
no function bytes or relocation tuples and were restored.

The next lever is register-role-audit with authenticated compiler-phase
or stock assembler evidence: establish why the selected-entry common
subexpression takes the first argument color instead of the second, and
why the known-false flag spill retains its live carrier instead of zero.
Do not infer a temporary-pool mechanism from the visible register rotation.
The command packing change proves a useful source spelling here, not an
unmeasured claim about allocator reservations.

Validation: configured full-TU comparison and diagnosis, retained flag
rescoring, exact linked assembly-owned range comparison against the baserom,
finalize_plateau.py, full gmake verify with the expected US ROM hash,
check-docs, cleanroom and check-nonmatching-builds. No C matching credit is
claimed for this plateau.

#### Four words to three: `var_t2 = 0` belongs after the selection

The known-false flag's initializer was the fourth word. Declared at the top of
the function it acquires a live carrier, and the spill IDO emits in the
`func_8005AF14` delay slot then stores that register; the target stores the
literal zero. Moving the initializer below the selection block keeps the
`move t2,zero` the schedule wants at the top -- IDO still hoists it -- while
the delay-slot spill becomes `sw zero`. 4 -> 3, with every other word
unchanged. This is the previous handoff's "known-false flag spill retains its
live carrier instead of zero" question, answered by statement placement rather
than by a register-role trace.

#### The remaining word is one caller-saved colour, and these do not move it

The residual is now exactly one web -- the selected entry loaded before the
mode branch -- taking `a0` in the candidate and `a1` in the target, at three
sites (`lw`, the stack spill, and the `move` into the saved carrier). Opcodes,
schedule, frame, constants and all 17 relocation identities are exact.

Eliminated in this lane, each measured against the real full-TU object:

- **Declaration order.** All 132 single-move permutations of the twelve
  declared locals were compiled and scored. None beat three words; the best
  ties, the rest regress. Declaration order is not the handle here.
- **Dead-store colour reservation** (L87's `value = 0` clause). Ten spellings
  that assign a never-read initial value to an existing or fresh local before
  the selection. None changed the colour; a fresh local instead moves the
  frame.
- **Selection-block structure**: inverted condition with swapped arms,
  statement order inside the arms, hoisting the default assignment out,
  pointer-deref instead of index, uncast index, and moving the `temp_a1`
  definition after the branch. Best result is unchanged; the rest regress.
- **Parameter shape**: a second and third declared parameter (with the two
  call sites updated) both regress the prologue wholesale.

The same `a0`-versus-`a1` residual appears in `func_80004454` and
`func_8000471C` in this TU, on webs of different types and in different
control-flow positions, with the target choosing `a1` every time. Whatever
reserves `$a0` in the retail build is a property of the compilation, not of
these three source shapes. That is the next thing to authenticate.

<!-- plateau-handoff:func_80009AA8:end -->
