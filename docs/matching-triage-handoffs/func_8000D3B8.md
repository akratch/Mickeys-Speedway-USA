<!-- plateau-handoff:func_8000D3B8:start -->
### `func_8000D3B8` plateau handoff

- source: `src/main/track.c`
- score: 105 differing words
- frame: 0x38
- relocations: 16
- first mismatch: +0x4
- summary: Five m2c carrier forms leave the residual flat or regress size; baseline restored. Next: initial pool-size expression and lifetime evidence.

#### Mickey m2c reconstruction audit, TU batch 2026-09-08

- Gate: `base-only` at batch base `d0bdcaf16a9b0a7fb7371eb435c0940cb33f5c19`. Fresh configured baseline: 109 candidate versus 110 target words, 105 raw/masked differences from `+0x4`, matching `0x38` frames, and 16 candidate/target relocations with four exact offset/type/identity tuples. This is nonexact compiled evidence.
- Workbench verdict `structure-mismatch`, playbook `structure-buckets`. Lever 4 confirms the configured full-TU flags and context; the earliest missing operation remains the initial pool-size temporary, with a different saved-register assignment upstream. Mickey m2c confirms the allocator/free identities, 128-byte light record, 8-byte allocation record, 64-byte segment and signed-halfword per-segment count. No numeric constant or call target was corrected.
- Five source attempts tested the newly reconstructed carrier forms while retaining the real zero-count loop behavior: inline unsigned pool extent; unsigned shift spelling from the draft; typed allocation result before global publication; distinct immutable allocation extent and mutable copy counter; typed per-segment allocation result before publication. The first three produce 109 words and 105 raw/masked differences; the last two regress to 108 words with the same 105 differences. All retain frame `0x38` and the same 16-record relocation surface with four exact tuples.
- Stall evidence: all five attempts leave the best residual, identities and structure unchanged or worse. They show that these m2c-inferred carriers collapse into the existing code or remove another required copy. The 109-word baseline is restored and recompiled. No flag change, signed-shift assumption, dummy operation, allocator intervention or permuter was used for these attempts.
- The TU-wide lattice requested by the batch was compiled once using the eligible sized `func_8000DB34`: 119 configurations attempted, 53 scored and 66 retained tool/compile failures. Configured `-O2 -mips2 -32 -Wab,-r4300_mul` remains tied for the best result there. No flag proposal is made, and failed configurations are not claimed as search coverage.
- Next concrete lever: authenticated original source expression and lifetime for the initial pool-size argument, beyond the m2c carrier forms measured here. A fresh donor or compiler mechanism must explain the missing temporary and initial saved-register order; repeating these forms is closed.
- Batch attempts 3 through 7, baseline at count 2, source/object/report snapshots and hypotheses are retained under ignored `build/wb/tu-track/func_8000D3B8/`. Commands: full-TU `wb_compare.sh`, workbench `diagnose`/`guide structure-buckets`, context-aware m2c, the one TU flag sweep, checkpoint updates, baseline restoration/reproof and `finalize_plateau.py`. The original guard/fallback remains active, with zero new matching credit.

#### Pinned donor audit, 2026-09-08

- Assignment gate: `base-only` at `32a75d648e8954f7455897fb8f16a8ef5f05df11` for this exact symbol and source path.
- Jet Force Gemini `efd5abb1c79636e297b831f7c2d5bf47eac39c0c` has 12 C implementations and 53 assembly placeholders in `src/track.c`. Its implemented routines do not supply this target's body. The closest masked track-object hit, `trackLightAllocate` (Jaccard 0.4604), corresponds by verified TU order to the still-assembly `trackLightAllocate` in that pinned source. This is diagnostic structural context, not an adopted name, ABI identity, or exact donor match.
- Fresh configured full-TU measurement: 110 target words and 109 candidate words; 105 raw and 105 relocation-masked differences; first +0x4; target frame 0x38, candidate frame 0x38. There are 16 candidate versus 16 target relocation records, with 4 stable identities at matching offsets/types.
- Workbench verdict `structure-mismatch`, playbook `structure-buckets`. The existing diagnosis still requires source-shape/lifetime evidence; the donor-only reopen does not authorize substituting a flag, allocator, or permuter mechanism.
- ADR 0018 stop: zero new source attempts for this target. The pinned source disproves the new matched-donor-C hypothesis. No unchanged flag lattice or previously exhausted source family was repeated. The compiled candidate is unchanged and remains behind `NON_MATCHING` and its original `GLOBAL_ASM` fallback; zero new matching bytes are credited.
- Next concrete lever: a published matched counterpart with an authenticated ABI and useful source lifetimes, followed by Mickey-specific field, branch and call proof. The remaining general levers below require separately authorized changed evidence.
- Commands: `tools/wb_compare.sh --summary-json func_8000D3B8`, workbench `diagnose` on the configured full-TU object, the track-object masked skeleton audit, `tools/finalize_plateau.py`, `gmake cleanroom`, and `gmake check-docs`. Source, object, scores, first mismatch and previous handoff are retained under ignored `build/wb/`; no instruction rows are included here.

#### Prior committed plateau evidence (historical)


- source: `src/main/track.c`
- score: 31 differing words
- frame: 0x38
- relocations: 16
- first mismatch: +0x4
- summary: Best aligned comparison is 79/110; one missing pre-call size temporary shifts relocation sites, with the initial s0/s1 pool colors swapped.
<!-- plateau-handoff:func_8000D3B8:end -->
