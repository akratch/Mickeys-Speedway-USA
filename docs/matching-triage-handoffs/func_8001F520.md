<!-- plateau-handoff:func_8001F520:start -->
### `func_8001F520` plateau handoff

- source: `src/main/models.c`
- score: 376 differing words
- frame: 0xB0
- relocations: 46
- first mismatch: +0x8
- summary: Configured candidate is 387 versus 401 target words; cache and texture-loop CFG lifetimes remain structural.

Fresh work corrected four semantic defects in the retained model-loader body:
the ROM offset table has four-byte entries; allocation failures use local
rollback without freeing an uninitialized model; newly loaded models create a
copy or point-backed instance before cache publication; and the display-list
boundary table and output fields use their observed byte and pointer widths.
Those changes improved the strict comparison from 394 to 376 differing words
and recovered 43 instructions.

The target and candidate each carry 46 relocations with identical type and
symbol multisets. The remaining offsets drift with the cache scan, texture
failure loops, and oversized local frame. The donor oracle and flag lattice
both fail closed because this resident fallback lacks a `size:` ownership
entry in `symbol_addrs.us.txt`; authenticate size `0x644` in a separate
metadata lane before another compiler-shape campaign. Permutation is not yet
appropriate for this broad structural residual.
<!-- plateau-handoff:func_8001F520:end -->
