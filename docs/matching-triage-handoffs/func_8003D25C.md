<!-- plateau-handoff:func_8003D25C:start -->
### `func_8003D25C` plateau handoff

- source: `src/main/particles.c`
- score: 68/168 words
- frame: 0xB8
- relocations: 2
- first mismatch: +0x50
- summary: Authenticated 202-force landscape: web 79 reaches 62, while neighbouring gains are rival footprints and their combinations decline; no force is exact.

- Fresh configured full-TU baseline: 168 target and candidate words, 100
  identical and 68 raw and relocation-masked differences, first `+0x50`,
  zero size delta, and matching `0xB8` frames. The owned resident range is
  `0x2A0` bytes with no padding. Both relocation records agree in relative
  offset, type, and resident symbol identity.
- Workbench verdict: `allocation-mismatch`; the opcode, schedule, and
  structural residuals are zero. The `forced-color-oracle` field-guide
  playbook routes this to lever 19, but ownership remains unknown without an
  authenticated globalcolor trace or reservation state.
- Authorized-mechanism audit: the guarded body is already the retained
  decompiler-assisted structural reconstruction, with calls, globals, types,
  CFG, extent, and relocation surface recovered. This target has no size
  deficit for the assigned mechanism to close. Under ADR 0018, zero source
  attempts is the stopping evidence; allocation, flags, permutation, and
  instrumentation were outside this reopen authorization.
- Next concrete lever: under a separately authorized allocator packet, capture
  authenticated per-side UOPT globalcolor state and apply forced-color-oracle
  lever 19 to the first causal web before considering a bounded permutation.
- Validation: `tools/wb_compare.sh func_8003D25C`, `function_preflight.py`,
  workbench `diagnose`, `guide forced-color-oracle`, and a same-object
  comparison; `gmake verify` reproduced the expected ROM hash and `gmake
  cleanroom` passed. `tools/finalize_plateau.py` wrote this source/shard
  metadata. Repository-wide `gmake check-docs` remains blocked only by the
  unrelated pre-existing stale `func_8000FAE0` shard; its files are unchanged
  from this lane's assignment base.
<!-- plateau-handoff:func_8003D25C:end -->
