<!-- plateau-handoff:func_overlay_008_F0002640_1860398:start -->
### `func_overlay_008_F0002640_1860398` plateau handoff

- source: `src/overlays/o008/overlay_008.c`
- score: 37/183 words
- frame: 0xD0
- relocations: 14
- first mismatch: +0xF0
- summary: The record/base home question was declaration order and the frame ladders are now identical; what is left is one preheader move of the emission count.

#### 2026-09-12, lane p9-mid: 62 -> 37, and the home question was declaration order

The closure says to "resume with the missing pool value first" and records the
record/base homes at +0x78 and +0xCC as the open question, with register,
aggregate and order probes negative. The home question was the whole of it, and
`tools/frame_census.py` -- which had never been run on this function -- named it
in one command. This is the third closure on this tree in two days that a frame
census reopened.

Aligner buckets before: 138 byte-exact, 7 register naming, 11 immediate-only,
29 really different, at 62 masked and size delta 0. After: 162 byte-exact, 2
register naming, 1 immediate-only, 20 really different, at 37 masked and delta
0. Displacement tax 15 before, 14 after.

**The frame, exactly.** Both sides carry 36 slots in a 0xD0 frame. Before these
edits the target homed the 52-byte record aggregate at +0x78 through +0xAB with
a four-byte local above it at +0xCC, while this candidate homed the record at
the frame top, +0x9C through +0xCF, and that local at +0x80 -- a 36-byte
displacement on every record access, which is where the immediate-only bucket
and most of the displacement tax came from. Declaring `baseValue` first and the
record tenth leaves nine four-byte autos above the aggregate, and **the two
ladders are now slot-for-slot identical**, with no slot either side uses alone.
62 to 53. Twenty-two other orders were measured: moving each of the eleven
locals to the front alone is worth at most one word, and the record's own
position is worth nothing anywhere except tenth.

**Two statement moves follow from reading the object.** Putting the spread
product before the magnitude product in the emission loop takes 53 to 45: the
shipped code interleaves the two integer-to-float conversions and issues the
spread first. All 24 permutations of the four independent loop-body statements
were measured; this is the unique optimum. Hoisting the magnitudeScale load
above the emission-count guard takes 45 to 37; its other fourteen positions
through the preamble are flat, as are eight positions of the phase14 store.

**The decision variable that is left.** The shipped code emits an extra move of
the emission count into a scratch register at +0x104, before the loop, and that
one word shifts +0x104 through +0x150 and accounts for essentially the whole
remaining residual. It is L113 read backwards: the shipped loop's index does not
die at strength reduction and this candidate's does. Ten loop-counter spellings
are recorded at 37 or worse -- post-decrement inside the guard, decrement then
test for negative, four tail conditions, two guard comparisons, an or-with-zero
probe on the count, and a separate counter local, which costs 25 words by
undoing the frame work.

**Not the lever, contrary to the closure.** There is no missing pool value. The
alignment gaps the closure read as inserted code were the 36-byte home
displacement plus the one preheader move, and once the ladders agree the
structural bucket drops from 29 words to 20, almost all of which is the single
one-word shift.
2026-09-09 lane `lane/tu2-o008` maintenance evidence (base `304d363e`, score unchanged at 62
relocation-masked words / 63 raw, exact `0xD0` frame):

- Re-measured and reclassified with alignment evidence: this is a genuine structure
  mismatch, not an allocation one, and it should not be scheduled as a near-miss
  alongside the unit's three allocation-class targets. Alignment inserts twelve gaps
  over the 183-instruction body, six insertions against six deletions, and the
  shift-tolerant edit script reports no moved blocks, so the positional count is
  reading real changed code rather than a permutation.
- The integer temp and shared lanes are identical (9/9 and 7/7), and divergence is at
  pool slot 8: the target materializes one extra pool value there that the candidate
  never produces. That missing value, not register choice, is the head of the
  residual.
- This confirms and locates the previously recorded record/base home gap at `+0x78`
  and `+0xCC`: the two objects place the same base scalar in different storage
  regions, the target near the top of its local region and the candidate far below
  it, which is the same region-assignment question the unit's other targets raise.
- Resume with the missing pool value first. Register, aggregate and order probes are
  recorded negative and the alignment evidence above explains why they could not have
  helped: the bodies are not the same code.
#### 2026-09-12, lane p23-lastmile3: proc7 exhaustive colour floor

Fresh residual map: 162 byte-exact, 2 naming, 1 immediate, 16 structural,
with two candidate-only and two target-only offsets. The mapped procedure is
proc7 with 124 decisions. Its every-colour footprint sampled 500 probes, 496
accepted; no accepted force beat 37, so the winner list is empty and the
lattice floor is 37. The named source question is the one preheader emission
count move and its resulting loop-index strength reduction.

<!-- plateau-handoff:func_overlay_008_F0002640_1860398:end -->
