<!-- plateau-handoff:func_overlay_008_F0002640_1860398:start -->
### `func_overlay_008_F0002640_1860398` plateau handoff

- source: `src/overlays/o008/overlay_008.c`
- score: 120/183 words
- frame: 0xD0
- relocations: 14
- first mismatch: +0xB8
- summary: allocator-only record/base homes remain; relocation-aware score is 121/183 with 9/14 offsets/types and 2/14 identities aligned
2026-09-09 lane `lane/tu2-o008` maintenance evidence (base `304d363e`, score unchanged at 62
relocation-masked words / 63 raw, exact `0xD0` frame):

- Re-measured and reclassified with alignment evidence: this is a genuine structure
  mismatch, not an allocation one, and it should not be scheduled as a near-miss
  alongside the unit's three allocation-class targets. Alignment inserts twelve gaps
  over the 183-instruction body, six insertions against six deletions, and the
  shift-tolerant edit script reports no moved blocks, so the positional count is
  reading real changed code rather than a permutation.
- The integer temp and shared lanes are identical (9/9 and 7/7), and divergence is at
  pool slot 8: the target materializes one extra pool value there that the candidate
  never produces. That missing value, not register choice, is the head of the
  residual.
- This confirms and locates the previously recorded record/base home gap at `+0x78`
  and `+0xCC`: the two objects place the same base scalar in different storage
  regions, the target near the top of its local region and the candidate far below
  it, which is the same region-assignment question the unit's other targets raise.
- Resume with the missing pool value first. Register, aggregate and order probes are
  recorded negative and the alignment evidence above explains why they could not have
  helped: the bodies are not the same code.
<!-- plateau-handoff:func_overlay_008_F0002640_1860398:end -->
