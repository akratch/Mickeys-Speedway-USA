<!-- plateau-handoff:func_80010B4C:start -->
### `func_80010B4C` plateau handoff

- source: `src/main/track.c`
- score: 662 differing words
- frame: 0x158
- relocations: 9
- first mismatch: +0x0
- summary: Mickey m2c fixes caller ABI and distance state; corrected candidate retains 662 diffs after five stalled follow-ups. Next: source-proved lifetimes.

#### Mickey m2c structural reconstruction, 2026-09-09

- A fresh `base-only` assignment gate and configured full-TU baseline establish 678 candidate/target words, 668 differing words, first +0x0, frame 0x158 versus target 0x148, and 9 candidate/target relocation records with zero exact offset/type/identity records. Workbench verdict `structure-mismatch`, playbook `constant-audit`; the complete Mickey m2c draft and relevant call/load/store instructions were audited before editing.
- Attempt 126 recovers the pointer-valued record argument and an unused seventh pointer supplied by the sole authenticated direct caller in `func_8001DD70`. Its C declaration and call-site stack store agree; the callee body does not read that seventh slot. The ABI correction preserves all 2,712 candidate bytes and all candidate relocation records exactly, as recorded in ignored `abi-proof.json`.
- Attempts 127–131 restore the initial intersection limit before either query can read it, the returned limit written to the per-point record after collision response, defined unsigned bit-mask arithmetic, the unconditional first point iteration, and immediate actor-position writes with field reloads during point updates. The initial limit and returned value share the target's same scratch field; the former candidate omitted one write and substituted the original path length for the later read. Cached actor offsets were unproved source temporaries and obscured the target read/write order; they are removed. These corrections remain regardless of score.
- Attempt 132's nested retry limit is flat. Attempt 133 recovers collision/auxiliary result initialization at retry entry, improving the corrected candidate from 687 to 662 differing words. Attempt 134 places the minimum-distance constant inside its actual branch without changing that result. Attempt 138 retains these changes with the established `TrackRayPoint` start/end types.
- Retained result: 692 candidate versus 678 target words, 662 raw and masked differences, first +0x0, candidate frame 0x158 versus target 0x148, and 9/9 relocations with zero aligned identities. The fourteen-word and sixteen-byte excesses remain. The original 678-word geometry lacked required distance state and is not an admissible better candidate or a match.
- Stopping evidence: five follow-ups 134–138 do not improve the 662-word residual or reveal another missing field/call identity. Constant placement is flat; explicit relative-point, failure-copy and reposition cursors give respectively 675/665, 662/665 and 645/664 candidate words/differences, all with frame 0x160. These streams shrink more code than the target needs and are preserved as discarded structural alternatives. The typed point fields are flat at the best corrected residual. The existing relative-array extent is retained; neither stack layout nor decompiler variable names prove a different capacity.
- All source/object/score/first-mismatch evidence is retained under ignored `build/wb/tu-track/func_80010B4C/`. No array-size adjustment, new flag sweep, declaration permutation, manual unrolling, inert padding or post-compile edits were introduced. The target's compiler-produced four-entry loops were treated as generated output, not copied into C as manual unrolling.
- Next concrete lever: authenticated original relative-point storage and call-result lifetimes explaining the frame and FP/pointer schedule. Reopen allocation only with that changed evidence; preserve the initialized query limit, returned-value store and target retry entry semantics. The m2c draft exposes no further missing structural identity.
- Validation: configured `wb_compare.sh`, workbench `diagnose` and `guide constant-audit`, caller and owned-assembly audits, ABI object/relocation comparison, `gmake check-nonmatching-builds`, `tools/finalize_plateau.py`, and `gmake verify cleanroom check-docs`. The function stays behind `NON_MATCHING` over its original assembly fallback; zero matching bytes are credited.

#### Pinned donor audit, 2026-09-08

- Assignment gate: `base-only` at `32a75d648e8954f7455897fb8f16a8ef5f05df11` for this exact symbol and source path.
- Jet Force Gemini `efd5abb1c79636e297b831f7c2d5bf47eac39c0c` has 12 C implementations and 53 assembly placeholders in `src/track.c`. Its implemented routines do not supply this target's body. The closest masked track-object hit, `trackGetPlayerIntersect` (Jaccard 0.0863), corresponds by verified TU order to the still-assembly `trackGetPlayerIntersect` in that pinned source. This is diagnostic structural context, not an adopted name, ABI identity, or exact donor match.
- Fresh configured full-TU measurement: 678 target words and 678 candidate words; 668 raw and 668 relocation-masked differences; first +0x0; target frame 0x148, candidate frame 0x158. There are 9 candidate versus 9 target relocation records, with 0 stable identities at matching offsets/types.
- Workbench verdict `structure-mismatch`, playbook `constant-audit`. The existing diagnosis still requires source-shape/lifetime evidence; the donor-only reopen does not authorize substituting a flag, allocator, or permuter mechanism.
- ADR 0018 stop: zero new source attempts for this target. The pinned source disproves the new matched-donor-C hypothesis. No unchanged flag lattice or previously exhausted source family was repeated. The compiled candidate is unchanged and remains behind `NON_MATCHING` and its original `GLOBAL_ASM` fallback; zero new matching bytes are credited.
- Next concrete lever: a published matched counterpart with an authenticated ABI and useful source lifetimes, followed by Mickey-specific field, branch and call proof. The remaining general levers below require separately authorized changed evidence.
- Commands: `tools/wb_compare.sh --summary-json func_80010B4C`, workbench `diagnose` on the configured full-TU object, the track-object masked skeleton audit, `tools/finalize_plateau.py`, `gmake cleanroom`, and `gmake check-docs`. Source, object, scores, first mismatch and previous handoff are retained under ignored `build/wb/`; no instruction rows are included here.

#### Prior committed plateau evidence (historical)


- source: `src/main/track.c`
- score: 668 differing words
- frame: 0x158
- relocations: 9
- first mismatch: +0x0
- summary: Recovered exact geometry and target retry/failure CFG; next lever is original declaration/lifetime evidence for the 16-byte frame and FP web.
<!-- plateau-handoff:func_80010B4C:end -->
