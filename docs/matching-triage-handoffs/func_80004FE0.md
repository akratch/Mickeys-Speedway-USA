<!-- plateau-handoff:func_80004FE0:start -->
### `func_80004FE0` plateau handoff

- source: `src/main/objects.c`
- score: 189 differing words
- frame: 0x100
- relocations: 83
- first mismatch: +0x38
- summary: Workbench structure-mismatch: structure-buckets. Next: resolve category-fill unrolling and packet/index carriers with the frame held exact.
The configured full-TU baseline had 347 words against 346 owned words,
337 raw/masked differences, first +0x0, a 0x128 frame against 0x100,
and 81 candidate relocations against 83 target relocations. The retained
candidate has the exact 1384-byte size and 0x100 frame, 157 exact words,
189 raw/masked differences, first +0x38, and 83 relocations on each side.
Authenticated relocation identities agree at 56 sites. This remains guarded
NON_MATCHING C and earns no matching credit.

The gate returned base-only before work. JFG objSetupPlayers was the weak
oracle neighbor, but its published body remains assembly; no external C
was adopted. The 53 usable cached TU flag objects remained nonexact, so
configured flags stayed unchanged. The linked assembly-owned range was
byte-compared against the baserom before the source work.

The draft omitted the positive-player-count condition, one category slot,
and a halfword packet initialization. Packet lengths are bytes, and the two
auxiliary packets have distinct twelve-byte layouts. Player setup writes
into the first player packet, not four independent locals. The allocator
consumes two arguments; its apparent third argument was an incidental
output-index carrier. Global flags must be reloaded after the intervening
call. These corrections were made from Mickey's own target and callees.

Ordering the scalar homes, player packets, category array and auxiliary
packets recovered the exact frame and named stack positions. A shared main
index and distinct pointer-list index recovered the saved-register role.
An explicit advancing mode-record cursor removed eight unnecessary saves
and reloads of the root mode pointer. The category fill has a two-entry
prefix and four-entry group in the target. Spelling that conventional
unroll reached the exact instruction count; an explicit byte index across
allocator calls improved the residual further. The candidate still retains
a group-loop branch that the target eliminates. Source store extents and
order remain bounded to the six category entries.

Attempt numbers are global to this lane. Baseline 141 was followed by
semantic/layout corrections 142–143, carrier and frame work 144–153, and
controlled fill/cursor experiments 154–164. An early cleanup at 159 was not
a stopping point: the straight-line fill had exposed a narrower unroll
hypothesis, so work continued. Attempt 165 reached the retained 189-word
residual. The final five probes 166–170 tested pre-advancement of the group,
short-local declaration order, equality-terminated player loops, native-int
counter type, and the unsigned clear bound. Four were flat and equality
termination regressed; none resolved the remaining mechanism. These five
probes are the stall evidence. Cleanup 171 preserved the best measurement.
All source/object pairs and measurements remain in ignored build artifacts.

The workbench verdict is structure-mismatch, routed to structure-buckets:
resolve instruction shape before allocator-only work. The next concrete
lever is to establish the source or stock-compiler condition that removes
the one-group loop while retaining the target group-index setup. Then fix
the clear-loop comparison and the packet/output-index carriers without
losing the exact frame. A straight-line fill collapses the group-index
setup; byte and equality loop forms either normalize or regress. The
remaining relocation identities require authentication before promotion.

Validation: configured full-TU compilation and workbench comparison,
diagnosis/guides, cached flag rescoring, linked fallback ownership proof,
finalize_plateau.py, full gmake verify with the expected US ROM hash,
check-docs, cleanroom and check-nonmatching-builds. ROM verification uses
this function's assembly fallback; the C remains nonexact.

#### 2026-09-11, lane f10-mid: the closure held fixed that `offset` is a declared local

Still 189. The function contains calls, so it is p1-only; the recorded
"short-local declaration order" probe measured an axis the function does not
have for colour. What the record never checked is the frame: a stack-home
census of both objects (frame 0x100 on both sides) shows exactly one home
present only in the candidate, 248, and one present only in the target, 44.
The `.mdebug` census (`-g3`, text-inert) names 248 as `offset`, the second
declared local. The target has no home there and instead spills a compiler
temp at 44, the bottom of the frame. This is the class that matched
`func_80041530` the same day: a declared local the target never declares,
carried by uopt as a temp.

Measured: indexing the third loop directly (`D_800C94F4[i] = object`, no
byte offset) removes the 248 home and scores 193 at delta 0; using `i` for
the first two loops instead adds two words (299, delta +8), and both together
300. So `offset` as one variable across three loops is not the target's shape,
but no single replacement reproduces the target's temp at 44 either; the
decision variable is which expression the target hoists across the allocator
calls, and the record that would settle it is the target's use sites of 44,
which were not read this pass.

<!-- plateau-handoff:func_80004FE0:end -->
