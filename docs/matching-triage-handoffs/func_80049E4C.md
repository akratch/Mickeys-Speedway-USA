<!-- plateau-handoff:func_80049E4C:start -->
### `func_80049E4C` plateau handoff

- source: `src/main/fx.c`
- score: 36 differing words
- frame: 0x60
- relocations: 9
- first mismatch: 0x20
- summary: JFG efd5abb remains assembly-only; zero source attempts. Need new VI stack homes and count-carrier topology evidence.


Reopening audit (2026-09-08), evidence D: PROVENANCE inspection of Jet Force
Gemini public decomp `src/fx.c` and `src/fx.h` at
`efd5abb1c79636e297b831f7c2d5bf47eac39c0c` found no new target C body.
No donor source, names or values were adopted. Mickey remains authoritative.

Configured full-TU measurement: candidate 668 bytes / 167 words,
target 676 bytes / 169 words; size delta -8 bytes.
Raw and relocation-masked differences are 36 and 36, respectively,
first mismatch +0x20; candidate/target frames are
0x60/0x60. Candidate/target static relocation counts are
9/9; 6 tuples agree in function-relative offset, type and symbol.
These are fallback-object comparisons, not linked-C promotion evidence.

Workbench comparison: `structure-mismatch`. Diagnosis: `mixed(constant:4, structural:12, register:3)`;
playbook `constant-audit`, lever `stack-home`. The named guides were read.
This heuristic routing supplies no new donor evidence to reopen exhausted forms.
Next concrete lever requires new drawClearScreen donor C exposing declaration order for the VI size pair and the count carrier,
then a fresh gate and configured full-TU comparison.

Stopping evidence: zero source attempts; the authorized donor mechanism supplies
no new implementation. This is the assignment's early-exhaustion stop, not a
five-attempt stall. The prior TU lattice is not repeated: since its recorded
`func_8004ACC4` audit at `4be95a3d`, this TU differs only in EOF handoff comments.
See [the anchor donor audit](func_80049E4C.md) for the full delta and batch disposition.

Ignored evidence is retained in `build/tu-fx-audit/` and `build/wb/`: source and
full-TU object, gate receipts, summaries, diagnoses and relocation tuples.
Commands: `lane_status.py --symbol`, `wb_compare.sh --summary-json`, workbench
`diagnose` and `guide`, and `finalize_plateau.py`. The unchanged guarded C stays
NON_MATCHING and receives zero new exact bytes.

The pinned JFG `src/fx.c` contains only GLOBAL_ASM bodies. Relative to the old
`c82affff` reference, its changes are US placeholder-name corrections, an
`fx.h` include and a link to the `setupClearScreen` scratch `fDplg`; the last
TU change is `6ff6177`. The new header supplies that scratch's small input
structure and declarations, including the already-used six-argument
`fxSPDPRipple` signature. It supplies no drawClearScreen implementation or
VI/count local declarations. The linked scratch was already evaluated in the
committed `func_800498FC` handoff; those flat/regressing forms stay exhausted.

The retained anchor is 668/676 bytes, with all nine relocation names present
but only six exact tuples. Diagnosis refines the scheduling packet's
`structure-buckets` route to `constant-audit` and the `stack-home` lever:
the two VI output homes differ while the count carrier remains structural.
Fresh first mismatch is +0x20, superseding the packet's scheduling-only +0x18.
No generic declaration permutation is authorized by this donor-only packet.

The TU gate audit checked all 19 queued functions: 14 returned base-only and
were measured in ascending target-byte order after the anchor baseline.
Four were already-integrated/exhausted: `func_80048760`, `func_800498FC`,
`func_8004ACC4`, `func_8004AF68`. `func_80049000` was active in
`lane/m16-fx2`. Those five were skipped without source or handoff edits.
All 14 open functions had only assembly-backed donor counterparts, so none
received source attempts or new matching credit. The baseline canonical
`gmake verify` passed; its assembly-backed equality is not linked-C proof.

<!-- plateau-handoff:func_80049E4C:end -->
