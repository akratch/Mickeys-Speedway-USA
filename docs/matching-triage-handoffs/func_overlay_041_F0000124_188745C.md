<!-- plateau-handoff:func_overlay_041_F0000124_188745C:start -->
### `func_overlay_041_F0000124_188745C` plateau handoff

- source: `src/overlays/o041/overlay41UpdateColorRecords.c`
- score: 83/98 words
- frame: 0x30
- relocations: 4
- first mismatch: +0x30
- summary: Absolute-address reshape exhausted: symbolic C grows the function; exact-size baseline has zero opcode differences and a 15-word color/snapshot web.
- assignment base: `05cbca2025f87f1b4b670eaa73c6cd181ffd0dc7`
- owned range: Overlay 41 `+0x124..+0x2AC`, 392 bytes / 98 words, with the next owner beginning at `+0x2AC`
- baseline: configured `-O2 -mips2 -32` full-TU C has the exact 98-word extent and `0x30` frame; 20 raw / 15 relocation-masked words differ, with the first mismatch at `+0x30`
- relocation proof: target and candidate each have four records with all offsets/types aligned: `R_MIPS_HI16` at `+0x14`, `R_MIPS_LO16` at `+0x28`, and `R_MIPS_26` at `+0x128/+0x134`. Candidate symbols are `gOverlay41ColorRecords`, `overlay41SetColor`, and `overlay41SetAlpha`; preflight resolves 0/4 stable identities and therefore fails closed.
- call surface: export record 1453 owns `+0x124`; one resident `R_MIPS_26` inbound is authenticated at `0x80051640`
- attempts: initial-field-first forms regressed to 25 masked differences; a compound decrement with the retained local added one instruction; declaration order, statement order, `s16` remaining, `u8` snapshots, direct-field decrement, and a register hint were byte-flat. The full 119-row flag lattice found no exact result and tied the canonical O2/MIPS-II family for best.
- donor evidence: the pinned DKR v77/v80 and JFG overlay scans have no Overlay 41 candidate. The nearest permitted skeleton is JFG `animseqUpdateLightChangers` at 0.559, but its public source remains `GLOBAL_ASM` and supplies no C donor.
- next action: reopen only with a fidelity-clean allocator trace or source-faithful evidence for the six-way remaining/color/snapshot web, then authenticate the four candidate runtime identities before any promotion proof; do not repeat these ten forms or the flag lattice

#### 2026-09-10 lane `c2-reopen`: loop shape does not rotate the six-way web

Baseline reproduces: 392 bytes, raw 20, relocation-masked 15, zero size delta.

The residual reads as a two-group allocation, not a ring rotation. Taking the
nine values in emission order -- the remaining load, the four colour loads,
then the four snapshot copies -- the target's colours are `t2` then the four
argument registers then `v1 t0 t1 s2`, and the candidate's are `a0`, then
`v1 t0 t1 s2`, then the remaining three argument registers and `t2`. Neither
list is a rotation of the other: the two sides put the *loads* and the
*copies* in opposite groups, which is the same shape as the saved-FPR residual
recorded for `func_overlay_009_F0000540_1866BB8` and is not reachable by
reordering within either group.

The loop-shape law was the one new axis the record did not already cover, and
it is byte-inert here: rewriting `do { ... } while (i--)` as a top-tested
`while (i--)` with the initial value bumped by one, and as the equivalent
`for`, each produce an object byte-identical to the baseline -- uopt rotates
the two forms together. Counting up with an explicit `!=` exit test instead
regresses to 100 words and eight bytes of growth.

Verdict: **the closure stands**, and its next action is unchanged. The residual
is now stated as a group-membership question rather than a register-permutation
one, which is the same reopen bar the overlay 9 shard carries.
<!-- plateau-handoff:func_overlay_041_F0000124_188745C:end -->
