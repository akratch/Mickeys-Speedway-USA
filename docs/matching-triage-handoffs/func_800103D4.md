<!-- plateau-handoff:func_800103D4:start -->
### `func_800103D4` plateau handoff

- source: `src/main/track.c`
- score: 158 differing words
- frame: 0x60
- relocations: 12
- first mismatch: +0x0
- summary: Correct Mickey object-kind dispatch retained at 158 differences; frame and relocation deficits remain. Next: saved-FP lifetime evidence.

#### Mickey jump-table reconstruction, TU batch 2026-09-08

- The fresh assignment gate returned `base-only`. Original configured baseline: 159 candidate versus 160 target words, 122 raw/120 masked differences, frame `0x60` versus `0x38`, 12 versus 20 relocation records and three exact offset/type/identity tuples. That lower scalar score is not a valid retained candidate: the source dispatched several object kinds to the wrong actions.
- The first context-aware m2c invocation failed because it lacked the jump-table input. Supplying Mickey's extracted rodata to both the structured and goto views exposed the actual ranges: one table starts at kind 63 and contains 26 entries; the other starts at kind 1 and contains 29 entries. The old C applied the table families to the opposite groups. The default structured m2c view also omits no-op dispatch entries, so it was checked against every original table entry instead of copied literally.
- The retained switch reconstructs all five state-dependent actions and all seven no-op kinds. A separate source-dispatch audit compares the reconstructed C case groups to the decoded table actions over all 65,536 signed-halfword inputs: zero discrepancies across the 55 table entries and the out-of-range default. Game-mode gating, field widths, fade behavior and the original plane-loop body are preserved.
- Attempt 17 implements the corrected unified switch and reaches 156 candidate versus 160 target words, 158 raw/masked differences from `+0x0`, frame `0x60` versus `0x38`, and 12 versus 20 relocations with one exact offset/type/identity tuple. This is the retained admissible result; the incorrect 120-difference baseline is rejected despite the heartbeat's scalar-best history.
- Workbench verdict remains `structure-mismatch`, playbook `constant-audit`, lever 1. The switch/table identity correction resolves the concrete wrong-control-flow input. Attempts 18 through 21 then test m2c's direct object accesses/pointer-first loop condition, reconstructed invariant FP storage, nested per-case state accesses and the word-width distance-limit carrier. None reduces the 158-word residual; they produce 157 words with frames `0x60` or `0x68`. These source forms are retained only as ignored experiments. The corrected switch over the original reviewed fade/plane body is restored and recompiled.
- Stop condition: the new table mechanism is resolved and independently checked, while the remaining saved-FP/frame and relocation geometry has no new identity or CFG input from m2c. This records four unproductive follow-ups after the semantic correction rather than claiming a five-attempt stall. No flag change, permuter, instruction editing or further allocator mechanism is substituted.
- Next concrete lever: authenticate the source storage/lifetimes that retain the four plane-loop invariants in FP registers with the target `0x38` frame, then reconcile the 12-versus-20 static relocation surface without confusing compiler-owned jump-table contents with text exactness. The initial wrong dispatch must never be restored merely for its lower scalar score.
- Batch attempts 17 through 21, baseline at count 16, the source-dispatch audit, decoded table evidence, both m2c views, source/object snapshots and summaries remain under ignored `build/wb/tu-track/func_800103D4/`. Commands: configured `wb_compare.sh`, workbench `diagnose`/`guide constant-audit`, m2c with supplemental rodata, the complete signed-kind dispatch check, checkpoint updates, restoration/reproof and `finalize_plateau.py`. C remains guarded with the original fallback; zero new matching credit is added.

#### Pinned donor audit, 2026-09-08

- Assignment gate: `base-only` at `32a75d648e8954f7455897fb8f16a8ef5f05df11` for this exact symbol and source path.
- Jet Force Gemini `efd5abb1c79636e297b831f7c2d5bf47eac39c0c` has 12 C implementations and 53 assembly placeholders in `src/track.c`. Its implemented routines do not supply this target's body. The closest masked track-object hit, `func_80015D54` (Jaccard 0.1143), corresponds by verified TU order to the still-assembly `func_80015ED8_16AD8` in that pinned source. This is diagnostic structural context, not an adopted name, ABI identity, or exact donor match.
- Fresh configured full-TU measurement: 160 target words and 159 candidate words; 122 raw and 120 relocation-masked differences; first +0x0; target frame 0x38, candidate frame 0x60. There are 12 candidate versus 20 target relocation records, with 3 stable identities at matching offsets/types.
- Workbench verdict `structure-mismatch`, playbook `constant-audit`. The existing diagnosis still requires source-shape/lifetime evidence; the donor-only reopen does not authorize substituting a flag, allocator, or permuter mechanism.
- ADR 0018 stop: zero new source attempts for this target. The pinned source disproves the new matched-donor-C hypothesis. No unchanged flag lattice or previously exhausted source family was repeated. The compiled candidate is unchanged and remains behind `NON_MATCHING` and its original `GLOBAL_ASM` fallback; zero new matching bytes are credited.
- Next concrete lever: a published matched counterpart with an authenticated ABI and useful source lifetimes, followed by Mickey-specific field, branch and call proof. The remaining general levers below require separately authorized changed evidence.
- Commands: `tools/wb_compare.sh --summary-json func_800103D4`, workbench `diagnose` on the configured full-TU object, the track-object masked skeleton audit, `tools/finalize_plateau.py`, `gmake cleanroom`, and `gmake check-docs`. Source, object, scores, first mismatch and previous handoff are retained under ignored `build/wb/`; no instruction rows are included here.

#### Prior committed plateau evidence (historical)


- source: `src/main/track.c`
- score: 132 differing words
- frame: 0x60
- relocations: 20
- first mismatch: +0x0
- summary: Best is 159/160 with 120 positional differences; saved-FP storage class and a target 20 versus candidate 12 relocation deficit remain.
<!-- plateau-handoff:func_800103D4:end -->
