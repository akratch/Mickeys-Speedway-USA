<!-- plateau-handoff:func_800133FC:start -->
### `func_800133FC` plateau handoff

- source: `src/main/track.c`
- score: 98 differing words
- frame: 0x98
- relocations: 1
- first mismatch: +0x0
- summary: m2c coordinate types do not close the structure; baseline retained. Next: original FP and integer-coordinate lifetimes.

#### Mickey m2c reconstruction audit, TU batch 2026-09-08

- The assignment gate returned `base-only` at batch base `d0bdcaf16a9b0a7fb7371eb435c0940cb33f5c19`. Configured full-TU baseline: 99 candidate versus 96 target words, 98 raw and masked differences from `+0x0`, frame `0x98` versus `0xA0`, and one `sqrtf` relocation on each side. The call identity resolves but its offset is eight bytes late, so zero relocation identities align at exact offsets.
- Workbench verdict `structure-mismatch`, playbook `constant-audit`, lever 1: the earliest immediate is the frame extent. Mickey's signed coordinate fields, zero-length check and three-component plane calculation contain no wrong numeric constant. The fresh context-aware m2c draft reproduces every existing expression and call; its material difference is signed-halfword coordinate temporaries instead of the retained word carriers.
- Attempt 1 compiled that direct draft with its natural FP declarations. Attempt 2 isolated the inferred halfword carriers while restoring the existing register-qualified FP components. Both retained 99 words and 98 raw/masked differences but regressed the frame to `0x90`; neither recovered the target's word spill homes. The actual IDO-preprocessed context comparison accepts the changed function with unchanged surrounding declarations and definitions.
- These measurements eliminate the only changed type input supplied by the new draft. Its CFG and identities are already those of the retained reconstruction; it supplies no new evidence to reopen the historical ten natural FP/lifetime forms. This is mechanism exhaustion after two source attempts, not a count-based plateau or a claim that five uninformative variants were run. Baseline source was restored and recompiled; no new matching bytes are credited.
- Next concrete lever: original FP-component and retained integer-coordinate lifetimes, with target word spill widths preserved. A new allocator or random-search mechanism was not substituted for the authorized decompiler reconstruction.
- Source, objects, hypotheses, scalar summaries, the context receipt and draft remain under ignored `build/wb/tu-track/func_800133FC/`. Commands: `wb_compare.sh --summary-json`, workbench `diagnose` and `guide constant-audit`, m2c with the full-TU context, `reloc_surface.py compare`, `candidate_context.py`, and `finalize_plateau.py`. The original `NON_MATCHING` guard and assembly fallback remain active.

#### Pinned donor audit, 2026-09-08

- Assignment gate: `base-only` at `32a75d648e8954f7455897fb8f16a8ef5f05df11` for this exact symbol and source path.
- Jet Force Gemini `efd5abb1c79636e297b831f7c2d5bf47eac39c0c` has 12 C implementations and 53 assembly placeholders in `src/track.c`. Its implemented routines do not supply this target's body. The closest masked track-object hit, `trackSphereIntersect` (Jaccard 0.0140), corresponds by verified TU order to the still-assembly `trackSphereIntersect` in that pinned source. This is diagnostic structural context, not an adopted name, ABI identity, or exact donor match.
- Fresh configured full-TU measurement: 96 target words and 99 candidate words; 98 raw and 98 relocation-masked differences; first +0x0; target frame 0xa0, candidate frame 0x98. There are 1 candidate versus 1 target relocation records, with 0 stable identities at matching offsets/types.
- Workbench verdict `structure-mismatch`, playbook `constant-audit`. The existing diagnosis still requires source-shape/lifetime evidence; the donor-only reopen does not authorize substituting a flag, allocator, or permuter mechanism.
- ADR 0018 stop: zero new source attempts for this target. The pinned source disproves the new matched-donor-C hypothesis. No unchanged flag lattice or previously exhausted source family was repeated. The compiled candidate is unchanged and remains behind `NON_MATCHING` and its original `GLOBAL_ASM` fallback; zero new matching bytes are credited.
- Next concrete lever: a published matched counterpart with an authenticated ABI and useful source lifetimes, followed by Mickey-specific field, branch and call proof. The remaining general levers below require separately authorized changed evidence.
- Commands: `tools/wb_compare.sh --summary-json func_800133FC`, workbench `diagnose` on the configured full-TU object, the track-object masked skeleton audit, `tools/finalize_plateau.py`, `gmake cleanroom`, and `gmake check-docs`. Source, object, scores, first mismatch and previous handoff are retained under ignored `build/wb/`; no instruction rows are included here.

#### Prior committed plateau evidence (historical)


- source: `src/main/track.c`
- score: 98 differing words
- frame: 0x98
- relocations: 1
- first mismatch: +0x0
- summary: Candidate is 99 words/frame 0x98 vs target 96/0xA0; sqrtf identity is exact but +0x8 late. Ten natural forms were nonexact; next needs original FP lifetimes.
<!-- plateau-handoff:func_800133FC:end -->
