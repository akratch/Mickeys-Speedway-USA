<!-- plateau-handoff:func_overlay_031_F00002E8_187F808:start -->
### `func_overlay_031_F00002E8_187F808` plateau handoff

- source: `src/overlays/o031/overlay31InitializeParticleAssets.c`
- score: 9/132 words
- frame: 0x30
- relocations: 24
- first mismatch: +0xC
- summary: Fresh V0 is +1 word; its 0x4 TU overrun closes relocation preflight. Current-context structured reproof only; body unchanged.

Fresh maintenance evidence on base
`831d8044e547eab952d29b9bc35ae04f6888f0ed`:

- the fallback and adjacent owners uniquely bind overlay 31 `.text`
  `+0x2E8..+0x4F8`, ROM `0x187F808..0x187FA18`: `0x210` / 528
  executable bytes with no target padding before `overlay31BuildPalettes`;
- the ABI is `void (void)`. Export table entry 1672 owns the target, and
  overlay 18 relocation table 1 entry 24 supplies the sole authenticated
  cross-overlay inbound `R_MIPS_26` call;
- configured IDO 5.3 `-O2 -mips2 -32` emits 532 bytes / 133 words against the
  528-byte / 132-word target. Both frames are `0x30`; the positional result is
  nine matches and 123 raw/masked differences, first `+0xC`;
- workbench reports five aligned structural and six schedule residuals with
  three insertions and two deletions. The candidate's single extra instruction
  is a real `+0x4` function-size drift, not alignment padding;
- target runtime and candidate static text surfaces each contain 24
  relocations (nine HI16, nine LO16, six R_MIPS_26 in the target). Because the
  candidate occupies TU `.text +0x0..+0x214` while the atlas owner ends at
  `+0x210`, `function_preflight.py` refuses to reinterpret the overrun and the
  structured relocation comparison remains unavailable;
- the donor scan confirms the known family: JFG `partInitLib` is closest at
  `0.4328`, followed by the disclosed DKR `init_particle_assets` analogue at
  `0.2384`. These are context only; the current source already records DKR
  provenance and this evidence pass adopted no new body;
- prior direct/chained indexing, count-pointer carrier, and other recorded p7
  forms remain exhausted. No body mutation, flags, or permutation was run.

Next lever: a source-backed form must first remove exactly the extra word and
restore the `0x210` owner boundary. Only then can preflight authenticate the
24 relocation identities; do not weaken the owner gate or treat the overrun as
padding.
<!-- plateau-handoff:func_overlay_031_F00002E8_187F808:end -->
