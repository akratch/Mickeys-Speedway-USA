<!-- plateau-handoff:func_8000DB34:start -->
### `func_8000DB34` plateau handoff

- source: `src/main/track.c`
- score: 144 differing words
- frame: 0x190
- relocations: 6
- first mismatch: +0x5c
- summary: Correct inclusive object traversal improves to 144 differences, frame 0x190 exact. Next: map and traversal declaration evidence.

#### Mickey m2c structural reconstruction, 2026-09-08

- Fresh assignment gate returned `base-only`. Baseline: 170 candidate versus 172 target words, 146 raw/masked differences, first +0x38, exact 0x190 frame, 6 candidate/target relocations and zero exact offset/type/identity matches. Workbench verdict `structure-mismatch`, playbook `constant-audit`; its earliest displaced immediate is a stack-array address, not a wrong map extent.
- Mickey m2c plus the original branch-delay order proves that the reverse object range includes its first index. The previous C decremented before the first iteration and again before the continuation test, skipping that final object when the range contains several objects. For bounds first=2 and end=5, target calls indices 4, 3, 2; old C called 4, 3. The corrected body decrements the address-taken end at the top of each iteration, stores it before the lookup call, and compares the stored index afterward.
- The output parameter of `func_8000A244` is signed, confirmed by its Mickey definition and the target signed comparison. Its local is now `s32`. The unused result of `func_8000A39C` was incorrectly declared `s32`; the TU declaration now agrees with its Mickey `void` definition. Both callers ignore the result.
- Attempt 27 fixes those semantics: 168 words, 148 differences. Attempt 28 uses the fresh draft byte cursor for map initialization: 166 words, 160 differences. Attempt 29 separates the computed object index from the address-taken count: 169 words, 144 differences, first +0x5C, frame 0x190; this is the retained best admissible candidate.
- Follow-ups 30–34 test the m2c diameter definition point, halfword selected-segment inference, separate reverse countdown, separate reverse cursor, and shared post-visibility segment load. They produce respectively 144, 150, 167, 166, and 166 differences. None improves attempt 29 or proves an additional identity. Five consecutive nonprogressing follow-ups meet the assignment stopping rule.
- Final retained candidate: 169/172 words, 144 raw/masked differences, first +0x5C, exact 0x190 frame, 6/6 relocation records with zero exact offset/type/identity sites. The prior 146-word baseline has the wrong object range and must not be restored merely for its historical source shape. All attempts are saved as ignored source/object/score artifacts.
- The one TU flag sweep was run on the original pinned TU input: 119 combinations attempted, 53 scored, 66 compiler/unsupported failures. Configured O2/mips2/r4300_mul ties the best DB34 result; no flag change is proposed and failed combinations are not claimed as coverage.
- Next concrete lever: evidence for original map initialization and traversal declaration lifetimes. m2c supplies the algorithm but the remaining three-word and relocation-offset gaps require a separately authorized compiler-lifetime mechanism.
- Validation: configured full-TU comparison after each attempt, `gmake check-nonmatching-builds`, finalizer, `gmake verify`, `gmake cleanroom`, and `gmake check-docs`. The C remains guarded with its original assembly fallback; zero new matching bytes.

#### Pinned donor audit, 2026-09-08

- Assignment gate: `base-only` at `32a75d648e8954f7455897fb8f16a8ef5f05df11` for this exact symbol and source path.
- Jet Force Gemini `efd5abb1c79636e297b831f7c2d5bf47eac39c0c` has 12 C implementations and 53 assembly placeholders in `src/track.c`. Its implemented routines do not supply this target's body. The closest masked track-object hit, `func_8001412C` (Jaccard 0.0323), corresponds by verified TU order to the still-assembly `func_8001436C_14F6C` in that pinned source. This is diagnostic structural context, not an adopted name, ABI identity, or exact donor match.
- Fresh configured full-TU measurement: 172 target words and 170 candidate words; 146 raw and 146 relocation-masked differences; first +0x38; target frame 0x190, candidate frame 0x190. There are 6 candidate versus 6 target relocation records, with 0 stable identities at matching offsets/types.
- Workbench verdict `structure-mismatch`, playbook `constant-audit`. The existing diagnosis still requires source-shape/lifetime evidence; the donor-only reopen does not authorize substituting a flag, allocator, or permuter mechanism.
- ADR 0018 stop: zero new source attempts for this target. The pinned source disproves the new matched-donor-C hypothesis. No unchanged flag lattice or previously exhausted source family was repeated. The compiled candidate is unchanged and remains behind `NON_MATCHING` and its original `GLOBAL_ASM` fallback; zero new matching bytes are credited.
- Next concrete lever: a published matched counterpart with an authenticated ABI and useful source lifetimes, followed by Mickey-specific field, branch and call proof. The remaining general levers below require separately authorized changed evidence.
- Commands: `tools/wb_compare.sh --summary-json func_8000DB34`, workbench `diagnose` on the configured full-TU object, the track-object masked skeleton audit, `tools/finalize_plateau.py`, `gmake cleanroom`, and `gmake check-docs`. Source, object, scores, first mismatch and previous handoff are retained under ignored `build/wb/`; no instruction rows are included here.

#### Prior committed plateau evidence (historical)


- source: `src/main/track.c`
- score: 146 differing words
- frame: 0x190
- relocations: 6
- first mismatch: +0x38
- summary: Fresh V0 is 170/172 words with 146 differences; frame 0x190 exact. Both have 6 relocations but zero sites align. Prior mechanisms closed.
<!-- plateau-handoff:func_8000DB34:end -->
