<!-- plateau-handoff:func_overlay_008_F00034A0_18611F8:start -->
### `func_overlay_008_F00034A0_18611F8` plateau handoff

- source: `src/overlays/o008/overlay_008.c`
- score: 466/490 words
- frame: 0x80
- relocations: 107
- first mismatch: +0x1C
- summary: declaration order moves the sample pointer phase from 482 to 466 positional differences; pointer home, ugen ring, and buffer reload remain

#### Phase census and the allocator's real share (2026-09-11)

**Census first: uopt procedure 14 of 21, 53 globalcolor decisions, all phase
one, zero phase two.** 26 are integer and 27 are floating. So the phase-two
web-numbering axis is not live here and no amount of moving a defining
statement is a colour lever for this function. This is not special to this
function: across the 61 procedures of this unit, `overlay_001_tail.c` and
`overlay52TailB.c`, every procedure that issues a call emitted phase-one
records only and every leaf emitted phase-two records only, with no
counterexample. See `docs/ido-learnings.md`.

Aligned against the target on a register-erased shape (`tools/align_symbol.py`):
897 candidate words against 898, positional 482, displacement tax 172, and the
residual splits 592 byte-exact / 209 register-naming / 101 really-different.

**The size deficit is not a missing source operation.** The aligner places
four extra candidate words and five extra target words in seven separate
spans, so "one instruction short" is a net, not a site. Forcing floating web
597 down the split path (`CDX_FORCE=p1:w597=s`) alone produces an 898-word
object: the target spills that web where the candidate keeps it in `f18`, and
its reload is the missing word. Positional falls 482 to 365 and the
really-different bucket 101 to 94 with no source change at all.

**Region partition, 22 call-delimited regions**, as words / byte-exact /
naming / really-different:

  - region 0, +0x0: 222 words, 185 exact, 30 naming, 7 different
  - region 3, +0x434: 54 words, 22 exact, 4 naming, 28 different
  - region 4, +0x508: 166 words, 155 exact, 8 naming, 3 different
  - region 8, +0x854: 48 words, 26 exact, 10 naming, 12 different
  - region 9, +0x910: 41 words, 21 exact, 13 naming, 7 different
  - region 10, +0x9B4: 79 words, 36 exact, 38 naming, 5 different
  - region 12, +0xB20: 24 words, 7 exact, 10 naming, 7 different
  - region 18, +0xC08: 87 words, 25 exact, 59 naming, 3 different
  - region 21, +0xDBC: 19 words, 7 exact, 11 naming, 1 different

The remaining thirteen regions hold 26 naming and 28 different words between
them. Region 18 is the largest naming region and region 3 the only large
structural one.

**Named decision variable per residual class.**

- *The single largest naming item is 27 rows of `a3` where the target names
  `a2`, and it is one web.* Phase-one web 584 (`sym=0`, a compiler temp) is
  the twentieth integer web to take a colour, at `save=0.917`
  (`totalsave=11`, `nocs=12`), and it reaches its decision with `forbidden0=0x7d020000` and `available0=0x02000000`: `a2` is
  already forbidden and `a3` is the only colour left. This is L101's
  `forbidden=<mask>` case, so no save ratio or spelling of *this* web moves it;
  the lever is whatever holds `a2` across its range.
- *The float residual is a ring rotation, and the ring is not the allocator's.*
  `f4`->`f10`, `f6`->`f4`, `f8`->`f6`, `f10`->`f8` accounts for 118 of the
  field substitutions, a one-step rotation of a four-register ring. None of
  `f4`, `f6`, `f8`, `f10` is ever a globalcolor colour: the class-2 pool is
  c24=`f0`, c25=`f2`, c26=`f12`, c27=`f14`, c28=`f16`, c29=`f18`, decoded by
  force-and-diff on webs 187 and 29 of this very procedure. Of the 105 rows
  with a float difference -- 92 float-only, 13 alongside an integer one --
  only 9 have both sides inside the allocator pool.
- *Sixty-two of the 101 really-different rows are not different code.* 36 are
  the same instruction at a different `sp` displacement, 16 at a different
  non-`sp` displacement and 10 the same mnemonic with another immediate; only
  30 are a different opcode and 9 are present on one side alone.
- *The frame is one escaping pointer out of place, and nothing else.* Both
  frames are 128 bytes and every home pairs one-to-one by its load/store
  profile. The only reordering is the escaping four-byte pointer
  `&query.samples0`: the candidate puts it at `+0x54`, immediately under the
  four-word scratch block at `+0x58`, and the shipped frame puts it at `+0x40`,
  *under* three ordinary float homes at `+0x44`/`+0x48`/`+0x4C`. Every other
  home is displaced by exactly -4 as a consequence -- 32 of the 36 `sp` rows
  are that constant. This is the same defect the earlier note names; the new
  content is that it is the *whole* of the frame difference, so a spelling that
  moves that one object closes 32 rows and nothing else needs to move.

**How much of this is the allocator's to give: at most 50 words of 310.**
Greedy force ceiling, sweeping all 47 webs against every colour and the split
path each round and keeping the best (736 compiles per round, 21 seconds):
592/209/101 -> 620/189/94 (`p1:w597=s`) -> 632/177/94 (`+p1:w4=c2`) ->
637/172/94 (`+p1:w449=c27`) -> 641/168/94 (`+p1:w593=c25`) -> 642/167/94
(`+p1:w451=c28`), and the sixth round found nothing. Positional 482 -> 347.
The surviving 167 naming rows are still the `a3`/`a2` web, an `f6`/`f8`
transposition and the integer `t3`..`t9` ring, and the 94 really-different
rows do not move at all. **Do not open an allocator lattice on this function
for more than about a sixth of its residual; the rest is ugen's temp ring, the
one escaping-pointer home, and the global re-read in the -3 region.**

Nothing above is a source change, a match claim or a credit claim. The
`GLOBAL_ASM` fallback remains canonical and `gmake verify` passes at this
commit.
<!-- plateau-handoff:func_overlay_008_F00034A0_18611F8:end -->
