<!-- plateau-handoff:levelFreeAll:start -->
### `levelFreeAll` plateau handoff

- source: `src/main/level.c`
- score: 114/117 words
- frame: 0x28
- relocations: 36
- first mismatch: +0x13C
- summary: Fidelity-clean IDO 5.3 UGEN tracing improved the pointer-add block to three register-only words. Next lever: a natural mask -> pointer -> scale allocation order; flags and blind permutation are exhausted.
- relocation proof: 36/36 offsets, types, and identities exact
- trace proof: A hash-pinned IDO 5.3 UGEN build passed stock object fidelity for text, rodata, data, relocations, and symbols. Its source-line trace proved the baseline allocates pointer, mask, then scale at the one mismatching expression.
- retained gain: Semantically equivalent byte-offset arithmetic changes that order to mask, scale, then pointer. It makes the masked index exact and improves the body from 112/117 to 114/117 words without changing size, frame, calls, or relocations.
- residual: Three register-only words remain: the pointer and scale temporaries are exchanged. The required natural allocation order is mask, pointer, then scale. Do not repeat the 119-flag lattice, blind permutation, typed-pointer commutations, casts, or assignment forms.
- current baseline proof: The September 6 causal packet independently reproduced 468 bytes, frame 40 and all 36 exact relocation offsets, types and identities. The actual prepared compiler input compared unchanged with itself and emitted the same owned fields as the configured full TU. Fresh stock, trace-off and trace-on fidelity passed for text, data, rodata, relocations and symbols; named Ucode authenticated procedure 17 of 21 before interpreting the trace.
- predicate hypothesis and domain: After the existing two high-bit arms fail, the signed-16-bit ID is in 0..32767. Comparing its low-14-bit mask with the original ID selects exactly the same third arm as the bit-14 test; all 65536 input patterns were checked. Table indexing retains the existing valid resource-table assumption, supported by matched levelInit's identical table/index access. No cleanup call, access count or call order changes were proposed.
- controlled rejection: Replacing only that predicate with low-mask inequality, with either byte-offset or typed indexing, produced 464 bytes instead of 468. Both had 43 differing common-extent words plus one missing word. Workbench alignment was not directly comparable: its eleven classified sites were nine register and two structural sites, including the deletion rather than adding it again. All 36 relocation identities resolved, but only 25 offset/type/identity tuples aligned. Both remain nonexact diagnostics, not improved canonical candidates.
- measured cause: Predicate reuse made the mask a colored value across the branch and removed its separate body calculation. GP allocator-result events fell from 12 to 10. The byte-offset and typed variants still differed in three pointer/scale register fields, demonstrating a tree-order effect independently of the shared-mask class change. The first positional mismatch at +0xD4 was a shifted branch displacement, not evidence of an earlier source error. No target compiler trace is claimed.
- control and follow-on: Reproducing the old typed indexing with the original predicate under the same current TU returned 112/117 words. A condition-side masked assignment with the original predicate and a defined comma-one operand retained 468 bytes, frame 40 and all 36 exact relocations, but only 106/117 words matched, first +0xE0. It remains an explicitly inert diagnostic; no source was adopted.
- follow-on trace: Faithful stock/trace-off/trace-on controls showed the condition-side mask store and literal-true branch before UOPT, but neither survived after it. The optimized expression restored pointer -> mask -> scale demand, not the desired order. GP allocator-result events returned to 12. Loop-register roles also changed; different priority diagnostics are observed, but numeric web IDs are not asserted to be stable cross-run source identities. The eleven differing words comprise five typed temporary-role differences and six pool-register-field differences; workbench aligns two initializer rows as scheduling differences. This is a failed staging probe, not evidence that a new mask boundary survived optimization.
- scope and stop: Four source forms were compiled, including the reproduced old-typed control; no random search or flag sweep ran. All 20 neighboring functions retained identical normalized executable fields, and data/rodata were unchanged. Raw source-relocation correspondence passed for 16 neighbors; four local-symbol cases were explicitly unverified, not claimed exact. The original canonical C and assembly fallback remain unchanged. These predicate-sharing forms are exhausted, not every alternative allocation history. A new experiment must explain how it recovers the target's separate calculations and register roles; another equivalent spelling of this rejected route alone is not new evidence.
<!-- plateau-handoff:levelFreeAll:end -->
