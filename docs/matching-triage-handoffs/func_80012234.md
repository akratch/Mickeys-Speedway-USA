<!-- plateau-handoff:func_80012234:start -->
### `func_80012234` plateau handoff

- source: `src/main/track.c`
- score: 187 differing words
- frame: 0x60
- relocations: 3
- first mismatch: +0xc
- summary: Fresh Mickey m2c reproduces existing typed CFG; zero new attempts, 187 differences. Next: source-attributed FP lifetime evidence.

#### Mickey m2c structural audit, 2026-09-08

- Fresh assignment gate returned `base-only`. Configured baseline remains 211 candidate / 208 target words, 187 raw/masked differences, first +0xC, exact 0x60 frame, and 3/3 `sqrtf` relocations with only the first site exact in offset, type and identity. Workbench verdict `structure-mismatch`, playbook `constant-audit`; zero/radius tests and every cross-product sign and axis were checked against Mickey's draft and assembly before considering source changes.
- Ordinary and carrier-preserving fresh Mickey m2c drafts recover exactly the existing float-vector ABI, delta/normal/cross identities, three square-root calls, early degenerate return, absolute-dot radius test, and interval endpoint stores. Their expression association and division order are already present in the guarded C. There is no missing call, global, field identity or CFG edge to reconstruct.
- The draft's additional stack-slot names describe compiler spill copies in the outgoing-argument area. They do not establish original declarations or different source-level storage. The existing committed plateau already identifies FP allocation and cross-product scheduling after recovering local vectors and the exact frame. Copying those spill assignments or retrying scalar/array spellings would substitute the closed source-shaping/allocation mechanism.
- ADR 0018 early stop: zero new source attempts. This is an audit of the newly authorized decompiler input, not a five-attempt stall claim. The unchanged configured baseline and both fresh drafts are retained under ignored `build/wb/`; the original `NON_MATCHING` and assembly fallback remain. No new matching bytes.
- Next concrete lever: authenticated original declarations or source-attributed compiler lifetime evidence explaining the two displaced `sqrtf` sites. m2c alone supplies neither; reopen only when that evidence changes.
- Validation: fresh full-TU workbench comparison and diagnosis, finalizer, `gmake verify`, `gmake cleanroom`, and `gmake check-docs` before commit.

#### Pinned donor audit, 2026-09-08

- Assignment gate: `base-only` at `32a75d648e8954f7455897fb8f16a8ef5f05df11` for this exact symbol and source path.
- Jet Force Gemini `efd5abb1c79636e297b831f7c2d5bf47eac39c0c` has 12 C implementations and 53 assembly placeholders in `src/track.c`. Its implemented routines do not supply this target's body. The closest masked track-object hit, `trackCylinderIntersect` (Jaccard 0.1870), corresponds by verified TU order to the still-assembly `trackCylinderIntersect` in that pinned source. This is diagnostic structural context, not an adopted name, ABI identity, or exact donor match.
- Fresh configured full-TU measurement: 208 target words and 211 candidate words; 187 raw and 187 relocation-masked differences; first +0xC; target frame 0x60, candidate frame 0x60. There are 3 candidate versus 3 target relocation records, with 1 stable identities at matching offsets/types.
- Workbench verdict `structure-mismatch`, playbook `constant-audit`. The existing diagnosis still requires source-shape/lifetime evidence; the donor-only reopen does not authorize substituting a flag, allocator, or permuter mechanism.
- ADR 0018 stop: zero new source attempts for this target. The pinned source disproves the new matched-donor-C hypothesis. No unchanged flag lattice or previously exhausted source family was repeated. The compiled candidate is unchanged and remains behind `NON_MATCHING` and its original `GLOBAL_ASM` fallback; zero new matching bytes are credited.
- Next concrete lever: a published matched counterpart with an authenticated ABI and useful source lifetimes, followed by Mickey-specific field, branch and call proof. The remaining general levers below require separately authorized changed evidence.
- Commands: `tools/wb_compare.sh --summary-json func_80012234`, workbench `diagnose` on the configured full-TU object, the track-object masked skeleton audit, `tools/finalize_plateau.py`, `gmake cleanroom`, and `gmake check-docs`. Source, object, scores, first mismatch and previous handoff are retained under ignored `build/wb/`; no instruction rows are included here.

#### Prior committed plateau evidence (historical)


- source: `src/main/track.c`
- score: 187/208 words
- frame: 0x60
- relocations: 3
- first mismatch: +0xC
- summary: 211/208 words; exact frame. Only the first sqrtf relocation aligns. FP allocation and cross-product scheduling remain; flag sweep lacks a sized owner.
<!-- plateau-handoff:func_80012234:end -->
