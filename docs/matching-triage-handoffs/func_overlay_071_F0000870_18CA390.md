<!-- plateau-handoff:func_overlay_071_F0000870_18CA390:start -->
### `func_overlay_071_F0000870_18CA390` plateau handoff

- source: `src/overlays/o071/func_overlay_071_F0000870_18CA390.c`
- score: 171/182 words
- frame: 0x30
- relocations: 13
- first mismatch: +0x94
- summary: Folding each command's w0/w1 pair onto one physical line makes their as1 line-number tie fall through to ready-list position and reproduces the shipped store order at all ten simple command sites, 33 -> 11 words at unchanged size and frame. The residue is one colour: the four state->flags reads want a2 and take a1/a0. Instrumented globalcolor proves it reachable (forcing both load webs to c5 gives 0 words); no source form yet parks an interfering web on a1.

#### 2026-09-10, lane `w8-tu`: the colour requirement stated as an ordering, from the trace

Baseline reproduces: 728 bytes, eleven relocation-masked words, zero size
delta. All eleven follow from the two flag-load webs; the two argument-setup
words that move with them are consequences, not separate residuals.

**The two webs are named and their decision records read out.** Both are p1
(callee-saved sweep) with `class=1`, `save=2.000000`, `bestcost=0`:

- web 27 -- the first two reads -- `forbidden0=0x70020000`, i.e. c1, c2, c3 and
  s0 are taken, so it takes c4;
- web 50 -- the last two reads -- `forbidden0=0x60020000`, i.e. c1, c2 and s0,
  so it takes c3.

Both want c5. So web 27 needs **one** further interferer holding c4, and web 50
needs **two**, on c3 and c4.

**The order the sweep runs in is strictly descending `save`, with ties broken
by ascending web number**, which is L100's p1 half reproduced on a third
function: 15 at 16.0 takes c1, 11 at 5.6 takes s0, 1 at 3.5 takes c2, then
25, 27 and 50 all at exactly 2.0 in web-number order. Web 25 is the invisible
`u16 flags` carrier the earlier pass named; it interferes with web 27 and
**not** with web 50, which is precisely why the two land one colour apart.

**That turns the residual into an ordering requirement with no free slot.**
A web that fixes both must interfere with 27 and 50, must be coloured before
them -- so `save` strictly above 2.0, or exactly 2.0 with a web number between
25 and 27, and no web 26 exists -- and must land on c3, pushing web 25 to c4.
Every other interferer of 27 and 50 is a **constant** web (the display-list
command words, the bank stride, the two segment bases) with `save` between 0.0
and 1.0, so all of them are coloured after both and none can help. There is no
candidate in the current web set, and the lever has to *create* one.

**Newly falsified this pass, each measured against the whole 182-instruction
target text.** All seven subsets of reusing the single `flags` carrier at the
second, third and fourth test sites; a separate carrier per site and per pair;
a chained second carrier on the first test; the carrier's type over `u8`,
`u16`, `u32`, `s32` and `s16` (`s16` is 109 words, the rest are 11 or 12); and
uopt region boundaries before the second, third and fourth tests and around the
first test's assignment. Floor 11 throughout, and every cell that moves at all
moves upward.

<!-- plateau-handoff:func_overlay_071_F0000870_18CA390:end -->
