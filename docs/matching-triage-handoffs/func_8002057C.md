<!-- plateau-handoff:func_8002057C:start -->
### `func_8002057C` plateau handoff

- source: `src/main/models.c`
- score: 229/342 words
- frame: 0xD0
- relocations: 21
- first mismatch: +0x3C
- summary: Largest +0x200 window: 47 naming, 11 immediate, 44 structural; frame exact, local-home order unresolved.

## 2026-09-11 (lane `lane/w3-mid`): the closure never measured the frame, and the frame was the lever

Before: 249 positional masked words; aligned 100 byte-exact, 106 register
naming, 50 immediate only, 93 really different; first mismatch +0x0.
After: 229 positional masked words; aligned 122 byte-exact, 112 register
naming, 28 immediate only, 87 really different; first mismatch +0x3C.
Size delta 0 and 21 relocations on each side, unchanged.

`tools/frame_census.py` reports the candidate frame as 0xC0 against the
target's 0xD0. The 2026-08-31 header recorded that pair and then swept
declaration, scope and parameter forms on top of it, which is a space that
cannot contain the answer. Both sides carry seven incoming argument words and
the whole argument block maps one-to-one under a constant sixteen-byte shift,
so the entire deficit sits in the locals and temps region.

`s8 slots[3]` is the only local array in the function. It is filled by
`func_80020B10` and read under a bound the ROM cannot observe, which makes its
length exactly L112's free parameter. Solving the frame identity by sweep:
lengths 13, 14, 15 and 16 all give frame 0xD0 and the same 231-word score,
lengths 12 and below give 0xC8 or 0xC0, and 17 and above give 0xD0 with two
more structural words. The length is pinned only to the range 13 to 16; 16 is
adopted as the round value in that range.

With the frame right, the declaration position of `slots` was swept over all
seventeen positions: any of the first nine scores 229 and the last eight score
231. Adopted at position nine, after `partIndex`. That axis was flat at frame
0xC0, which is why the earlier sweep found nothing in it.

What is left. Frames and the argument ladder now agree exactly. The residual is
the order of the ten homed locals: the target's ladder runs +0x78 +0x7C +0x80
+0x88 +0x90 +0x94 +0x9C +0xA4 +0xB8 +0xCC and ours runs +0x68 +0x80 +0x90 +0xA4
+0xAC +0xB0 +0xB4 +0xB8 +0xBC +0xC4, and the address-taken `slots` home sits at
+0x90 in the target against +0xA0 here. The next lever is a declaration-order
search over the memory-class locals with the frame held at 0xD0. That is a
different space from the one the 2026-08-31 lattice covered, so its flat result
does not carry over.
<!-- plateau-handoff:func_8002057C:end -->
