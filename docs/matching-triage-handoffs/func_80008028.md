<!-- plateau-handoff:func_80008028:start -->
### `func_80008028` plateau handoff

- source: `src/main/objects.c`
- score: 28 differing words
- frame: 0x8
- relocations: 8
- first mismatch: +0x24
- summary: Fresh 13-draw baseline confirms prior offset and loop controls; no new schedule lever, one relocation identity still unresolved.
#### Remeasured: 28 words, one pool colour and a rotated shared ring

The previously recorded 32/60 was stale. The frame (`0x8`), the instruction
count and seven of eight relocation identities are exact; the eighth is still
unresolved. First mismatch is `+0x24`, and it is `lui t1` against `lui t3`:
`objectOffset` takes `a1` in the target and `a0` here, and every shared-lane
draw from that row rotates with it. The pool lanes agree on the four webs before
it and on `s0`.

The tempting reading -- that the target has no `objectOffset` and simply indexes
`((Object **)D_800C9494)[objectIndex]` -- is wrong: the target does emit
`sll a1,v0,2` inside the loop and the candidate hoists its shift to the
preheader with a `+= 4` in the loop, but every index-only rewrite measured here
costs one word (61 against 60), in six spellings covering pre- and
post-increment, pointer arithmetic, an explicit byte multiply, and moving the
`data` load ahead of the increment.

Also flat, over two rounds of coordinate descent on the real full-TU object:
the flag's ternary and if/else forms; the inner bound spelled through `data`
against `object->unk40`; the inner loop as a `for`; the model-reference read
inlined at both uses; the model pointer re-read at its second use; and the two
increments joined onto one physical line or swapped.

Next lever: what puts `objectOffset` second in uopt's colouring order. Its first
surviving definition already precedes `object`'s in statement order, which is
the handle that closed `func_80006448` in this same TU, so the remaining
candidate is an interference the candidate does not create rather than an
ordering one.
#### 2026-09-13, lane h1: unsigned offset emission differential

The fresh configured baseline has 60 words, frame 0x8, 28 raw/masked
differences and first +0x24. Aligned buckets are 32 exact, 23 naming,
zero immediate and five structural rows, with no gaps. Static relocation
counts are eight on each side, with seven matching tuples; the remaining
identity is unresolved. Named Ucode and the complete index map identify
procedure 38 of 65. Stock and instrumented full-TU text compare identically.
Its census contains 13 draws and 104 emission records.

The prior indexed-loop forms and order controls were read. One new probe
computes the byte offset inside the loop using an unsigned literal and
unsigned index conversion, then advances the index, removing the separate
offset recurrence. It tests whether the literal/IR type boundary avoids the
previous strength-reduced cursor schedule without changing accesses.
All per-line draw counts and the complete draw sequence remain unchanged;
emission attribution moves from the preheader and offset tail onto the loop
head and tail. The candidate grows to 62 words with the same frame, 52
masked differences, and aligned buckets 21 exact, 30 naming, two immediate
and seven structural paired rows, plus two candidate-only words. The
unsigned source shape does not provide the desired zero-cost schedule.

Restore the guarded baseline. This rejects the typed-offset extension of the
recorded index family; ADR 0018 early stop applies because the remaining
identified order and carrier routes already have negative receipts. No
colour sweep was repeated. Sources, objects, profiles, static relocations and
aligned deltas remain ignored under build/h1/func_80008028. No new byte credit.
Commands: configured stock compilation, allocator_trace_receipt.py --map-only,
draw_census.py --save/--compare, residual_map.py --object/--against,
finalize_plateau.py and tools/gates.sh.


#### 2026-09-13, lane `j1`: baseline and evidence stop

The fresh configured stock and traced full-TU text agree. Procedure 38
reproduces thirteen draws and 104 emission records. The candidate remains
60 words, delta zero, frame 0x8, masked 28, first +0x24. Alignment is 32
exact, 23 naming, zero immediate and five structural rows without gaps.
Both static relocation tables contain eight entries, seven exact tuples;
the remaining identity is unresolved.

The source, original indexed-loop controls and h1's new unsigned-offset
census were read before considering another edit. They already reject the
identified index/offset and order mechanisms, including the typed offset
extension. No different causal source lever was identified. Stop under
ADR 0018 with zero new source attempts, preserving the guarded baseline.
This is current baseline confirmation, not a new proof of schedule exhaustion.
The open requirement remains the outer offset/interference schedule at the
original extent, with the unresolved relocation separately requiring proof.

Private evidence retains source, stock/traced objects, the census and aligned
metrics. Commands: assignment gate, configured stock/traced compilation,
draw census, aligned comparison, finalize_plateau.py and tools/gates.sh.
ROM verification covers the fallback; no new executable bytes are credited.
<!-- plateau-handoff:func_80008028:end -->
