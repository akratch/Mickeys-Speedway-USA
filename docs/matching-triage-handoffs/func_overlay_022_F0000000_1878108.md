<!-- plateau-handoff:func_overlay_022_F0000000_1878108:start -->
### `func_overlay_022_F0000000_1878108` plateau handoff

- source: `src/overlays/o022/overlay22InitializeObject.c`
- score: 167/172 words
- frame: 0x58
- relocations: 21
- first mismatch: +0x70
- summary: fidelity-clean stack-home trace exposes no producer offsets and ugen has no target comparison; no trace-directed source probe is justified

#### Configured relocation and producer-trace reproof (2026-08-31)

- Base `1bdd54cbfe2593a321039e6a666c2e3b9d5c1b4a` reproduces the configured `-O2 -mips2 -32 -Wab,-r4300_mul` full-TU candidate at exactly 688 bytes / 172 words, 167/172 machine words, and the target `0x58` frame. The workbench's first mismatch is the relocation identity at `+0x70`; the five machine-operand sites start at `+0xCC`. The owned interval is Overlay 22 `+0x0..+0x2B0`, with no owned padding before the next owner at `+0x2B0`, and ORT 1229 plus the resident call at `vram:0x8000AD34` authenticate the function ABI and ownership.
- A stack-slot audit isolates one displacement family: the same three call-argument uses occupy candidate `sp+0x28` versus target `sp+0x30`. The other fourteen slot signatures and the complete integer, temporary, FP-pool, and FP-temporary register lanes agree. This confirms the historical `objectPosition` home diagnosis without assigning an unsupported producer identity.
- Candidate and runtime each contain 21 relocation records and all offsets/types agree. The eight resident calls retain their typed source identities; the local pairs reconcile as `D_EE0` to Overlay 22 `+0xEE0`, `D_A7C` to `+0xA7C`, `D_0` to the node array at `+0xEA0`, and `D_30` to the count at `+0xED0`; the final call is the named local owner `+0xD30`. Runtime records authenticate those identities and stored addends, and relocation synthesis reproduces the required zero, `+0x30`, and `+0xA7C` linker values. The fail-closed fallback-static report still resolves only 9/21 candidate identities and aligns one stable alias because it deliberately does not infer local ownership from synthetic values; this is a diagnostic reconciliation, not promotion proof.
- The campaign-pinned IDO 5.3 UOPT source hash is `b0058f1559441c1a194d649271eb43b8637ec255682cfdd629031340b915b13f`. Retained Ucode maps this sole source procedure uniquely to allocator ordinal 0 with 17 decisions. Instrumented `.text`, `.data`, `.rodata`, relocations, and symbols are all identical to stock. Sixteen integer and one FP colour decisions are captured, but `trace-stack-homes` reports zero producer-emitted virtual/final offsets and `trace-webs` reports zero source-attributed webs; opaque raw fields were not treated as frame offsets.
- A separate procedure-scoped ugen producer capture is also section/relocation/symbol faithful and records 257 candidate free-list events with source lines. No target producer event stream exists, so its temp-event comparison remains unavailable and it cannot select a target-directed source edit. No source variants, flag sweep, or permutation were run; the prior ten natural forms were not repeated and functional baseline C is unchanged.
- Next lever: add and calibrate a fidelity-preserving producer hook that records the virtual and final homes for the `sp+0x28` carrier, then make only the natural lifetime or declaration change named by that trace. Promotion additionally requires a complete linked/runtime relocation receipt rather than the current partial static alias comparison.
<!-- plateau-handoff:func_overlay_022_F0000000_1878108:end -->
