<!-- plateau-handoff:func_8003A754:start -->
### `func_8003A754` plateau handoff

- source: `src/main/menu_3B1A0.c`
- score: 12/31 words
- frame: 0x18
- relocations: 1
- first mismatch: +0x10
- summary: Trace isolates ugen line-order; next lever is authentic call-result C that schedules t0 before a3 without #line or inert scaffolding. The -Wo,-loopunroll,0 removal from this TU is byte-inert here as well: 36 loop shapes (4 head orders x 5 inner forms x 2 outer forms) all measure flat at 12, so the reopen this flag change would otherwise justify is already spent.

Fresh configured evidence:

- `tools/wb_compare.sh func_8003A754` reproduced a 124-byte, 31-word
  candidate with 12 raw and relocation-masked differences, a 0x18-byte frame,
  one call relocation, and first mismatch at +0x10. The workbench verdict was
  `structure-mismatch`; its routed playbook was `structure-buckets` and its
  lever was C front-end spelling.
- The complete 119-entry flag lattice found no exact configuration. The
  canonical `-O2 -mips2 -32 -Wo,-loopunroll,0` preset remained tied for best
  at 12 differing words, ruling out a per-function flag correction.
- Two bounded report-only permuter searches each reached internal score 20 but
  not zero. Their candidates reduce to two differing words, but depend on
  redundant temporaries and empty control flow. Those diagnostics were not
  adopted as source.
- A fidelity-clean instrumented IDO trace classified that two-word residual as
  `schedule-mismatch`: the outer zero initialization and call-result carrier
  receive adjacent ugen source lines in the wrong priority order. A temporary
  `#line` probe made all 31 instruction words identical, proving the causal
  mechanism; it was removed because source-line manipulation is not a source
  reconstruction.
- Natural variants covering `do`/`while`, typed and untyped call-result
  temporaries, pointer aliases, indexed loops, comma expressions, declaration
  initializers, and `register` qualifiers either stayed flat or regressed.
  Declaration initialization specifically changed the frame from 0x18 to 0x20
  and produced 17 differing words. The only two-word forms retained an inert
  empty branch and redundant zero carrier, so they are diagnostic-only.

Stopping evidence: the authorized C-spelling route is exhausted by causal
trace. Authentic spellings tested do not change the final schedule, while the
only exact spelling requires line metadata and inert allocation scaffolding.
Do not resume those forms without changed source or compiler evidence. The
next concrete lever is an authentic call-result expression or recovered type
that gives the outer zero carrier earlier ugen priority while preserving the
0x18 frame and the already-exact register lanes.

Full-ROM verification was not reached in this base tree: the existing link
fails in `overlay94UpdateController.c.o` on unrelated unresolved resident
references. The target-owned configured object comparison and source-only
gates are unaffected.
#### Owned pass, 2026-09-10 (lane/nm-resident2)

Still 12, but the inner loop's exit test is no longer an open question. The
target tests the inner counter against an immediate 6; every spelling of a
source-level counter tests it against a register instead, and that one
difference drags four more words of colouring with it.

**Measured law (new).** uopt rewrites a *basic* induction variable's `i < K`
exit test into `i != K` and hoists K into a register. It does this for every
spelling of the test (nine measured, including `<=`, the reversed operands,
the negated `>=`, and `!= K` itself), for every statement order inside the
loop (all 12 order x initialization permutations), and independently of the
step and the bound -- even when the step does not divide the range, so the
rewrite is unconditional normalization, not a proof-guarded transformation.
`-O1` keeps the immediate compare, which places the rewrite in uopt rather
than in ugen or the assembler. The four `-Wo,-loopunroll` settings are inert.

**uopt does not rewrite a *derived* induction variable.** Spelling the exit
test as a pointer difference against the row base reproduces the target's
inner loop instruction for instruction: counter increment, the immediate
compare, the pointer increment, the branch, and the store in the delay slot.
That is the first source-reachable route to the target's inner loop found
here, and it moves the outer loop's branch displacement back to the target's
too.

That form scores 16, and exactly **one** of its words is structural: uopt
synthesizes the derived variable's initial value as a subtraction of the base
register from itself where the target has a zero move. The other differences
are a rotation of the five carrier colours that follows from it.

**The two behaviours cannot currently be had at once.** uopt *does* fold
`p - p` to zero when the source writes it as an explicit assignment -- but the
variable then becomes a basic induction variable again and the exit test is
rewritten, back to 12. Five spellings of that composition were measured. The
fold is present in the scalar path and absent from the derived-variable
initialization path; closing this residual means finding a derived form whose
synthesized initializer is a constant.

Also retired here: counter types `u32`, `long`, `char *`, `u8 *`, `s32 *` are
all inert at 12; `s8` does keep the immediate compare but pays for it in sign
extension (28/33); `register` on the counter is inert; index-through-the-base
addressing is strength-reduced back to the same code as the pointer form.
<!-- plateau-handoff:func_8003A754:end -->
