<!-- plateau-handoff:func_800349A4:start -->
### `func_800349A4` plateau handoff

- source: `src/main/textures_354C8.c`
- score: 265 differing words
- frame: 0x30 (target 0x40)
- relocations: 37
- first mismatch: +0x8
- summary: Complete semantic C emits 274 versus 272 instructions at normalized distance 238; all 119 flags and ten natural forms are nonexact, and a fidelity-clean trace isolates the missing saved-register flag web.

Fresh configured evidence on base `13bbeb8a862b04e3ab7de28af3f70d760fdd0b98`:

- the unique resident owner is `0x800349A4..0x80034DE4` / ROM
  `0x355A4..0x359E4`, exactly `0x440` bytes with no target padding before
  `func_80034DE4`. The function has no export; 31 `R_MIPS_26` records from
  Overlays 11, 12, 13, 17, 20, 27, 28, 37, 38, 46, 47, 56, 57, 58, 60, 65,
  71, 73, 83, 96, and 101 authenticate its identity and four-argument ABI;
- the historical placeholder emitted only 16 instructions and two text
  relocations. The retained guarded body reconstructs the animation-frame
  selection, current/next texture commands, render-state table selection,
  cached state, depth/fog geometry toggles, and final two display-list records;
- configured IDO `-O2 -mips2 -32` emits 274 instructions and a `0x30` frame
  versus the target's 272 instructions and `0x40` frame. It has 265 differing
  target-offset words from `+0x8`; the shift-tolerant normalized distance is
  238. Linked equality remains fallback-only;
- all 37 candidate text relocation identities resolve. The target owns 39;
  five offsets/types and two effective identities currently align. Splitting
  each final table-record copy into explicit word stores is the retained strict
  gain, improving normalized distance from 295 to 238;
- all 119 supported flag identities are nonexact, with canonical O2/MIPS-II
  best. Ten bounded source families covered parameter/register declarations,
  K&R and local flag carriers, literal donor-shaped control flow, render-table
  pointer timing, texture-count width, explicit table copies, display-list
  output cursors, and animation-wrap branch spelling;
- one fidelity-clean allocator trace maps the long-lived flag web. A diagnostic
  force reproduces the target's initial `s0` flag carrier but shrinks the body
  to 263 instructions with a `0x38` frame, so it is neither exact nor eligible
  for promotion. The output-cursor and ternary-wrap follow-ups regress to 280
  and 276 instructions respectively.

Preserve the fallback. Reopen only with new source-authentic declaration or
lifetime evidence that naturally carries `flags` in `s0` while retaining the
command topology; do not repeat the flag lattice, forced-color diagnostic, or
the ten bounded source families.
<!-- plateau-handoff:func_800349A4:end -->
