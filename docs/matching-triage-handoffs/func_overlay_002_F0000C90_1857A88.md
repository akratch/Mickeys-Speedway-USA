<!-- plateau-handoff:func_overlay_002_F0000C90_1857A88:start -->
### `func_overlay_002_F0000C90_1857A88` plateau handoff

- source: `src/overlays/o002/func_overlay_002_F0000C90_1857A88.c`
- score: 98/355 words
- frame: 0x60
- relocations: 65
- first mismatch: +0x0
- summary: Exhaustive 171-cell landscape has floor 84 with two additive diagnostics; source still needs target 8-byte frame and includeLinked home.
#### 2026-09-11, lane p5-ovl: frame-home evidence and stall closure

The fresh baseline is 98/355 relocation-masked words: 296 exact, 51 register
naming, five immediate-only, and four structural. Geometry is exact at 1,420
bytes; the candidate frame is 0x60 versus target 0x68. Save slots account for
44 bytes on both sides; the only displaced non-save home is the `includeLinked`
parameter at +0x64 in the candidate versus +0x6c in the target. Instrumented
text is stock-identical, with 37 p1 decisions and no p2; the register mapping
is 88% coherent with one closed ring and an a1/a0 cycle. One and two unused
`s32` declarations and removing `volatile` from `includeLinked` were all flat
at 98/355 and left the frame unchanged; all were reverted. Three consecutive
new hypotheses produced no information, so the unresolved mechanism is the
compiler's local/home allocation rather than a proved source spelling.

#### 2026-09-12 colour landscape and lattice

`tools/web_footprint.py func_overlay_002_F0000C90_1857A88 --proc 0
--every-colour` sampled 171 accepted probes across all 27 coloured webs and
12 residual windows. Two legal size-zero winners were found: `p1:w39=c4`
scored 90/355 and `p1:w127=c4` scored 92/355, against the 98/355 baseline.
The single-force floor is therefore 90/355 over the complete accepted
landscape. The first winner removes two residual words in each of windows
0x80, 0x100, 0x180 and 0x200; the second removes three words in each of 0x280
and 0x400.

The all-winner `tools/force_lattice.py --proc 0` pair scored 84/355 at the
same size, exactly the additive prediction. Its blast radii are disjoint:
`w39=c4` is -2 in 0x80, 0x100, 0x180 and 0x200, while `w127=c4` is -3 in
0x280 and 0x400. The two legal size-zero colour winners are real but do not
identify the frame-home mechanism.

The source question remains the measured frame/home allocation: introduce the
target's extra 8-byte frame and move `includeLinked` from +0x64 to +0x6c
without changing the 44 bytes of shared save slots or the exact 1,420-byte
function geometry. The previous unused-local and volatility probes were flat;
the winning forces are diagnostics only and are not source candidates.

<!-- plateau-handoff:func_overlay_002_F0000C90_1857A88:end -->
