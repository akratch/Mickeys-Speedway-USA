<!-- plateau-handoff:func_8000E920:start -->
### `func_8000E920` plateau handoff

- source: `src/main/track.c`
- score: 459 differing words
- frame: 0x100
- relocations: 112
- first mismatch: +0x0
- summary: Mickey m2c fixes visibility stride and shadow ABI, then improves 491 to 459 diffs. Next: source-proved frame and reverse-pass lifetimes.

#### Mickey m2c reconstruction, 2026-09-09

- The fresh gate returned `base-only`. The configured baseline had 558 candidate versus 542 target words, 491 differing words, first +0x38, exact 0xf8 frame, and 114 candidate/target relocations with 23 exact offset/type/identity records. Workbench verdict `structure-mismatch`, playbook `constant-audit`; the m2c draft and actual memory/call instructions were reviewed before editing.
- Attempt 91 corrects two visibility identities: entries are four-byte words indexed by segment plus one, and the single-segment path writes the slot for segment zero. The prior sixteen-byte stride and segment-one singleton slot were wrong. This also restores the negative-one segment sentinel's lookup at the base slot. The word-array identity is corroborated by the owned coordinator and target loads/stores.
- Attempt 92 removes two spurious third shadow-draw arguments and supplies the actual two-pointer prototype from the owned `func_800140CC` definition, reducing the candidate by two words and the residual by five. Attempt 93 adopts the existing typed segment-count field. Attempts 94–97 investigate the first-pass guard, later reverse count tests, forward equality latches and persistent last-selected index; none improves that 556-word, 486-difference result.
- Attempt 98's reverse object byte-offset traversal improves to 544/542 words and 475 differences with the exact 0xf8 frame. Moving the byte decrement before dispatch gives attempt 99's 547/473. Recovering the first visible pass's unsigned address limit then gives 545/464 (attempt 101); retaining the better object-selection form improves to 545/463 (102). Typed forward object cursors give 547/459 (103), retained with the owned route-object segment field and formatting in attempt 107.
- Retained result: 547 candidate versus 542 target words, 459 differing words, first +0x0, candidate frame 0x100 versus target 0xf8, and 112 candidate versus 114 target relocations with 26 exact offset/type/identity records. This is a 32-word residual improvement and eleven-word size reduction from the original baseline, not a match. The nearest-size alternative, attempts 104/105, is 543/542 words with 463 differences, frame 0x100 and 112 relocations; the exact-frame alternative at attempt 98 is also preserved.
- Stopping evidence: five follow-ups 104–108 fail to improve the best differing-word residual or expose another missing call/global identity. The later explicit visible cursors trade size for a worse residual (104), the forward visible cursor is flat (105), reverse do-loop guards regress to frame 0x108 and 467 differences (106), the typed route field is flat (107), and guarded object selection gives 460 differences (108). The size tradeoff is retained as evidence rather than called exact. Remaining work is the previously closed lifetime/allocation family, now against corrected table and ABI inputs; no arbitrary stack padding, declaration permutation, or new flag/permuter mechanism was substituted.
- Integer address cursors express the target's unsigned address comparison without forming a C pointer before the visible array. The old value tested by each target branch and its delay-slot decrement were audited rather than copied literally from m2c pseudocode. All source/object/measurement variants and first mismatches are retained under ignored `build/wb/tu-track/func_8000E920/`.
- Next concrete lever: authenticated declaration/lifetime evidence explaining the eight-byte frame excess and missing global relocation pair under the recovered traversals. Preserve the near-size and exact-frame alternatives. Do not restore the wrong visibility stride, singleton index or surplus call arguments to recover an obsolete score.
- Validation: configured `wb_compare.sh`, workbench `diagnose` and `guide constant-audit`, own-assembly field/call audit, `gmake check-nonmatching-builds`, `tools/finalize_plateau.py`, and `gmake verify cleanroom check-docs`. The function remains `NON_MATCHING` over its original assembly fallback, with zero matching bytes credited.

#### Pinned donor audit, 2026-09-08

- Assignment gate: `base-only` at `32a75d648e8954f7455897fb8f16a8ef5f05df11` for this exact symbol and source path.
- Jet Force Gemini `efd5abb1c79636e297b831f7c2d5bf47eac39c0c` has 12 C implementations and 53 assembly placeholders in `src/track.c`. Its implemented routines do not supply this target's body. The closest masked track-object hit, `func_8001424C` (Jaccard 0.1623), corresponds by verified TU order to the still-assembly `func_8001448C_1508C` in that pinned source. This is diagnostic structural context, not an adopted name, ABI identity, or exact donor match.
- Fresh configured full-TU measurement: 542 target words and 558 candidate words; 491 raw and 491 relocation-masked differences; first +0x38; target frame 0xf8, candidate frame 0xf8. There are 114 candidate versus 114 target relocation records, with 23 stable identities at matching offsets/types.
- Workbench verdict `structure-mismatch`, playbook `constant-audit`. The existing diagnosis still requires source-shape/lifetime evidence; the donor-only reopen does not authorize substituting a flag, allocator, or permuter mechanism.
- ADR 0018 stop: zero new source attempts for this target. The pinned source disproves the new matched-donor-C hypothesis. No unchanged flag lattice or previously exhausted source family was repeated. The compiled candidate is unchanged and remains behind `NON_MATCHING` and its original `GLOBAL_ASM` fallback; zero new matching bytes are credited.
- Next concrete lever: a published matched counterpart with an authenticated ABI and useful source lifetimes, followed by Mickey-specific field, branch and call proof. The remaining general levers below require separately authorized changed evidence.
- Commands: `tools/wb_compare.sh --summary-json func_8000E920`, workbench `diagnose` on the configured full-TU object, the track-object masked skeleton audit, `tools/finalize_plateau.py`, `gmake cleanroom`, and `gmake check-docs`. Source, object, scores, first mismatch and previous handoff are retained under ignored `build/wb/`; no instruction rows are included here.

#### Prior committed plateau evidence (historical)


- source: `src/main/track.c`
- score: 491 differing words
- frame: 0xF8
- relocations: 114
- first mismatch: +0x38
- summary: Recovered exact frame and relocation count plus target call identities and field widths; next lever is reverse-pass lifetime scoping.
<!-- plateau-handoff:func_8000E920:end -->
