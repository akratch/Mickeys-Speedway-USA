<!-- plateau-handoff:func_8003A5A0:start -->
### `func_8003A5A0` plateau handoff

- source: `src/main/menu_3B1A0.c`
- score: 9/56 words
- frame: frameless
- relocations: 22
- first mismatch: +0x0
- summary: Accepted p2 force w0=a1,w73=v0 cuts direct residual to 3 aligned rows; declaration and empty-comparison forms cannot reproduce it.
#### Owned pass, 2026-09-10 (lane/nm-resident2)

Still 9. The residual is one colouring decision and nothing else: the target
puts the walking key pointer in `a1` and the fixed bound in `v0`; every
spelling reachable from this shape puts the walker in `v0` and the bound in
`a1`. The offset carrier is `v1` on both sides, so the two colours simply
exchange.

**Measured law (new).** IDO gives `v0` to the loop's *walking* pointer and the
next free colour to the bound, independently of which variable is declared or
initialized first. Two synthetic probes compiled in this TU settle it: the
same loop written twice, once with the low pointer walking and once with the
high pointer walking, puts the walker in `v0` in both. The rule is structural,
not lexical, so no cursor/end spelling can reach the target's assignment. A
reopen needs a shape in which the walking pointer is *not* the loop's primary
induction variable.

Retired at this residual, each measured over a full lattice rather than
sampled, all flat at 9:

- All 36 declaration-order x initialization-order permutations of the three
  locals, crossed with both compare operand orders.
- Variable names, including reverse-alphabetical renamings: cfe's symbol
  order does not reach the colouring.
- 72 body spellings: three key-read forms (offset deref, `s16` index, address
  of element) x three return-index forms x four loop-tail forms (test, swapped
  increments, `continue`, `break`) x two declaration heads.
- Signature variants: `void *` / `s32` / `u8 *` return, and a K&R
  parameter list.

Regressions, all worse, all explained:

- Comparing against the address constant instead of a local (with or without
  an `end` local) reverses the compare's operand order: 10.
- Deriving the cursor from the end minus 0x30, in three placements: 10.
- An extra unused parameter buys a parameter home and one extra word: 57.
- Top-tested and rotated shapes cost three words (the guard is not folded
  away): 59. `while (end != cursor)` and the equivalent `for` still unroll to
  155 words, as the earlier pass recorded.
<!-- plateau-handoff:func_8003A5A0:end -->
