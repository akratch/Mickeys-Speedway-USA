<!-- plateau-handoff:func_80040B88:start -->
### `func_80040B88` plateau handoff

- source: `src/main/particles.c`
- score: 72/302 words
- frame: 0x68
- relocations: 12
- first mismatch: +0x48
- summary: Proc29 landscape 134 probes; forced floor 63, but winners only affect late rival webs and cannot reach the +0x74 extra draw.

Measured and flat against that draw, all on this base: both L144 address forms grow
the frame, `*(T **)&trigger` at 264 words delta +4 and `(T *)*(s32 *)&trigger` at 299 delta +8;
a redundant AND-0xFF on the u8 result does add a draw at the right line but recycles t8 rather
than t7 and costs 105; hoisting the scale product above the pointCount test 127, above the
pointCount read 127, above the entry computation 130, above the result test 284; an if-1 region
before the scale statement 72; swapping the multiply operands 72; restoring entryIndex while
inlining pointCount 73; removing both entryIndex and pointCount 111 on the pre-sweep shape.

The precise open question is the MOVE_END record the trace shows on t7 at the result-test line.
It pushes t7 to the tail of the free list, which is why an added draw recycles t8 instead. A
source form that spends one extra draw before the line-entry-table statement WITHOUT provoking
that MOVE_END should land the ring and close the function.

#### 2026-09-12 colour landscape and lattice

The procedure mapping was established by direct effect on this target: the
particles TU emits 43 instrumented procedure ordinals, but only proc 29's
accepted force changed `func_80040B88`. The exhaustive
`tools/web_footprint.py --proc 29 --every-colour` run sampled 134 accepted
legal probes across all 21 coloured webs and 9 residual windows; two
size-changing webs were reported but excluded from aligned colour scoring. The
three winners at size delta 0 were `p1:w150=c27` at 63/302, `p1:w194=c25`
at 67/302, and `p1:w233=c25` at 67/302. Thus the single-force floor is 63/302
over the complete accepted landscape.

`tools/force_lattice.py --proc 29` measured every subset of those three
winners. The best lattice result remained 63/302 with `w150=c27`; the pairs
`w150=c27 + w194=c25` and `w150=c27 + w233=c25` both scored 67, while
`w194=c25 + w233=c25` scored 75. The three-force combination scored 67. All
three pair interactions were antagonistic: each winner's blast radius overlaps
0x280 and 0x300. Individual blast radii were `w150=c27`: -4 at 0x280, -1 at
0x300, -4 at 0x380; `w194=c25`: -4 at 0x280, -1 at 0x300; and `w233=c25`:
-4 at 0x280, -1 at 0x300. These late-window winners do not reach the +0x74
one-draw structural window.

The source question remains precise: produce one additional integer ugen draw
before the line-entry-table statement without adding bytes or triggering the
observed MOVE_END on t7. The tested address-taking reloads grow the frame, and
the accepted colour winners only recolour the later rival webs; neither is a
shippable source solution for the remaining ring phase.
<!-- plateau-handoff:func_80040B88:end -->
