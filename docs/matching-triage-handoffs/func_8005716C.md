<!-- plateau-handoff:func_8005716C:start -->
### `func_8005716C` plateau handoff

- source: `src/main/anim.c`
- score: 7 differing words
- frame: 0x28
- relocations: 2
- first mismatch: +0x78
- summary: 80/80 words; the f16/f18 rotation is closed and the residual is four middle values the target spends as ring temporaries.

#### 2026-09-09: 44 differing words to 7, the colour rotation closed

Two separate defects, neither an operand order, and each a regression on its
own axis only after the other is in place (the L88 composition rule).

- The x component needs three carriers, not one. Spelling it
  `normalXProduct = normal->x; doubled = (normalX = normalXProduct);` gives
  the read its own name, the sum's carrier its first definition and `normalX`
  the lasting copy. That alone is 44 to 10 and puts every surviving FP
  temporary on the target's rotation.
- `doubled` still took the fourth pool colour and `normal->x` the fifth,
  because uopt colours pool webs in first-surviving-definition order and the
  x read is defined before the sum *inside the same statement*, so no
  statement or declaration order can separate them. A dead `doubled = 0.0f`
  before the flag test defines that web first and emits nothing (law L87: a
  genuinely dead store reserves no colour, but `value = 0` does). 10 to 7,
  and the f16/f18 rotation is gone.

The permuter reached the second one as an uninitialised self-add inside a
`do {} while (0)` -- undefined behaviour, not kept. The zero store is its
semantics-preserving re-derivation, and it was verified to be free (80/80
words, zero opcode mismatches). Its `target->unk4 * 1.0f` was byte-inert.

Remaining seven words: the target spends ring temporaries on the negation,
the volatile reload, the doubling and the product; the candidate keeps all
four in `doubled`'s pool colour. Measured against that: splitting the
negation out as `bounce = -doubled` (46), moving the reload into
`normalXProduct` (81 words), moving the product there (81), a second reserved
colour on `normalXProduct` or `normalX` (7, inert), three placements of the
zero store (7/10/10), and both declaration orders of the velocity locals (7).
All 512 commutative operand orders and every declaration order stay
byte-identical, so the expression tree is not the lever.

Donor and stopping evidence:

Donor and stopping evidence:

- Configured full-TU preflight and workbench comparison re-established an
  80-word target and candidate, equal `-0x28` frames, and two relocation
  records agreeing in offset, type, and resident identity. The workbench
  verdict is `mixed(constant:2, structural:2, register:15)` and its reported
  lever is `stack-home`, with constants routed before structure and registers.
- Jet Force Gemini commit
  `efd5abb1c79636e297b831f7c2d5bf47eac39c0c` still represents
  `hitGetInelasticVelocity` with a `GLOBAL_ASM` entry in `src/hit.c`; that file
  contains 36 assembly fallbacks and supplies no donor C body for this target.
  A fresh masked-skeleton scan ranks it first at only 0.0484 Jaccard.
- Zero source attempts were made. The only reopened mechanism supplied no new
  C input, while the prior handoff already closes allocator and source-shaping
  variants. Repeating those variants would not meet ADR 0018's resumption bar.
<!-- plateau-handoff:func_8005716C:end -->
