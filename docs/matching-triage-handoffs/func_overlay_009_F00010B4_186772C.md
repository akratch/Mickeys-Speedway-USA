<!-- plateau-handoff:func_overlay_009_F00010B4_186772C:start -->
### `func_overlay_009_F00010B4_186772C` plateau handoff

- source: `src/overlays/o009/overlay_009.c`
- score: 3/282 words
- frame: 0x98
- relocations: 31
- first mismatch: +0x238
- summary: 6 -> 3 (2026-09-11, lane s1-one). No new FP web was ever needed: blend already holds c27 and its live range overlaps baseX's, so carrying the cross product in baseX merges the two chains into ONE web (L115, block-set union), which inherits blend's edge and takes c28 -- zero instructions, zero frame change, cross lands on the shipped f16. cross must stay declared or the home ladder moves (28). The last word is web 108's colour: available c27 and c29 at equal p1cost, ascending scan takes c27, and forcing c29 is ACCEPTED and returns masked 0 at delta 0. c27 is held by blend, which spans no call and is never spilled, and the shipped function has neither a spare FP home nor a spare instruction for an interferer -- so this is now a cost-model question, not a source-form one. Ladder decoded from this procedure's own records: c24 f0, c25 f2, c26 f12, c27 f14, c28 f16, c29 f18.

#### Evidence (2026-09-09)

Measured with a direct `tools/ido/cc` compile of the whole translation unit,
confirmed byte-identical in `.text` to the asm-processor `NON_MATCHING` build,
so one candidate costs about 30 ms. Workbench cross-check on the real
`build_non_matching` object, before and after: aligned_total 70 to 59, words
52 to 41, raw 59 to 48, aligned_constant 25 to 15, aligned_register 34 to 33,
fp 11 unchanged, insns 282 of 282, frame -152 of -152, opcodes 0.

What moved it:

- Declaration order is the frame. Splitting the combined `f32 a, b, c;`
  declarations one per line and moving `cross` to sit before `yawB` places the
  two f32 spill homes at 0x4C and 0x50 instead of 0x50 and 0x54, closing ten
  words in one edit. An iterated local search over the 22-local declaration
  list (insertion and swap neighbourhoods, random kicks, 344,946 evaluations)
  found no order better than that one, so this axis is a proved global optimum
  for this body.
- Spelling the first smoothing loop's update as `x = x + (f(...) >> 4)` rather
  than `x += f(...) >> 4` is worth one further word. The identical rewrite on
  the other five accumulators is flat, so it is a site, not a rule.

Eliminated, all flat at 41 or worse:

- An 864-point body-spelling lattice over commutative operand order,
  parenthesis grouping, ternary versus if/else, explicit casts and statement
  splitting.
- Adding one dummy `s32`, `f32`, `s16` or `void *` local at each of 23
  positions. The best is still 41, and every `f32` insertion costs 38 words by
  regrowing the frame, so the declared-local set is right, not just its order.
- Spelling `steps` as `(s32) stepsFloat` at every use, which scores 70, and
  naming the `&D_388[mode]` cursor as a `u8 *` local, which scores 80. The
  target's `steps` is a named local and its D_388 cursor is not.
- Ten spellings of the `D_388[mode] &= 3; tableIndex = ...` statement group.
- Loop-shape and cast variants: `while (i-- != 0)`, hoisting `i = steps - 1`
  out of the guard, `(s32)` casts on both `0x8000 - ...` operands and on the
  yaw difference.

The remaining residual is four disjoint allocation classes and nothing else.
Nine words are the GPR spill-home pair: the target homes the D_388 cursor at
0x34 and `steps` at 0x38, and the candidate has exactly those two swapped,
with `s0` at 0x30 on both sides. Seven are the tableIndex temp ring, same
schedule and same opcodes, where the target numbers the `lbu` web below the
call-result `andi` web and the candidate above it. Nine more are the two
angle-loop temp rings, showing the same inversion: the target numbers the
`0x8000` literal web above the reloaded field web. Eleven are the FPR ring,
where the target spends f18, f16, f12 and f2 where the candidate spends f16,
f14, f2 and f14; the ring order is identical, so by L88 this is a site and not
a phase. The last five are the bank loop, downstream of the same GPR ring
inversion.

Next lever: the three GPR ring classes share one signature. The target numbers
a memory-read web before a literal-constant web defined in the same statement,
and the candidate does the reverse. Any construct that reorders web creation
between a field read and a materialized constant inside one statement is the
thing to search; the commutative-operand and statement-splitting space above
has already been swept and does not reach it.
#### Evidence (2026-09-10, lane/c6-band-a)

Same fast direct-`cc` loop. 41 -> 25 masked (48 -> 32 raw), still 282/282
instructions, frame -152, delta 0. Two edits, both in the first six statements:

- **`steps = (s32) stepsFloat;` moved above the `D_388` block: -8 words.**
  That statement's spill web is then numbered below the `&D_388[mode]` cursor's,
  and the pair of GPR homes comes out at 0x34 (cursor) / 0x38 (steps) instead of
  the reverse. `i` stays at 0x30 on both sides. as1 still schedules the
  conversion itself back down to its old rows, so only the homes move. The
  previous handoff read this class as a declaration-order fact; it is a
  statement-order fact, which is why the 344,946-evaluation declaration search
  could not reach it.
- **`tableIndex = ((u8)(ext_o0_214c8() & 3) * 4) + D_388[mode];` : -8 more.**
  Measured on `cc -S`, ugen emits a commutative `uadd`'s **right** operand
  subtree first and puts its result in `rs`; the target's `addu` has the `lbu`
  of `D_388[mode]` in `rs`, so `D_388[mode]` is the right operand in the
  original. That swap alone is +25 (66 words) because it also drops the
  redundant `& 0xFF` node; keeping an unsigned node on the other operand
  (L52 -- a `(u8)` conversion is a node) restores the temp count. The pair
  measures 25 against 33 for the hoist alone and 29 for the cast alone.
  `(u8)`, `& 0xFF` on the shifted term, `<< 2`, and `4 *` are all 25 and
  mutually byte-identical; `(u8)` is retained as the natural reading.

Re-measured and still flat at the new base: a 362-evaluation insertion search
over the 20 movable declarations (no order beats the one in the source), the
`+=` versus `= x + ...` rewrite at all four accumulators, and the commutative
swap at the angle and bank accumulators (L92 inert -- the object's operand
order does not follow the source there).

**Third edit, the FP carriers: 25 -> 20.** The tilt target and its blend rate
are carried in `yawA` and `yawB`, which are dead by then, not in `targetTilt`
and `blend`. A 30-row carrier-identity cross product over the six locals dead
at that point selects that pair uniquely (`yawA`/`yawB` 20; `cross`/`blend`,
`cross`/`yawA`, `yawB`/`yawA` and `targetTilt`/`yawA` all 22; the rest 23 or
worse, and `targetX`/`targetY` regress the size). It removes two FP webs from
the colouring and puts `cross` on f16 and the D_70 rate on f2, both exactly as
the target has them. `targetTilt` must still be **declared**: dropping it moves
every home below it and costs 15 words, so it now reserves a home and carries
nothing -- the same shape overlay 8's `motionTarget` has. Carrying the speed
target in it instead of in `speedTarget` is byte-identical, so the object
cannot say which of the two names the original used.

Also flat at the 20-word base: the whole table-index spelling lattice again
(8 forms), the three blend-rate globals read inline instead of through a local,
the tilt if/else as a ternary and as a negate, a 22-row head-block statement
permutation over the `steps`, `targetAngle` and `i` positions, and the
second-half carrier cross product over `trigA`/`trigB`, the D_78 rate and the
speed target (`speedTarget` -> `targetTilt` ties byte-for-byte; everything else
is worse).

Residual, three classes, 20 words:

- **10 words, one ugen ring fact inside the table-index statement and its
  downstream.** The target spends three ring temps there (`lbu $24`, `and $25`,
  `mul $8`), colours `tableIndex` into `a0`, and lets the scaled index take the
  fourth ring slot `$9`; the candidate spends four (the `(u8)` node emits an
  `and 255` that uopt then deletes), colours `tableIndex` into `a1` and the
  scaled index into `a0`. Both forms leave the ring at the same slot for the
  next statement, so this is not a phase error -- it is which value holds which
  register inside the statement. The no-cast swapped form (`((rand & 3) * 4) +
  D_388[mode]`) emits *exactly* the target's four instructions and ring temps
  and still scores 61, because `tableIndex` then takes `a1` and the scaled index
  `a0`, which shifts every later colour. **The deciding variable is uopt's
  colour for the scaled-index web against `tableIndex`'s, not the operand order,
  which is now right.** 18 forms of the statement, 8 of them re-measured at the
  20-word base, leave that pair untouched; so do all 22 head-block statement
  orders and the `u8`/`u16`/`u32`/`s16` widths of `tableIndex`. Reaching it
  needs an interfering integer web this body does not have, or evidence that the
  two array reads share one scaled index in the original source.
  Six words of this class are in the statement itself, one at the first angle
  accumulator's `addu` operand order, and three at the second
  `0x8000 - state->angle` call's ring rotation.
- **3 words, `yawA`'s FP pool colour**: the target gives it f18, the candidate
  f14.
- **2 words, the tilt constant's FP pool colour**: the target f12, the candidate
  f14. Both residual FP webs want a *higher* colour than they get, so the target
  has an interfering FP web this body does not; the fp ring order is identical
  on both sides, so by L88 these are sites, not a phase.

#### 2026-09-11, lane f9-audit: the closure held one variable fixed, and it fell 20 -> 6

Read adversarially, the 20-word closure named its deciding variable as "uopt's
colour for the scaled-index web against `tableIndex`'s". That is a description
of the candidate, not of the target: **the target has no scaled-index web at
all.** Its scaled index sits in a ring temp (the same register is later used as
a plain ring temp in the first angle loop, so it is not a claimed colour), while
every candidate form made it a coloured uopt temp (save 3.0, nocs 1, cost 0 at
every caller-saved colour -- a web like that is always coloured, so no colouring
lever, force sweep or operand order could ever remove it; forcing it to split
costs 4 bytes, forcing it to the target's register costs 69 words because the
register leaves the ring).

**The variable the 18 forms held fixed: what the declared local carries.** All
18 kept `tableIndex` as the element index and scaled it at each of the two
reads, so the shared `<< 2` was always a uopt CSE temp. Carrying the *byte
offset* instead:

```c
tableIndex = (((ext_o0_214c8() & 3) * 4) + D_388[mode]) * 4;
targetX = *(f32 *)((u8 *)D_300 + tableIndex) + (D_2D0 * 75.0f);
targetY = *(f32 *)((u8 *)D_340 + tableIndex);
```

is **20 -> 8**. The pre-scale sum becomes the coloured web (a0, as the target
has it) and the local is the ring temp. The `(u8)` node the previous lane added
to fix the temp count is no longer needed and is +54 with it. A standalone
probe reproduces the mechanism outside this function (index local: two
coloured webs; byte-offset local: one), so it is a rule, not a site. Six other
spellings that keep the index (`+ 16` on one table, `(u8 *)`/`(u32)` base casts
on an unscaled index, `<< 2`, region boundaries around the pair, comma joining,
nested assignment) are 20, 26, 62 or worse.

**Second edit, 8 -> 6.** The first angle accumulator written as
`state->angle += ext_o0_2a5bc(...) >> 4;` closes the two-word ring-order swap
after that call. The earlier note recorded `+=` versus `= x + ...` as flat at
all four accumulators; it was measured at a ring phase the first edit has since
moved, which is the general trap with "flat" spelling results.

**Third edit, same 6 but structurally right.** Re-running the 90-row tilt
target x rate carrier product at the new base: `speedTarget`/`yawB` is the
only pair that puts the +-10 constant on f12 (the target's colour) with the
D_70 rate on f2; the retained `yawA`/`yawB` pair had merged the constant into
yawA's web and coloured both f14. (`speedTarget`/`yawA` scores 5 but moves the
rate to f14, which is the wrong register; do not adopt a lower count that
regresses a matched register.)

**Residual, 6 words, and it is exactly two colours.** `CDX_FORCE=p1:w114=c28,
p1:w108=c29` (cross to f16, yawA's post-call piece to f18) returns **masked 0,
delta 0** -- only the 8 known relocation-artifact words remain. Both webs
currently take the first free colour after c24..c26 (cost 0 at c27, c28, c29
alike; scan order picks c27 for cross, then c28 for yawA); the target takes one
higher for each. So the target has one more caller-saved FP web, coloured c27
(f14), decided before cross (save 3.0) and interfering with both. In the target
f14 carries only the two loop rates (D_58 in the smoothing loop, D_78 in the
speed loop), neither live in the cross block, and the candidate's `blend` web
already owns c27 with the same two segments. Hoisting `blend = D_78` above the
trig calls is either dead-store-eliminated (byte-identical) or +12 bytes.
The next lever is a source form that gives *cross's block* a third f14
segment without an instruction -- e.g. an FP local defined after the last trig
call and used after the tilt loop -- and it has to be found by reading the
whole 0x234..0x31C region, not by re-running carrier lattices.

**Phase census ([L108]):** 915 p1 records, 0 p2. Definition order, declaration
order and statement order are one dead axis on this procedure; the 22
head-block orders and the 344,946-evaluation declaration search were measuring
it. Only `save = totalsave/nocs` and interference order anything here.

Instruments: direct `cc` needs this TU's `-Wab,-r4300_mul` (without it the
symbol reads as a 20-byte size mismatch); the procedure is globalcolor
ordinal 7 of 8 in this TU.

**Second bounded round, same day: the mechanism confirmed, the web not yet
identified.** Fourteen delta-0-or-not forms, none below 6:

- A `tilt` carrier for `state->tilt` (loaded before cross, loop on the local,
  base products on the local; store after the loop or inside it; as a new
  local or in `targetX`/`targetY`/`targetTilt`) is -4 or -16 bytes in every
  form: uopt does not register-promote the field on its own, and the carrier
  removes reloads the target keeps. Naming only the post-loop `state->tilt`
  read (the CSE web that ties cross at save 3.0) is +4 or -12.
- The D_70 tilt rate loaded before cross is +4. `blend = D_78` hoisted is
  byte-identical or +12 (recorded above).
- The +-10 select defined before cross, in `speedTarget`, as a ternary, or
  in `targetTilt`: 40, 40, 28. **The 28 form is the receipt for the
  mechanism**: the select web (save 6.0, nocs 2) is decided before cross,
  takes c27 at cost 0 with forbidden `0xe0`, and cross then takes **c28 (f16)**
  and yawA's piece **c29 (f18)** -- both target colours -- but the select's
  own blocks move ahead of the smoothX load (22 structural rows) and the
  constant lands on f14 where the target has it on f12 after cross. So the
  target's c27 web is *not* the select. Moving the select between crossA/dot
  or dot/crossB is +4/-8; after crossB it is the retained 6.

What the c27 web must be, from the records: save >= 3.0, live across the
cross block, not overlapping `blend`'s two segments (a web that does takes
c28 behind blend, as the `speedTarget` select did), and touching no
instruction the target emits there. Read the whole +0x234..+0x31C region for
a value the original could have held in a register without an instruction --
the remaining candidates are the two `out->smooth*` reads and the trig
reloads, whose webs here are temps or split pieces.

#### 2026-09-11, lane s1-one: 6 to 3, and no new web was ever needed

Baseline reproduces: 1128 bytes, 282 of 282 instructions, delta 0, frame 0x98,
6 relocation-masked words (raw 14), first mismatch +0x238. Aligner: 276
byte-exact, 6 register naming, 0 immediate-only, 0 really-different.

**Adopted, 6 to 3 at delta 0.** Carry the cross product in `baseX` rather than
in `cross`, at both of its uses. `f32 cross` must stay declared -- dropping it
is 28, it holds the home ladder. The frame census is unchanged by the edit
(frame 152 both sides, 22 slots, no slot on one side only, no shared slot with
differing traffic), the three remaining rows are a strict subset of the
previous six, and the two registers the last pass warned about are untouched:
the D_70 rate stays f2 and the plus-or-minus-ten constant stays f12. `cross`
moves to f16, which is the shipped register.

**The previous pass's prescription was wrong even though its diagnosis was
right.** It said the target has one more caller-saved FP web, coloured c27,
that this body does not, and sent the next lane to find a value that could
carry one at no instruction cost. No new web is needed. The FP ladder for this
procedure, decoded from its own records rather than from the law's wording, is
c24 f0, c25 f2, c26 f12, c27 f14, c28 f16, c29 f18. `blend` already holds c27
and is live across part of the cross block; `baseX` is live across an
overlapping range. Merging the cross product into `baseX` makes one web of the
two chains -- L115, one web per symbol, block-set union -- and that web
inherits blend's edge, so its forbidden set gains c27 and it takes c28. Zero
instructions, zero frame change, no new declaration, no dead expression.

**The last word is one colour and it is fully characterised.** Web 108, the
`yawA` reload piece, has `available0` = c27 and c29 with `p1cost` 2.0 at both;
the ascending scan takes c27. `CDX_FORCE=p1:w108=c29` is *accepted* and returns
masked 0 at delta 0, so the whole residual is that one colour. There is no cost
lever -- the two costs are equal -- so c27 must be forbidden, which needs a
c27 neighbour for yawA. Three force receipts show yawA is not simply one above
cross: forcing blend to c28 puts cross on c27 and yawA on c28, and forcing
cross to c29 puts yawA on c27.

**And that is where it becomes a cost-model question rather than a source-form
one.** c27 is held by `blend`, whose `p1cost` is 0.0 at every caller-saved
colour, so it spans no call and is never spilled. Read from the shipped bytes:
the ROM writes only the FP homes at 76, 80, 92, 96, 100, 104, 108, 112, 116,
120 and 124, while 64, 68, 72, 84 and 88 are reserved and never touched; and
the cross block from +0x234 to +0x280 never uses f14 at all. A c27 web live in
that block would have to be defined before the four trig and yaw calls and used
after them, which for a caller-saved colour needs a spill store and a load, and
the shipped function has neither a spare home nor a spare instruction. Every
source route says the interferer cannot exist in the object, yet the force says
the shipped allocator produced exactly that outcome. The next move is to
instrument uopt's `p1cost` term, not to sweep more spellings.

##### Newly falsified this pass, each against the whole 282-instruction target

- A 72-row cross-carrier by yawA-carrier product over nine locals. Only the
  cross-to-baseX cell wins. **Every yawA carrier fails structurally rather than
  by score**: the shipped function spills yawA at 92 and baseZ at 116, which is
  two homes and therefore two symbols, so a shared carrier for yawA is ruled
  out by the frame.
- A 100-row rate-carrier cube over the three loop rates and five locals:
  entirely byte-inert at 3. Rate carrier identity is a dead axis here.
- Second trig-pair carriers, 200 cells over two bases. The second trigB carrier
  is completely inert because its value is a forwarded call result that never
  becomes a coloured web (L105), which also shows that symbol-level merging
  does not propagate interference into a split piece.
- Cross-block statement order (6 legal orders), operand order (8), the
  `blend` hoist at 10 positions, the three output-component orders, the three
  base orders, compound versus explicit assignment at seven sites, both clamp
  ternaries, and inlining each of the three products. Then 400 random two- and
  three-way combinations of the fifteen knobs that are byte-inert alone: all
  flat at 3.
- **Not a negative:** a first force sweep of 17 webs by 7 colours returned a
  uniform 3, and the records show every one of those forces was *declined* --
  the colour was absent from the web's own available set. By L101 that proves
  nothing and it is not reported as a result.

##### Two instrument facts

The `CDX_DETAIL_WEB` interference dump is **capped at fifteen rows**, and the
reported neighbour count does not equal the number of rows printed. A web was
nearly cleared of having a c27 neighbour because `blend` was absent from a
fifteen-row list; it was the sixteenth, the list being sorted descending by web
number. Check the row count against fifteen before calling an edge missing.

L114's ceiling is wrong as it was written, and this procedure is a second
witness: the caller-saved FP class here is six wide and f4, f6, f8 and f10 sit
outside it, which is a per-procedure fact read from `available0`, not a
constant. Decode the table from the records on the procedure in hand.

<!-- plateau-handoff:func_overlay_009_F00010B4_186772C:end -->
