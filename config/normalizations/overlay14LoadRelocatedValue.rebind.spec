# Bind the compiler's private jump-table references to the retained overlay
# table. Only relocation symbol indices change; instructions stay untouched.
0x98:.rodata:gOverlay14LoadJumpTableReloc
0xA0:.rodata:gOverlay14LoadJumpTableReloc
