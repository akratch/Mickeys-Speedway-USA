# Compiler switch-table HI16/LO16 pair; the function starts at TU +0x0.
# The retained seven-entry table has runtime LOCAL addend +0x4.
# Only existing relocation symbol indices change; instructions stay untouched.
0x9C:.rodata:gOverlay7ModeSwitchTableReloc
0xA4:.rodata:gOverlay7ModeSwitchTableReloc
